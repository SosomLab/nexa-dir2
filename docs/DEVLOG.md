# DEVLOG — 개발 진행 기록 (시간 역순)

> **전체 개발 진행을 시간순(최신이 위)** 으로 관찰하는 단일 기록. 세부 커밋은 [BRANCHES.md](BRANCHES.md)·git 로그.
> 짝 문서: 목적·기능·마일스톤 관점 = **[MILESTONES.md](MILESTONES.md)**.
> 기록 규약(원본 차용): 하루 상세는 `journal/YYYY-MM-DD.md`(내부 시간 역순), 이 문서에는 그날 요약을 맨 위에 추가.

---

## 2026-09-04

- **메인 파일 목록 오버레이 스크롤바 — 세로+가로(사용자 요청 — `feat(rows)`, X-47 1/2)**: 설정 창 규약을 nexa-gui 위젯에 이식 — **`Invalidations::request_tick`**(위젯→호스트 틱 요청 일반 경로·`TIMER_WIDGET_TICK` 40ms) + **`DrawCtx::fill_round_rect_alpha`**(DwCtx = 메모리 DC GDI+ — 그동안 라운드 도형 no-op) · rows.rs 세로/가로 썸·드래그·트랙 클릭·호버·키보드 스크롤 flash. 사용자 실기로 세로 확인 → 가로 추가 → **축별 독립 표시**(스크롤한 축만, 겹치면 둘 다). 잔여 = NxGrid·카드 호스트 확산.
- **글꼴 폴백 체인 실적용 — "두부 방지"(사용자 요청 — `feat(font)`)**: 쉼표 목록 = 폴백 체인 규약이 **터미널(X-3)에서만** 살아 있었고 UI 슬롯·대화상자·미리보기는 체인 문자열 전체를 패밀리 이름으로 넘겨 **1순위조차 미적용**이던 실측 → [fontchain.rs](../crates/nexa-app/src/fontchain.rs) 신설(설치된 첫 패밀리·폴백 목록) · DW 슬롯 3종에 `IDWriteFontFallback`(터미널 빌더 공용화) · 미리보기 창 GDI는 **글리프 단위 런 폴백**(`GetGlyphIndicesW` 판정·측정=그리기 런 규칙) · GDI 대화상자/제목은 1순위 정정. 네이티브 컨트롤은 OS 글꼴 연결 위임(한계 명시). 글리프 실기 확인 대기 = **X-48**. 상세 [journal/2026-09-04.md](journal/2026-09-04.md).
- **트리 하이라이트 잔존 픽스(사용자 QA — `eedd92f`)**: 오너드로 선택 판정이 `st.category`인데 LISTBOX는 LBN_SELCHANGE 전에 옛/새 행을 그려 옛 행이 남던 **잠복 결함** — 예전엔 창 전체 무효화가 가렸고 컨테이너 분리로 드러남 → rebuild 끝 트리 무효화.
- **설정 창 본문 스크롤 재구축(사용자 QA 3건 + 추가 지시 2건 — `f5b484a`)**: 휠마다 **전 컨트롤 파괴·재생성**하던 구조(QA 07-15 최소 구현)가 창 전체 깜박임의 진범 → **본문 컨테이너 `NexaPrefsPane` 분리 + 자식 일괄 이동**(DeferWindowPos 비트 복사·통지는 메인 창 전달) · 휠 잔량 누적으로 **트랙패드 픽셀 스크롤**(델타 30×3=45px) · **오버레이 스크롤바**(그리드 07-18 규약 + **반투명·단계 페이드·호버 두꺼움**·드래그 트랙 — `GdipCtx::fill_round_rect_alpha`) · 더블버퍼+소거 차단으로 **썸 블링크 해소**. 실측 교훈: **WS_EX_COMPOSITED 위에선 GDI+ 알파가 안 그려진다**. 기존 대조 = NxGrid/카드 호스트 바는 불투명·호버 없음, 메인 파일 목록은 스크롤바 없음 → 확산은 **X-47**(사용자 결정). 깜박임 실기 확인 대기. 상세 [journal/2026-09-04.md](journal/2026-09-04.md).

---

## 2026-09-02

- **내용 정리 3차 — MILESTONES가 포스트 M5를 표 한 줄로만 갖고 있었다(사용자 지시·코드 무변경)**: M0~M5는 마일스톤마다 상세 절이 있는데 **포스트 M5는 개요 표 한 줄이 전부**였고 그 줄이 `0.17.0`·**B2 3.71MB**·winget 0.16.0에 정지 = **X-46 압축 미리보기가 기능·마일스톤 SSOT에 아예 없었다**(위키 타임라인 3주 공백과 **같은 병리** — 버전 숫자는 릴리스마다 고치지만 "무엇이 들어갔는지" 적는 자리는 안 건드린다). ① **개요 표 갱신**(목표 열에 X-38~X-46 · 게이트 **B2 3.83MB·320 green** · 상태 `0.7.0`~`0.18.1` + 채널 09-02 실측) ② **`## 포스트 M5` 절 신설**(81→109줄 — 릴리스 묶음별로 무엇이 왜 들어갔는지: 플러그인 wasmi 전환 → 클라우드 → 드롭·보기 범위 → 자동 갱신 → 가상 파일·셸 통지 → **압축 미리보기·플러그인 동봉·Defender 오탐 해소**. **마일스톤이 아니라 릴리스 단위** — 이 구간은 사용자 QA가 다음 작업을 정한다). **무변동**: choco `0.18.1` `Submitted` 유지·winget 열린 PR 0건·0/0·clean·CI 3잡 green·병합 대상 없음·README 정확·STATUS 172줄 유지. 상세 [journal/2026-09-02.md](journal/2026-09-02.md).
- **내용 정리 2차 + 진행사항 최신화(사용자 지시 — 하루 마감·코드 무변경)**: 실측 결과 **고칠 것은 하나뿐이었다** — **STATUS 201줄로 "한 장" 재이탈**(오늘 작업 블록 3개가 08-24 X-46 원문 50줄 **위에** 쌓인 08-11·08-23과 동일 병리) → 오늘 것을 **주제별 4불릿으로 통합**하고 **08-24를 하루 한 줄 색인으로 강등**해 **201→172줄**(원문은 journal 보존). **무변동 확인**: choco `0.18.1` 두 패키지 `Submitted` 유지·winget 열린 NexaDir PR 0건 · `main`=`origin/main` 0/0·트리 clean·**CI 3잡 green** · 병합 대상 없음(`feat/x13-launcher-crud`는 보존 규약) · 위키 파리티 유지 · TODO/CLAUDE.md 표기 실제와 일치. 상세 [journal/2026-09-02.md](journal/2026-09-02.md).
- **사용자 문서 최신화 + 위키 발행(사용자 지시 — "위키에도 설명을 추가해서 반영")**: 착수 즉시 어긋남 하나가 나왔다 — **위키 타임라인이 `0.13.0`(08-02)에서 멈춰 있었다**(예산 표는 `0.18.1`까지 갱신돼 있는데 서사만 3주·릴리스 5개가 비어 있었다 = 버전 숫자는 릴리스마다 고쳤지만 **"무슨 일이 있었는지"는 아무도 안 적었다**). ① **[개발 여정] 절 4개 신설**(08-02~08-11 자동 갱신 = 갱신 계기를 하나→셋 · 08-23 원격 붙여넣기와 **자리표시자 생성은 통지도 mtime도 안 남긴다**는 실측 · 08-24 **압축을 풀 필요가 없다**는 사실이 설계를 정한 압축 미리보기 + 설치형만 파일 버전이 비어 Defender에 걸리던 건 · 09-02 배포 채널 정착) ② **[설치와 다운로드] "채널별 현재 버전" 표**(승인만 적으면 choco에서 최신을 받는 줄 안다 — Release/winget `0.18.1` vs choco `0.8.1`·심사 중을 나란히) ③ **Home** choco 한 줄 ④ **[개발 빌드와 테스트]에 `core` 잡 사각지대 설명**(08-02 컴파일·09-02 런타임 UB 두 사례 → "push 후 3잡 확인이 유일한 그물") + 릴리스 절차에 채널 제출 규칙·`resubmit-chocolatey` ⑤ **발행 완료**(위키 저장소 4쪽 `0e66da5` — 재복사 diff 0으로 파리티 확인). 상세 [journal/2026-09-02.md](journal/2026-09-02.md).
- **main 적색 발견·복구 — `Secret` 소거 테스트가 해제된 메모리를 읽고 있었다(push 직후 CI 확인에서 발각)**: **08-24부터 9일·8커밋 동안 `core (ubuntu)`·`core (macos)` 실패**(windows는 green) — 08-02 사각지대의 **거울상**(그때는 비Windows 컴파일, 이번엔 **Windows에서만 우연히 통과하는 테스트**). 원인은 `clone_is_independent_and_drop_zeroizes`가 `drop(c)` **후** 원래 주소를 `from_raw_parts`로 다시 읽은 것 = **해제 메모리 읽기 UB**. glibc·macOS 할당자는 free 블록 앞머리에 free 리스트 포인터를 써 넣어 `abc` 자리가 0이 아니게 되고, Windows 힙은 마침 그러지 않아 통과했다 → **소거 구현은 옳았고 검사 방식이 틀렸다**(기능 결함 아님). 수정 = `Drop` 본문을 `Secret::zeroize()`로 분리하고 테스트는 **살아 있는 버퍼**에서 같은 경로를 호출(사본 독립성 단언 유지). `-p nexa-core` **6 green**·clippy 0·**linux 타깃 check green**. 교훈 = "비Windows도 검사"가 `cargo check`만으로는 부족하다(이번 건은 컴파일이 아니라 **런타임 차이**) — **push 후 CI 3잡 확인**이 유일한 그물. 상세 [journal/2026-09-02.md](journal/2026-09-02.md).
- **내용 정리 + 진행사항 최신화(사용자 지시 — 코드 무변경·main push)**: 승인이 바꾼 것은 내부 기록만이 아니라 **사용자가 읽는 면**이었다. ① **위키 설치 페이지가 choco를 "⏳ 심사 중(승인 후 제공)"으로 안내** → 표 1행 + "준비 중인 채널" 절을 **설치 명령 절로 교체**(`choco install nexa-dir`/`nexa-dir.portable`·upgrade/uninstall). 단 **승인된 것은 `0.8.1`, 최신 `0.18.1`은 검수 중**이라 "최신을 원하면 winget·Release" 경고를 함께 뒀다(승인 사실만 적으면 8개월 전 기능을 최신인 줄 알고 받는다) ② **게이트 서술 2곳이 "기본 꺼짐"에 정지**([18 §3](18-build-and-test.md)·[21 §7-3](21-distribution.md)) → 등록·재개 반영 + 18에 **`resubmit-chocolatey` 경로**(빌드 없이 재제출·main 체크아웃이라 패키징 수정 반영) 명문화 ③ **그 외 어긋남 없음**(README·나머지 위키는 카탈로그 버전 미명시라 무수정 · 남은 `CHOCO_PUSH` 언급은 전부 날짜 붙은 과거 기록 · BRANCHES 신규 없음) ④ **병합할 브랜치 없음**(`feat/x13-launcher-crud`는 X-13 2/2 착수 시 병합 규약으로 보존). **위키 발행은 별도 저장소라 사용자 승인 후 별건**. 상세 [journal/2026-09-02.md](journal/2026-09-02.md).
- **Chocolatey 승인 확인 → `0.18.1` 두 패키지 제출(사용자 지시 — "choco 승인됨·현재 버전 재배포")**: 실측 셋으로 상황을 확정했다 — ① **choco 2종 승인 완료**(OData `FindPackagesById()`에 `nexa-dir`·`nexa-dir.portable` 둘 다 `0.8.1`/**`Approved`** — 07-20 스캔 플래그 후 **44일** 만. 미승인분은 공개 피드에 안 나오므로 조회에 잡힌 것 자체가 증거) ② **winget은 이미 최신과 동기**(`0.18.1` PR [#423330](https://github.com/microsoft/winget-pkgs/pull/423330)·[#423331](https://github.com/microsoft/winget-pkgs/pull/423331)이 08-24 제출 ~1시간 만에 MERGED·카탈로그 디렉터리 실측 = **제출할 것 없음**) ③ 저장소 `main`=`origin/main` 0/0·clean. ∴ 이번 "재배포"의 실체는 **choco 한 채널**([채널 제출 규칙](21-distribution.md#릴리스-시-채널-제출-규칙-2026-08-23--사용자-지시상시-규칙) 그대로 — 대기 해소된 채널에 **최신 버전만**, 중간 버전 생략). **빌드 없음**: 태그 `0.18.1` 이후 7커밋이 docs 4·choco 패키징 fix 2·ci 1뿐이고 `crates/`·`Cargo.*` 무변 = 배포 바이너리 동일. 오히려 재빌드는 **무서명 새 해시로 Defender ML 프리밸런스 0을 되살릴 뿐**([12 §4-2](12-packaging-single-exe.md)). 절차 = `CHOCO_PUSH=true` 등록(07-21 게이트의 설계된 재개 조건) → **`resubmit-chocolatey` dispatch**(08-31 상설화 경로의 첫 실사용 — 빌드 없이 릴리스 자산 → 해시 → pack → push). **이 경로를 고른 결정적 이유**: 모더레이터 지적 반영분(VERIFICATION.txt 제거·`owners=kiros33`·ps1 BOM·제거 스크립트 파스 오류)이 **태그에는 없고 main에만** 있어 release.yml 재실행은 낡은 패키징으로 되돌아갔을 것. 결과 = [run](https://github.com/SosomLab/nexa-dir2/actions/runs/33578690160) 23초·두 nupkg push 성공·해시가 릴리스 `SHA256SUMS.txt`와 일치 → `Packages(Id='…',Version='0.18.1')` 조회 **`Submitted`** 확인. **채널 현황**: winget 2채널 `0.18.1` 라이브 · choco `0.8.1` 라이브 + `0.18.1` 검수 중 · Release 자산 6종. 상세 [journal/2026-09-02.md](journal/2026-09-02.md).

## 2026-08-24

- **Defender 오탐 해소 확인(사용자 실기 — "지금은 다운로드 시 탐지 안 됨")**: `0.18.1` 설치형은 다운로드 시 격리되지 않음(재다운로드 실측 확증 — 즉시 격리 없음·격리함 비어 있음·온디맨드 clean·FileVersion 채워짐). **VERSIONINFO 보강이 실효**(0.17.0/0.18.0의 `Wacatac.*!ml` 해소) — 압축을 안 건드렸으니 완화 요인은 빈 파일 버전 채우기 하나로 좁혀진다([12 §4-2] 실측 뒷받침). 이번 회차 오탐 종결. 상세 [journal/2026-08-24.md](journal/2026-08-24.md).
- **릴리스 `0.18.1`(사용자 지시 — 오탐 대응 재배포)**: 0.18.0 이후 바이너리 영향 = 설치형 VERSIONINFO 하나(exe 무변경)라 패치 배포. **압축 완화는 실측으로 기각**(비솔리드 lzma 엔트로피 변화 0·무압축 크기 +81%·네 변형 온디맨드 전부 clean = 클라우드 평판이 유일 메커니즘 — [12 §4-2] 표). workflow_dispatch로 설치형 빌드 사전 검증 후 태그 → [Release 0.18.1](https://github.com/SosomLab/nexa-dir2/releases/tag/0.18.1) 자산 6종 · **설치형 FileVersion=0.18.1 채워짐 실측**(0.18.0 공백 해소)·플러그인 zip 폴더 구조 정상(재포장 불요)·해시 6종 일치·choco skipped · winget 제출 [#423330](https://github.com/microsoft/winget-pkgs/pull/423330)·[#423331](https://github.com/microsoft/winget-pkgs/pull/423331). 상세 [journal/2026-08-24.md](journal/2026-08-24.md).
- **Defender ML 오탐(설치형) 규명·완화(사용자 보고 — "왜 갑자기 문제가 되나")**: 격리 목록 실측으로 **0.17.0도 같은 날 오전 `Wacatac.B!ml`로 이미 격리**돼 있었음을 확인 — 0.18.0의 새 결함이 아니라 무서명 설치형의 반복 현상. 범위 축소(포터블 exe·wasm 2종 = 클린 · 0.16/0.17/0.18 설치형 온디맨드 스캔 = 셋 다 클린 · 브라우저 다운로드 맥락에서만 격리 · SHA-256 일치) → 원인 = **무서명 + 새 해시 + 프리밸런스 0**에 대한 클라우드 ML 판정. 조치 = 격리 해제(2건 복원·해시 재확인) · **Inno 설치형의 빈 파일 버전 발견 → VERSIONINFO 보강** (포터블은 있고 설치형만 공백이었다) · 오탐 신고 절차 상설화(packaging/av-false-positive.md·12 §4-2·21 §6 체크리스트) · 0.18.0 제출값 생성 후 WDSI 폼 오픈. 상세 [journal/2026-08-24.md](journal/2026-08-24.md).
- **릴리스 `0.18.0` + winget 2건 제출(사용자 지시 — "wasm을 포함한 버전 릴리스")**: ① **채널 검증**(제출 규칙 2회째) — winget = NexaDir 대기 PR **0건** 실측(0.17.0 PR 2건은 08-23 제출 ~2시간 만에 병합) → 제출 · choco = `CHOCO_PUSH` 미등록 → 제외(`skipped` 로그 확인) ② **배포** — 승격 → 태그 push → 게이트 통과 → [Release 0.18.0](https://github.com/SosomLab/nexa-dir2/releases/tag/0.18.0) **자산 6종**(포터블 3.83MB·포터블 zip 1.77MB[플러그인 포함]·설치형 3.35MB·설치형 zip 2.79MB·**플러그인 zip 48.6KB**·SHA256SUMS) — CI가 태그 소스로 `.wasm`을 빌드해 실은 **첫 릴리스** ③ **자산 1건 재포장** — 플러그인 zip이 평면 구조로 나가 문서 안내(`plugins\` 폴더)와 어긋난 것을 실측 발견 → 워크플로 수정 + 이번 자산은 재포장·`--clobber`·SHA256SUMS 해당 줄만 갱신(exe 해시 불변) ④ **winget PR 2건 제출 → 40분 만에 둘 다 병합**([#423258](https://github.com/microsoft/winget-pkgs/pull/423258)·[#423259](https://github.com/microsoft/winget-pkgs/pull/423259) — 역대 최단·두 채널 최신과 완전 동기) ⑤ **사용자 문서 동일 트랜잭션**(위키 압축 미리보기 매뉴얼 신설 + 6쪽 갱신·README). 상세 [journal/2026-08-24.md](journal/2026-08-24.md).
- **동봉 플러그인 배포 신설(사용자 지시 — "빌드해서 markdown.wasm과 함께 배포")**: 착수 전 실측에서 **릴리스 파이프라인에 `wasm`이 한 번도 등장하지 않는다**는 사실을 확인 — 플러그인은 저장소에만 있고 **배포된 적이 없었다**(markdown.wasm 포함). 새 파일 끼워넣기가 아니라 **배포 경로를 처음 만드는 일**. ① 빌드 단일 출처 `scripts/build-plugins.ps1`(두 샘플 → 각 dist + 배포 스테이징 · 새 플러그인은 배열 한 줄) + **CI 편입**(워크스페이스 밖 크레이트라 test/build가 안 건드림 = 릴리스 당일 첫 실패 방지) ② **탐색 경로 2원화**(`data\plugins` 사용자 설치분 → `<exe>\plugins` 동봉분·같은 id는 사용자분 우선 — Program Files 설치도 읽기만이라 권한 무관) ③ **배포 형태**(포터블 zip에 plugins 동봉 · 설치형 `{{app}}\plugins` · **NexaDir-Plugins-<ver>.zip** 신규 자산 · 단일 exe 자산은 최소파일 규율 유지) ④ **설명 문서**(18 §3-1 빌드 SSOT · 21 §5-2 배포 형태 · 24 §4-2 두 경로·동봉본 수정 절차 · README·위키 2쪽·샘플 README·설정 문구 3종). 127 green·clippy 0·B2 3.83MB·B3 무변. 상세 [journal/2026-08-24.md](journal/2026-08-24.md).
- **X-46 압축 파일 미리보기(사용자 요청 — 그리드·별도 창·내장+플러그인·암호 비기록·확장 용이)**: 착수 전 확인한 사실 하나가 설계를 결정했다 — **거의 모든 압축 포맷은 항목 표가 평문**이라 미리보기는 압축을 풀 필요가 없다(예외 = 헤더가 LZMA인 7z → "코덱 필요 = 플러그인"으로 분리). ① **목록 계층 = nexa-vfs**(OS 비의존·전 플랫폼 테스트 + 훗날 "아카이브를 폴더처럼 탐색"의 자리) — 포맷 레지스트리(**새 포맷 = 파일 1개 + 한 줄**)·ZIP(Zip64·AES 표시·SFX 델타 보정·CD 암호화 = 암호 요구)·TAR·CAB·RAR 5/4·7z 판정·단일 스트림 ② **표시** = 도크 요약 + **그리드 창**(NxGrid 재사용 = 파일 그리드 규약 계승·8열·수치 정렬·Ctrl+C TSV) · **시각 이중 보정 함정 차단**(DOS 시각은 현지 벽시계 그대로·Unix epoch만 보정) · 구형 zip CP949 이름 디코더 주입 ③ **암호 = 구조로 강제**(`Secret` — Debug `Secret(***)`·길이도 비노출·Drop 소거 / 마스킹 모달 / **저장 경로가 코드에 없음**·세션 메모리 한정·경유 버퍼·EDIT 잔상 소거) ④ **플러그인 ABI v2**(nx_meta 4번째 줄 능력 선언·`nx_archive`·`file_size`/`read_at`/`password`) + 참조 구현 `samples/archive-viewer-wasm`(**ISO 9660/ar/cpio를 31KB `.wasm` 하나**로 — 앱 재빌드 없이 포맷 확장). 워크스페이스 **319 green**(신규 44)·clippy 0·비Windows check·**B2 3.83MB**·**B3 21종 무변**·외부 crate 0. **실기 QA 대기**. 상세 [journal/2026-08-24.md](journal/2026-08-24.md).

## 2026-08-23

- **내용 정리 3차(위키 포함) + 진행사항 최신화(사용자 지시 — 릴리스 직후 마감)**: 트리 clean·0/0·winget 0.17.0 PR 2건 OPEN(`New-Manifest` — 문서 일치)이라 무변동 확인이 주. 실제 어긋남은 **기능-개요 요약 2줄**뿐("폴더 watcher" 구세대 표기 → 가상 파일 붙여넣기·자동 반영[이벤트+보험] · 도구 모음 요약에 항상 맨 위 보충) — 정정·발행. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **릴리스 `0.17.0` + winget 2건 제출(사용자 지시 — 채널 제출 규칙 첫 적용)**: ① **검증** — winget = NexaDir 대기 PR 없음 실측 → 제출 대상 · choco = `CHOCO_PUSH` 미등록 → 제외(게이트 자동 skip) ② **배포** — 승격(Cargo workspace·lang 3종 `@app`) → 태그 push → 게이트 통과 → [Release 0.17.0](https://github.com/SosomLab/nexa-dir2/releases/tag/0.17.0) 자산 5종(포터블 **3.71MB**·SHA 재다운로드 이중 확인·choco 팩 success/push skipped). 내용 = X-42 1·2차·X-43+β·X-44 1~5차·X-45 ③ **winget PR 2건** [#422930](https://github.com/microsoft/winget-pkgs/pull/422930)·[#422931](https://github.com/microsoft/winget-pkgs/pull/422931)(`winget validate` 통과·Contents API 제출) ④ **사용자 문서 동일 트랜잭션** — 기능 문서 4종(가상 파일 붙여넣기·자동 반영 3계기 표·빈 폴더 글리프·압정 버튼) + 버전/실측(README·위키 3종·예산 표) + 위키 발행. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **내용 정리 2차 + 진행사항 최신화 + main push(사용자 지시 — 하루 마감·코드 무변경)**: 트리 clean·병합 대상 없음. ① **STATUS 재팽창 압축**(199→167줄 — 오전 정리 후 하루 안에 차수 블록 10개가 쌓인 08-11 동일 병리 → 08-23 분을 **주제별 5블록**으로 통합) ② **X-44 차수 표기 노후 정정**(CLAUDE §7·STATUS §5가 "1·2차"에 정지 → 1~5차·핵심 QA = 셸 통지 즉시 표시) ③ B2 로컬 실측 3.71MB 반영 ④ 메모리 `app-restart-routine` 등재 ⑤ **main push 17커밋**(feat 5·fix 4·docs 8 — 태그 없음·위키 무수정). 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-44 5차 — 셸 변경 통지 구독: 주기 감시 → 즉시 이벤트(사용자 확정 — `shellnotify.rs` 신설)**: FS 계층이 침묵해도 **OneDrive는 셸에 SHChangeNotify로 알린다**(탐색기 즉시 갱신의 원리) → `SHChangeNotifyRegister`(ShellLevel|NewDelivery·DISKEVENTS·**재귀**)로 패널 루트 구독(sync_watchers 길목 재등록·Drop 해제·페이로드 Lock/Unlock 반납·shell32 인박스 = B3 무변). 수신 = 해석 없이 디바운스 합류(폭주 300ms+1s 흡수·재귀라 하위 전체 커버). **스윕은 보험 강등**(비활성 10s→30s — 셸 통지 미발생 예외만. 활성 3s·RDCW 존치) = **이벤트 1선 + 얇은 보험 2선** 구조. 116 green·clippy 0·B2 3.71MB·B3 통과·재시작 반영. QA = 클라우드 생성 → 로컬 반영 순간 비활성에서도 수백 ms~수 초 내 표시. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-44 4차 — 뷰포트 폴더 점검을 mtime → 프로브 서명 승격(`2bc8dba` — 실측 확정·비활성 캡처 검증)**: 3차 후에도 재현(파일이 로컬에 실재하는데 글리프 없음) → 실측 3종으로 확정 — **OneDrive 플레이스홀더 생성(CfCreatePlaceholders)은 부모 폴더 mtime도 RDCW 통지도 남기지 않는다**(같은 파일의 삭제는 통지됨 — FSW 로그 대조. "삭제는 되는데 생성만 안 뜨던" 비대칭의 최종 원인). 새 자식은 **열거만이 본다** → 뷰포트 폴더(펼침+접힘)를 루트 프로브와 같은 **fsprobe 서명(상한 내 열거 count/fold)**으로 점검(비용 = 보이는 폴더 수 유계·기존 틱 편승·신규 타이머 0). 신규 테스트 = SetFileTime으로 부모 mtime을 되돌려 침묵 재현 → count/fold 검출 계약화. **116 green·clippy 0·비Windows check** · 재시작 후 **비활성 유지 캡처로 글리프 표시 확인**. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-44 3차 — 비활성(보이는 창) 프로브 폴링 유지(사용자 확정 — `ce6c0ee`·재시작 반영)**: 클라우드 웹 생성분이 비활성 앱에 안 뜨는 재보고를 **디스크 실측으로 확정** — 원격 생성분은 SharePoint 하향 동기화가 내려오기 전까지 **로컬에 존재하지 않아**(수 분 지연 실측) 통지가 원리적으로 없고, 내려오는 시점에 비활성이면 폴링이 꺼져 있었다("활성화하면 생긴다" = 그 사이 동기화 완료 + 활성화 재열람). 수정 = 비활성 시 KillTimer → **감속 재무장(10s — 활성 3s)**, **정지는 최소화에서만**(유휴 0% 규율 유지 — X-40 "활성일 때만" 규정 개정). 내려오는 순간의 RDCW 발생 여부는 백그라운드 감시로 실측 예정. 115 green·clippy 0. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-45 항상 맨 위에 표시 토글(사용자 요청 — View 메뉴+툴바·영속)**: `CMD_TOGGLE_TOPMOST` 단일 명령 — View 메뉴 체크 항목 + 툴바 panel 블록 5번째 버튼(압정 SVG·순수 스트로크). `SetWindowPos` TOPMOST/NOTOPMOST 밴드 전환(활성화·위치·크기 불변)·즉시 적용·체크 동기·설정 영속(`always_on_top` — 기동 복원). 기존 `toolbar_order`엔 파서가 `ontop` 보충 삽입(foldersfirst 전례). **115 green·clippy 0·비Windows check·B2 3.71MB·B3 통과** · 사용자 지시로 앱 재시작(오늘 작업 전부 반영). 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-44 2차 실측 검증(재보고 대응 — 코드 무변경)**: "여전히 글리프 안 뜸" 재보고를 **비활성 유지 캡처 실험**(SWP_NOACTIVATE로 창만 올려 관찰·로컬 파일 생성/삭제)으로 분리 — **앱 체인은 양방향 정상**(생성 ~1.8초 내 글리프 출현·삭제 시 소멸, 비활성 그대로). ∴ 재보고의 유력 원인 = **클라우드→로컬 동기화 시점**(SharePoint 라이브러리 동기화 지연 — 로컬에 파일이 내려오기 전엔 어떤 감시로도 못 뜬다). QA 요령 = 로컬 폴더 도착 기준으로 확인. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-42 2차 완결 — ⓐ DnD 수신·ⓑ 워커화·ⓒ undo·ⓓ 클라우드(사용자 지시 — `3e8854c`+`2d6dbdf`·실기 QA 대기)**: ⓐ CF_HDROP 부재 + `FileGroupDescriptorW` 광고 = 가상 드롭 수신(항상 복사·Drop **반환 전** 동기 추출 — 수혜는 **Outlook 첨부·zip 내부·MTP**. 정정: RDP는 프로토콜상 파일 드래그 없음) ⓑ **워커화** — UI 스레드는 계획(파싱·" (2)" 대상·GetData 스트림 확보)까지, IStream은 `CoMarshalInterThreadInterfaceInStream`으로 워커(MTA)에 — **out-of-proc 소스는 언마샬 프록시가 원 프로세스로 직접 이어져** Read가 UI 펌프 비의존 = 대용량 RDP 붙여넣기 중 창 반응 유지. 진행 창·취소·세그먼트·in_flight는 **클라우드 전송 공용 상태 재사용**(동시 1개·바쁘면 동기 폴백·취소 시 파편 소거·마샬 패킷은 취소여도 소비) ⓒ `VPasteOp`(DeleteBatchOp **역방향** — undo=생성물 휴지통·redo=셸 복원. 재복사 불가 소스의 올바른 왕복) 세 경로 모두 기록 ⓓ 클라우드 대상 = 스테이징 추출 후 **기존 업로드 경로 재사용**(X-37 임시 폴더 경유 전례). Cargo `Win32_System_Com_Marshal`(ole32 — B3 무변)·워커 종단 테스트 신설(300KB 다중 청크·실 클립보드 비접촉). **115 green·clippy 0·비Windows check·B2 3.70MB·B3 통과**. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-44 2차 — 뷰포트의 접힌 폴더도 감시·스윕(사용자 QA 즉시 대응 — `3155915`)**: 비활성 중 접힌 빈 폴더 안 파일 생성이 글리프에 반영 안 되고(활성화해야 출현) **펼친 폴더의 삭제는 되던 비대칭** — 원인 = watcher 대상이 "루트+펼침 폴더"뿐(접힌 폴더 내부 변경은 부모 비재귀 통지로 신뢰 불가·비활성 중 폴링 꺼짐). 수정 = `watch_dirs`에 **뷰포트에 보이는 접힌 폴더 합류**(상한 64 공유·이벤트 기반 = 상시 비용 0·비활성에도 동작) + `viewport_dirs(expanded_only)` 일반화로 활성 시 mtime 스윕도 접힌 폴더 포함. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-43 β — 느린 경로 프로브 생략(`2430bde`)**: UNC·원격 드라이브·광학은 read_dir가 수십 ms~수 초일 수 있어 **페인트 경로 프로브를 생략하고 글리프 유지**(안전 방향 — GetDriveType 1회·NodeId 캐시·폴더당 1회). 리무버블은 로컬 FS라 유지·비Windows 무생략. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-44 간헐 무갱신 보완(사용자 지시 — `a80d2dc` main 직커밋·실기 QA 대기)**: 전수 조사의 유실 시나리오를 구현으로 보완. **사용자 확정 원칙** = ① 탭·앱이 **활성화되는 순간 보이는 영역(트리 펼침 포함)을 갱신** ② **성능을 깎는 추가 감시/폴링 배제**(전 계기 이벤트 기반·기존 3s 틱에 O(1) stat만 편승·신규 타이머 0) ③ 비활성 중 watcher 즉시 갱신(현행 만족 동작)은 무변. 구현 = **S1 `Tab.stale`**(전환·닫기로 드러난 탭을 update_status 길목이 무간섭 재열람 — 비활성 탭은 감시 비대상이라 그동안의 변경이 어디에도 안 잡혀 있던 유력 원인 1위) · **S2 프로브 기준선 즉시 수립**(종전엔 다음 틱의 "경로 변경=기준선만" 분기가 열거~틱 사이 변경을 영영 삼킴 → update_status 즉시 수립 + reload_both 끝 재수립[전송 후 여분 재로드 잔결함도 소거]) · **S3 서명 강화**(bytes 합+최신 mtime max → 항목별 (크기,mtime) SplitMix 접기 — **같은 크기·비최신 mtime 덮어쓰기** 포착·비용 동일) · **"보이는 영역" 원칙**(뷰포트 펼침 폴더 mtime O(1) 점검 편승[`visible_expanded_dirs`·`rows.viewport()`] + 앱 활성화는 프로브 게이트 대신 **보이는 영역 재열람**으로 개정) · **S5 WM_DEVICECHANGE**(USB 삽입/제거 → 내 PC 뷰 디바운스 재로드 — 핸들러 부재였다) · **S8 소실 폴더 조상 폴백**(유령 목록+F5 무효 고착 해소). 라운드로빈 폴링은 사용자 지시로 배제. 신규 테스트 4·기존 1 의미론 갱신 — **114 green(app)·78(gui)·clippy 0·비Windows check·B2 3.69MB·B3 무변**. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **내용 정리 + 진행사항 최신화 + main push(사용자 지시 — 하루 마감·코드 무변경)**: 트리 clean·병합할 브랜치 없음(`feat/x13-launcher-crud`는 X-13 보존 규약)이라 정리 대상은 **외부 채널의 진행**이었다. ① **winget 0.16.0 PR 2건 병합 확인**(원천 실측) — Portable [#415214](https://github.com/microsoft/winget-pkgs/pull/415214) **제출 당일(08-11) 병합**(waiver 없이 — 역대 최단) · 설치형 [#415215](https://github.com/microsoft/winget-pkgs/pull/415215) **08-14 병합**(`Waived-Policy-Test-1.2` — **버전 업데이트도 waiver 경로를 탄다**는 확인). 카탈로그 실측 두 채널 다 `0.16.0` = **winget이 처음으로 최신 릴리스와 완전 동기, 심사 대기는 choco 2건만** ② choco 무변동(0.8.1 flagged 34일째) ③ 낡은 "심사 중" 표기 4곳 정정(CLAUDE §1·§7 / STATUS §5 / MILESTONES / [21 §8](21-distribution.md) 채널 표 — **위키는 무수정**: 카탈로그 버전을 명시하지 않는 서술이라 낡은 곳 없음·발행 불요) ④ 그 외 어긋남 없음(STATUS 166줄 "한 장" 내·신설 문서 없음 — 억지로 만들 일이 없으면 없다고 적는다) ⑤ **main push 5커밋**(X-42·X-43 feat 2 + docs 3 — 태그 없음). 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **파일 변경 감시 파이프라인 전수 조사(사용자 보고 "간혹 F5 필요" — 코드 무변경)**: watcher(RDCW)·fsprobe·디바운스·세대 가드·자기 조작 재로드 전 경로를 실코드로 점검 — 계기별 커버 지도 + **유실 시나리오 8건**. 유력 원인 톱 3 = ① **백그라운드 탭 낡음**(탭 전환은 재열거하지 않고 비활성 탭은 watcher·프로브 둘 다 비대상 — 전환 후 프로브가 경로 변경을 "기준선 재설정"으로 처리해 **전환 전 변경을 영영 삼킨다**. 이후 새 변경이 오면 저절로 고쳐져 정확히 "간혹·재현 불가"로 체감) ② **열거→구독/기준선 갭**(진입/복귀 직후 0~3초 창의 외부 변경은 어느 계기도 못 잡음) ③ **프로브 서명 한계**(같은 크기·비최신 mtime 덮어쓰기 = 서명 무변·4096 초과·펼친 하위 폴더 사각). 부수 발견 = `::PC::` 뷰 **WM_DEVICECHANGE 부재**(USB 무반영)·소실 폴더 reopen 조용한 실패(F5도 무효). 디바운스·세대 가드·무간섭 재로드 자체는 건전 확인. 보완 6건 [TODO](TODO.md) **X-44** 등재(착수 = 사용자 결정). 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-43 빈 폴더 펼침 글리프 억제(사용자 요청 — `b1a04ca` main 직커밋·실기 QA 대기)**: 현재 보기(숨김/점 필터)로 보여줄 자식이 없는 폴더는 트리 ▸ 글리프를 그리지 않는다(보기 옵션이 내용을 드러내면 재열거 = 새 소스로 복귀). **설정 토글 `hide_empty_glyph`(기본 = 숨김·설정 창 "파일 목록" 체크박스·lang 3종)**. 판정 이원화 = 열거 완료 폴더는 트리 실측(`loaded_child_count` — 필터가 열거에 반영돼 children이 곧 답) · 미열거 폴더는 `read_dir` **조기 종료 프로브**(같은 필터 규칙·NodeId 캐시 = 폴더당 IO 1회·캐시 수명 = 소스 수명이라 무효화 코드 불요·**열기 실패 = 글리프 유지** — 클라우드 센티널 보호). 설계 지점 = **marker ≠ is_dir 분리**(`RowItem.is_dir` 신설 — 억제된 빈 폴더도 굵게·리네임 전체 선택 등 폴더 서식 유지. `has_children` 의미는 불변: 트리 층에서 바꾸면 `watch_dirs`에서 빈 폴더가 감시 제외되는 부작용 — 검토에서 차단) · 전파 = NavCtx → `open_source` 관문(전 열거 경로) + 설정 토글은 전 탭 즉시. 신규 테스트 2 — **110 green(app)·78(gui)·36(tree)·clippy 0·비Windows check·B2 3.67MB(별도 target 실측 — 실행 중 exe 빌드 잠금)·B3 무변**(std::fs만). 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **릴리스 채널 제출 규칙 명문화(사용자 지시 — 릴리스마다 반영)**: winget·choco의 배포 요청 상태를 릴리스마다 원천 실측해 **대기 중인 버전이 없으면 그 채널에 새 버전을 제출, 대기 중이면 그 채널은 제외**(중복 큐·PR 겹침 방지 — 재개 시 중간 버전은 건너뛰고 최신만). 08-02·08-11에 건별로 두 번 내린 판단을 **상시 규칙으로 승격**한 것 — [21 §8 "릴리스 시 채널 제출 규칙"](21-distribution.md) 신설 + CLAUDE.md §7 링크. 판정 원천: winget = 우리 PR OPEN 여부 · choco = 모더레이션 미승인 버전 여부(현재 0.8.1 잠김 = 계속 제외, `CHOCO_PUSH` 게이트와 합치). 상세 [journal/2026-08-23.md](journal/2026-08-23.md).
- **X-42 가상 파일 붙여넣기 — RDP·Outlook·압축 폴더 폴백(사용자 보고 → 검토 → 구현 `227ed4c`, main 직커밋·실기 QA 대기)**: 맥 RDP 세션에서 복사한 파일이 **탐색기에는 붙여넣기 되고 앱에서는 안 되던** 결함. 원인 = 읽기측이 **CF_HDROP 단일 포맷**인데 원격 소스는 실경로가 없어 rdpclip이 **가상 파일 2종**(`FileGroupDescriptorW` 항목 목록 + `FileContents` 항목별 IStream 지연 스트리밍 — CFSTR_* MS 규약)으로 게시한다(Outlook 첨부·탐색기 zip 내부·MTP도 동일 규약 — X-38 7-Zip과 친척이지만 그쪽은 CF_HDROP을 **늦게**, RDP는 **아예 안** 만든다). 구현 = `has_files()`에 가상 판정 추가 + `extract_virtual_files`(OleGetClipboard → IDataObject — FileContents가 **lindex 지정**을 요구해 원시 GetClipboardData로는 불가) → 4개 붙여넣기 지점(Ctrl+V·배경 paste·고유·pasteInto)에 **CF_HDROP 부재 시 폴백** 배선(실경로는 전송 엔진 undo 경로 우선). 안전장치 = 경로 탈출 기각(`..`·절대·드라이브)·cItems 과대 방어·항목별 실패 격리·충돌 " (2)"(`unique_dest` 재사용). 함정 = `FILEDESCRIPTORW`가 **packed 구조체**라 배열 필드 참조가 E0793 → 값 복사 우회. 테스트 2 신설(파싱 + **가짜 IDataObject 종단** — 실 클립보드 비접촉·100KB 스트림·충돌까지, `extract_virtual_from` 분리가 열쇠·후속 DnD 재사용 지점). **110 green·clippy 0·비Windows check 통과·B2 3.67MB·B3 무변(신규 DLL 0 — ole32 기존)**. 1차 = UI 스레드 동기 추출(대기 커서) — 워커화·undo·DnD 수신은 X-42 β. **실기 QA(맥 RDP) = 사용자 몫**. 상세 [journal/2026-08-23.md](journal/2026-08-23.md).

## 2026-08-11

- **내용 정리 3차 — STATUS "한 장" 복원 + TODO 상태 정정(사용자 지시 — 코드 무변경)**: 트리 clean·0/0이라 커밋 대기분은 없었고, 실측으로 찾은 어긋남 **2건만** 고쳤다(**억지로 만들 일이 없으면 없다고 적는 것도 정리의 일부** — 나머지는 "무변동"으로 남겼다). ① **STATUS가 "한 장" 규약 재이탈(236줄)** — 08-02 3차에 565→161줄로 압축한 것이 **오늘 하루 4개 차수를 쌓아 재팽창**했다. **하루 안에서 차수를 쌓는 방식 자체가 원인**이라, 오늘 것을 차수별이 아니라 **주제별 한 블록으로 통합**(X-40·X-41·릴리스 2건·배포 채널·정리/발행)하고 **08-02 5~6차는 하루 한 줄 색인으로** 내렸다 → **236→162줄**(원문은 journal 보존 — [16 §1](16-doc-git-conventions.md)) ② **TODO X-40이 삭제된 브랜치를 가리킴** — `🚧 실기 QA 대기(fix/x40-refresh-probe)`인데 그 브랜치는 병합 후 삭제됐고 기능은 **`0.15.0`으로 배포**됐다 → **"배포 완료 — 클라우드 실검증 대기"** 로 정정하고 **무엇이 진짜 검증 대상인지**(로컬 NTFS가 아니라 동기화 폴더·네트워크 경로)와 사용자 매뉴얼 링크를 함께 달았다. **무변동 확인**: winget PR 2건 여전히 OPEN·`New-Manifest`(우리 측 조치 불요) · choco 무변동 · **위키는 직전 발행분과 일치**(추가 발행 불요) · 구버전 문자열 잔존 없음(남은 `0.13.0`/`0.8.1`은 **카탈로그 현재 버전**이라 정확). 상세 [journal/2026-08-11.md](journal/2026-08-11.md).
- **내용 정리 2차 + 진행사항 최신화(사용자 지시 — 릴리스 후·코드 무변경)**: 착수 시 트리 clean·main↔origin 0/0이라 커밋 대기분은 없었고, 대상은 **릴리스가 만들어 낸 문서 지연 5건**. 오전 정리에서 "릴리스마다 사용자 문서를 같은 트랜잭션에 넣지 않으면 반복된다"고 적었는데 **기능 문서 쪽에서 정확히 그 일이 또 났다**(오전엔 버전 표기·설치 페이지만 챙기고 **기능 설명 페이지는 손대지 않았다**). ① **위키가 없는 버튼 크기를 안내** — [기능-도구모음과-메뉴](wiki/기능-도구모음과-메뉴.md)의 **"26px 바"**(07-19 값. 오늘만 26→24→32→28로 세 번 더 움직였다) → **도구 모음 28/아이콘 20 · 퀵 런처 24/아이콘 16**으로 정정 + **두 바 높이 독립** 명시 ② **같은 페이지에 버튼 하나 누락** — show 그룹이 `숨김·점 파일` 2개로만 적혀 **08-02 폴더 우선 정렬 토글**(X-39)이 빠져 있었다 ③ **자동 갱신 설명이 X-40 이전 세대** — [기능-파일-조작](wiki/기능-파일-조작.md) "폴더 watcher" 절이 `300ms 디바운스 재로드·실패 시 F5`만 말해, 오늘 추가한 **두 번째 갱신 계기(변경 프로브)** 가 통째로 빠져 있었다. 사용자는 **클라우드에서 자동 갱신되는 이유를 알 길이 없었다** → 계기 2종 표 + 왜 필요한가(통지가 안 오는 클라우드 가상 드라이브·네트워크) + 배터리(비활성 시 완전 정지) + **한계 3종**(4096 초과 폴더의 내용 변경·`::CLOUD:` 직접 연결 탭 비대상·F5) ④ **CLAUDE.md·STATUS 다음 단계가 이미 한 일을 "판단 대기"로** — 둘 다 "winget `0.14.0` 제출 여부 = 사용자 판단"에 멈춰 있었고(저녁에 `0.16.0`으로 제출 완료), STATUS 최우선 QA는 **"`fix/x40-refresh-probe` 미병합"**(병합·배포됨)이었다 → 정정하고 X-40 QA는 **"로컬 NTFS는 기존 watcher로도 잡히므로 클라우드/네트워크가 진짜 검증 대상"** 임을 못박음 ⑤ **[21 §8](21-distribution.md) 채널 표** — 카탈로그 버전(Portable 0.13.0·설치형 0.8.1)과 **제출 중인 0.16.0 PR 2건**을 분리 기재·choco는 "push 제외 유지(사용자 지시)" 명시. 제출 이력에 **0.14.0·0.15.0을 건너뛴 이유**(winget은 중간 버전 건너뛰어도 무방 — 사용자는 최신만 받는다)와 **체크섬 절차**(SHA256SUMS + 재다운로드 `Get-FileHash` 이중 확인)·**제출 방식**(포크 브랜치 + Contents API — 클론이 무겁다)을 규약으로 기록. **채널 실측**: winget PR 2건 **OPEN·`New-Manifest`**(검증 대기) · 카탈로그 아직 Portable 0.13.0/설치형 0.8.1 · choco 무변동. ⑥ **위키 발행**(사용자 승인 — `8d76cc4..01178dc`, 5파일 58+/15−): 마지막 발행이 **08-02**여서 오늘 고친 분이 전부 미발행이었다 — 08-02에 "발행을 트랜잭션 꼬리에 붙이지 않으면 반복된다"고 적어 둔 일이 **9일 만에 재발**(그때는 11일치). **`README.md`는 발행 제외**(위키 페이지가 아니라 `docs/wiki/` 폴더 자체의 설명서 — 그대로 올렸으면 엉뚱한 `README` 페이지가 생겼다). **"미발행 15건"은 과대 집계였다** — 파일 해시 비교로 15건이 나왔으나 스테이징하니 **실제 내용 차이는 5건**이고 나머지는 **CRLF/LF 줄바꿈**이었다(해시 비교는 발행 판단에 부적절 — 다음부터 `git diff --stat`). 발행 후 소스를 덮어써 `git status` 빈 출력 = **로컬 소스 ≡ 발행본** 확인. 상세 [journal/2026-08-11.md](journal/2026-08-11.md).
- **릴리스 `0.15.0`·`0.16.0` + winget 2건 제출(사용자 지시 — main 직커밋)**: ① **`0.15.0` 배포**(X-40 자동 갱신 + X-41 1라운드 값) 직후 **사용자 실물 QA로 바 크기가 3라운드 만에 갈렸다** — 시안 3택으로 고른 셀 24가 **실물에서 "충분치 않다, 32 정도"** 로 뒤집혔고(참고 이미지 제시), 32로 올리자 이번엔 **"빠른 실행은 이전 크기가 낫다"+"도구 모음은 기존과 현재의 중간"** → **확정 = 도구 모음 28(아이콘 20)·퀵 런처 24(아이콘 16)**. **시안 미리보기로는 안 잡히고 실물에서만 갈린 건**이다 ② **구조 변경 = 두 바의 높이 분리**(`launch_row_h` 신설 — 종전엔 `launch_h = tool_h`로 도구 모음 값을 재사용해 **한쪽만 키울 수 없었다**). 아이콘은 chrome.rs `b.h-8`이 따라오고(28→20·24→16) **둘 다 래스터 버킷과 정확히 일치**해 늘림이 없다 ③ **dw.rs 24 버킷 신설** — 2라운드(아이콘 24)에서 종전 구간이 **24를 20 버킷으로 보내 20px 래스터를 24로 늘려 그리던 것**을 발견. 최종값(20)에선 미사용이나 **고DPI 125%에서 쓰인다**(TODO X-41 잔여 β를 앞당겨 해소) · 런처 아이콘 원본도 `L|`(라지 32px)에서 **스몰 16px로 원복** ④ **[Release 0.16.0](https://github.com/SosomLab/nexa-dir2/releases/tag/0.16.0) 자산 5종**(포터블 **3.66MB** ≤10·설치형 3.26MB·zip 2·`SHA256SUMS.txt`) ⑤ **winget PR 2건**(0.15.0 보류분을 한 번에 소화 — 같은 패키지에 PR이 겹치는 08-02 상황 회피) [#415214](https://github.com/microsoft/winget-pkgs/pull/415214) Portable · [#415215](https://github.com/microsoft/winget-pkgs/pull/415215) **설치형 = 0.8.1 최초 등록(08-06 병합) 이후 첫 버전 업데이트**(07-31 피드백대로 `DisplayVersion` 계속 제외 · locale 설명이 `0.8.1` 시점 **"Dual-language UI"**·클라우드 없음에 멈춰 있어 현행 기능으로 갱신). 체크섬은 릴리스가 발행한 `SHA256SUMS.txt` + **자산 재다운로드 `Get-FileHash` 이중 확인** · 제출은 **Contents API로 포크 브랜치에 파일만 올리는 방식**(winget-pkgs 클론이 매우 무겁다)·올리기 전 **1.12.0 스키마 파싱 검증** ⑥ **choco는 사용자 지시로 제외** — 0.8.1이 아직 모더레이션에 잠겨 있어 **이중 큐를 만들지 않는다**. 두 릴리스 모두 **팩 success·push 미실행**을 로그로 확인 ⑦ **사용자 문서를 같은 트랜잭션에**(README·위키 3종 `0.16.0`·예산 표 2행) — 오전 정리에서 "릴리스마다 함께 갱신하지 않으면 반복된다"고 적은 것의 이행. ⚠️ **플래키 1건** — 재조정 도중 `cargo test --workspace` 한 번에서 nexa-app 106개 중 1개가 실패했으나 **단독 실행과 연속 8회 재실행이 모두 green**이라 어느 테스트인지 특정하지 못했다(근거 없는 지목은 하지 않고 기록만 남긴다). 상세 [journal/2026-08-11.md](journal/2026-08-11.md).
- **내용 정리 + 진행사항 최신화(사용자 지시 — 같은 브랜치)**: 정리 4건 중 **셋이 "바깥으로 보이는 면"의 노후**였고, **08-02 4차가 내린 같은 진단이 9일 만에 재발**했다(내부 문서만 갱신하는 습관이 원인 — 릴리스 때마다 사용자 문서를 같은 트랜잭션에 넣지 않으면 반복된다). ① **위키가 사실과 반대를 안내** — [설치와 다운로드](wiki/설치와-다운로드.md)가 winget 설치형을 **"⏳ 심사 중(승인 후 제공)"** 으로 적어 두었으나 **08-06 병합돼 이미 설치 가능**했다(오늘 1차 점검에서 확인한 사실이 사용자 문서에 반영되지 않은 것). → **✅ 제공 중** + **§4 winget 설치형 절 신설**(`winget install SosomLab.NexaDir`·`--scope machine`·**제거해도 데이터 보존**이 포터블 채널과 다른 점 명시)·"준비 중"에는 **Chocolatey만** 남기고 "자동 검증 통과·사람 검토만 남음" 명기 ② **버전 표기 3주 노후** — 루트 README·위키 Home·개발 여정·설치 페이지가 전부 `0.13.0` → **`0.14.0`** + README 설치 절에 **설치형 winget 명령 추가** + 개발 여정 예산 표 `0.14.0` 행 ③ **자산 크기를 추측하지 않고 실측**(`gh release view 0.14.0` — 포터블 exe **3.65MB**·zip 1.64MB·설치형 **3.25MB**·zip 2.71MB). 이 과정에서 **단위 혼용을 발견** — 프로젝트는 GitHub 표기와 같은 **10진 MB**인데 오전에 내가 적은 `B2 3.49MB`는 **MiB**였다(같은 파일 3,659,264 B = 3.66 MB = 3.49 MiB) → STATUS·DEVLOG·TODO·BRANCHES·journal **5곳을 3.66MB로 정정** ④ **clippy 게이트 복구** — `cross_move_tab` 인자 8개를 **`CrossMove` 구조체로 묶어**(다섯 값이 "어느 탭을 어디로"라는 한 뜻이라 호출부도 읽기 낫다·`adopt`에 미리보기/커밋 의미 주석) **`-D warnings` exit 0** 확인. 08-02부터 깨져 있던 게이트가 닫혔다. 상세 [journal/2026-08-11.md](journal/2026-08-11.md).
- **X-40 외부·클라우드 변경 무갱신 해소 + X-41 도구 모음 24×24(사용자 요청 2건 — `fix/x40-refresh-probe` 2커밋, 실기 QA 대기)**: 보고 = "외부·특히 클라우드에서 저장·동기화되면 목록이 안 바뀌어 **갱신 버튼을 눌러야 한다**". 통지 경로 전수 점검으로 원인을 넷으로 갈랐고, **공통 뿌리는 갱신 계기가 `ReadDirectoryChangesW` 통지 하나뿐**이었다는 것 — ① **활성화 갱신 계기 부재**(`WM_ACTIVATEAPP` 핸들러가 **아예 없었다**. 밖에서 저장하고 돌아와도 재열거를 시킬 자리가 없어 F5 전까지 낡은 채. 탐색기는 활성화 시 갱신한다) ② **통지가 오지 않는 경로에 폴백 부재**(RDCW는 로컬 NTFS 규약 — 클라우드 가상 드라이브[DriveFS 등]·네트워크는 **구독이 성공해도 통지가 안 올 수 있다**. 그때 watcher는 죽은 게 아니라 **조용한** 것이라 `is_alive()`가 계속 true = **07-31 생존 자가 치유가 원리적으로 못 잡는 사각지대**) ③ **디바운스 무한 연기**(`SetTimer` 재무장이 만료를 미루므로 동기화가 300ms보다 촘촘히 변경을 쏟으면 **동기화가 끝날 때까지 한 번도 재로드되지 않는다** — 코얼레싱이 의도대로 동작한 결과라 결함으로 안 보였고, 보고의 "동기화된 경우"가 정확히 이 경로) ④ **죽은 watcher 치유가 사용자 상호작용 의존**(`sync_watchers`는 `update_status`에서만 호출 — 07-31이 죽음 **관측**은 만들었지만 **호출 계기**는 안 만들었다. 유휴로 보는 동안 죽으면 되살릴 자리가 없고, 자니터는 트림만 하며 유휴 트림 후엔 자기 타이머까지 끈다). **해결 = 통지에 의존하지 않는 두 번째 계기** — **[fsprobe.rs](../crates/nexa-app/src/fsprobe.rs) 신설**(1단계 폴더 mtime **O(1)** = 추가/삭제/이름변경 · 2단계 상한 4096 내 1회 열거[수·바이트·최신 mtime] = **같은 이름 덮어쓰기**. 상한 초과는 2단계 포기로 폴링 비용 상한. `std::fs`만 써 **전 플랫폼 테스트**) + **`WM_ACTIVATEAPP`·최소화 복원 → `refresh_on_return`**(재구독→즉시 프로브→폴링 재개. **변경 없으면 재로드도 없다**) + **`TIMER_FSPOLL` 3s 폴링**(**창 활성일 때만** — 비활성 시 `KillTimer`로 **유휴 백그라운드 0% 규율[DR-2] 유지**, 안 보는 동안의 낡음은 다음 활성화가 해소. 틱마다 `sync_watchers`도 불러 ④도 닫힌다) + **`WATCH_MAX_MS` 1s 연장 상한**(폭주 중에도 최대 1.3초 안에 1회 갱신). 함정 둘을 설계로 막았다 — **재로드 직후 기준선 재프로브**(안 하면 방금 반영한 변경으로 폴링이 다시 무장해 **진동**) · **`WM_SIZE`는 최소화 복원만 계기로**(리사이즈 드래그 내내 오므로 무조건 프로브하면 **드래그마다 폴더 재열거**. `note_activity`가 `trimmed`를 지우므로 호출 **전에** 읽는다). **X-41** = 시안 3택 제시 후 사용자 **C안 확정**(버튼 셀 24×24 축소·아이콘 16 유지) → `tool_h` 26→24 DIP(**퀵 런처 바 공용** = "도구모음 및 툴바")·chrome.rs 아이콘 산식 `b.h-10`→`b.h-8`(여백 5→4px로 아이콘 크기 불변)·SVG 버킷 무변. 신규 테스트 5 — **106 green(nexa-app)·78(nexa-gui)·비Windows check 통과·B2 3.66MB·B3 통과**·release 기동 스모크(파일 생성이 버튼 없이 반영됨을 확인 — 단 이 경로는 기존 watcher로도 잡히므로 **무회귀 확인이지 폴백 검증은 아니다**. 클라우드 실검증 = 사용자 QA). 곁들여 **clippy 게이트가 08-02부터 깨져 있음을 발견**(`cross_move_tab` 인자 8/7 — stash 대조로 **이번 변경 이전 HEAD에서도 재현**. clippy는 ci.yml에 없어 로컬 규율로만 지켜진다). 상세 [journal/2026-08-11.md](journal/2026-08-11.md).
- **저장소 최신화 + 배포 채널 상태 확인(사용자 지시 — main 직커밋·코드 무변경)**: ① **저장소는 최신화할 것이 없었다** — 트리 clean·`fetch` 무변·**main↔origin 0/0**·최신 태그·릴리스 `0.14.0`(08-02). 9일간 커밋 무변동(사용자 QA 대기 구간) ② **winget 심사 대기 2건이 08-06에 둘 다 병합됐다** — Portable 0.13.0 [#410978](https://github.com/microsoft/winget-pkgs/pull/410978) **MERGED 00:16 UTC**(`Waived-Policy-Test-1.2`+`Moderator-Approved` = 0.11/0.12와 같은 패턴이나 waiver까지 5일) · 설치형 [#404528](https://github.com/microsoft/winget-pkgs/pull/404528) **MERGED 00:46 UTC** = **최초 등록 완료**(07-19 제출 → 18일. 07-31 `Needs-Author-Feedback`[`DisplayVersion` 제거] 대응이 마지막 병목이었고, 그 뒤 `Policy-Test-1.2`는 **waiver 라벨 없이 그냥 제거**된 채 승인 — 포터블과 처리 방식이 달랐다). `winget search SosomLab` 실측으로 **두 채널 라이브 확인**(설치형 `0.8.1` · Portable `0.13.0` — `show --versions` = 0.8.1/0.11.0/0.12.0/0.13.0) ③ **∴ "심사 대기 4건"이 "choco 2건"으로 줄고, winget에 남은 것은 심사가 아니라 버전 격차** — Portable `0.14.0`은 08-02에 "0.13.0 PR OPEN 중복 회피"로 보류했던 조건이 해소돼 지금 제출 가능하고, 설치형은 `0.8.1`로 **6버전 뒤짐**(첫 등록이 오래 걸려 후속을 안 올렸다. 최초 등록이 끝났으니 이후는 통상 경로). 제출 여부 = 사용자 판단 ④ **Chocolatey 2종 무변동** — 페이지 실측(OData는 미승인분 미반환) 결과 둘 다 0.8.1 **Ready = awaiting moderation**이고 마지막 이벤트가 **07-20 02:2x 스캔 Flagged** 그대로 = **22일 무이벤트**. 자동 3단계(Validation·Verification·Scan)는 07-19~20에 끝났고 **사람 검토만 안 붙었다** ⑤ **`CHOCO_PUSH` 실측 정정** — `gh variable list`가 **exit 0·빈 목록**을 반환(08-02 기록의 "변수는 토큰 권한으로 못 읽어 스텝 결과로 확증"은 현재 해당 없음). 결과 = **미등록**이 맞고, 승인 시 여기에 `CHOCO_PUSH=true`를 넣으면 후속 버전부터 자동 재개(코드 무변). 상세 [journal/2026-08-11.md](journal/2026-08-11.md).

## 2026-08-02

- **릴리스 0.14.0 + 채널 상태 재점검(사용자 지시 — main 직커밋)**: ① **채널 재점검(원천)** — winget Portable 0.13.0 [#410978](https://github.com/microsoft/winget-pkgs/pull/410978) OPEN(`Azure-Pipeline-Passed`+`Policy-Test-1.2` waiver 대기 — 0.11/0.12와 같은 패턴) · 설치형 [#404528](https://github.com/microsoft/winget-pkgs/pull/404528) OPEN(`Needs-Author-Feedback` 해제 유지) · **choco 2종 모더레이션 미승인 유지**(07-20 플래그 후 무이벤트 — OData는 미승인을 안 돌려줘 페이지 실측). 셋 다 조치 불요 ② **0.14.0 배포** — 승격 `1eb11b1` → 태그 push → 게이트 전부 통과 → [Release 0.14.0](https://github.com/SosomLab/nexa-dir2/releases/tag/0.14.0) 자산 5종(포터블 **3.65MB**·`SHA256SUMS.txt`) · **choco 팩 success/push skipped 확인**(`CHOCO_PUSH` 게이트 의도대로). 내용 = X-38 7-Zip 드롭 픽스 + X-39 보기 옵션 범위·탭 DnD ③ **winget Portable 0.14.0 PR 보류** — 같은 패키지 0.13.0 PR이 OPEN이라 중복 제출 회피(병합 후 최신 버전 제출) ④ [21 §8](21-distribution.md) 표 저녁 재점검·CLAUDE.md `0.14.0`. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **탭 드래그 미리 보기·ESC 취소(사용자 QA — 같은 브랜치 2차)**: 보기 옵션·설정은 QA 통과. 개선 2건 — 패널 간 이동이 해제 시점에만 일어나 **위치가 안 보였고**, ESC 취소가 없었다. 수정 = **"미리 보기 = 실이동" 규약 통일**: 반대 패널 **탭 바** 위로 끌면 즉시 구조 이동 + `begin_drag` **드래그 이양**(그 바 안 재정렬 연속·원 패널 복귀도 대칭). **값 채택은 커밋으로 유예**(`cross_move_tab(adopt)` 분리 — 미리 보기 중 값을 건드리면 ESC 복귀가 손실. 커밋 기증자는 대상 패널의 **다른 탭**: 이동 탭이 이미 활성이라 active 값을 쓰면 자기 채택 함정). **ESC** = 프레스 시점 원위치 스냅샷(임계 통과 후 무장하면 첫 Move가 이미 지나가 틀어짐) → `cancel_tab_drag`가 동일/교차 어디에 있든 원 패널 원위치·포커스 복귀. tabbar 테스트 1 추가 — **78 green(nexa-gui)·101(nexa-app)·clippy 0**·비Windows check 통과. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **보기 옵션 적용 범위(전체/좌-우 패널/활성 탭) + 패널 간 탭 DnD(사용자 요청 4건 증분 — `feat/view-scope-tab-dnd` 2커밋, 스택·실기 QA 중)**: 숨김/Dot/폴더우선의 적용 방식을 **설정 3택**(`view_scope` — 기본 좌-우 패널)으로. 설계 = **값의 SSOT를 탭으로 내리고**(Tab 필드 3 + 열거 스탬프) 범위 설정은 토글의 **전파 폭만** 결정(tab=활성 탭·panel=활성 패널 전 탭·global=양 패널 전 탭) — 세 모드가 한 구조로 수렴·모드 전환 마이그레이션 없음. State 전역 필드는 **활성 탭 미러**로 재정의해 기존 읽기 지점 무수정, `update_status` 단일 길목이 미러·툴바/메뉴 체크를 동기(**탭/패널 전환 즉시 추종**) + **stale 수렴**(비활성 탭에 닿은 토글·세션 복원 불일치는 가시화될 때만 재열람 — `enum_filters` 스탬프). 열거는 panel.rs `active_ctx()` 관문으로 탭 값 보정(교차 패널 오염 차단). **탭별 저장 부담 검토(질의) = 무시 수준**(세션이 이미 탭 단위 — `panel{i}.views` 탭당 3비트 1줄·디바운스 편승). 툴팁 = "라벨 — 범위"(prefs 옵션 키 재사용·범위 변경 시 툴바 재구성). 설정 창 체크박스 3종 = **범위 무관 전체 일괄**로 규정(desc 명문화). **패널 간 탭 DnD**: tabbar `dragging()` 노출(모듈 doc "후속" 이행) — 반대 패널에서 해제 시 `cross_move_tab`(탭 바 위치 삽입/끝·이동 탭 활성+포커스 이양·범위≠tab이면 대상 패널 값 채택·마지막 탭 거부). 신규 테스트 2·세션 왕복 확장. **101 green·clippy 0·비Windows check 통과**. 잔여 = Ctrl 복제 드래그·드래그 중 시각 피드백. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **폴더 우선 정렬 툴바 토글(사용자 요청 — `feat/toolbar-folders-first` 1커밋, dnd 브랜치 스택·실기 QA 대기)**: show 블록 세 번째 버튼(숨김→Dot→폴더우선). **시안 4종 브라우저 미리보기**(16/20/32px × 4상태) 제시 후 **사용자가 D(폴더 안 ↑) 확정**. `folders-first.svg`(32×32 규약·순수 스트로크 = 다크 전용판 불요) + `TOOLBAR_BLOCKS` `foldersfirst`(기존 설정은 누락 보충 자동 삽입) + `CMD_TOGGLE_FOLDERS_FIRST`(툴팁 = `pref.sortFoldersFirst` 재사용 — 신규 lang 키 0 · 전역 값이라 **양 패널 전 탭 즉시 재정렬** · 설정 창 G-13 체크박스와 양방향 동기·영속). 99 green·clippy 0·비Windows check 통과. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **7-Zip 드롭 수신 결함 픽스 1~2차(사용자 QA 연속 — `fix/dnd-delayed-render` 2커밋, 실기 QA 후 병합 대기)**: 7-Zip에서 패널로 끌어 놓으면 **`7z<hex>` 빈 폴더만 생기던** 결함. **1차** — 원인 = `DropTarget`이 DragEnter에서 한 번 캐시한 CF_HDROP을 Drop에서 그대로 사용. 7-Zip은 **지연 렌더링 소스**라 드래그 중엔 추출 전 임시 폴더만 광고하고, 드롭 확정 후 타깃이 `GetData(CF_HDROP)`를 **재호출해야 실제 추출**이 일어난다(탐색기 규약) → `hdrop_paths()` 공용화 + Drop 재조회. **2차 QA가 잔여 결함을 드러냄**(단일 파일만 정상 — 4개 중 2~3개·폴더 안 1개): 추출은 되지만 우리 전송이 **비동기**라, Drop 반환 직후 `DoDragDrop`을 끝낸 7-Zip의 **임시 폴더 삭제와 경쟁**해 앞쪽 항목만 생존. 수정 = **Drop 반환 전 확보** — 재조회 결과가 DragEnter 광고와 다르면(지연 렌더링 시그니처 — 일반 소스는 불발동) `steal_volatile()`이 항목을 `%TEMP%\NexaDir\dnd-…`로 **같은 볼륨 rename**(크기 무관 즉시·소스는 우리가 반환해야 삭제 시작 = 무경쟁) 후 전송을 **Move 강제**(스테이징 자연 소거). rename 실패 항목만 기존 동작 강등. 신규 테스트 3(왕복·집합 비교·스테이징 확보). **99 green(nexa-app)·clippy 0**·비Windows check 통과. 실기 QA 대기(다중·폴더·대용량 — 실행 중 exe가 빌드 잠가 앱 종료 후 재빌드). 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **사용자 대상 문서 정리 2차 — 루트 README·위키(사용자 요청 — `docs/user-docs-tidy-0802`)**: 같은 지시가 재차 와서, 1차가 훑은 내부 문서 대신 **손대지 않은 쪽 = 사용자가 보는 문서**(루트 README·`docs/wiki/` 23페이지)를 대상으로 삼았다. 6건 전부 진짜 노후. ① **루트 README가 3주 낡음** — `현재 상태: M0 기반·게이트 진행`(07-11 문구)이 저장소 첫 화면에 남아 있었다. 내부 문서만 갱신해 온 결과 **바깥 면이 가장 오래 방치**된 셈 → 포스트 M5·`0.13.0`+실측 한 줄로 교체하고 **설치 절 신설**(README에 설치 방법이 아예 없어 위키로 가는 경로가 없었다) ② **B3 종수는 추측하지 않고 실측** — 위키 Home `실측(0.11.0) 1.50MB·17종`을 고치며 "17+4=21"로 적을 뻔했으나 08-02 빌드로 게이트를 실제 실행(`api-ms-*` 포함 **21종·B3 통과**). 산술과 일치했지만 일치를 확인한 것과 계산해 적는 것은 다르다 ③ **위키 설계-결정이 자기 자신과 모순** — 같은 페이지에서 DR-7 표는 `Starlark`(07-14 판), 아래 ADR-0004 절은 wasmi(07-27 정정이 본문만 고치고 표를 지나침) → 표 정정 + DR-2에 B3 +4 반영 + **ADR-0006 절 신설**(`0.13.0` 핵심 기능인 클라우드가 설계 결정 페이지에 아예 없었다) ④ **개발 여정이 `0.7.0`에서 멈춤** — "단 9일(07-11~07-19)" 프레이밍 그대로여서 이후 2주·릴리스 6개·플러그인 교체·클라우드가 통째로 비어 있었다 → 4개 절 추가 + 예산 표 `0.11.0`~`0.13.0` 행. **모르는 값은 비웠다**(0.11/0.12 B3 종수는 기록이 없어 "통과"로만·B1은 07-15 이후 미재실측 각주). 교훈 문단에 08-01 `bcrypt.dll` 사례 추가 — **"기존 bcryptprimitives로 되겠지"를 게이트 실행이 뒤집은** 건 ⑤ **기능-하단-도크가 있는 기능을 없다고 말함** — `향후 Starlark 플러그인 예정·현재는 내장만`(런타임도 틀리고 "예정"도 아니다 — md 뷰어 플러그인이 Mermaid까지 렌더하며 동봉돼 있다) ⑥ **설치 페이지 자산 정보가 07-31 이전** — 07-31 추가·08-02 첫 실배포된 **zip 2종·`SHA256SUMS.txt`가 안내에 없었다**(브라우저 차단 회피용으로 만든 자산인데 정작 안내가 빠져 목적 미달) → 자산 5종·차단 시 zip·`Get-FileHash` 대조 보강. **위키 발행**(사용자 승인 — `ce22af5..8d76cc4`): 클론해 보니 마지막 발행이 **07-22**였다 — 보류하던 것이 오늘분이 아니라 **11일치**였고, 그중엔 08-02 1차에 쓴 **기능-클라우드 222줄이 통째로 미발행**이었다(설정 "파일 직접 편집"·개발-플러그인 wasmi 판·설계-결정 ADR-0006도 함께). 11파일 +383/-111 발행. **문서를 고친 시점과 사용자가 볼 수 있는 시점이 11일 벌어져 있었다** — 위키는 별도 저장소라 main push에 딸려가지 않으므로, 발행을 트랜잭션 꼬리에 붙이지 않으면 반복된다. 코드 무변경. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **문서 내용 정리 + 진행사항 최신화(사용자 요청 — `docs/content-tidy-0802`)**: 착수 실측이 트리 clean·main↔origin 0/0이라 **커밋 대기분이 없었고**, 정리 대상은 문서 자체의 어긋남 5건이었다. ① **STATUS "한 장" 규약 재이탈**(07-24에 175줄로 압축한 것이 차수 25건·**565줄**로 재팽창) → 08-02 2건만 원문 유지·**08-01 이전은 하루 한 줄 색인**으로 압축(**565→161줄**, 원문은 journal 보존) ② **DR-7이 두 세대 낡음** — STATUS §1은 `.NET 비이관`(07-14 이전 판)·[10](10-decision-record.md) §1은 `Starlark`(07-14 판)인데 실제는 **wasmi**([ADR-0005](25-adr-0005-wasm-plugins.md) 07-26). DR 표는 결정 SSOT라 **새 세션이 틀린 전제로 출발**하는 자리 → 규약대로 과거 문구는 취소선 보존 + 재개정 명기 ③ **ADR 색인 결손 2건** — [10 §1-1](10-decision-record.md)이 ADR-0004에서 멈춰 **ADR-0005·0006이 결정 기록 본문에 없었다**(README에는 있어 비대칭). ADR-0004는 통째 Superseded가 아니라 **런타임 부분만 대체**임을 상태 열에 명기 ④ **예산 표 13일 정체** — B2 `1.45MB`(07-20) → **3.44MB**(08-02): wasmi +1.68·클라우드 +0.26을 거쳤는데 표가 멈춰 있어 **게이트 여유를 8.55MB로 오인**할 수 있었다(실제 6.56). B3 시점 07-13 → **08-01 개정**(+4 DLL). B1은 07-15 이후 미재측정이라 **값을 고치지 않고 "미재측정" 명기**(없는 실측을 만들지 않는다) ⑤ **완료분이 진행 중으로 남아 있었다** — [TODO](TODO.md) X-36 🚧→**✅**(08-02 병합·`0.13.0` 배포) · X-37 라벨을 **"구현 완료 — 잔여는 배포 선행 조건"**으로 재분류(판단은 08-02에 내렸는데 라벨이 08-01 문구) · [MILESTONES](MILESTONES.md) 포스트 M5 행이 `0.11.0`에서 멈춘 것을 `0.13.0`·클라우드까지 확장 · [10 §3](10-decision-record.md)이 **M0 착수 계획 그대로**여서 완료 표기 + "진행 상태의 원천은 STATUS·TODO" 역할 분리 명시. 곁들여 **CLAUDE.md** §7 헤더 날짜(07-24→08-02)·`0.11.0`→`0.13.0`·**클라우드 선행 조건 3건 항목 승격**(TODO 안에만 있어 진입 문서에서 안 보였다)·X-33 착수 시 ADR 번호 **0005→0007 정정**(0005는 wasmi가 사용 중) + §4에 비Windows cfg 검사 교훈 한 줄. 코드 무변경. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **릴리스 0.13.0(Chocolatey 제외) + winget Portable 업그레이드 + 비Windows CI 복구 2건(사용자 지시)**: ① **0.13.0 승격·태그**(X-36 클라우드 링크 + X-37 직접 클라이언트 — Cargo.toml workspace·lang 3종 `@app`. 태그 push = 공개 Release 생성이라 규약대로 사용자 확인 후 진행) → [Release 0.13.0](https://github.com/SosomLab/nexa-dir2/releases/tag/0.13.0) **자산 5종 첫 실배포 확인**(07-31에 추가한 zip 2 + `SHA256SUMS.txt`가 실제 태그에서 정상 생성 — 그때는 dry-run만 했다) · **Chocolatey push 스텝 `skipped` 확인**(`vars.CHOCO_PUSH` 게이트가 의도대로 동작 — 변수는 토큰 권한으로 못 읽어 스텝 결과로 확증) ② **winget Portable 0.13.0 PR** [#410978](https://github.com/microsoft/winget-pkgs/pull/410978)(3파일 생성·SHA-256은 릴리스가 발행한 `SHA256SUMS.txt`와 **대조 확인**·Description 하이라이트에 클라우드 추가·`kiros33` 계정 제출 후 원복) ③ **main CI가 비Windows에서 깨져 있던 것을 발견·복구**(Windows 잡·릴리스 산출물은 무사) — **1차** `cannot find win/dialog in crate`: `cloudfs`가 전 플랫폼 컴파일 대상인데 `crate::win`·`crate::dialog`를 **43곳** 참조하는 UI 결합 워커였고 소비자도 `win.rs` 하나뿐이라 `ctl`·`watcher`와 같은 `#[cfg(windows)]`로 정렬 · **2차** 첫 오류를 걷어내자 드러난 `E0282`: `cloud::detect()`의 `Vec::new()`가 비Windows에서 원소 타입 추론 근거를 잃는다(cfg 블록 안 `detect_*(&mut out)` 호출이 사라지므로) → 타입 명시 + `Path` cfg 게이팅 · **3차** 컴파일이 뚫리자 드러난 테스트 실패 2건 — CI 이력 확인 결과 core 잡은 **최소 07-27부터 연속 실패**였다(클라우드 작업이 만든 게 아니라 **원래 깨져 있던 것**. Windows 잡만 통과해 일주일 넘게 미발견 — "main 항상 green"이 그동안 깨져 있었다): `open_any_root_finds_a_root`는 후보가 드라이브 문자·`%USERPROFILE%`뿐이라 비Windows에서 항상 None → cfg 게이팅 · `sample_wasm_plugin_end_to_end`는 Mermaid flowchart의 **3단 폴백**(이미지 → 아트 → **원문 모노 블록**) 중 앞의 둘만 단언해 실패 → 단언 확장. **3잡 전부 green 복귀** ④ **절차 보완**([18 §2·§4](18-build-and-test.md)) — 두 번 연속 같은 사각지대에 빠진 것이 문제다. Windows에서 `cargo test`가 green이어도 **cfg 블록이 사라지는 경로는 전혀 검증되지 않는다** → `cargo check --workspace --all-targets --target x86_64-unknown-linux-gnu`를 명령 목록에 추가하고 core 잡이 사각지대임을 명시(링커 불필요·2차 수정은 실제로 이 명령으로 검증). [21 §8](21-distribution.md) 채널 표 갱신 — **choco 정체의 성격 정정**(큐 대기가 아니라 **07-20 바이러스 스캔 플래그** 후 13일 무이벤트 — 무서명 Rust exe 오탐이라 모더레이터 면제 처리가 필요). 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **클라우드 사용자 매뉴얼 신설 + 콘솔 브랜딩 자산(사용자 요청)**: "사용자별 Client ID 설정을 위키·설명서에 꼭 넣어 달라" → 착수하며 **위키에 클라우드 문서가 아예 없음**을 확인(X-36 링크·X-37 직접 연결 전체 미기재) → 요청 항목만 끼우면 문맥 없는 파편이 되므로 **기능 전체를 매뉴얼화**. ① **`기능-클라우드.md` 신설**(222줄) — **Link vs Connect 구분표를 맨 앞에**(둘을 섞으면 나머지가 전부 헷갈린다)·연결 절차·다른 계정 로그인(프라이빗 창 URL)·가능한 작업·**토큰 DPAPI 보관**(다른 PC 이동 시 재로그인 = 불편이 아니라 **의도된 안전장치**)·**서비스별 발급 절차 3종**(실제로 걸려 넘어졌던 함정 그대로: OneDrive는 **퍼블릭 클라이언트**여야 하고 **시크릿 금지**·`127.0.0.1`은 포털 등록 거부 / Google은 **데스크톱 앱에도 시크릿 필수**·`drive.file`은 앱 생성 파일만 보여 탐색기로 무용 / Dropbox는 **Full Dropbox**[사후 변경 불가]·리디렉션 **3개 정확 일치**)·한도표·문제 해결표 8행 ② **`기능-설정.md` "파일 직접 편집" 절 신설**(설정 창에 없는 항목을 다루는 자리를 처음 마련 — `cloud_client_id/secret` 키 표·**서비스별 시크릿 필요 여부**·**변경 후 재연결 필수**·`data\secrets\` 영속 위치) + 색인 3곳 배선 ③ **앱 안내 문구의 잘못된 리디렉션 URI 정정**(lang 3종 — `http://127.0.0.1(포트 임의)`로 안내하고 있었으나 실제는 **localhost 고정 포트**. **안내대로 따르면 등록이 실패**했고, 시크릿 안내도 없어 Google·Dropbox는 발급해도 로그인 불가한 문구였다) + **사문 키 2개 제거**(`cloud.readonly`·`cloud.err.crossAccount` — 구현 후 **사실과 반대**가 됐고 참조도 없음) ④ **`packaging/branding/` 신설** — 콘솔 3사 문구 SSOT(**영문 Description**·**권한 사유표**[심사 답변용]·콘솔별 입력 위치) + 아이콘 64/256 PNG(**`.ico`에서 무손실 추출** — 내부가 이미 PNG·투명도 보존·재생성 스니펫 동봉). 특기 = Dropbox 아이콘은 **계정에 먼저 올린 뒤 고르는** 방식(로컬 선택 아님) · **Privacy policy URL이 비어 있어 프로덕션 신청 전 필요**. 254 green·clippy 0·3.44MB. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **3사 배포 인원 한도 점검 — ADR-0006 §2-4-1 신설(사용자 질의)**: "OneDrive·Dropbox는 사용자들이 쓸 수 있어 보이는데 전체 점검해 달라" → 원천 확인 결과 **셋 다 무제한이 아니고 벽의 종류가 다르다**. **Dropbox** = 누구나(500명)이나 **50명에서 2주 카운트다운**(미신청 시 신규 연결 동결·승인은 무료·수일) · **OneDrive** = **개인 MS 계정 무제한**이나 **조직 계정은 동의 차단** · **Google** = 100명(누적·리셋 불가). **기존 ADR 서술 2건 정정** — ① OneDrive "게시자 인증(**선택**)"은 **틀렸다**: 2020-11-08 이후 등록 멀티테넌트 앱은 기본 로그인·프로필 읽기를 넘는 권한에 **테넌트 밖 사용자가 동의 불가**하며 `Files.ReadWrite`가 정확히 대상(개인 MS 계정은 테넌트 밖이 아니라 무관 — 08-01 QA의 outlook.com 연결 성공이 방증). 표의 scope도 현행으로 정정 ② Google은 **프로덕션 게시로도 100명 상한이 유지**되고 그 100명은 **프로젝트 수명 누적·리셋 불가**임을 명시. **함의** = 조직 계정은 **동기화 폴더 Link로 우회 가능**(Connect만 막힐 뿐 기능 전체가 막히는 게 아니다). 상세 [journal/2026-08-02.md](journal/2026-08-02.md).
- **3사 앱 등록 완료 — 잔여를 "배포 선행 조건"으로 재분류(사용자 확인)**: Dropbox·Google·Entra 콘솔 등록 완료 화면 확인 → 코드 `default_client_id` 3종과 일치. Entra 앱은 **All Microsoft account users·퍼블릭 클라이언트·시크릿 없음**으로 우리 PKCE 흐름과 정확히 부합(**테넌트 등록**인 점도 다행 — 개인 계정 등록 앱은 게시자 확인 자체가 불가해 재등록 필요했을 것). TODO X-37 잔여가 **"구현 잔여"가 아니라 "공개 배포 선행 조건"** 임이 분명해져 재분류. 상세 [journal/2026-08-02.md](journal/2026-08-02.md).

## 2026-08-01

- **X-37 7~13차 클라우드 실사용 완성(사용자 QA 연속 17건 — 같은 브랜치 22커밋)**: 6차까지가 "기능을 놓는" 단계였다면 이후는 **실기 QA가 결함을 뽑아낸** 단계 — 보고 17건이 전부 진짜 결함이었고 다수가 **한 증상 = 두 원인**이었다. ① **Google 연결 완성**: 사용자 콘솔 `drive.file`은 **탐색기로 성립 불가**(앱 생성 파일만 열거) → `drive` 전체 + 테스트 모드(CASA 면제·**refresh 7일 만료**가 대가). 조용히 실패하던 진범 = Google이 데스크톱 앱에도 요구하는 `client_secret`(PKCE 대체 불가) → `default_client_secret` 신설·동봉(사용자 결정). 계정명은 Drive `about?fields=user`(추가 scope 불요)·**인증 실패 모달 승격**(타이틀 바만이라 놓쳤다)·**Link/Connect 용어 분리**(기존 동기화 폴더 얹기 vs OAuth 새 계정). ② **진행 표시**: 폴더 열기 = 플레이스홀더 행 + 플로팅 배지(A+B 둘 다·사용자 확정)·전송 = **로컬 복사 진행 창 재사용**. ③ **로딩 무한 5건**: `::PC::`가 클라우드 경로가 아니라 매칭에서 빠짐(`panel_shows_cloud`) · Google ID 캐시 메모리 전용(루트부터 재탐색) · **실패를 캐시하지 않아 무한 재시도 + 오류 비가시**(`[!] 사유` 행) · 세션 복원 탭은 콜백 등록 전 생성이라 빈 채(`reopen_cloud_tabs`) · 경로 바 조각 클릭이 루프 안 `?`로 **비클라우드 추가 루트 하나에 함수 전체 조기 종료**. ④ **계정 간 복사**(서버 사이드는 동일 드라이브 한정 → 임시 폴더 경유): **대상 폴더 미생성**으로 "되는 것처럼" 진행되고 결과 없음(+OneDrive 폴더 생성이 `conflictBehavior: replace`라 **기존 폴더를 지울 수 있던 것** 발견) · 건수를 바이트 자리에 넣어 "0 B / 6 B" · **36MB가 끝나야 한 번에 반영**(Google 단일 multipart라 중간 보고 원리적 불가 → resumable 5MB 청크) · **100%에서 멈추고 [취소] 무반응**(다운로드만 집계 + 취소 판정이 진행 통지 처리기 안에만 있어 통지가 멎으면 버튼을 읽지도 않음 → 세그먼트 절반 분할 + `TIMER_CLOUD_POLL` 200ms 분리). ⑤ **UI**: **[홈] 네비 버튼**(홈/이전/이후/상위 — This PC 직행·클라우드 안이면 연결 루트 선택)·글리프 U+EA8A·13 DIP(`with_large_glyphs` 인스턴스 한정 — 코드포인트 라우팅이면 segmented 컨트롤까지 커졌을 자리·MDL2 대역 `E700..F8FF` 교정) · **컬럼 폭 동기가 기동 시 어긋나던 것**(세션이 패널별로 저장 → 좌측 기준 정렬·토글 ON은 폭이 아니라 **구성 전체** 동기). ⑥ **Dropbox 실사용 5건**(사용자 App key/secret 발급): **scope가 읽기 전용**(4차 이전 값) → write 2종 · **리디렉션 정확 일치**(등록값 `:5368{2,3,4}` 슬래시 없음 → `redirect_ports`/`redirect_slash`·**임의 포트 폴백 금지**) · 라벨 "Dropbox – account"+This PC 미표시(**`is_api()`가 account 유무를 조건으로 삼아 계정 못 받은 연결이 통째로 누락** — 라벨 부실과 연결 부재는 별개) · **한글 업로드 실패**(경로가 `Dropbox-API-Arg` **헤더**인데 헤더는 ASCII 전용 → `\uXXXX` 이스케이프) · **"ABC새폴더"→"ABCuc0c8ud3f4ub354"**(업로드는 정상·**읽기**가 범인 — `json_str`이 `\uXXXX`를 몰라 백슬래시만 버렸고, 깨진 이름으로 경로를 재조립해 **HTTP 409 not_found**까지 연쇄). **254 green·clippy 0·3.44MB(B2)·B3 통과**. 잔여 = Dropbox Development 상태·Google 테스트 7일·온디맨드 파리티 미착수. 상세 [journal/2026-08-01.md](journal/2026-08-01.md).
- **client_id 소유 모델 정정 — 하이브리드(사용자 지적 · ADR-0006 §2-4 개정)**: "NexaDir 기준으로 발급받아야 하지 않나" → **타당**. 초판의 "1차 사용자 제공"은 **Google 제약을 3사 전체에 과적용**한 것. 원천 조회로 정정 — **OneDrive·Dropbox는 벤더 등록 무료**(Dropbox는 50명 시 프로덕션 승인 신청·OneDrive는 scope를 `Files.Read`로 하향해 관리자 동의 회피), **Google만 `drive.readonly` restricted → CASA 연 $500~4,500 감사**로 보류. 채택 = **하이브리드**(rclone 관례) `Service::default_client_id` 상수 + 설정 우선 해석 — 등록 시 상수만 채우면 전 사용자 적용. 근거 = 동봉 ID는 **쿼터 공유·악용 시 전원 차단** 위험이라 재정의 경로 상시 개방. `resolve_client_id()`+회귀 테스트·안내 문구 3종 개정. **235 green·clippy 0·3.28MB·B3 통과**. 사용자 액션 = OneDrive·Dropbox 등록(무료). 상세 [journal/2026-08-01.md](journal/2026-08-01.md).
- **X-37 2~6차 클라우드 실사용화(사용자 QA 연속 — 같은 브랜치 5커밋)**: 인증만 되고 아무것도 못 하던 상태 → **3사 탐색 + OneDrive/Dropbox 전 기능**. **2차 탐색**(`::CLOUD:` 센티널·열거 콜백·워커 캐시·JSON 배열 파서 — UI 스레드 네트워크 금지 준수) · **기동 순서 픽스**(패널 복원이 `sync_cloud_roots`보다 먼저라 F5 전엔 미표시) · **3차 다운로드**(HTTP가 응답을 UTF-8로 변환해 **이진 파일 손상**하던 결함 → `Vec<u8>` 전환. 더블클릭 열기·붙여넣기·DnD) · **4차 쓰기**(scope `Files.ReadWrite` 상향[재로그인 1회]·**HTTP 상태 코드 미검사 결함** 수정·업로드 세션 청크·삭제·이름 변경·새 폴더) · **5차 표시명/폴더 재귀**(센티널 노출 4곳 라벨화·**Alt+↑ 무동작 결함** 해소·재귀 업/다운로드·클라우드 간 서버 사이드 복사/이동) · **6차 Dropbox 전체 신규 + Google 하위 폴더 탐색**(부모 ID 기반). **248 green·clippy 0·3.38MB(B2)·B3 통과**. 상세 [journal/2026-08-01.md](journal/2026-08-01.md).
- **X-37 Connect Cloud — 브라우저 OAuth 직접 연결(사용자 결정 — 같은 브랜치 6커밋째)**: "다른 계정을 브라우저로 추가" 요구. 조사 = **3사 모두 계정 추가 CLI 미노출**(GUI 전용)·클라이언트 다계정 수 제한 → 앱 직접 API 인증이 유일 해법. 사용자가 **OAuth 직접 연결(Phase B)** 채택. **[ADR-0006](27-adr-0006-cloud-oauth.md) 신설**(PKCE 공용 클라이언트·루프백[커스텀 스킴은 DR-3 충돌]·WinHTTP[대안 4종 비교]·DPAPI·client_id 사용자 제공·플러그인화는 ABI 실측 후로 순서만 규정) + **DR-2 개정 = B3 +4**(winhttp·crypt32·ws2_32·**bcrypt** — 마지막은 게이트 실행으로 발견[CNG 해시가 bcryptprimitives와 별개 DLL]). 구현 = `oauth.rs`(PKCE·base64url/percent/JSON 수제·루프백 state 대조·WinHTTP 교환/refresh·RAII) · `secret.rs`(DPAPI hex 1줄·타 PC 복호 실패 = 재로그인) · `config` account/client_id · `win.rs` **Connect Cloud 메뉴**(→ client_id 안내 → **[브라우저 열기]/[URL 복사]/[취소]** → 워커 대기·교환 → 연결 등록·토큰 저장) · i18n 13키. **234 green·clippy 0·3.28MB(B2)·B3 통과**. 탐색(2차)은 후속. 상세 [journal/2026-08-01.md](journal/2026-08-01.md).
- **X-36 클라우드 연결 구현(사용자 요청 — `feat/x36-cloud-connections` 4커밋, 실기 QA 중)**: 검토서 26 §2 Phase A + 메뉴 요구 4건 + URL 복사(추가 요청 — 프라이빗 창 다계정 접속용). ① **MenuBar 하위 메뉴**(nexa-gui — 플라이아웃·▸·1단계) ② **vfs 링크형 Entry**(`target` 표시명≠경로 + `extra_roots` 등록부 → 내 PC 열거 합류) ③ **cloud.rs 탐지**(OneDrive 레지스트리 Accounts\*[BusinessN 다계정]·GDrive 볼륨 라벨·Dropbox info.json 수제 파서 — 네트워크 0·crate 0) ④ config `cloudN` 직렬화(상한 32) ⑤ **Cloud 상단 메뉴**(연결별: 바로 가기/온라인 보기/URL 복사/연결 해제 + 연결 추가하기[감지 후보]) ⑥ **This PC 배경 우클릭 사용자 메뉴**(TrackPopupMenuEx — "{후보} 연결하기"·연결됨 체크) ⑦ `apply_cloud_change` 공용 마감(영속·vfs·메뉴·내 PC 재로드). 온라인 보기 로그인은 브라우저 세션 소관(토큰 주입 불가 — URL 복사가 다계정 경로). **224 green·clippy 0·3.24MB(B2)**·release 기동 스모크. 상세 [journal/2026-08-01.md](journal/2026-08-01.md).
- **클라우드 연계 검토(사용자 요청 — `docs/cloud-integration-study` 1커밋)**: This PC 클라우드 노출 + 기능 플러그인 클라이언트 요청 2건 → **[26 검토서](26-cloud-integration-study.md) 신설·2단계 분리 권고**. **Phase A(X-36)** = 동기화 폴더 탐지(OneDrive `Accounts\*`·DriveFS·Dropbox `info.json`·iCloud) → `::PC::` "클라우드" 섹션(X-17 join-대체 재사용·crate 0·B2/B3 무변·선행 = Entry 표시명 분리·함정 = 온디맨드 플레이스홀더/watcher[07-31 S1·S2 기해소]) · **Phase B(X-37)** = 제2종 플러그인(`kind: cloud`·공급자 ABI·호스트 `nx_http`[WinHTTP — B3 +1]·DPAPI 토큰·OAuth2 PKCE·**연결 모델 = 클라우드별 2+계정·토큰 플러그인 비노출**·Provider 승격 `::CLOUD:` 읽기 전용 1차 — ADR-0006 전제). 대안 기각 3종(셸 네임스페이스·rclone·직접 소켓). TODO X-36/X-37 등록·README 색인. 원본 대조는 맥 세션 유보. 착수 = 사용자 결정 5건(검토서 §6). 상세 [journal/2026-08-01.md](journal/2026-08-01.md).
- **CLAUDE.md 노후화 정정 + 기록 마감·push(사용자 지시)**: §1 최신 릴리스 `0.12.0` 반영·§7-2 채널 전제 08-01 기준 정정(07-31 점검 예고분 이행) 후 **08-01 4커밋 main push**(배포 점검 기록·winget 매니페스트 수정·CLAUDE.md·기록 마감 — 전부 main 직커밋, 병합 브랜치 없음). 잔여 = 실기 QA 4건·외부 심사 3건 추적·백로그(TODO §7). 상세 [journal/2026-08-01.md](journal/2026-08-01.md).
- **winget 설치형 #404528 피드백 처리(사용자 승인)**: `Needs-Author-Feedback`(`DisplayVersion`=PackageVersion 동일값 제거 요청) 대응 — 포크 PR 브랜치 installer.yaml 1줄 제거(**커밋 `7e49694`**, gh contents API·계정 kiros33 전환 후 원복) + 회신 코멘트 + 로컬 사본 `packaging/winget/0.8.1` 동기. author 쪽 블로커 해소 — 잔여(`Policy-Test-1.2` waiver·라벨 해제)는 모더레이터 몫, 상태 추적만. [21 §8](21-distribution.md) 표 갱신. 상세 [journal/2026-08-01.md](journal/2026-08-01.md).

## 2026-07-31

- **winget·choco 배포 상태 점검(사용자 요청 — 원천 실측)**: ① **winget Portable 0.12.0(#408280) MERGED**(07-27 제출 당일 — "심사 대기" 기록 정정. 릴리스와 완전 동기화된 유일 채널) ② **winget 설치형(#404528) "조치 불요" 전제 깨짐** — 07-31 모더레이터 자동화가 `Needs-Author-Feedback` 부착(`DisplayVersion`=PackageVersion 동일값 제거 요청·무응답 시 자동 클로즈 → **포크 수정 push 필요**, 사용자 승인 대기. `Policy-Test-1.2`는 잔존) ③ choco 2종 0.8.1 "Ready for review" 무변동(12일째 큐). [21 §8](21-distribution.md) 채널 표 갱신. 상세 [journal/2026-07-31.md](journal/2026-07-31.md).
- **3건 마감 — main 병합·push(사용자 지시)**: `feat/dist-zip-assets`(ZIP 자산)·`fix/foreground-activation`(포그라운드 양도)·`fix/stability-hardening`(안정성 감사 7건) 선형 스택 **main ff 병합·push·브랜치 삭제**(BRANCHES 표 이동). 219 green·clippy 0·exe 3.20MB. 잔여 실기 QA = 다음 태그 자산 5종·pptx 활성화·OneDrive 자동 갱신·터미널 스레드 무누적. 상세 [journal/2026-07-31.md](journal/2026-07-31.md).
- **전 소스 안정성 감사 — 스레드/프로세스 중단 예방(사용자 요청 — `fix/stability-hardening` 1커밋, 실기 QA 후 병합 대기)**: 워커 6곳·unwrap 131건·INFINITE 대기·핸들 수명 전수 감사(전제 = 릴리스 panic=abort — panic 1건이 프로세스 즉사). 런타임 도달 결함 6건 수정 — **S1** watcher가 `ERROR_NOTIFY_ENUM_DIR`(버퍼 넘침 = 복구 가능)에도 죽어 폴더 영구 무갱신(**OneDrive 간헐 무갱신 보고의 유력 원인** — 엑셀 저장 임시파일 연쇄+동기화 폭주가 트리거) → 통지 후 계속 · **S2** 죽은 watcher가 경로 비교에 잔존해 재구독 불가 → `alive` 플래그+retain 합류(자가 치유) · **S3** conpty 종료 대기가 원시 핸들로 INFINITE 대기(Drop 닫힘+값 재활용 = 영구 대기 스레드 누수) → `DuplicateHandle`(watcher 07-14 규약 통일) · **S4/S5** 삭제·전송 완료 통지 1발 유실 시 `pending_delete`/`st.transfer` 영구 고착(자동 갱신 정지+기능 차단) → `post_final_notify` 유한 재시도 · **S6** 기동 `C:\ expect` 2개소(C: 부재/잠김 = 즉시 abort) → `open_any_root()` 드라이브 순회 · **S7** 공유 Mutex 15개소 poison 내성 `plock()` + panic 후크 `data\crash.txt`(즉사 단서 확보). 나머지 125건 = 테스트 전용/가드된 불변식 판정(근거 journal). **219 green(신규 2 — watcher 죽음 관측 계약·시작 폴백)·clippy 0·릴리스 3.20MB(B2)·스모크 6초 생존**. 상세 [journal/2026-07-31.md](journal/2026-07-31.md).
- **연 프로그램이 뒤에 숨는 결함 픽스(사용자 QA — `fix/foreground-activation` 1커밋, 실기 QA 후 병합 대기)**: 파워포인트 등을 더블클릭하면 문서는 열리나 창이 활성화되지 않고 뒤에 숨던 결함. 원인 = **Windows 포그라운드 잠금** — `ShellExecute`·`InvokeCommand`는 연결 프로그램이 **이미 떠 있으면** 새 프로세스 대신 DDE/COM으로 기존 프로세스에 위임하는데, 그 프로세스는 우리 자식이 아니라 포그라운드 권한을 물려받지 못해 `SetForegroundWindow`가 거부된다(작업 표시줄만 깜빡임). Office는 단일 인스턴스라 **새 문서도 같은 경로**를 타므로 "새로 여는 파일에서도 발생"이 설명된다. 수정 = `win::allow_foreground_handoff()`(`AllowSetForegroundWindow(ASFW_ANY)`) 신설 후 실행 위임 **4개소**(`shell_open` 더블클릭 · `shellmenu::invoke` 셸 우클릭 열기/연결 프로그램 · `launcher::launch` 퀵 런처 · `about` 링크)에서 호출 직전 적용 — 탐색기와 동일 규약. `recycle.rs` InvokeCommand는 삭제라 제외. **217 green·clippy 0**. 실기 QA 대기(자동 검증 불가 — 핵심 회귀 지점 = PowerPoint를 띄워 둔 채 다른 .pptx 더블클릭). 상세 [journal/2026-07-31.md](journal/2026-07-31.md).
- **ZIP 자산 + SHA256SUMS 추가(사용자 요청 — `feat/dist-zip-assets` 1커밋)**: 사용자가 브라우저에서 Release exe 다운로드를 **보안 차단**당한 건 대응. 원인 = 무서명(DR-3) exe에 대한 브라우저 SmartScreen **다운로드 평판 필터**(서명 없으면 게시자 평판 미축적·해시 평판은 버전마다 0에서 재시작). 조치 = release.yml에 스텝 2개 신설 — `NexaDir-<버전>-win-x64.zip`·`NexaDir-Setup-<버전>.zip`(각각 exe + 안내 `README.txt` 한/영 UTF-8 BOM) + 자산 4종 `SHA256SUMS.txt`. **기존 exe 자산은 유지**(winget·choco 매니페스트가 URL+SHA256 직참조 — 교체 시 3채널 동시 파손). 결과 자산 5종. 함정 = `run: |` 안 PowerShell here-string 종료자가 컬럼 0이라 YAML 블록 스칼라 파손 → 문자열 배열+`Set-Content`로 우회. 로컬 dry-run 검증(zip 엔트리·BOM·체크섬 형식) 완료, 실 검증은 다음 태그 push. 상세 [journal/2026-07-31.md](journal/2026-07-31.md).
- **서명 경로 재조사 — [12 §4-1](12-packaging-single-exe.md) 신설(기존 기록 2건 정정)**: MS 공식 문서 기준 ① **EV의 SmartScreen 즉시 통과는 2024년 폐지**(OV와 동일·프리미엄 근거 소멸) ② **Azure Artifact Signing은 한국 이용 불가**(조직=미·캐·EU·영/개인=미·캐 지역 제한) ③ SignPath Foundation은 "OSI 승인 + 상업 듀얼 라이선싱 금지" 요구로 PolyForm NC(DR-6)+상업 라이선스 정책에 **이중 결격**. → 서명은 어느 경로든 초기 경고를 없애지 못하고 *버전 간 평판 승계*만 준다. 경고를 실제로 없애는 유일 경로 = **Microsoft Store(MSIX 재서명·무료·등록비 2026 폐지)** — [00-vision](00-vision.md)의 MSIX 배제 전제가 무너져 재검토 대상(착수는 사용자 결정 대기·MSIX 컨테이너 실측 + `broadFileSystemAccess` 심사가 관문). 상세 [journal/2026-07-31.md](journal/2026-07-31.md).

## 2026-07-27

- **릴리스 0.12.0 + winget Portable 업데이트 PR(사용자 요청 — choco 제외)**: 0.12.0 승격(X-34 새로 만들기·X-35 삭제 잠금 처리 포함) → 태그 push → Actions 게이트 전부 통과 → [Release 0.12.0](https://github.com/SosomLab/nexa-dir2/releases/tag/0.12.0)(포터블+설치형 — choco는 `CHOCO_PUSH` 꺼짐 자동 제외). winget `SosomLab.NexaDir.Portable` 0.12.0 매니페스트(SHA 실측) 작성 → [winget-pkgs#408280](https://github.com/microsoft/winget-pkgs/pull/408280) 제출(심사 대기). [21 §8](21-distribution.md) 채널 표 갱신. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **X-35 마감 — main 병합·push(사용자 지시)**: `feat/x35-delete-locked` 5커밋(사전 프로브+실패 통지·재시도 + 펼친 하위 폴더 감시) **main ff 병합·push·브랜치 삭제**. 217 green·clippy 0·exe 3.36MB. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **X-35 QA 1차 — 펼친 하위 폴더 감시(사용자 QA)**: 잠금 삭제 차단은 정상, 엑셀 `~$` 임시파일이 펼친 하위 폴더에서 정리돼도 잔존 — M3-6 α(비재귀·루트만 감시, 원본도 동일) 해소: `Panel::watch_dirs`(루트+가시 펼침 폴더·상한 64) + watcher 패널별 Vec + `sync_watchers` diff 재구독(재귀 감시 대신 폴더별 비재귀 핸들 — 대형 트리 무부담). 217 green·clippy 0. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **X-35 구현(사용자 지시 — `feat/x35-delete-locked` 1커밋, 실기 QA 후 병합 대기)**: 확정 설계 그대로 — `start_recycle_delete`(사전 잠금 프로브 `CreateFileW(DELETE)` µs 판정 → 잠긴 항목 배치 모달 1회 [건너뛰고 삭제(N)]/[다시 시도]/[취소]·숨김 제외) + `on_delete_message` 사후 `exists()` diff 백스톱(성공분만 부분 undo·실패분 선택 강조+캐럿·실패 모달 [다시 시도]=실패분 재진입) + watcher `pending_delete` 가드 + lang 3종 키 10개(`dialog::show_buttons` 재사용 — comctl32 비의존 유지). 217 green·clippy 0·exe 3.36MB. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **X-35 휴지통 삭제 실패 무통지 — 분석·설계 확정(사용자 보고 — 코드 무수정)**: 열린 파일 삭제 실패 시 행만 재출현·무통지 — 결함 4건 진단(타이틀 한 줄 통지·배치 1비트 결과·부분 undo 미기록·watcher 가드 부재) + 원본 대비 회귀 3건 확인. 설계 = **레이어드 P+B**(사용자 제안 사전 잠금 프로브 + `exists()` diff 백스톱 + 모달 [건너뛰고 삭제]/[다시 시도] + 부분 undo 복원 + watcher 가드 — 통지·부분 진행은 사용자 확정). IFileOperation은 후속 재검토. TODO X-35 등록. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **X-34 마감 — main 병합·push(사용자 지시)**: QA 1차 픽스 반영 재빌드(exe 3.35MB) 후 `feat/ctx-new-menu` 5커밋 **main ff 병합·push·브랜치 삭제**. 217 green·clippy 0. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **X-34 QA 1차 픽스(사용자 QA — 스크린샷)**: New 템플릿이 서브메뉴 안이 아니라 **주 메뉴에 평탄 삽입**되던 결함 — 진범 = 전 핸들러 브로드캐스트 포워딩(주 메뉴 `WM_INITMENUPOPUP`이 CNewMenu에 전달돼 주 메뉴를 채움) → `MenuHost`(소유 서브메뉴 핸들·명령 대역) **선별 라우팅**으로 전환(INITMENUPOPUP=HMENU 소유자·DRAW/MEASUREITEM=itemID 대역·미매치=주 메뉴 폴백). 66 green·clippy 0. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **항목 우클릭 "새로 만들기" + 생성 후 인라인 리네임(사용자 요청 — `feat/ctx-new-menu` 1커밋, QA 후 병합 대기)**: 셸은 New(ShellNew)를 배경 메뉴에만 제공 → **CLSID_NewMenu 직접 호스팅**(IShellExtInit 대상 폴더 초기화·전용 대역 0x7000·포워딩 ACTIVE Vec 확장)으로 항목 메뉴 하단에 배경 메뉴와 동일한 전체 템플릿 서브메뉴 병합. 단일 선택만(파일=부모·폴더=자신 — 다중은 숨김, 사용자 확정)·라벨 앱 언어(`ctx.new` lang 3종)·설정 `ctx_menu_order` row `new` 키. **생성 후 리네임 진입**: 셸 invoke는 파일명 미반환 → 대상 폴더 전후 diff(정확히 1개)로 `Outcome::Created` → 재로드·캐럿·begin_rename(`focus_created_and_rename` 공용화 — 접힌 폴더는 hover_expand 후 진입·배경 메뉴에도 휴리스틱 적용). 217 green·clippy 0·3.34MB. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **로컬 브랜치 정리(사용자 지시 — 선별)**: `feat/md-preview` 삭제(폐기 지시분·자산은 wasm 샘플로 이식 완료·tip `6f47512` 기록) / `feat/x13-launcher-crud`는 **보존**(launcherN.icon = main 미반영 고유 작업·X-13 백로그가 참조). BRANCHES에 사유·SHA 기입. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **플러그인 개발 문서 정리·가이드 24 보강(사용자 요청)**: 프로젝트 구성(Cargo 필수 4항목)·**최소 예제 전체 코드**(19KB — 컴파일+wasmi 런타임 동작 검증 후 수록)·ABI export 표·**빌드→적용→확인 3단**(복사 위치·로드 순서·설정 제어·오류 격리) 추가. 116→193줄, 코드 무변경. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).
- **`.star` → `.wasm` 표기 정정(사용자 QA — main 직커밋)**: wasmi 전환 후 잔존하던 표시 문구를 현행 표면 전체에서 정정 — lang 3종 설정 문구(`data\plugins\*.wasm`)·wiki 개발-플러그인 **wasmi 판 재작성**·기능-설정·설계-결정·ADR-0004 상단 구판 표기. 과거 기록(journal/DEVLOG/BRANCHES)은 규약대로 보존. 217 green·clippy 0. 상세 [journal/2026-07-27.md](journal/2026-07-27.md).

## 2026-07-26

- **wasm 참조 플러그인 + 가이드 24 개정(10차 — wasmi 전환 마감)**: markdown-viewer-wasm 러스트 크레이트 → **80KB .wasm**(GitHub 태그 렌더·<br/>·표·Mermaid flowchart SVG 이미지/sequence 아트) + dist 동봉 실런타임 E2E. 가이드 24 = wasmi 판 전면 개정(ABI·import·태그 계약·빌드/배포). 217 green·clippy 0. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **플러그인 런타임 Starlark→wasmi 전환(사용자 결정 — 9차, `feat/wasmi-plugin` 3커밋)**: 부분 교체(1안 — 시임·독립 창·설정·격리·태그 계약 유지, revert로 원복 가능). ADR-0005 신설·Starlark 코어 제거·wasmi 1.1 런타임(.wasm 로더·nx_meta/nx_preview ABI·read_text/render_svg/is_dark/disp_width import·fuel 2억[무한 루프 트랩 실증]·메모리 64MB). **B2 실측 +1.68MB(1.50→3.18MB — starlark 대비 -2.58)**. 65 green·clippy 0. 잔여 = 러스트 wasm 샘플(md 뷰어 이식)·가이드 24 개정. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **Mermaid `<br/>` 라벨 QA 픽스(사용자 QA — 7차·main 직커밋)**: `<br/>` 미해석 → 폭 폭증 → 상한 초과 → 원문 폴백이던 진범 — 멀티라인 박스(폭=최장 줄·높이=줄 수)·따옴표 간선 라벨·캔버스 2000px·노드 24/간선 60·폴백 라벨 평탄화. E2E 6·218 green. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **Mermaid 이미지 수준 렌더 + 실행 격리 상한(사용자 승인·요구 — 6차)**: 호스트 `render_svg`(svg.rs→GDI+ AA 래스터→BMP 캐시)·`is_dark()` + 인라인 이미지 마커(`\u{1}img|`+pad — 도크/독립 창 렌더·복사 제외) — markdown.star flowchart가 테마 연동 SVG로 이미지 수준 표시(실패 = 텍스트 폴백). 격리: 실행 300ms/로드 500ms·연료 5천만 틱·힙 64MB(초과 = 플러그인만 오류 1줄). 218 green·clippy 0. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **리네임 재클릭 결함 픽스 + Mermaid 이미지 수준 검토(사용자 QA·질의 — 5차)**: 슬로우클릭 리네임의 `is_renaming()` 전체 가드가 취소 클릭의 slow_click 이력을 소실시키던 진범 — 가드를 편집 필드 안으로 축소(`rename_field_hit`)+취소 클릭은 시드만. 검토: 현 구조에서 Mermaid 이미지 수준 = **가능**(계약에 `{"draw": 명령}` 추가 → 호스트 GDI+ AA 렌더 — 크레이트 0·라이선스 무관), 완전판(mermaid.js)은 웹뷰 필요로 구조 밖. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **↗ 이미지 버튼 + 렌더 대안 검토(사용자 요청 — 같은 브랜치 4차)**: 오버레이를 `emb:popout` SVG 이미지 버튼으로(3상태 배경 pressed=accent 38%>hover=sel_bg>기본·1px 누름 오프셋·안 릴리스 발화). 검토: Python md+Mermaid 라이브러리 = 전부 HTML 패스스루(mermaid.js 의존)·Starlark에서 import 불가 → 채택 불가(라이선스 아닌 기술 제약) · sosomlab-nexa-viewer(자사 MIT·Tauri+react-markdown+mermaid.js) = 웹뷰 렌더라 DR-1/B3 상충 — 연동(외부 열기)·GFM 서브셋 이식이 현실 경로. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **미리보기 UX 6종(사용자 요청 — 같은 브랜치 3차)**: ① 도크 미리보기 우상단 **↗ '크게' 오버레이**(클릭 = 독립 창) ② 독립 창 **모달 전환** ③ 스크롤 — 도크 휠 세로(3줄/노치·절대 라인 재정렬)+독립 창 세로/가로(최장 라인 실측 상한) ④ 독립 창 **드래그 문자 선택**(도크 규약·3분할 렌더) ⑤ **rich 복사**(CF_UNICODETEXT+모노 RTF 동시 게시 — 표/박스 정렬 유지·도크 Ctrl+C 승격) ⑥ 드래그 자동 스크롤(도크 상/하·창 상하좌우+50ms 타이머 연속). 218 green(+3)·clippy 0·5.76MB. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **플러그인 목록·사용 여부 설정 페이지(사용자 요청 — 같은 브랜치 2차)**: 설정 창 사이드바 **플러그인** 카테고리 — 로드된 .star 목록을 `NAME (id) — 확장자` 체크박스로 나열(동적 — ID_PLUGIN_BASE 2100 = 기존 즉시 적용 경로 재사용), 체크 해제 = `plugins_disabled`(왕복·영속) → resolve에서 오버라이드/선언 매치 제외(내장 = 폴백 면역) → 도크 즉시 갱신. 빈 목록 = data\plugins 안내. 215 green·clippy 0·5.74MB 불변. 가이드 24 §7-4. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).
- **X-2 Starlark 플러그인 시스템 + MarkdownViewerPlugin 샘플(사용자 지시 — `feat/starlark-plugin` 6커밋, QA 후 병합 대기)**: 1차 내장 러스트 md 뷰어(`feat/md-preview` — 미병합 보존)를 사용자 지시로 폐기하고 **Starlark 기반 재설계**. ① S1 시임 + **preview_map 설정 오버라이드**(스크립트 EXTS 내부 선언 = 기본값 > 설정 재정의 > 폴백) ② **S2 starlark 0.14.2 런타임**(DR-8 원장 확정 — B2 실측 +4.24MB, 1.50→5.74MB ≤10 통과): `data\plugins\*.star` 파일명 순 로드·동결 캐시·오류 격리·호스트 API(file.path/ext/size·read_text[대상 파일만 256KB]·disp_width[CJK 2칸]) ③ **독립 미리보기 창 F3**(콘솔 그리드·스크롤 — 플러그인 기준 캔버스, 벤치는 Shift+F3) ④ **samples/markdown-viewer 독립 프로젝트**(markdown.star 참조 구현: md 서브셋 + **Mermaid flowchart/sequence 텍스트 다이어그램** 순수 Starlark·픽스처·README) + 실제 런타임 E2E 테스트 ⑤ **개발자 가이드 [24](24-plugin-dev-guide.md)**(프로젝트 생성→개발→테스트→배포 8단계 순차). 214 green·clippy 0·B3 통과. 상세 [journal/2026-07-26.md](journal/2026-07-26.md).

## 2026-07-24

- **문서 내용 정리 — STATUS 한 장 복원·README 색인 결손·CLAUDE 현행화(사용자 요청)**: 커밋 대기분이 없어 문서 자체의 규약 위반을 찾아 정리 — ① **STATUS**가 차수 22건·본문 전 340줄(전체의 89%)로 불어 [16 §1](16-doc-git-conventions.md)의 "한 장" 규약을 벗어남 → 07-22 이후 7건은 원문 유지, **07-21~07-15 15건은 하루 한 줄 색인(journal 링크)으로 압축**해 **382→175줄**(원문은 journal에 그대로 = 정보 손실 없음, 압축 블록에 근거 명시) ② **docs/README 색인 결손 7건 보충**(07·08·09 ADR·19 패리티 갭·20 세션 코얼레싱·21 배포·22 일괄 이름변경 v2 — "ADR · 기능 설계" 하위 표 신설) ③ **CLAUDE.md 13일 노후화 정정** — §1 "M5 진행"→**포스트 M5(`0.11.0`)**, §7 "다음 단계(2026-07-11)"의 M0/M1 항목→**07-24 기준**(실기 QA·심사 대기 3건·백로그·X-33 착수 대기)으로 교체 + "최신 현황은 항상 STATUS" 문구로 재노후화 예방. 코드 무변경. 상세 [journal/2026-07-24.md](journal/2026-07-24.md).
- **macOS·Linux 확장 타당성 검토 문서 신설(사용자 요청)**: 추정 대신 코드베이스를 직접 계측해 [23 검토서](23-cross-platform-feasibility.md) 작성 — 전체 **40,041 LOC 중 중립 16,102(40.2%)**·Windows 결합 23,939(59.8%), **맥 네이티브 `cargo test` 147 green**·`nexa-app` 스텁 빌드 성공(M0 cfg 격리 골격 생존 확인). 핵심 3건: ① **화면 로직이 이미 중립** — `nexa-gui/widgets`(rows.rs 2,885 등 5,519 LOC)가 `DrawCtx` ~10메서드에만 의존 ⇒ 플랫폼별 백엔드 1개로 주 화면 렌더 가능 가설 ② **최대 비용 = `ctl` 18종 7,139 LOC**(HWND 골격 종속 — 단 07-17 "AA=DrawCtx 백엔드만" 개정으로 그리기는 이미 중립) ③ **진짜 관문은 DR** — DR-1/DR-2/DR-8이 "Windows 인박스 API 존재" 전제라 **Linux는 문안 그대로면 성립 불가**(ADR-0005 개정 필요). 패리티: 셸 컨텍스트 메뉴 = 등가물 없음(기능 상실), ConPTY→`forkpty` = 오히려 단순, MDL2 의존 = 중립 SVG 파서로 대체 가능. 선택지 4종 중 **C(winit+softbuffer+cosmic-text 위 커스텀 드로잉 유지·전부 퍼미시브)**를 현실적 후보로 제시하되 **권고 = 지금 결정하지 말고 맥 렌더 스파이크 먼저**(소요 추정은 스파이크 전엔 추측임을 명기). 사용자 결정 5건(동기·우선 플랫폼·패리티 포기 범위·무서명 방침[macOS 공증]·QA 3배 부담)은 §7. [TODO](TODO.md) X-33 등록 + **X-32 누락분 소급 등록**. 코드 무변경. 상세 [journal/2026-07-24.md](journal/2026-07-24.md).
- **배포 채널 상태 점검 — winget·Chocolatey 4항목 실측 최신화(사용자 요청)**: PR 라벨 타임라인·winget-pkgs master 매니페스트·choco 패키지 페이지 모더레이션 로그를 직접 조회해 4항목을 재확인 → **정정 2건**. ① **winget Portable `0.11.0`(#405973)은 이미 MERGED**(2026-07-22 18:44 — 문서엔 "OPEN·검증 대기"로 남아 있었음) → **포터블 = 최신 릴리스까지 배포된 유일한 채널**(master에 `Portable/0.8.1`·`0.11.0` 매니페스트 상주 확인). ② **설치형(#404528)이 막힌 진짜 이유 = `Policy-Test-1.2` waiver 미부여** — 포터블은 07-20 22:57 모더레이터가 `Waived-Policy-Test-1.2`를 달아 해제 후 승인됐으나 설치형은 플래그 잔존·**07-19 13:46 이후 무변동**이며 신규 기여자는 재실행 권한이 없어 **우리 측 조치 수단 없음**. **Chocolatey 2종**은 자동 3단계(validation·verification·virus scan[Flagged Note = 차단 아님]) 완료 후 **07-20 02:2x 이후 4일 무변동** = 큐 대기. 최신 릴리스 0.11.0 대비 choco는 0.8.1 고정(보류 방침 정상 작동 — 변경 없음). [21 배포](21-distribution.md) §7 모더레이션 상태 표·§8 waiver 서술·**채널 상태 요약 표** 신설. 위키는 이미 실제와 일치해 수정 불요. 상세 [journal/2026-07-24.md](journal/2026-07-24.md).
- **저장소 최신화 — 로컬 브랜치 정리 + BRANCHES 이력 보정(사용자 요청)**: 동기화 실측 결과 `main` ↔ `origin/main` **0 ahead/0 behind**·태그 12종 일치·트리 clean으로 **이미 최신**이었고, 잔여물은 **머지 완료 후 남은 로컬 전용 브랜치 3개**(`feat/chocolatey-packaging`·`feat/m0-render-spike`·`feat/m0-scaffold`) → `git branch -d` 안전 삭제. 이 과정에서 [BRANCHES](BRANCHES.md) 어긋남 2건 확인·보정: **`feat/chocolatey-packaging` 행 누락 소급 기록**(07-19 merge `0743472`·2커밋 — DEVLOG/journal에는 있었으나 표에만 빠짐) + M0 브랜치 2개 **삭제 열 정정**(`07-11`→`07-24(정정)` — 병합일을 삭제일로 적어 두었으나 ref는 오늘까지 잔존). 재발 방지로 **"삭제 열 = 로컬 ref를 실제로 지운 날" 정의를 문서 상단에 명문화**. 코드 무변경. 상세 [journal/2026-07-24.md](journal/2026-07-24.md).
- **문서·커밋/푸시 규약 문서화(사용자 요청 — 타 프로젝트 이식용)**: 현행 기준을 구두 요약에 그치지 않고 [16 문서·커밋/푸시 규약](16-doc-git-conventions.md)으로 고정 — **§0 붙여넣기용 지시문 블록**(새 프로젝트 세션에 그대로 투입) + 문서 **4층 체계**(진입/현황/경과/지식, 시간축·목적축·상태축 분리 — 상세는 journal 한 곳·나머지는 요약+링크) + 작성 필수 규칙 8(한 작업 = 한 트랜잭션 갱신·SSOT 동시 갱신·"왜+실측값"·문서 번호 불변) + 커밋 규약(Conventional + 맥락 태그) + **🔴 push는 명시 요청 시에만·태그 push 별도 승인·파괴적 작업 사전 확인** + 릴리스 4단계 + 새 프로젝트 적용 체크리스트. docs/README ④ 색인 배선. 상세 [journal/2026-07-24.md](journal/2026-07-24.md).

## 2026-07-22

- **Wiki 설치 페이지 신설 + 배포 문서 이력 갱신(사용자 요청)**: 채널 분석에서 ① 문서 다수가 winget Portable 병합(07-21) 미반영 ② **Wiki에 설치/다운로드 페이지 부재**를 확인 → 둘 다 해소. Wiki [설치와-다운로드] 신설(**포터블 winget 설치 안내** install/upgrade/uninstall·제거 시 데이터 소멸 경고 + GitHub Release 포터블/설치형·시스템 요구사항·준비 중 채널) + Home/_Sidebar/프로젝트-소개 배선·실측 0.11.0 갱신 → wiki.git 발행(`ce22af5`). `21-distribution.md` §8에 winget Portable MERGED(#404533)·0.11.0(#405973)·Setup OPEN·gh CLI 절차 기록. 상세 [journal/2026-07-22.md](journal/2026-07-22.md).
- **winget Portable `0.11.0` 배포 — 버전 업데이트 PR(사용자 요청)**: 채널 재점검에서 winget **Portable은 0.8.1이 이미 병합·배포 완료**(PR #404533 MERGED 07-21 — 문서엔 OPEN으로 남아 있던 것 정정)임을 확인 → 최신 릴리스 **0.11.0**으로 버전 업데이트 매니페스트 3종 작성(`packaging/winget/portable/0.11.0/` — SHA-256 `523D864F…02772` Release 자산 대조·locale 언어 설명 en/ko/**ja** 정확화) 후 microsoft/winget-pkgs **PR #405973** 제출(OPEN·검증 대기). 이미 승인된 패키지의 버전 업데이트라 choco 보류 방침과 무관. 나머지 3채널(winget Setup #404528·choco 2종)은 여전히 모더레이션 대기. 상세 [journal/2026-07-22.md](journal/2026-07-22.md).
- **릴리스 `0.11.0` — GitHub Release 전용(사용자 요청)**: 0.10.0 이후분(컬럼 리사이즈 단독 조절·Ctrl+V 파일 선택=그 파일의 폴더) 승격 — choco push 스위치 꺼짐 유지. 위키 수정분 발행. 상세 [journal/2026-07-22.md](journal/2026-07-22.md).
- **Ctrl+V 대상 보완(사용자 QA — main 직커밋)**: 파일 1개 선택 시 가시 루트가 아니라 **그 파일이 있는 폴더**로 붙여넣기(`paste_dest` — 폴더=그 안/파일=parent/없음·다중=현재 폴더). 위키 동기. 상세 [journal/2026-07-22.md](journal/2026-07-22.md).
- **컬럼 리사이즈 = 단독 조절(사용자 확정 — main 직커밋)**: 경계 드래그의 한 쌍 동시 조절(총폭 보존 — QA 07-15) 폐지 → **해당 컬럼만** 변경·이웃 불변·총폭 가변(초과분 = 가로 스크롤, 탐색기 규약). `ResizeDrag.start_w2` 제거·테스트에 이웃 불변/총폭 검증 보강·위키 리사이즈 서술 동기. 상세 [journal/2026-07-22.md](journal/2026-07-22.md).
- **릴리스 `0.10.0` — GitHub Release 전용(사용자 요청)**: X-32 브랜치 main 병합(ff `ec8d727`) 후 0.10.0 승격(Cargo workspace·lang 3종 `@app`) — choco push는 `vars.CHOCO_PUSH` 스위치 꺼짐 유지(추가 조치 불요·0.9.0에서 skipped 검증). **위키 반영(필수 요청)**: 기능-파일-조작(붙여넣기 대상·잘라내기 흐림·엣지 스크롤·호버 전환)·기능-설정(파일 전송 카테고리 행 신설·내장 3언어 표기)·키보드-단축키(Ctrl+X/V) 갱신 후 wiki.git 발행. 로컬 release exe 1.50MB. 상세 [journal/2026-07-22.md](journal/2026-07-22.md).
- **X-32 클립보드·DnD UX 보완 4종(사용자 요청 — `feat/clipboard-dnd-ux`)**: ① **Ctrl+X 잘라내기 흐림 표시** — OS 클립보드 미러 `CUT_MARKS`(`AddClipboardFormatListener`+WM_CLIPBOARDUPDATE 단일 길목 — 자기/외부 탐색기 잘라내기·해제 전부 수렴) + `RowSource::is_ghosted`(이름·셀 text_dim, 리스트/타일 공통) ② **Ctrl+V 선택 폴더 붙여넣기** — `paste_dest()`: 폴더 1개 선택 = 그 안, 그 외 = 현재 폴더 ③ **DnD 엣지 자동 스크롤** — `drag_scroll_edge`(본문 상/하단 한 행 존 ±1행·폴링 반복 = 연속) ④ **DnD 호버 대기** — DropHooks `track/leave` + `TIMER_DND` 100ms 폴링(GetCursorPos — 정지 커서 보강): 비활성 탭 3초 = 탭 전환·접힌 폴더 3초 = 펼침(펼침 전용·중첩 = 재대기·행-경로 재검증), **설정 `dnd_hover_ms`**(기본 3000·200~10000 — prefs "파일 전송"·lang 3종). 209 green·clippy 0 — 실기 QA 대기. 상세 [journal/2026-07-22.md](journal/2026-07-22.md).

## 2026-07-21

- **릴리스 `0.9.0` — choco push 스위치 + GitHub 전용 배포(사용자 요청)**: 보류 방침을 **choco push만**으로 축소(새 Release는 대기 중 0.8.1 심사에 무영향·winget은 수동 PR) — release.yml choco push 조건에 `vars.CHOCO_PUSH == 'true'` 게이트 추가(기본 꺼짐 = GitHub Release까지만, 승인 후 저장소 변수로 재개). 0.9.0 = 0.8.1 이후 전체(07-20 X-25~29 + 07-21 X-30 전송 UX·QA 시리즈 + X-31 일본어). 상세 [journal/2026-07-21.md](journal/2026-07-21.md).
- **X-31 일본어 내장 언어팩(사용자 요청)**: `ja.lang` 신설(en 기준 전 키 번역 — Windows 일본어 UI 용어 관례) + i18n `BUILTIN_JA`/`discover` 등록(내장 3언어 en·ko·ja — 메뉴/설정 라디오 자동 노출·`data\lang` 오버라이드·OS `ja-JP` 시스템 추종). 파리티 테스트를 내장 전 언어 루프로 일반화, 기존 "신규 언어" 시나리오는 fr로 교체. 61 green·clippy 0·exe 1.56MB. 상세 [journal/2026-07-21.md](journal/2026-07-21.md).
- **설정 창 "View _Sort" 표기 수정(사용자 QA)**: 라벨 `View & Sort`의 `&`를 STATIC·트리 오너드로가 단축키 접두(밑줄)로 해석 — prefs 텍스트 STATIC 8곳 `SS_NOPREFIX` + 사이드바 트리 `DT_NOPREFIX`(라벨 원문 그대로·향후 `&` 라벨 공통 안전). 상세 [journal/2026-07-21.md](journal/2026-07-21.md).
- **휴지통 삭제 지연 = 셸 API 고정 비용 판단 + 낙관적 숨김(사용자 QA 연속)**: 완전 삭제(std::fs)는 즉시인데 휴지통(SHFileOperationW)만 수 초 = **API 자체가 병목**($I/$R 생성·휴지통 설정 확인·SHChangeNotify·셸 확장/AV — 파일 크기 무관 고정 비용, 환경 따라 3~4초). 코드로 API를 빠르게 할 수 없어 **탐색기 동일 UX 채택**: DEL 즉시 행을 화면에서 제거(nexa-tree `remove_paths` 신설 — 하위 포함 표시 제외·선택 정리·FS 무변, panel `hide_paths` 양 패널 적용) 후 워커가 뒤에서 처리, **실패 시 완료 재로드가 원복**. 워커 COM STA 초기화 추가. 상세 [journal/2026-07-21.md](journal/2026-07-21.md).
- **X-30 QA 후속 3건(사용자 — 같은 브랜치 연속)**: ① **DEL 휴지통 삭제 3~4초 UI 멈춤 해소** — `SHFileOperationW` 동기 호출(UI 스레드)이 원인, **워커 스레드 이관**(`pending_delete` 동시 1잡 가드 → `WM_APP_DELETE` 완료 통지에서 undo 기록·재로드, 진행 중 "삭제 중..." 표시. Shift+Del 완전 삭제는 즉시라 동기 유지) ② **세그먼트 4파일→3구간 보임 수정** — 작은 파일 구간이 반올림 소멸, **전 항목 최소 3px 보장**(부족분 = 최대 구간에서 차감) ③ **파일별 진행색 5색 순환 팔레트**(파랑[accent]·초록·주황·보라·분홍 — 인덱스 % 5, 건너뜀/실패 상태색 유지). 상세 [journal/2026-07-21.md](journal/2026-07-21.md).
- **X-30 전송 진행 UX 개편(사용자 요청 5건 — `feat/transfer-progress-ux`)**: ① **"1 B / 1 B" 오표기 수정** — 완료 표시 `set_done`이 바 100%를 위해 실제 바이트를 1/1로 덮어쓰던 것을 실제 값 유지로(취소 시 부분 진행 정직 표기) ② **nexa-ops 이벤트 프로토콜**(`Event::Plan/ItemStart/Bytes/ItemEnd` — 항목별 크기 선통지·실제 대상 경로·항목 종결 상태, 테스트 +2=35) ③ **진행 창 개편** — B~TB 적응 단위·`진행/전체 (pct%) · 파일 {cur}/{count}`(바 바로 위)·**항목별 크기 비례 세그먼트 바**(완료 accent/진행 부분 채움/건너뜀 회색/실패 적색·경계선·512+ 폴백) ④ **설정 `transfer_close_ms`**(0~10000ms, 기본 2000 — prefs 신설 "파일 전송" 카테고리, **0=진행 창 미표시**·닫힘 시점 = 정확히 ms[버튼 표시는 올림 초]) ⑤ **전송 중 대상 잠금** — `in_flight` 경로(+하위) 더블클릭/Enter/Alt+↓ 실행 차단(비프+제목줄)·드래그 발신 제외. 전 테스트 green·clippy 0 — 실기 QA 후 병합 대기. 상세 [journal/2026-07-21.md](journal/2026-07-21.md).
- **배포 채널 등록 상태 실측 점검(사용자)**: ① **Chocolatey 진전** — `nexa-dir`·`nexa-dir.portable` 0.8.1 자동 검사 3종이 **Pending에서 완료로 전환**(Validation·Verification **Passing**, Package Scan **Flagged Note** = 참고성 플래그·차단 아님) → `awaiting moderation`. 07-19 기록의 "3종 Pending" 서술 정정. 미승인 패키지는 community OData 피드에 안 뜨므로 상태 확인은 패키지 페이지로 해야 함(조회 노하우). ② **winget 변동 없음** — #404528·#404533 모두 `OPEN`·`Azure-Pipeline-Passed` 유지, 07-19 14:07 `@wingetbot run` 권한 거부 이후 신규 코멘트 0 = 모더레이터 검토 대기 지속. **양 채널 우리 측 조치 불요.** 상세 [journal/2026-07-21.md](journal/2026-07-21.md).
- **릴리스 배포 보류 방침(사용자)**: **Chocolatey 모더레이션 승인 전까지 새 버전 태그·GitHub Release 배포 보류** — 대기 중 새 버전 push 시 두 버전이 각자 큐에 쌓이는 것을 회피(첫 승인 후 후속 버전은 심사 가속). 실기 QA는 로컬 릴리즈 빌드로 계속(07-20분 exe 1.45MB). 상세 [journal/2026-07-21.md](journal/2026-07-21.md).

## 2026-07-20

- **탭 UX·미리보기 선택 + 오늘 백로그 구현(사용자 요청 5건)**: ① **X-28 탭 바 UX** — 우클릭 메뉴 New Tab(닫기 위) + **멀티라인 탭**(폭 초과 줄바꿈 — 줄 수는 paint 측정 `lines()`→호스트 재레이아웃 1프레임 수렴) + 상하 드래그 이동(x 중간점 규칙이 교차 줄에 그대로 성립) ② **X-29 도크 텍스트 문자 단위 선택 복사** — 정보/미리보기 드래그 = (라인,문자) 영역 선택(edit.rs 오프셋 캐시 규약)·Ctrl+C 무변 ③ **X-27 ✅** 툴바 hover=`sel_bg` 통일(종전 header_bg는 chrome_bg와 명도 차 없음) ④ **X-25 1차 ✅** `Conflict::Nested`(개명 예정 조상의 경로 접두 검출)로 자손 표기·적용 차단 ⑤ **X-26 ③ ✅** About 창(about.rs — dialog.rs 규약·도움말 메뉴 신설·링크 3종은 홈페이지 확정 전 GitHub). 전 테스트 green·clippy 0 — 실기 QA 대기. 상세 [journal/2026-07-20.md](journal/2026-07-20.md).
- **백로그 등록 3건(사용자 요청)**: ① **X-25 일괄 이름변경 조상·자손 동시 선택 처리** — 코드 확인 결과 결함 재현: 트리에서 상위 폴더와 그 하위 항목을 함께 선택하면 [`open_bulk_rename`](crates/nexa-app/src/win.rs) 순차 rename 루프가 부모를 먼저 개명할 때 자손 `old_path`가 소멸(선택 순서 = `selected_paths()` 삽입 순서라 비결정적) → 부모 우선 = 자손 실패·자손 우선 = undo/`rename_expanded` 경로 추적 손상, [`conflicts()`](crates/nexa-ops/src/batch_rename.rs)도 미감지. 대응 = 1차 감지 후 차단(`Nested` 충돌)·2차 경로 rebase 완전 지원(차후). ② **X-26 SosomLab·제품 홈페이지 + About 화면**. ③ **X-27 툴바 내비(이전/다음/상위) 버튼 mouseover 배경색** — 경로 바 세그먼트 hover 색 재사용. 문서 반영만(코드 무변). 상세 [journal/2026-07-20.md](journal/2026-07-20.md).

## 2026-07-19

- **배포 채널 검토 + 제품명 정리(사용자)**: ① **제품명 nexa-dir2→nexa-dir 전면 정리**(저장소만 nexa-dir2 유지 — 창 클래스 NexaDirMain·데이터 경로 NexaDir[rename 마이그레이션]·산출물·문서·위키). ② **배포 4종 큐 상태**: winget 2 PR = Azure 검증 통과·partiallySucceeded는 **MS Guardian 내부 인프라 경고**(매니페스트 무관)·`@wingetbot run`은 신규 기여자 권한 없음→거부, **모더레이터 검토 대기**. Chocolatey 2 = 이메일 수정 repush 후 자동 검사 Pending→모더레이션. **우리 측 추가 조치 없음**(전부 서드파티 큐 대기). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **Chocolatey `nexa-dir.portable` 0.8.1 게시(사용자 실행)**: 수동 `pack-and-push.ps1` → `choco push` 성공 — **모더레이션 대기**(자동 검사 3종 Pending·신규 패키지 사람 검토 — 승인까지 며칠~2주). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **Chocolatey 포터블 패키지 `nexa-dir.portable`(사용자 요청)**: 신규 ID라 **기존 0.8.1 릴리스를 재사용**(재빌드·새 태그 불필요 — choco가 막는 건 같은 ID+버전 재push뿐). `.portable` 접미사 = choco 관례(`git.install`/`git.portable`), 설치형은 모더레이션 중이라 개명 없이 `nexa-dir` 유지. `Get-ChocolateyWebFile` + **`NexaDir.exe.gui` 마커**(GUI shim). release.yml 2패키지 루프 확장 + **Windows 전용 `pack-and-push.ps1`** 신설(태그 소진 버전용, try/finally 원본 복원). 한계: 제거 시 데이터 소멸(winget portable과 동일). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **winget 포터블 변형 `SosomLab.NexaDir.Portable` 0.8.1(사용자 요청)**: 명명은 **점 구분 변형 세그먼트** 채택(Authoring.md + `calibre.calibre.portable`·`Neovim.Neovim.Nightly` 선례 확인 — 붙여 쓰면 검색에서 형제로 안 묶임). `InstallerType: portable` + `PortableCommandAlias: nexadir`. **주의: winget portable은 제거 시 폴더째 삭제 = 사용자 데이터 소멸**(설치형의 보존 규칙 미적용 — 문서·locale 명시). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **winget 등록 `SosomLab.NexaDir` 0.8.1(사용자 요청)**: 매니페스트 3종(스키마 1.12.0) 작성 → microsoft/winget-pkgs PR. **user·machine 두 스코프**(`/CURRENTUSER`·`/ALLUSERS` — Chocolatey 때의 iss `commandline` 허용 재사용) · `ProductCode = {AppId}_is1` · SHA-256은 Release 자산 직접 다운로드로 대조. 설계 [21 §8](21-distribution.md). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **릴리스 `0.8.1` — Chocolatey 최초 게시(사용자 `CHOCO_API_KEY` 등록 후)**: 0.8.0은 태그 소진으로 재실행 불가 → **코드 변경 없는 버전 승격**으로 파이프라인을 돌려 `choco push`를 최초 발생시킴. 맥 검증 194 green + Windows 타깃 check green. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **Chocolatey 배포 채널 등록(`feat/chocolatey-packaging` — 사용자 요청)**: `packaging/chocolatey/` 신설(nuspec + install/uninstall/VERIFICATION) — **바이너리 미동봉**, 설치 시 Release의 설치형 exe를 **SHA-256 검증 후 다운로드**(PolyForm NC = 비-FOSS → 공식 URL 정석). 함정 2건 해소: ① choco의 관리자 권한만으로는 `PrivilegesRequired=lowest`가 *사용자별* 설치로 새어 **`/ALLUSERS` 명시 + iss `commandline` 허용** 추가 ② step `if`에서 `secrets` 미인식 → job env 승격. release.yml = 체크섬 주입 → `choco pack` → 시크릿 있을 때만 `choco push`. **잔여: API 키 발급·`CHOCO_API_KEY` 시크릿 등록·첫 패키지 모더레이션 심사**. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **릴리스 `0.8.0`(사용자 요청)**: 0.7.0 이후분 — **GitHub Wiki 신설**(19페이지 + docs/wiki 소스 미러) · **다크모드 툴바 아이콘**(테마 본문색 잉크·`-dark` 전용 에셋 11종·svg 파서 **루트 색 상속** 수정·**4배 슈퍼샘플** — 외곽선 흐림 해소) · **exe 리소스 아이콘/VERSIONINFO**(build.rs+rc.exe, 작업표시줄 고정 빈 아이콘 해소) · **배포명 "Nexa Dir"**(창 제목·installer·NexaDir-* 산출물·버전 동기). 게이트: B2 1.43MB·B3 17종. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **릴리스 `0.7.0`(사용자 요청 — 태그 push → GitHub Release)**: **B3 원장 수리** — 07-17 GDI+ 백엔드·07-18 SVG 파이프라인의 gdiplus.dll·shlwapi.dll(SHCreateMemStream) 화이트리스트 누락으로 **main CI 적색** 발견 → 근거 등재로 해소. 게이트 실측(B2 1.40MB·B3 인박스 17종)·@app 0.7.0. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **하단 도크 기본 표시 승격(main 직커밋 — 사용자 요청)**: 실행 중 설정 대조 — 도구모음 순서·표시는 기본값과 이미 일치, 차이는 dock뿐 → `Settings::default` dock=true(테마/언어/글꼴 등 개인 설정은 제외 — DR-5 다크 기본 유지). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **하단 도크 버튼·용어 체계·좌측 도크 설계(main 직커밋 — 사용자 요청 연속)**: ① 툴바 **하단 도크 토글**(사용자 SVG — 듀얼 파일 패널 옆·순서 병합 = 누락 자식 **정의 위치 삽입**으로 개선) ② 도크 꺼짐 = 정보 패널 토글 비활성 ③ **UI 용어 체계 확정** — 파일 패널/File Panel·**하단 도크**/Bottom Dock(사용자 명명)·정보 패널/Info Pane, 영어 라벨 Title Case("Dual File Panels")·"컬럼 넓이→**너비**" 오타 수정(ko/en 31키+주석) ④ **좌측 도크(X-24) 설계 등록**(디렉토리 트리 내비게이터 — 클릭 = 활성 탭 이동) ⑤ 편집 트리 레이아웃 확정(체크 1열 고정·셰브론 체크 뒤·하위 없는 그룹 셰브론 생략)·드라이브 탭 "D:" 표기. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **순서 편집 창 완성(main 직커밋 — 사용자 요청 연속)**: NxOrderTree **블록 드래그 이동**(그룹 = 자식 포함·같은 부모 한정·스왑 미리보기·고스트 박스·가장자리 자동 스크롤·ESC 취소 — 확정 = delta 통지로 모델 정합) · **오버레이 스크롤**(휠+썸 — 그리드 차용·12행 캡) · **키보드 내비**(↑/↓ 단일·Shift 다중[형제 한정]·Ctrl 순서 이동·Space 체크) · ▲▼ = **사용자 SVG 16px**(iconbutton `create_svg`·gdipctx `svg_to_image`·잉크 = style.text) + **선택 없으면 비활성**. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **우클릭 설정 팝업 + 기본 순서 승격(main 직커밋)**: 도구모음 빈 영역 = [순서 편집…][설정…]·컬럼 헤더 = [파일 컬럼…](main 소유 ordereditor 경로·prefs 스펙 공용화) · Context 설정 = prefs **ctxmenu 서브메뉴** 분리 · TOOLBAR_BLOCKS 기본 = **refresh|panel|view|show|settings**(사용자 배치 승격). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **컬럼 드래그 재배열 고도화(main 직커밋)**: 활성 드래그 = **라이브 미리보기 + 고스트 헤더**(커서 추종·세로 고정)·**ESC 취소**(win ESC 체인 선두). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **컬럼 auto-fit·정보 패널 토글(main 직커밋)**: 컬럼 경계 **더블클릭 = 콘텐츠 auto-fit**(가시 행+헤더 실측·상한 = 설정 `col_autofit_max` 400px 기본) · **듀얼 정보 패널 토글 버튼**(CMD_INFO_TOGGLE — 사용자 SVG·요소별 stroke-width 확장·싱글 패널 비활성). 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **툴바 아이콘 SVG 완결(main 직커밋 — 사용자 SVG 연속 제공)**: colsync(fill+text 확장)·dotfiles·hidden(요소별 fill)·refresh(**원호 A 명령** 지원)·settings(톱니) 전환으로 **9종 전부 SVG** — PNG 버킷 기구(emb!/EMBEDDED/png_to_hicon) 폐지. 잉크 3상태 확정(기본 검정/켜짐 = accent 38% 블렌드 배경 + 검정 유지/비활성 알파 38%)·규격 통일(32vb·여백 1·stroke 2)·최대 크기 판·도구 모음 26px. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **트리 보기 SVG 전환(main 직커밋 — 사용자 SVG 제공)**: view-tree.svg 등록(EMBEDDED_SVG 3호)·PNG 3종 제거 — **보기 모드 3종 전부 SVG 단독** 완결. 194 green. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **설정 편집기 3종 공통 창 + 컬럼 기능(`feat/order-editors` — 사용자 요청 3건)**: **ordereditor 공통 모달**(EditorSpec 어댑터 — config 제네릭 순서 문법·NxOrderTree 체크 열) → 툴바/파일 컬럼/컨텍스트 메뉴 설정을 **각각 별도 창**으로(prefs = [편집…] 버튼·실시간 적용). **컬럼 헤더 드래그 재배열**(무드래그 = 정렬·인디케이터·colsync 연동·세션 `panel{i}.cols`)·컨텍스트 메뉴 표시/순서 반영. 실기 QA 전 항목·196+ green. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).
- **타일 보기 SVG 전환(main 직커밋 — 사용자 SVG 제공)**: view-tiles.svg 등록(EMBEDDED_SVG 2호)·PNG 3종 제거(SVG = 전 크기 즉석 래스터). 릴리스 재빌드·캡처 확인. 194 green. 상세 [journal/2026-07-19.md](journal/2026-07-19.md).

## 2026-07-18

- **SVG 아이콘 파이프라인(`feat/svg-icons` — 사용자 요청·SVG 제공)**: "svg 방식도 적용" — **svg.rs 서브셋 파서 신설**(플랫폼 중립·테스트 6종·외부 crate 0: viewBox/rect·circle·line·polyline·path[M·L·H·V·C·Z]·스트로크 전용) + gdipctx `svg_to_hicon`(GDI+ ARGB 오프스크린 → HICON — **요청 크기 즉석 래스터**, 버킷 불필요) + icons `EMBEDDED_SVG` **PNG보다 우선·실패 폴백**. 1호 = 사용자 제공 view-flat.svg(SSOT 보관). 193 green·clippy 0. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **도구 모음 툴팁 i18n(`feat/toolbar-tips` — 사용자 요청)**: hover 500ms 후 버튼 하단 툴팁 — **tip.rs**(NOACTIVATE 팝업·테마 색·dlg_font·NCDESTROY 해제) + chrome `ToolButton.tip`/`hover_tip`/`set_buttons` + win `TIMER_TIP`(250ms 틱·GetCursorPos 실측 이탈 해제·클릭 파괴). 텍스트 = **기존 메뉴 i18n 키 재사용(신규 키 0)**·apply_lang 재주입(언어 전환 즉시). 실기 en/ko 캡처. 187 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **도구 모음 16×16 이미지화 + 패널 모드 버튼(`feat/toolbar-icons` — 사용자 요청 5건)**: ① 보기 모드 라벨 = **Tree/Flat/Tile view**(ko 트리/플랫/타일 보기) ② 툴바 전 버튼 = **임베드 16×16 이미지**(64px GDI+ 벡터 → 16/20/32 DPI 버킷 다운스케일 — `emb:` 키 → gdipctx `png_to_hicon` → 기존 HICON 캐시 동기 분기, 글리프 = 미로드 폴백) ③ refresh = 300° 호 + 접선 화살촉·view-tree = 타일 동일 점유 확대(사용자 시안 반영 2건) ④ **[듀얼][싱글][컬럼동기] 그룹 신설** — 싱글 모드 = 듀얼·컬럼동기 **비활성**(비활성 = **알파 38% 변형 이미지** — 라이트 테마 반전 결함 QA 발굴·수정, ToolButton `enable` 빌더·`#dis`→`-disabled` 해석) ⑤ 핸들러 툴바 checked/enabled 동기. 실기 라디오 전환·비활성 클릭 무시·영속 왕복 QA. 187 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **카드 스크롤·Ctrl 다중 정렬·컬럼 폭 동기화(`feat/card-scroll` — 사용자 요청 6건)**: ① **NexaCardHost** 스크롤 뷰포트(그리드 오버레이 바 규약·하단 바 상시·kind별 높이 fitting·하단 5px) ② 다중 정렬 트리거 **+Ctrl**·순번 ①② **상시 표시**(rows·NxGrid) ③ **컬럼 넓이 동기화** 설정(보기 메뉴 4종 하단·기본 on·영속·off=패널 독립·탭=패널 상속·세션 `panel{i}.colw` — 탭별 확장 예약·DPI 리셋 이후 복원 픽스) ④ Dual/Single file panel(s) 라벨. 실기 on/off/복원 3종. 188 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **ImageFit + ctl 문서 세트(`feat/imagebtn-fit` — 사용자 요청 2건)**: NxIconButton **이미지 표시 모드**(`Native` 원본 크기 중앙/`Stretch` 컨트롤 맞춤 — gdipctx image_size) · **docs/ctl/ 신설**(내비게이션 허브 README + overview 공통 규약 + 컨트롤별 13문서 — 색인·API·메시지 계약 표, docs/README 연결). 187 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **ctl 재사용 리팩터 + 전수 검토(`refactor/ctl-base` — 사용자 요청)**: **base.rs** 신설로 14개 Nx 컨트롤 생명주기 보일러플레이트 통합(state/attach/drop_state/notify/register_class — 순 −107줄)·GDI 자원 RAII화(segmented icon_font·textbox bg_brush = Drop). **전수 검토**: GDI 누수 0(gdipctx 오류 분기까지 짝 맞음·region 시스템 소유·grid 더블버퍼 정상)·**잠재 크래시 1건 수정**(segmented 빈 텍스트→DrawTextW 댕글링, grid 크래시 동일 클래스)·나눗셈/캐스트/인덱스/MDL2 라우팅/내비 활성 모두 안전. clippy 0·187 green. 후속: 팝업 3종 골격 계층화(보류 — 안전 우선). 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **디스클로저 MDL2 + 내비 활성 상태(`feat/mdl2-disclosure` — 사용자 요청 2건)**: ① 폴더 마커 ▸/▾ → **MDL2 E76C/E70D**(glyph_opaque 전환·`mdl2_small_format` 9 DIP — 원본 규약) ② **ToolButton.enabled**(비활성 = text_dim 흐림·hover/클릭 무시) — 이전/다음 = 히스토리 가능 시만·상위 = **This PC 비활성**(sync_chrome 동기). 실기: 초기/이동 후/내 PC 상태 3종 + 닫힘/펼침 셰브론 캡처. 187 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **Segoe MDL2 글리프 통일(`feat/mdl2-glyphs` — 사용자 확인·요청 3건)**: 원본 화살표 정체 = **Segoe MDL2 Assets 글리프**(내비 E72B/E72A/E74A·디스클로저 E76C/E70D) 확인 → 현 앱 적용: DwBackend `mdl2_format` 신설 + glyph_opaque **U+E700 대역 자동 라우팅**(내비 버튼 ←→↑), NxSegmented 화살표 = 벡터 폐기·MDL2 E72A/E72B(컨트롤 소유 icon_font). 실기 캡처 검증. 187 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **다중열 정렬·6종 확정·ExtDot 픽스·화살표(`feat/nxgrid-sort` — 사용자 요청 5건)**: ① **NxGrid 다중열 정렬** = 원본 docs/24 §4 규약 이식(메인 파일 목록은 기이식 확인) — 3상태 순환·Shift 다중키·▲▼ 앞/①② 뒤·비교 = 호스트(NXGR_SORT+sort_spec), bulkrename은 order 맵으로 표시만 재배열(토글 매핑 유지) ② **ExtDot 이중 점 진범** = "점 유실 시 복원" 규약 — 결과 그대로 채택+전체 이름 재분해로 픽스, 전수 점검(연번/날짜 동일 증상·치환 점 제거 무효화·Case 무영향) ③ **동작 6종 확정**(Replace Text/RegEx·Insert Text·Change Case·Add Number Sequence·Add Date — 이동/확장자 카드 제거·구 프리셋 블록 스킵) ④ **NxSegmented 화살표** = AA 벡터(긴 샤프트+큰 머리 — 원본 내비 참고). 187 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **Tab 내비 + 3단 체크·헤더 전체 토글(`feat/bulk-tab-tristate` — 사용자 요청 3건)**: ① **Tab = 배치 순서 이동** — IsDialogMessageW(모달 3곳)+CONTROLPARENT(카드·텍스트박스·스핀 래퍼 — 탭 착지 = 내부 EDIT)+WANTARROWS(콤보/세그/메뉴 방향키 보존) ② **NxCheckBox 2단/3단**(CheckMode — 0/1/2·3단 순환·부분 = accent 박스+흐릿한 ✓ 블렌드) ③ **NxGrid 헤더 체크** = 전 행 집계(전체/부분/해제) 표시·클릭 전체 토글(NXGR_ROW_ALL→호스트 재동기) ④ 체크 열 폭 = 필요 폭만. 실기 24↔0·3단 순환. 186 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **NxGrid 깜빡임 제거(`fix/grid-flicker` — 사용자 QA)**: 진범 = 전체 무효화 erase=true+WM_ERASEBKGND 배경 플래시 → **WM_PAINT 더블버퍼**(메모리 DC→1회 BitBlt)+erase 전면 배제. 입력·키보드 내비 blink 소멸. 186 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **하단 행 재편(`feat/bulk-bottom-bar` — 사용자 요청 5건)**: Apply → **"이름 변경(Rename)" NxButton Default**(활성 = NXBTN_SETENABLE — idle 회색·변경 시 파랑) · 하단 우측 [취소][이름 변경] 실측 정렬 · 프리셋 [… ⌄]+건수 = 구 Apply 자리(하단 좌측) · 미리보기 그리드 = 우측 컬럼 전고 확장 · **GridOpts.row_h**(파일 목록 20px @96dpi 동일 산식 — 미리보기·관리 목록 적용). 실기 왕복(24건 실변경·원복). 186 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **NxGrid(ctl 14호) + 프리셋 v2 + macOS 팝업 시안(`feat/nx-grid` — 사용자 요청 7건·시안 5장)**: ① **NxGrid** = 컬럼 리사이즈·체크 마크 열(미리보기 Apply — 행별 제외)·**오버레이 스크롤바 세로/가로**(macOS — 스크롤 시 얇은 썸 6px→900ms 페이드·썸 드래그 = 일반 바 10px+트랙, 픽셀 실측 검증)·**행 선택 파일 목록 규약**(Shift 연속·Ctrl 비연속·Ctrl+방향키+Space·Ctrl+A·포커스 프레임)·**GridOpts 확장**(no_header·zebra·outline·Mark::Check/Minus — "셀 데이터화" 계약) ② **user32 AV 진범** = 빈 문자열 셀의 빈 Vec을 DrawTextW에 전달(런타임 이분 — 원장 채록) ③ **프리셋 v2**: […⌄] 메뉴 = [프리셋들/─/Save/Edit] + 변경 건수 라벨 동일 행 · Save = **무캐션 DWM 라운드 팝업**(기본 이름 전체 선택·[취소][OK] 우하단) · Edit = **지브라 무헤더 목록 + 행별 빨간 ⊖**(스테이징 — OK 확정/취소 폐기) ④ NxMenuButton 구분선·NxTextBox EM_SETSEL 위임·Style.danger. 실기 왕복 QA(저장→보존→삭제·선택 3종·오버레이 수치). 186 green. 상세 [journal/2026-07-18.md](journal/2026-07-18.md).
- **프리셋 = `…` 메뉴버튼 + 라벨 우측 정렬(`feat/preset-menubutton` — 사용자 확정)**: 프리셋 행 = [이름 NxTextBox][`…` 메뉴] 한 줄(항목 0 = 저장·1.. = 프리셋 클릭 불러오기 — 카드 스택 복원·저장 후 메뉴 재생성) · 카드 라벨 = 우측 정렬. 실기 저장/재구성/복원 왕복 검증. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).

## 2026-07-17

- **카드 폼 마감(`fix/ctl-borders` — 사용자 QA·요청 4건)**: ① **테두리 위계**(동색 배경 구별 — 콤보 외곽선·타이틀 필 진하게·비활성 진하게·체크/버튼 외곽선) ② **NxLabel(ctl 12호)** + 카드 전 kind **좌측 정렬 라벨 열**(PF 구도·한 줄 복수 쌍[시작 값/접두]) — **라벨 폭 = 언어별 실측**(한/영 정렬 유지 검증) ③ **NxMenuButton(ctl 13호)** `… ⌄` 오버플로 메뉴(좁은 자리 액션 — NXMB_PICK/GETPICK·팝업 폭 실측). 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **X-23 카드 재편 — 일괄 이름 변경 = 카드 스택(`feat/x23-card-rename` — 사용자 시안 5장·후속 확정)**: 아키텍처 전환 "폼+리스트박스" → **NxGroupCard 스택 = 파이프라인**(카드 타이틀 = 동작 콤보 8종 + ⊕⊖ — **+ = 아래 카드 추가·− = 삭제·마지막 1장 불가**), kind 본문 **동적 재구성**·지역 id·**실시간 미리보기**. Replace **Text/RegEx 분리**·Number **Padding 콤보**·Date **Format = `${토큰}` 텍스트 입력 + ? 도움말**(코어 문법 개정·구식 프리셋 자동 이행)·프리셋 = 카드 복원(전 동작 역매핑). 실기: 카드 왕복·실시간 카운트 1건. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **NxSegmented 개선 + NxButton 신설(`feat/nx-segmented-style` — 사용자 시안)**: 세그먼트 = `SegOpts`(라운드/스퀘어·**간격 기본 0px**) — 연회색 컨테이너 + **선택 accent 필**·AA·호출부 5곳 이관 · **NxButton**(ctl 11호) = 기본/Default(accent)/Disabled 3상태·라벨 자동 폭·**컴팩트 기본 높이(글꼴+상/하 2px** — 사용자 QA)·Space/Enter. 실기 3상태 렌더·통지·비활성 무통지. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **NxSpin 재배치(`fix/nx-spin-layout` — 사용자 시안 재개정)**: 독립 라운드 글상자(**숫자 우측 정렬**·1px 하향) + **우측 분리 버튼 블록**(폭 = 높이 2/3 축소·높이 = 글상자 종속·min/max 비활성 유지·behind 블렌드 전신 AA). 콤보·글상자 텍스트 1px 하향(main 직커밋)도 동일 QA 흐름. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **NxSpin 버튼 재설계 + NxDropList 은퇴(`feat/nx-spin-buttons` — 사용자 확정)**: ① 스핀 버튼 블록 = **정사각(변=높이)·개별 상/하 1/2**·**min/max 도달 방향 비활성**(연한 셰브론+클릭 무시·타이핑 도달도 재도장) — AA 셰브론(시안 범위만) ② **NxDropList 제거**(중복 보고→결정: NxComboBox 통일 — bulkrename 3곳·갤러리 1곳 이관, ✓ 팝업 스타일) ③ NxIconButton 기본 지름 = **글꼴 높이(체크박스 박스 동일** — 15px). 실기: min ⌄ 무시·⌃ +1·벌크 콤보 3개 정상. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **ctl AA 렌더링 — GDI+ DrawCtx 백엔드(`feat/ctl-gdiplus-aa` — 사용자 QA "raster 이미지 느낌"·지시 "GDI+는 DrawCtx 백엔드로만, ctl 직접 호출 금지")**: 진단 = GDI 무AA + **1비트 리전 클립이 곡선 계단 진범**. DrawCtx에 AA 프리미티브 4종(fill_ellipse/fill_round_rect/stroke_round_rect/polyline — 기본 no-op) 신설, **GdipCtx 백엔드 = 유일한 GDI+ 접점**(gdiplus.dll 인박스·크레이트 0 유지, D2D 승격 시 이 모듈만 교체). 규약 개정(사용자 승인): ctl → DrawCtx 트레이트 참조 허용. 4종 적용(체크·아이콘버튼·텍스트박스·콤보+팝업), 리전 클립 폐기 → **Style.behind**(부모 배경색) 블렌드, 텍스트 = GDI/DW 유지(사용자 확정). 실기 캡처 계단 제거·기능 회귀 없음. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **ctl 9·10호 NxTextBox·NxIconButton + 공통 자동 높이(`feat/nx-textbox-iconbtn` — 사용자 요청·macOS 시안)**: ① **공통 자동 높이** `style::auto_height`(글꼴+상/하 4px) — 전 Nx 컨트롤 `h<=0` 동일(23px 실측, "수정 없이 배치해도 반듯") ② **NxTextBox**(라운드+포커스 accent 2px 링·내부 EDIT 세로 중앙·텍스트 API 위임·CTLCOLOR 브러시 보관) ③ **NxIconButton**(설계 확정: **단일 컨트롤+Icon enum** — 도형별 서브클래스 대신 도형=데이터·PNG 알파는 enum 확장 변형 후속): **shape 투명 = 원형 리전 클립**(어떤 부모 배경에서도 도형만) · 비활성 = sel_bg 원·클릭 무시. 갤러리 검증(+ 통지·− 무통지·파랑 포커스 링·회색 밴드 위 원형). 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **ctl 7·8호 NxComboBox·NxCheckBox + Nx 명명 규약(`feat/nx-combobox` — 사용자 요청·macOS 시안)**: ① 규약 확정 — 컨트롤 이름 **Nx 접두어·네임스페이스 Nexa**(Win32 클래스 `Nexa.Nx*` 전면 개명) ② **NxComboBox**: 라운드 필 본체(리전 클립)+이중 셰브론(펜)·**✓ 체크 팝업**(hover=accent)·**높이 = `h<=0` 자동(글꼴+상/하 최소 여백 4px)·크면 균등 여백 중앙**(사용자 확정)·owner 승격 함정 선반영 ③ **NxCheckBox**: 미체크 연회색/체크 accent+흰 ✓(시안 1:1)·클릭/Space 토글 ④ **GroupCard 타이틀 자식 배치**(`title_rect` — 타이틀 자리에 동작 선택 콤보 = PF 구도). 갤러리 실기: 콤보 선택 "Change Case" 반영·체크 토글 0→1→0·통지 투과. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **ctl 6호 GroupCard + 검증 갤러리(`feat/ctl-groupcard` — 사용자 요청)**: 타이틀 밴드+본문 그룹 컨테이너(라운드/각진 선택 — 창 리전 클립, **영역별 높이 각각 지정**, 타이틀 = 윈도우 텍스트 위임, **자식 통지 투과** = WM_COMMAND/CTLCOLOR* 등을 카드 부모로 — 중첩 투명성). 검증은 기존 기능 무변경 원칙대로 **별도 갤러리 창**(ctldemo — 0x8009 주입 전용): 카드 2종에 droplist/segmented/spin 혼합·투과 통지 상태줄·캡처+픽셀 판독. 향후 일괄 이름 변경 동작별 카드 재편(X-23 등재)의 토대. **임시 도구 모음 버튼 🃏**(`feat/ctldemo-toolbar` — 연타 중복 가드, X-23 완료 시 제거)로 실기에서 바로 연다. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **X-22 실기 QA 2건(`fix/x22-qa-droplist` — 사용자 QA)**: ① droplist 팝업 크래시 진범 = **WS_POPUP owner 승격**(자식 컨트롤 owner → 시스템이 최상위로 교체 — GetParent(팝업)이 다이얼로그를 돌려줘 BrState를 DlState로 오독·크래시) — 팝업 자신의 USERDATA에 owner 저장으로 해소 + fontbox KillTimer 동일 진범(60ms 타이머 누수) 수리 ② **메뉴 명령 설정 유실** — run_command 경로(언어·테마·토글 6곳)는 종료 저장 의존 → `persist_settings()` 즉시 영속(비정상 종료 유실 방지). 실기: 팝업 4항목 렌더·선택 반영·생존 + F6 직후 settings.cfg 생성 확인. 186 green. 상세 [journal/2026-07-17.md](journal/2026-07-17.md).
- **X-22 일괄 이름 변경 v2 구현(`feat/x22-batch-rename-v2` — 사용자 지시: 전체 직접 개발 컴포넌트)**: 코어 전면 재작성(스코프·위치 클램프·Mode·감싸기·**Date 토큰 엔진**·v1 프리셋 하위호환 — 32 green) + **ctl 3종 신설**(segmented·spin·droplist — 판매용 추상화: Style 인자·계약 명세) + UI 재구성(적용 대상/Mode droplist·PF식 케이스 세그먼트·위치 spin+방향·날짜 패널·**✓ 미리보기+건수**). 실기 캡처+기능 왕복 검증. 186 green. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **일괄 이름 변경 v2 설계(사용자 주도 PF 분석)**: Path Finder 6동작 전수 대조 → 갭 6건(스코프·Mode·**Add Date**·임의 위치·Number 감싸기·미리보기 ✓/건수) 확정, 설계 [22-batch-rename-v2.md](22-batch-rename-v2.md) 신설·X-22 등재(S1 코어/S2 UI/S3 미리보기 슬라이스). 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **shell: 특수 폴더(`feat/shell-folders` — 사용자 요청)**: 경로 바에서 `shell:startup`·`shell:common startup` 등 **탐색기 전체 이름 지원** — 수동 테이블 대신 `SHParseDisplayName` 위임(KnownFolders 전부+GUID 형태 자동 커버). 가상 폴더/미지 이름 = 원문 유지(기존 격리). 테스트 3건. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **X-20 우 패널 무반응 진범(`fix/panel-modes-click` — 사용자 QA)**: 도크 클릭 라우팅 `y >= dock.y`가 0-rect 도크(싱글 정보의 우 패널)에서 **항상 참** — 클릭 전부가 빈 도크로 삼켜짐. `h > 0` 가드(panel 1곳+win 2곳). 클릭 주입 검증(선택 1). 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **X-20 공백 화면 진범(`fix/panel-modes-splitter` — 사용자 QA)**: 싱글 모드에서 스플리터 폭이 음수 → GDI ETO_OPAQUE **rect 정규화**가 패널/도크 전체를 border 색으로 덮어칠 — 폭 양수 가드 + 공유 도크 재도장(우 부분 행 침범). PrintWindow 캡처 판독으로 두 모드 정상 확인. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).

## 2026-07-16

- **패널/정보 모드(`feat/panel-modes` — 사용자 요청·원본 FR-C1)**: **싱글/듀얼 패널**(싱글 = 우 패널 레이아웃만 숨김 — 탭/세션 보존, 복귀 원복)·**싱글/듀얼 정보**(싱글 = 전폭 공유 도크, 내용 = 활성 패널 추종). **선호값/효과 분리**로 "싱글 패널 = 싱글 정보 고정(변경 불가·상태바 안내), 듀얼 복귀 시 정보 모드 원복" 규칙 충족. 메뉴 라디오 4종·영속·왕복 테스트. 179 green. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **fontbox 캐럿 마무리(`fix/fontbox-caret` — 사용자 QA)**: 선택 확정 후 캐럿을 텍스트 맨 뒤로(EM_SETSEL — `,` 체인 이어 입력 자연). 실기 Enter 경로 재검증(캐럿 14/14). 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **fontbox 교체 규칙·Enter(`fix/fontbox-enter` — 사용자 QA)**: 선택 규칙 개정(**콤마 선입력 시에만 체인** — 없으면 A→B 교체)·Enter 미확정 진범 = **모달 펌프 VK_RETURN 가로채기** → `FBM_HAS_DROP` 질의로 드롭 열림 시 컨트롤 통과. 실기: ↓↓+Enter 교체 확정. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **fontbox QA 4건(`fix/fontbox-qa` — 사용자 QA)**: 클릭 미반영 진범 2중(**컨테이너 MA_ACTIVATE + LISTBOX DOWN의 SetFocus** → 킬포커스가 클릭 전에 팝업 파괴) — NOACTIVATE·DOWN 삼킴(좌표 선택)·킬포커스 가드. 하단 회색 띠 = LBS_NOINTEGRALHEIGHT. **바깥 클릭 닫기**(60ms 감시). **HUD 제거**(사용자 확정 — 매칭 스크롤만). 실기 재검증: 항목0 클릭 → "Segoe UI, Agency FB". 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **ctl 2호 글꼴 입력 fontbox(`feat/ctl-fontbox` — 사용자 요청·WT 피커 참조)**: 클릭 = **설치 글꼴 드롭다운**(각 항목을 그 글꼴로 렌더·NOACTIVATE 팝업 — 에디트 포커스 유지)·키보드/마우스 탐색·**쉼표 체인 선택 규칙**(접두사=교체·완결=뒤에 추가 — 단위 테스트)·**커서 위치 타입어헤드 HUD**+매칭 스크롤. prefs 글꼴 6곳 교체·확정=즉시 적용 재발행. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **ctl 커스텀 컨트롤 라이브러리 + 검색박스 컴포넌트(`fix/search-clear-zorder` — 사용자 요청)**: ✕ 겹친 형제 접근의 z-순서 진범을 **자기완결 컨트롤**(NexaSearchBox — 테두리·✕를 자기 영역에 그리고 자식 EDIT가 입력)로 근본 해소. **세로 중앙 정렬**(한글 상단 붙음 — EDIT를 글꼴 높이로 중앙 배치)·드롭인 계약(텍스트 위임+EN_CHANGE 재발행). `ctl/` = Win32 커스텀 컨트롤 묶음 1호. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **폰트 폼 QA 3건(`fix/font-form-qa` — 사용자 QA)**: **스크롤 공백 진범**(rebuild가 파괴 후 editors 비움 → 파괴 중 EN_KILLFOCUS 재진입 harvest가 빈 값 수확 — 순서 교정)·크기 = **입력 가능한 콤보**(프리셋+CBN_SELCHANGE 즉시 적용)·**Enter 즉시 적용**(모달 펌프 VK_RETURN — 이름/크기 공통)·검색 ✕ = SS_NOTIFY 스태틱 전환(실클릭 검증). 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **X-12 폰트 슬롯 + 검색박스 내장 ✕(`feat/x12-font-slots` — 사용자 요청)**: DwBackend **스타일 포맷 맵**(기본/파일 목록[+굵게·이탤릭 장식]/상태바 — 캐시 키 스타일 비트 합성) + `DrawCtx::select_font`(위젯이 페인트 시작에 자기 슬롯 선택) + 설정 **Kind::Font(이름+크기 1줄)** — 기본/콘솔(터미널에서 이동)/우클릭 메뉴(저장만 — HMENU=OS 폰트 α)/상태바/파일 목록+**폴더 굵게**/헤더 **굵게·이탤릭**/대화상자. 검색창 = **내장 ✕ 커스텀**(입력 시만 표시·클릭 전체 지우기 — 실기 검증). 178 green. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **보기 모드 탭별 전환(`feat/view-mode-per-tab` — 사용자 요청)**: 모드를 **탭별 상태**로(활성 탭 전용 적용·새 탭 계승·`panelN.modes` 세션 영속) + **도구 모음 라디오 3버튼**([├─][☰][▦] — 1개만 선택) + **update_status 단일 동기 지점**(탭/패널 전환 시 현재 탭 상태 자동 반영). 178 green. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **설정 그룹 페이지 위계(`fix/prefs-group-page` — 사용자 QA)**: 하위 메뉴 링크를 본문 폰트로 축소(제목과 구분)·그룹 직속 상세 설정은 링크 아래 렌더(메커니즘 선비치). 도구 모음 토글은 연한 배경만(하단 줄 제거 — main 직커밋). 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **보기 모드 3종(`feat/view-modes` — 사용자 요청·스크린샷 확정)**: **타일 보기**(탐색기 타일 — 아이콘 32px `L|` 라지 키+이름/보조 줄 그리드·러버밴드 rect 교차·그리드 키보드)·**일반 폴더 보기**(펼침 차단 평탄 리스트)·**트리 폴더 보기**(기존) — 보기 메뉴 라디오·영속·전 탭/새 소스 계승. **내 PC**: 드라이브 컬럼(종류·전체 크기·사용 가능한 공간) 자동 전환 + 타일 **용량 바**(GetDiskFreeSpaceExW). rows 기하 그리드 일반화·Tree::collapse_all. 테스트 177 green. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **X-17 가상 최상위 "내 PC"(`feat/x17-my-pc` — 사용자 요청)**: 드라이브 루트에서 Alt+↑ = **드라이브 목록 뷰**(내 PC) → 드라이브 간 이동. 센티널 `::PC::` + 항목 이름=절대 경로(`C:\`)로 **기존 파이프라인(진입·선택·아이콘·세션) 그대로 동작** — 열거부 분기·표시(i18n)만 추가. 떠난 드라이브 자동 선택(G-7)·수정일 빈 셀·kind.drive. 테스트 3건, 173 green. 볼륨명·용량 데코=β. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **배포 2채널(`feat/dist-installer` — 사용자 요청·DR-3 개정)**: 포터블(exe 1개 = 배포 전부 — 최소파일 명문화) + **설치형 exe(Inno Setup — 사용자별 설치 기본·제거기·설치 파일도 exe 1개)** 병행 — release.yml이 태그 push 시 **2종 동시 첨부**. 전제 조건 **data_dir 폴백**(쓰기 프로브 실패 시 `%LOCALAPPDATA%\NexaDir\data` — Program Files 대응) 구현+테스트. 설계 [21-distribution.md](21-distribution.md) 신설. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **최적화 패스 X-16(`refactor/optimization-pass` — 사용자 요청)**: 전체 코드 3축 점검(구조·핫패스·크레이트 경계 — 의존 그래프 깨끗·죽은 코드 없음 확인) 후 5커밋 — redundant clone 8건 · `[workspace.lints]`+widgets 파사드 · **셀/아이콘 렌더 핫패스 `Tree::row_ref`**(통 VisibleRow 클론 제거 — 행당 힙 할당 ~5회↓) · **DW 레이아웃 캐시 중첩 맵**(히트 경로 무할당) · **아이콘 틱 전창 무효화 → 목록·런처 rect**(초기 로딩 초당 ~12회 전창 재도장 제거)+mouse_xy/Invalidations 정리. 대형 구조 개편(모달 공통화·설정 매핑 단일화·win.rs 분할·중립 모듈 lib 승격 등 7건)은 X-16 백로그로 등재. 170 green. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **X-13 런처 CRUD 1/2(`feat/x13-launcher-crud` — 병합 보류)**: LauncherItem.icon + `launcherN.icon` 별도 키 직렬화(하위호환)·바 아이콘 오버라이드. 2/2 설정 편집기 잔여. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).
- **할 일 등록(사용자 요청)**: X-15 오프라인 라이선스 인증(CNG 서명 검증 — crate 0 유지 설계) · X-17 가상 최상위 "내 PC"(드라이브 간 이동). 배포 구성(포터블 최소파일+설치형 exe CI)은 별도 브랜치 진행. 상세 [journal/2026-07-16.md](journal/2026-07-16.md).

## 2026-07-15

- **세션 디바운스 자동 저장+진행 창 카운트다운(`feat/session-autosave` — 사용자 요청)**: 탭/경로 변경 = Panel 더티 플래그 → **1초 디바운스 재무장**(폭주 시 마지막 상태만 1회 flush — 원본 SESS 코얼레싱 이행·크래시 방어) · 진행 창 완료 = **[닫기 (N)] 카운트다운 버튼**(자동/클릭 즉시 닫힘). 168 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **설정 배치 2 QA 5건(`fix/prefs-batch2-qa`)**: 진행 창 중첩(erase=true)·**공백 라우팅**(WM_CHAR — 접두사 입력 중 통과)·파일 목록 **그룹 분할+본문 휠 스크롤**·**3×3 이미지 피커**(PosGrid 오너드로)·리셋 ms **표시값 누락=저장 리셋 진범** 수정. 168 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **설정 배치 2(`feat/prefs-batch2` — 사용자 요청 ①~③)**: 전송 완료 **2초 자동 닫기**(PROG-WIN) · **탭 더블클릭 동작**(닫기 기본/고정/잠금)+**탭 고정(핀) 신규**(📌·그룹 앞 정렬·세션 영속·우클릭 메뉴) · **타입어헤드 옵션 전면 노출**(범위 FindScope 3종·리셋 ms·HUD 3×3·특수문자/공백/Backspace). ④런처 CRUD·⑤우클릭 메뉴 설정·⑥폰트 슬롯 = X-13/X-14/X-12 등재. 168 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **정렬 옵션 전파 수정(`fix/sort-opts-propagate` — 사용자 QA "순서 불변")**: replace_source가 빈 위젯 정렬로 소스 기본 정렬을 **열거 순서로 퇴행**시키던 결함(NTFS 열거 순서가 은폐) + 새 소스(탐색·재로드·새 탭)에 정렬 옵션 미전파 — Panel 보관·apply_sort_opts 재적용·TreeSource 기본 키 Name asc. 회귀 테스트(토글→재로드→새 탭 유지). 168 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **대소문자 정렬 = 대문자 그룹 상단(`fix/case-sort-group` — 사용자 스크린샷 확정)**: 코드포인트 순으로 교체(Abc.txt < a c.txt < abb.txt — NTFS 저장 순서와 무관·앱 정렬이라 제약 없음 확인). 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **대소문자 정렬 규칙 보완(`fix/case-sort-upper` — 사용자 QA)**: 코드포인트 비교 → `cmp_cs_upper_first`(**알파벳 순 유지 + 같은 이름은 대문자 우선** — Apple<apple<Banana·README<readme). 테스트 167 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **Alt+↑ 선택 위치·대소문자 정렬(`fix/navup-align` — 사용자 QA 2건)**: `nav_up_align` 설정(상단/중단/하단 라디오·기본 중단 — select_program_aligned 명시 배치) · `SortSpec.case_sensitive`+`sort_case_sensitive` 설정(폴더 우선 아래 체크박스·코드포인트 비교·전 탭 즉시 재정렬). 실기: 설정 노출·영속 확인. 테스트 166 green·B2 1.04MB. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **X-3·X-10 잔여·쉬운 갭 배치(사용자 지시 — 브랜치 3개)**: **X-10 완결**(트리 매치 수 "(N)"·다중 토큰 AND+"N개 일치"·설명 문장 9종·수정됨 accent 바·"카테고리: 항목" 접두 — 기본값 복원만 잔여) · **X-3 완결**(term_wrap·term_cols 80~1000+가로 스크롤 Shift+휠/가로 휠·설정 노출·영속) · **쉬운 갭**(G-7 위로 시 떠난 폴더 선택·G-12 webp·G-13 폴더 우선 정렬 옵션) · **런처 시드 v2**(pwsh·cmd 아이콘 — launcher_seed 마커 1회 마이그레이션). 테스트 166 green·B2 1.03MB. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **설정 창 드릴다운·검색 유지·✕(`fix/prefs-tree-drill` — 사용자 QA 3건)**: **그룹 = 하위 메뉴 링크 페이지**(세부는 하위 선택 시 — SS_NOTIFY 링크 클릭 이동) · **검색 유지 탐색**(트리/링크 클릭에 검색어 보존 — leaf = 그 카테고리 매치 상세만·그룹 = 매치 자식 링크만·비움 = 일반 복귀) · 검색창 **✕ 빠른 지우개**. 자동화 검증(덤프): 검색 중 터미널 클릭 = 검색어 유지+매치 2항목. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **설정 창 트리 검색 필터(`fix/prefs-tree-search` — 사용자 QA, X-10 ①)**: 검색 시 좌측 트리 = **라벨 매치 또는 하위 상세 설정 매치 노드만**(조상 경로 유지·그룹 매치=하위 전체·검색 중 마커 ▾ 고정·클릭=검색 종료 이동). 자동화 검증: "글꼴" → 4개(터미널은 상세 매치로 포함)·비우면 8개 복원. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **설정 창 전면 개편(`feat/prefs-tree` — 사용자 지시)**: 사이드바 = **계층 트리 단일 컴포넌트**(오너드로 LISTBOX 1개 — 들여쓰기·▸/▾·선택 하이라이트, 정적 pre-order TREE+펼침 상태 평면 투영) · 클릭 = 우측 세부(leaf=카테고리·**그룹=펼침 토글+하위 부제목 섹션 전체**) · 계층 = 일반(모양·글꼴·언어)/파일 목록/하단 패널(도크·터미널). 자동화 실기 검증(창 개방·항목 8·접기 8→5→8). 테스트 166 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **M5-1 보완 배치(`feat/m5-ui-polish` — 사용자 요청 4건)**: ① 런처 **exe 셸 아이콘 16×16 정사각 버튼**(`ToolButton.icon`·draw_icon 반환값 폴백) ② 런처 **그룹 구분선**(`launcherN=-`) ③ 도구 모음 **토글 시각 보강**(배경 sel_bg+글자 accent+하단 줄) ④ **일괄 이름변경 순서형 파이프라인 재설계** — RenameOp 목록(치환[**정규식** regex-lite·$1 캡처]·대소문자·삽입·연번·**구간 이동**·**확장자 변경**)+블록 추가/▲▼/삭제 UI+**프리셋 저장/불러오기**(`data\renames\*.cfg` 직렬화 왕복)+정규식 사전 검증. regex-lite 원장 등재(B2 +0.11MB → 1.01MB). 테스트 166 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **M5 마감 게이트 → `0.6.0`**: B1 유휴 RSS(10k·정보 뷰 도크·런처 바·300s·3회) **중앙값 16.86MB ≤ 30 통과**(18/16.86/4.12 — 재상승 편차 관찰은 β arena 회수와 공동 과제) · B2 **0.90MB** · B3 인박스 15종 무변. 테스트 163 green·fmt. **M5 마감 마일스톤 완료** — `0.6.0` 태그 push = 릴리스 파이프라인 첫 실행 검증(M5-2). 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **M5-1 일괄 이름변경 α(`feat/m5-bulk-rename` — 원본 docs/25 스펙 최초 구현·원본도 설계만)**: **nexa-ops::batch_rename**(순수 — 치환→대소문자→삽입→연번 고정 파이프라인·이름부만·충돌 4종 검출) + **bulkrename.rs 다이얼로그**(동작 폼+실시간 미리보기 LISTBOX·충돌 ⚠ 표시·[적용]=충돌 0일 때만) + 적용=**MoveBatchOp 트랜잭션 1건**(Ctrl+Z 배치 되돌림·F18 접두사 치환). 진입 = 편집 메뉴·Ctrl+Shift+R. 테스트 163 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **M5-1 퀵 런처 바(`feat/m5-launcher` — 원본 docs/44 이식)**: 도구 모음 아래 **상시 표시 런처 바**(Toolbar 재사용·라벨 버튼) — `%path%`→활성 패널 폴더 치환 ShellExecuteW·실패 상태바 격리·보기 메뉴 토글·settings.cfg 영속(`launcherN=라벨|exe|인자`·count로 부재/비움 구분)·첫 실행 VS Code 시드(후보 3경로). `current_settings` 헬퍼 추출. 실기 QA 대기. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **M5-3 접근성·IME 마감+서명 결정(`feat/m5-a11y`)**: **UIA SelectionItem 실동작**(Select/Add/Remove → `WM_APP_UIA_SELECT`로 UI 스레드 전달·`select_program` 범위 방어) · **구조 변경 이벤트**(uia_notify가 (패널·경로·행 수) 서명 추적 → ChildrenInvalidated 발행 — M2-7 1차 한계 2건 해소) · **리네임 인라인 IME 조합 창 배치**(`rename_edit_info` 신설·필드 기하 paint와 단일 원천 — M3-2 α 한계 해소) · **서명 = 무서명 유지 확정**(DR-3 갱신 — 원본 PKG-4 공동 보류·Store/OV 비용 결정 대기·SmartScreen 감수·인증서 확보 시 release.yml 서명 단계). 테스트 160 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **M5-2 릴리스 파이프라인(`feat/m5-release-pipeline`)**: `release.yml` 신설 — 버전 태그 push → test+**예산 게이트(B2·B3)** → `NexaDir-<버전>-win-x64.exe`(포터블 단일 exe — DR-3)를 **GitHub Release 자동 생성·첨부**(자동 노트). 릴리스 절차 = `git tag X.Y.Z && git push origin X.Y.Z`(docs/18 §5 SSOT 갱신·§6 재번호). 첫 태그 실행 검증 대기. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **UX QA 6차(main 직커밋, 사용자 QA/요청 4건)**: ① **설정 창 원본 UI 재현(X-9)** — 사이드바(검색+오너드로 카테고리 하이라이트)+구분선+우측 섹션 제목·라벨 일체형 체크박스·라디오 그룹(테마/언어)·터미널 카테고리 신설(Win32_UI_Controls 피처 — 신규 DLL 없음) ② **컬럼 짝 리사이즈** — 경계 드래그=좌우 동시 조절(총폭 보존·min 클램프)+경계 IDC_SIZEWE 커서 ③ **터미널 포커스 시 패널 전체 비활성**(탭 바·캐럿 테두리 포함) ④ **도크 Info/Preview 라인 선택·Ctrl+C 복사**(우선순위: 리네임>터미널>도크>파일). 테스트 158 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **포커스 영역 단위 강조 재설계(main 직커밋, 사용자 QA 스크린샷)**: 패널 단위 → **실제 키 포커스 영역 단위** — `sync_focus_visuals`(리스트 선택색=활성 패널+터미널 비포커스·도크 스트립 [Terminal]/[→] accent=그 터미널이 키 포커스일 때만·탭 바=활성 패널 유지), `Panel::set_focused`/`set_list_focused` 분리·클릭/키/기동 경로 동기 + **WM_CAPTURECHANGED 방어**(캡처 강탈 시 스플리터 드래그 플래그 잔존 해제). 상세 [journal/2026-07-15.md](journal/2026-07-15.md).
- **UX QA 5차(main 직커밋, 사용자 QA/요청 6건)**: ① **비활성 패널 구분** — 테마 토큰 `sel_bg_inactive` 신설(무채색), `Panel::set_focused`가 전 탭 rows·dock에 전파(선택 하이라이트·도크 활성 종류·터미널 [→] 버튼 모두 비활성 색) + 기동 시 복원 활성 패널 반영 ② **스플리터 강조 잔존 수정** — 도크 좌/우·높이 드래그 해제 시 재도장 누락 → InvalidateRect ③ **이름 복사 위치** — `CustomItem.after_id` 앵커 삽입으로 "경로 복사" 바로 아래 ④ **설정 창 X-8** — VS Code식 **즉시 적용**(저장/취소 제거·`WM_APP_PREFS_APPLY` 동기 통지·EDIT=포커스 이탈 시·`apply_prefs` 분리 멱등) + **리사이즈 창**(WS_THICKFRAME·WM_SIZE 재배치·최소=기본 크기). 테스트 158 green. 상세 [journal/2026-07-15.md](journal/2026-07-15.md).

## 2026-07-14

- **X-4 펼침 상태 유지(`feat/f18-expanded`) — 원본 F18 이식**: "B000 진입 후 복귀 시 A000 펼침 소실"(가시 트리 스냅샷 한계) → **탭별 영속 Expanded 집합**(BTreeMap 부모 우선·원본 PanelTab.Expanded) — 경계 동기(펼침=등재·접힘=말소·비가시 보존=재펼침 시 하위 복원)·리네임 접두사 치환(UpdateExpandedPaths)·**세션 영속**(panelN.expM — 원본 TabSession.Expanded, 상한 200/탭). 테스트 153 green(신규 4). 상세 [journal/2026-07-14.md](journal/2026-07-14.md).
- **M4 마감 게이트 통과 → `0.5.0` 태그**: B1 유휴 RSS 재실측(10k 픽스처 + **도크 터미널 상주·캐럿 깜빡임** 최악 케이스) — 활성 32.75 → 트림 직후 **0.22** → 180s 2.00 → **300s 5.07MB ≤ 30 통과**(ConPTY 자식은 별도 프로세스·M3 마감 6.29MB와 동급). **M4 하단 패널 마일스톤 완료** — 도크·정보·미리보기·ConPTY 터미널+상호작용 QA. 다음 M5 마감 · 백로그 X-2/X-3/X-4.
- **이동 프리즈 근본 해소(`fix/nav-freeze-watcher`) + 캐럿 깜빡임**: 실기 QA "Documents/Downloads 이동 시 5-10초 응답 없음"의 **진범 = watcher drop의 CloseHandle** — 비 OVERLAPPED 핸들은 파일 객체 잠금이 직렬화되어 대기 중인 동기 ReadDirectoryChangesW 완료(=이전 폴더에 변경 발생)까지 UI 블로킹. **OVERLAPPED+중지 이벤트 재구현**(drop=SetEvent 논블로킹·복제 핸들·정리는 스레드 몫). 자동 재현 하네스(PostMessage 키 주입+SendMessageTimeout 실측)로 **12,009ms→4~6ms** 검증·자동 갱신 정상. + 터미널 캐럿 깜빡임(GetCaretBlinkTime 주기·입력 위상 리셋·비포커스 상시). 상세 [journal/2026-07-14.md](journal/2026-07-14.md).
- **터미널 상호작용(`feat/m4-term-select`)**: 실기 QA/요청 5건 — **셀 단위 렌더**(런 레이아웃의 폴백 글꼴 전진폭이 셀 그리드와 어긋나 ls 한글 이름 컬럼 밀림 → 문자를 셀 x에 개별 배치+단일 글리프 캐시)·**마우스 드래그 선택**(반전 하이라이트·엣지 자동 스크롤 60ms 반복)·**스크롤백 보기**(휠 3줄/노치·보던 위치 고정·입력 시 스냅)·**Ctrl+C/V**(선택 복사/인터럽트·붙여넣기 — WT 규약)·**settings `term_font`**(DWrite 시스템 컬렉션 해석·Consolas 폴백 — 명시 폴백 체인은 X-3 잔여). 상세 [journal/2026-07-14.md](journal/2026-07-14.md).
- **실기 QA 배치 2(`fix/m4-qa-batch2`)**: ① 프리즈 1차 대응 — **파일별 아이콘(exe·lnk) SHGetFileInfoW를 워커 스레드로**(MotW=Defender 검사·OneDrive 하이드레이션이 UI 블로킹 — 원본은 WinRT 비동기) ② 편집 필드 Ctrl+C/X/V(CF_UNICODETEXT·EditState 선택 복사/잘라내기/붙여넣기) ③ 느린 재클릭 리네임을 더블클릭 시간 지연(더블클릭=열기 우선) ④ 휠=마우스 아래 패널 라우팅 ⑤ 바로가기 .lnk 숨김(NeverShowExt·리네임 시 복원). 상세 [journal/2026-07-14.md](journal/2026-07-14.md).
- **터미널 실기 QA 시리즈(`fix/m4-term-qa`·`fix/term-caret-color`)**: 세로바 캐럿(WT bar 동일·밝은 회색 고정 — 터미널 배경은 테마 무관 다크)·**Backspace=DEL(0x7F) 교차 매핑**(0x08은 ConPTY가 Ctrl+Backspace 단어 삭제로 해석 → 입력 전체 삭제 결함, 원본 규약 계승)·X-3 터미널 설정 백로그 등재. 상세 [journal/2026-07-14.md](journal/2026-07-14.md).
- **M4-3 ConPTY 터미널(`feat/m4-terminal`) → M4 전 항목 구현 완료**: 원본 VtScreen.cs·ConPtySession.cs(docs/37) 이식 — **nexa-term rlib**(VT 파서+셀 그리드: SGR 3계열·CSI 커서/지우기/삽입삭제·DECSTBM·스크롤백·전각, 테스트 9)·**ConPTY**(pwsh→cmd 폴백·UTF-8 경계 읽기 스레드·EXIT 통지·세대 가드)·**도크 [터미널]**(Consolas 모노 셀 그리드 — DrawCtx::term_text 신설·런 병합·reverse/faint·캐럿·클릭 포커스·VT 키 시퀀스·아무 키 재시작·리사이즈 동기). 테스트 148 green·B2 0.69MB·B3 무변. α: 스크롤백 스크롤·선택 복사 후속. 상세 [journal/2026-07-14.md](journal/2026-07-14.md).
- **ADR-0004 — 미리보기 플러그인(Starlark)·DR-7 개정(사용자 결정)**: "EXIF 미리보기를 Python처럼 개발" 요구 → **Starlark 임베드** 채택(문법 호환·샌드박스·정적 링크=단일 exe·Apache-2.0. CPython/WASM/.NET 기각·보류). 계약=`data\plugins\*.star`(ID/NAME/**EXTS 선언**+preview — lines/image/kv 반환·호스트 API file.read/exif) · **설정 preview_map 오버라이드**(확장자↔플러그인 직접 연결) · **다중 매치 시 도크 우상단 공급자 콤보**(선택 영속) · 내장은 폴백 존치. starlark crate=승인 대기(B2 실측 후 원장 확정). 백로그 TODO §7 X-2(S1 시임→S4 exif.star). 상세 [journal/2026-07-14.md](journal/2026-07-14.md).

## 2026-07-13

- **M4-2 내장 미리보기(`feat/m4-preview`)**: **내장 방식**(DR-7 — 플러그인 SDK 비이관, 사용자 질의 답변 명시) — 도크 [정보|미리보기] 종류 스트립(클릭 전환)·**텍스트**(16KB·UTF-8 lossy·이진 판정)·**이미지**(WIC Fant 스케일·비율 유지·캐시 8건·트림 시 소멸 — png/jpg/jpeg/bmp/gif/ico/tif). `DrawCtx::draw_image` 신설. WIC=CoCreateInstance 지연 활성화 → **임포트 무변(B3 통과)**. 독립 검증 예제 `examples/preview_image.rs`(동일 파이프라인·실기 jpg 표시 확인). 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M4-1 하단 도크(`feat/m4-dock`) — M4 착수**: 원본 BottomDockView(Info)·DockInfo·도크 대원칙(듀얼=패널별 아래) 이식 — **InfoDock 위젯**(라벨 스트립+라인·변경 시에만 무효화)·**정보 뷰**(다중=개수/단일=이름·종류·크기[원시 B]·수정·경로/없음=현재 폴더 — 선택 변경 자동 갱신)·**Ctrl+`/보기 메뉴 토글**·**높이 드래그**(경계 ±3px·비율 0.15~0.5·양 패널 동기)·**settings 영속**(dock·dock_ratio). 테스트 140 green·B3 무변. Preview(M4-2)·Terminal(M4-3)은 종류 스왑으로 확장 예정. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M3 마감 게이트 통과 → `0.4.0` 태그**: B1 유휴 RSS 재실측(10k 픽스처) — 활성 32.57MB → 트림 직후 **0.21MB** → 300s **6.29MB ≤ 30**(M3-5 OLE 상주분 회수 확인·07-13 관측 메모 해소) · B2 0.60MB · B3 인박스만. **M3 파일 조작 마일스톤 완료** — 원본 파일 조작 축 패리티(전송·삭제·리네임·새로 만들기·Undo/Redo·셸 메뉴·클립보드/DnD·watcher) + 탐색기 클릭/편집 시맨틱. 다음 M4 하단 패널.
- **M3-6 watcher(`feat/m3-watcher`) → M3 전 항목 구현 완료**: 원본 FolderWatcher.cs(B-12w) 이식 — `ReadDirectoryChangesW` **비재귀**(패널 현재 폴더)·300ms 디바운스·**무간섭 재로드**(펼침·선택·캐럿·스크롤 보존)·편집/전송 중 지연·세대 가드·중지=핸들 닫기(Drop)·실패=F5 폴백. **실기: 외부 생성(0→2)·삭제(2→1) 자동 반영**. + **폴더 이동 시 펼침 이월**(하위 진입/상위 이동 — apply_source 공통) + **드롭다운 첫 프레임 깨짐 수정**(원인: 열기 시 무효 영역이 이전 캐시[빈 rect]만 push → BitBlt 클립 누락 — 예상 영역 무효화+회귀 테스트). 테스트 138 green·B3 무변. 잔여: M3 마감 게이트(B1 재실측·`0.4.0`). 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **편집기·클릭/드래그 시맨틱 폴리시 시리즈(main 직커밋, 실기 QA 2~5차)**: ① **편집 필드 렌더 재설계**(`25bce68`·`a60447e`) — 런 분할 그리기의 트리밍/이음새·커닝 오차(캐럿 표시≠동작)를 `DrawCtx::text`(배경 없는 텍스트) 신설 + 접두사 폭 오프셋 + 1회 그리기로 해소, 텍스트 드래그 선택(click~release) 추가. ② **행 히트영역=컬럼 총폭**(원본 B-4) — 종류 컬럼 오른쪽=빈 본문(클릭=해제·드래그=밴드·우클릭=배경 메뉴). ③ **느린 재클릭=이름 바꾸기**(`7765cf9`·`a4118c9`) — 기선택 항목 1s+ 간격 재클릭 시 MouseUp에서 진입(드래그 시작=취소 → DnD 우선)·밴드 지터 임계 4px(선택 풀림 근본 해소)·기선택 행 프레스=선택 유지(press_pending — **다중 선택 드래그 전체 DnD**). ④ 브레드크럼 `\` 구분·여백 최소화(`98a460d`)·리네임 필드 텍스트 정렬(`4cc7e3d`)·배경 메뉴 고유 붙여넣기(CTX_PASTE_BG). 테스트 136 green. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **UX 결함 수정(`fix/ux-reload-editor`, 실기 QA 6건)**: ① **재로드 무간섭 갱신**(M3-6 선행 — 원본 NAV-UPFOCUS): reopen_filtered가 펼침·선택·캐럿·스크롤을 경로 기준 스냅샷→복원(파일 조작 후 좌측 패널 상태 소실 해소)+통합 테스트. ② **편집기 캐럿 모델**(`edit.rs` 신설 — 경로바 편집·리네임 공용 EditState): ←→/Home/End·Shift 선택·Ctrl+A·Delete·**클릭 캐럿 배치**(문자 경계 캐시)·**세로바 캐럿**·선택 하이라이트·진입 기본 선택(경로바=전체·리네임=이름부/폴더 전체). ③ **경로바 끝 정렬 오버플로**: 편집=캐럿 가시·브레드크럼=`…` 앞 생략(최근 폴더 가시). 테스트 135 green·B3 무변. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M3-5 클립보드 상호운용·OLE DnD(`feat/m3-clipboard-dnd`)**: 원본 OS 클립보드 읽기측·OleDropTarget.cs·docs/33 B-14dnd 이식 — WinUI DataPackage 대신 **Win32 원시 포맷+OLE COM 재구현**. ① **OS 클립보드 단일 출처**(`clipboard.rs` — CF_HDROP·Preferred DropEffect 읽기/쓰기): 내부 클립보드 제거, 탐색기↔앱 Ctrl+C/X/V 양방향·잘라내기 1회성·실 클립보드 왕복 테스트 통과. ② **DnD 수신**(`dnd.rs` IDropTarget+RegisterDragDrop): 폴더 행/패널 루트 대상 판정·Ctrl 복사/Shift 이동/기본 볼륨별(B-14dnd)·자기/하위 금지·**최적화 이동(NONE 반환 — 소스 삭제와 비동기 전송 경쟁 차단)**. ③ **DnD 발신**(SHCreateDataObject+IDropSource+DoDragDrop): 행 누름+임계 이동 시작·MOVE여도 원본 미삭제(안전 방향)·내부 패널 간도 동일 왕복. **B3 무변·테스트 green. DnD 실기 QA 대기**. α: spring-load hover·드롭 하이라이트 후속. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **도구 모음 정리(`fix/toolbar-refresh-only`, 사용자 지시)**: 전역 도구 모음 이전/다음 오동작 보고 → 네비는 패널별 네비 바([←][→][↑])가 전담하므로 전역 중복 제거, **⟳ 새로고침만 유지**. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M3-4 셸 컨텍스트 메뉴(`feat/m3-shellmenu`, [ADR-0003](08-adr-0003-shell-context-menu.md))**: 원본 ShellContextMenu.cs·ADR-0005 이식 — **행 우클릭 = 클래식 IContextMenu 셸 메뉴**(경로→PIDL→부모 IShellFolder→GetUIObjectOf→HMENU QueryContextMenu 1~0x7FFF→TrackPopupMenuEx→InvokeCommand — 7-Zip·보내기·열기 방법·속성 등 탐색기 동일) · **빈 영역 = 폴더 배경 메뉴**(CreateViewObject — 새로 만들기 서브메뉴·정렬·속성) · **고유 병합 0x8000+**(완전 삭제·폴더에 붙여넣기·배경 Undo/Redo[설명 라벨·활성 상태]) · **동사 가로채기**(원본 verbInterceptor — delete=undo 기록 합류·rename=인라인 리네임·paste=내부 클립보드 전송 엔진) · **Apps/Shift+F10**=캐럿 행 앵커. **dir2 특이점**: 자기 wndproc이 WM_INITMENUPOPUP/DRAWITEM/MEASUREITEM/MENUCHAR를 IContextMenu2/3로 직접 포워딩 — **원본의 comctl32 서브클래스 불요(신규 임포트 DLL 0·B3 무변)**, COM 인터페이스는 windows-rs 제공(수동 vtable 0). 우클릭 선택 규약(rows: 미선택=단독·선택=유지·빈=해제)+테스트. nexa-shell 크레이트 분리 대신 **앱 모듈**(shellmenu.rs — recycle.rs 동일 계층, 과분리 회피). **검증: 테스트 127 green·exe 0.60MB·스모크 RSS 26MB·B3 통과. 셸 메뉴 상호작용은 실기 QA 대기**. 후속(M5): 커스텀 레지스트리 §7·Checksum·VerbReplacement·교차 부모 선택. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M3-3 Undo/Redo(`feat/m3-undo`)**: 원본 OperationHistory.cs·RecycleBin.cs 이식(B-13u·docs/33) — **nexa-ops `history` 모듈**(플랫폼 중립): `ReversibleOp` trait + `OperationHistory`(스택 2개·새 push 시 redo 무효화·상한 100·**실패 연산 소실**=무결성 우선) + 연산 4종(Move 역이동·Copy 사본 삭제 주입·Rename·Create 재생성 주입), 오류는 `OpError` 구조화(i18n=앱). 앱 배선: `State.history` + push 3곳(전송 완료=수행 쌍만·이름변경·새로 만들기)·**Ctrl+Z / Ctrl+Y·Ctrl+Shift+Z**·편집 메뉴·실행 후 양쪽 재로드·타이틀 노트. **휴지통 복원**(`recycle.rs`): 휴지통 셸 폴더 열거→원위치 매칭→셸 **undelete 동사**(탐색기 Ctrl+Z 동일), STRRET 직접 파싱(shlwapi 회피)·`DeleteBatchOp`(undo=복원/redo=재삭제). **완전 삭제는 undo 불가**(설계). **검증: 실 휴지통 왕복 통합 테스트 통과·test 전부 green(history 9·ops 20)·exe 0.58MB(B2)·기동 RSS 25.51MB(B1)·B3 통과**(ole32 신규 등재 — OS 인박스). α: 메뉴 Undo/Redo 활성 표시 없음(노트로 알림)·다중 버전 최초 일치 1건. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M3-2 삭제·이름변경·새로 만들기(`feat/m3-fileops`)**: 원본 DeletePaths·B-6 인라인 리네임·BG-N1/N2 이식 — **Del=휴지통**(SHFileOperationW FOF_ALLOWUNDO — α, IFileOperation은 M3-3과 재검토)·**Shift+Del=완전**(MessageBoxW 확인창·기본 취소·개별 격리)·**F2=인라인 이름변경**(VirtualRows 오버레이 편집기 — 키 차단·클릭 취소·트리 컬럼 이름부 필드)·**Ctrl+Shift+N/파일 메뉴=새 폴더·새 파일**(생성→즉시 리네임 = RevealAndRename). nexa-ops에 delete_permanent/rename/create_new 추가. 대상=선택 or 캐럿. **실기: 생성+리네임("MyDir")·휴지통 삭제·확인창 취소/승인 4종 통과**. 테스트 116 green·B2 0.56MB·B3 무변. α: 리네임 IME 창 위치·휴지통 undo=M3-3. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M3-1 nexa-ops 전송 엔진(`feat/m3-ops-transfer`) — M3 파일 조작 착수**: 원본 docs/33 TRANSFER-ENGINE·FileOps.cs 이식 — **`nexa-ops` rlib 신설**(플랫폼 중립·crate 0): `transfer()` 단일 경로(같은 폴더 규칙=이동 무동작/복사 순번 복제·충돌 항목만 순차 Overwrite/Skip·4MB 청크 바이트 진행률·취소·항목 실패 개별 격리·동일 볼륨 rename fast path·순환 이동 금지). 안전 개선: 취소/실패 시 부분 파일 정리. 앱 배선 = 내부 클립보드 Ctrl+C/X/V → **워커 스레드+WM_APP_TRANSFER(세대 가드, 원본 A-1)** → 타이틀 진행 %·Esc 취소·완료 시 양쪽 재로드·i18n 결과 요약. **실기: 같은 폴더 복사 " (2)" 복제("전송 3")·제자리 이동 무동작("건너뜀 6")**. 테스트 113 green(ops 8)·B2 0.54MB·B3 무변. α 한계: 충돌=건너뜀(확인 모달 후속)·진행 창 없음·내부 클립보드만. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).
- **M2-7 IME(한글) 1차·UIA 1차(`feat/m2-ime-uia`) → M2 셸 골격 완료 `0.3.0`**: 원본 NFR-A1 대응 — ① **IME**: WM_IME_* 에서 조합 창을 편집 캐럿 옆에 배치(`ImmSetCompositionWindow` CFS_POINT, 경로바 편집 대상·결과 문자열은 기존 WM_CHAR 경로). ② **UIA**(`uia.rs`): WM_GETOBJECT 서버측 프로바이더 — **불변 스냅샷 모델**(UIA 콜백=임의 스레드라 가시 행 스냅샷을 Arc로 공유), List(Name=활성 경로)+ListItem(파일명·HasKeyboardFocus·SelectionItem 패턴)·캐럿 변경 시 FocusChanged 이벤트(리스닝 가드). **실기: .NET UIA 클라이언트로 리스트/항목 14·이름·IsSelected 조회 검증**·IME 경로 주입+한글 SendKeys 생존. B3에 imm32·uiautomationcore 등재. 테스트 105 green. **M2 게이트 통과**(듀얼·탭4 26.9MB ≤30) → 태그 `0.3.0`. 1차 한계(스냅샷 정적·선택 조작 no-op)는 M5-3 마감. 상세 [journal/2026-07-13.md](journal/2026-07-13.md).

## 2026-07-12

- **M2-8 상주 규율(`feat/m2-resident`)**: 원본 01 §5-1·docs/28 이식 — **유휴 60s(자니터 10s 틱)·최소화 시 트림**: DW 백엔드(백버퍼+레이아웃 캐시) 해제·셸 아이콘 HICON 전부 해제·`SetProcessWorkingSetSize(-1,-1)` 작업집합 반납. 화면 무효화 없이 **다음 페인트에서 지연 재적재**(ensure_dw 재생성·아이콘 재요청 — 원본 규약). 자니터는 트림 후 자기 소거, 입력에서 재가동(유휴 백그라운드 0% — 기존 타이머 2종도 자기 소거 확인). **실측: 활성 26.9MB → 최소화 2.9MB / 유휴 78s 0.21MB — 상주 RSS ≤30MB 게이트 통과**(99.5MB 장기 상주 관측 해소). 테스트 105 green·B3 무변(Win32_System_Threading=kernel32). 후속 β: 메모리 압박 구독(NFR-M4)·arena 회수·soak. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M2-6 i18n(`feat/m2-i18n`)**: 원본 docs/42 이식 — properties 스타일 `.lang`(crate 0, JSON 심 비이관)·**내장 en/ko**(include_str 임베드 안전망) + **exe 옆 `data\lang\` 키 단위 오버라이드**(DR-3)·폴백 체인(현재→en→키)·`@name` 자기 표기 발견 목록. **동적 전환**(07-12 설계 확정 이행): 보기 메뉴 언어 라디오(시스템+발견 언어) → 테이블 스왑+메뉴/컬럼 재구성(`MenuBar::set_menus`)+전체 재그리기 — 재시작 불요(행 셀·상태바는 페인트 시점 tr() 조회). "system"=OS 1차 서브태그(GetUserDefaultLocaleName — kernel32 계열, B3 무변)·settings `lang` 영속. **실기**: system→ko "[좌] 62개 항목"·en "[L] 62 items" 타이틀 검증. 테스트 104 green(i18n 4 — en/ko 키 파리티 포함). α 한계: 전환 시 컬럼 폭 리셋. **세션 인시던트**: 실기 확인 중 VSCode 비정상 종료 — 작업 트리 무손실·장기 상주(3h) 인스턴스 RSS 99.5MB 관측(M2-8 방증). 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M2-5 설정/세션 영속(`feat/m2-persistence`)**: 원본 docs/34·40·43 차용 + 포터블 규율(DR-3) — 영속물 = **exe 옆 `data\`**(레지스트리·%APPDATA% 비의존). serde 불채용(DR-8 crate 0) — **key=value 텍스트**(관용 파싱·탭 구분자 `|`)·**원자적 쓰기**(tmp→rename, 원본 SESS 계승). settings.txt(테마 모드·숨김/점 필터·스플리터 비율)·session.txt(활성 패널·패널별 탭 경로/활성 탭) — 기동 로드(`Panel::restore`, argv 경로 지정 시 새 패널)·종료 저장(WM_DESTROY). **실기 재실행 복원**: F6 라이트+Ctrl+T+Alt+↑ → 종료 → 재실행 시 라이트 픽셀·[좌] C:\Users·탭 2/2 복원 확인. 테스트 102 green(config 3)·B3 통과. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **패널별 네비 버튼(`feat/m2-panel-navbar`, 사용자 지시)**: [←][→][↑]를 **각 패널 경로바 옆**(탭 하단 네비 바 — 원본 docs/20 §2)에 배치, 해당 패널의 활성 탭 히스토리에 동작. 전역 도구 모음과 공존(원본 동일). 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M2-4 테마 시스템(`feat/m2-theme`)**: 원본 docs/39 §3 — **시스템(OS 추종)/라이트/다크** 모드. System = 레지스트리 AppsUseLightTheme + WM_SETTINGCHANGE 실시간 재해석, 보기 메뉴 라디오(체크 동기)·**F6 순환**·타이틀바 DWM 다크 동기. 팔레트는 M1-1 토큰 기이식이라 전환 = 재해석+재그리기뿐. B3에 dwmapi·advapi32 추가(로컬 사전 검증). **실기 픽셀 검증**: #1F242B(다크) ↔ #F5F7FA(라이트)·시스템 추종 확인. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M2-3 메뉴·도구·상태바(`feat/m2-chrome`)**: 크롬 3종 위젯(원본 docs/20 §2) — **MenuBar**(드롭다운을 오버레이로: 창 1개 원칙 유지, 체크 토글·Esc/외부 닫기·열린 상태 hover 전환)·**Toolbar**(←→↑⟳ → 활성 패널 네비)·**StatusBar**(항목/선택/탭·필터·페인트 관측). 명령 체계 CMD_* 단일화 — Ctrl+H/. 단축키도 경유해 메뉴 체크와 상시 동기. 실기: 숨김 토글 62→44행·도구 ↑ 이동. **QA 교훈: mouse_event 자동화는 클릭 간 대기 필수(입력 유실 플레이크)**. 테스트 96 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M2-2 듀얼 패널+패널별 탭(`feat/m2-panels`)**: 원본 docs/20·F20 이식 — **Panel 추출**(플랫폼 중립: 탭 바+경로 바+리스트, 탭 = 독립 뷰+**독립 히스토리**, win.rs 네비 로직 전부 이동)·**TabBar 위젯**(TabAction 통지·활성 accent 줄·× 닫기[마지막 불가]·[+])·**스플리터 드래그**(비율·최소 폭)·**활성 패널**(클릭/Tab 전환·타이틀 [좌/우]·비활성은 accent 흐림)·Ctrl+T/W/Ctrl+Tab. 실기: 패널 전환·탭 2/2·**탭별 독립 경로**·순환·닫기 활성 조정 확인. 듀얼 RSS 25.1MB·exe 0.38MB. 테스트 90 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M2-1 경로 바 α(`feat/m2-pathbar`) — M2 착수**: 원본 NexaPathBar(docs/27) 이식 — 브레드크럼(세그먼트 클릭 이동·**현재 비활성**·클릭 가능만 hover)+**편집 모드**(우클릭 진입·Enter 제출·Esc/포커스아웃 복귀). 네비게이션 비종속(위젯은 `take_navigation()` 통지만). 상단 바+리스트 레이아웃·이벤트 y-라우팅. 실기: "C:" 클릭 → C:\ 이동·편집 왕복 확인. 자동완성(PATH-SUG) 등은 β/γ 후속. 테스트 83 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-9 게이트·M1 완료(`0.2.0`, `feat/m1-gate`)**: 첫 렌더 계측(기동→첫 페인트 타이틀 표시) 추가 후 실기 실측 — **100k 첫 렌더 115ms(<150, 열거 포함)·스크롤 2,098µs/프레임(60fps 예산의 13%)·B1 10k 유휴 RSS 27.87MB(≤30)** 전 게이트 통과. 100k 스트레스 53MB는 예산 외 관측(노드 ~250B — arena 회수·M2-8 과제). **M1 뷰어 코어 완료**: 원본 뷰어 축 패리티(가상 리스트·5컬럼·★교차 선택·키보드·타입어헤드·아이콘·네비게이션). 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-8 네비게이션(`feat/m1-navigation`)**: 뷰어→탐색기 — **더블클릭/Enter 폴더 진입**(마커 존 제외 — 펼침과 공존), **히스토리**(nav.rs 순수 스택: push 시 앞으로 절단, Alt+←/→·마우스 X버튼), **위로**(Alt+↑), **숨김/점 파일 토글**(Ctrl+H·Ctrl+. — 현 위치 재열기). 위젯 `replace_source`는 뷰 리셋+**정렬 유지·재적용**(원본 규약). 실기: 진입→위로→뒤로 타이틀 왕복 확인·RSS 24.5MB. 테스트 80 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-7 셸 아이콘(`feat/m1-icons`)**: 원본 A-4(`IconKey`+`ShellIconCache`) 이식 — icon_key(폴더/확장자 공유/exe·lnk류는 파일별 경로 키)·`IconStore<T>`(LRU 256·dedupe 큐, 제네릭=전 플랫폼 테스트)·**속도 제한 로딩 큐**(WM_TIMER 80ms 틱당 4개 — 빠른 스크롤 셸 호출 폭주 방지)·SHGetFileInfoW(타입 아이콘은 USEFILEATTRIBUTES로 파일 무접근). 신규 임포트 shell32를 **push 전 로컬 B3 게이트가 검출**(재발 방지책 작동). 실측: 벤치 2,547µs·**유휴 RSS 27.95MB — B1 내지만 여유 2MB**(shell32 +10MB) → M2-8 유휴 트림 최우선 감시 항목 지정. 테스트 74 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-6 키보드 네비 전체+타입어헤드(`feat/m1-keyboard`)**: 원본 docs/32 확정 규약 이식 — **캐럿 키보드 네비**(이동+단일 선택+스크롤 추적, Shift 범위·Ctrl 캐럿만·Space 토글·→자식/←부모)와 **타입어헤드**(`typeahead.rs` 버퍼: 1s 타임아웃·반복 단일키 cycle·Backspace, 시각 주입 — 범위는 가시 스트림 위치상대+wrap[C], 코어 find_prefix 위임) + **HUD 배지**(본문 좌하단 플로팅·WM_TIMER 소거). 실기: Shift+End "선택 61"·타입어헤드 점프 정상. **QA 교훈: SendKeys는 확장 키에서 Shift를 일시 해제** — Shift 조합 자동화 검증은 keybd_event로. 테스트 69 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-5 ★ 플래그십 — 인라인 펼침+교차폴더 다중 선택+러버밴드(`feat/m1-select`)**: 원본 docs/07의 제1 차별화 기능 — 클릭 시맨틱 분리(**삼각형=인라인 펼침 vs 본문=선택**), Ctrl 토글·Shift 범위·Ctrl+A를 코어 OrderedSet·anchor 모델에 배선(**서로 다른 부모 동시 선택** AC2 테스트), **러버밴드**(빈 영역 드래그=세로 교차 행 선택·accent 외곽선), 캐럿(→/← 인라인 펼침/접힘), 선택 하이라이트 `sel_bg` 토큰. 실기: Ctrl+A 62행 타이틀 반영·벤치 1,540µs·RSS 18.2MB·B3 통과. 테스트 60 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-4 컬럼 시스템(`feat/m1-columns`)**: 원본 docs/24 이식 — 5컬럼(이름·확장자·크기·수정한 날짜·종류)·헤더 **정렬 3상태 순환**(▲→▼→없음, 단순 클릭=단일 리셋)·**Shift 다중열**(화살표 이름 앞·순번 원문자 뒤 — 원본 확정 규약)·드래그 리사이즈(min_width)·가로 스크롤(Shift+휠/HWheel)·컬럼 폭 초과 셀 **말줄임표 트리밍**. 날짜는 civil_from_days 순수 구현+TZ 오프셋(외부 crate 0 유지). 실측: 벤치 1,813µs/프레임(+8%)·RSS 18.1MB·exe 0.31MB·B3 로컬 게이트 사전 통과. 테스트 55 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-3 가상화 파일 리스트 초안(`feat/m1-virtual-list`)**: **실제 파일시스템 첫 표시** — nexa-tree 가시 노드 평면 스트림을 `TreeSource`로 `RowSource`에 배선(들여쓰기·▸/▾ 마커·클릭=펼침/접힘, `InputEvent::MouseDown` 신설). ADR-0002 §5 이행: GDI 경로·F2 삭제(DW 단일), 텍스트 키 레이아웃 캐시 도입 — 실 트리 벤치 **1,673µs/프레임**(스파이크 대비 −62%)·유휴 RSS 18.2MB·exe 0.27MB(전 게이트 내). 테스트 48 green. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **CI B3 red 복구(main)**: ADR-0002로 늘어난 `dwrite.dll`·`combase.dll`(OS 인박스)이 화이트리스트에 없어 windows job fail → 목록 갱신(`5a613b3`)·green 복구. 규약 추가: 인박스 DLL 변경 시 ci.yml·docs/18 동시 갱신. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).
- **M1-2 ADR-0002 렌더링 확정(`feat/m1-adr0002-render`)**: DirectWrite GDI interop 백엔드(`dw.rs` — `IDWriteBitmapRenderTarget`+`IDWriteTextLayout`+커스텀 `IDWriteTextRenderer`) 구현, F2 전환·F3 200프레임 벤치 하네스로 **같은 창 실측 비교**. 결과: DW가 GDI 대비 **−28%**(4,373µs vs 6,072µs, 캐시 미적용 상한치)·RSS +4.1MB(17.4MB)·exe 0.23MB — 전 게이트 내. **DW interop 채택**([07 ADR-0002](07-adr-0002-rendering.md) Accepted), 기본 백엔드 전환. `DrawCtx` 어휘는 무변경(추상 검증). GDI 경로·비교 하네스는 M1-3에서 제거 예정. 상세 [journal/2026-07-12.md](journal/2026-07-12.md).

## 2026-07-11

- **M1-1 nexa-gui 크레이트 분리(`feat/m1-gui`)**: **플랫폼 중립(의존 0)** GUI 기반 — 위젯 trait+무효화 수집(교차 rect 병합)·입력 이벤트(분수 노치 휠 누적기)·시맨틱 테마 토큰(**원본 docs/39 §4 차용**, 다크 기본)·`VirtualRows` 가상화 리스트(M0-7 스크롤 로직 이식, `RowSource` 추상 — M1-3에서 nexa-tree 배선). nexa-app은 WM_* 번역·더블 버퍼·GDI `DrawCtx` 백엔드(`gdi.rs`, ADR-0002 확정까지)로 축소. 테스트 43 green(신규 17)·clippy 0·실기 기동 확인(exe 0.21MB·RSS 12.4MB). 상세 [journal/2026-07-11.md](journal/2026-07-11.md).
- **M0-8 게이트 실측 · M0 완료(`0.1.0`)**: Windows 실기(Win11 26200)에서 DR-2 예산 전 항목 통과 — **B1 유휴 RSS 13.22MB**(100k 행 창·3회 중앙값·Private 1.70MB) · **B2 exe 0.20MB**(214KB, 정적 CRT+lto+strip) · **B3 임포트 6종 전부 OS 인박스**(user32·kernel32·gdi32·ntdll·oleaut32·api-ms-win-core-synch). 실기 테스트 26 green. CI B3를 목록 가시화→**화이트리스트 fail 게이트**로 강화. 원본 대비: 유휴 RSS 60MB+ → 13MB(1/4 이하)·배포 64MB → 0.2MB. 다음 M1-1(`nexa-gui`)·M1-2(ADR-0002). 상세 [journal/2026-07-11.md](journal/2026-07-11.md).
- **M0-7 GDI 렌더 스파이크(`feat/m0-render-spike`)**: WM_PAINT **더블 버퍼**(메모리 DC 캐시·ERASEBKGND 생략) + 합성 **100k 행 중 가시 행만** ExtTextOutW(ETO_OPAQUE, 교대 음영) — 프레임 비용이 창 높이에만 비례함을 코드로 실증(M1 가상 리스트 전제). 휠(분수 노치 누적)·키보드(↑↓/PgUp/PgDn/Home/End) 스크롤·WM_DPICHANGED 대응. 맥에서 windows 타깃 check·clippy green. 잔여 M0-8(실기 게이트 실측→`0.1.0`). 상세 [journal/2026-07-11.md](journal/2026-07-11.md).
- **M0 스캐폴딩·코어 이식·Win32 스켈레톤(`feat/m0-scaffold`)**: 워크스페이스(정적 CRT·lto fat·panic abort) → **nexa-core/vfs/tree 원본 이식**(rlib 직접 링크, 테스트 21+5 green — 인터롭/ABI 계층 소멸) → **Win32 창 스켈레톤**(windows-rs 0.62: 클래스·메시지 루프·WM_PAINT·PerMonitorV2, 비-Windows 스텁) — **맥에서 windows 타깃 cargo check green**(WinUI 시절 불가능하던 UI 코드 사전 검증) → CI(core ubuntu/mac + windows·예산 B2 10MB 게이트·아티팩트). 잔여 M0-7(렌더 스파이크)·M0-8(실기 게이트 실측→`0.1.0`). 상세 [journal/2026-07-11.md](journal/2026-07-11.md).
- **권한 파일 사고 2건·복구**: IDE "Allow always"가 세션 스냅샷으로 `.claude/settings.json`을 덮어씀(2회) → git 커밋본에서 병합 복원, rust/python 전 명령 자동 허용 추가. 규율 확정: **이 파일은 병합만, 덮어쓰기 금지**. 상세 [journal/2026-07-11.md](journal/2026-07-11.md).
- **프로젝트 발족 — 설계 문서 세트(`docs/foundation`)**: 원본 nexa-dir 분석(스택 한계 실측: RSS 60MB+·단일 exe 고위험) → 올 러스트 재구축 결정. 비전/요구(00·05)·아키텍처/ADR-0001(01·06)·결정기록 DR-1~8(10)·환경/패키징/방법론/빌드(11·12·15·18)·로드맵 M0~M5(02)·MILESTONES·TODO(M0-1~M5-3)·운영 문서 작성. 상세 [journal/2026-07-11.md](journal/2026-07-11.md).
- **저장소 초기화**: .gitignore·PolyForm NC 라이선스(원본 계승)·`.claude/settings.json` dev 자동 허용(원본 규약 차용). 원격 `SosomLab/nexa-dir2` 최초 push.
