# TODO — 할 일 백로그 (목표 순차 · living)

> **목표(예산 내 원본 패리티)를 향한 순차 진행 순서.** 새 항목은 맨 하단 §7에 append.
> **규모**: 소=반나절/1커밋 · 중=1~2일/1~3슬라이스 · 대=3일+/설계(ADR) 동반.
> **상태**: ☐ 대기 · 🚧 진행 · ✅ 완료(커밋) · ⏸ 보류. **우선**: P0(정체성·게이트) · P1 · P2.

## §1. M0 — 기반·게이트

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M0-1 | 워크스페이스 스캐폴딩(Cargo.toml·rust-toolchain·release 프로파일·.cargo 정적 CRT) | P0 | 소 | — | ✅ (07-11 `391e4bb`) |
| M0-2 | `nexa-core` 이식 + 테스트 | P0 | 소 | M0-1 | ✅ (07-11 `38f92ff`) |
| M0-3 | `nexa-vfs` 이식 + 테스트 | P0 | 소 | M0-2 | ✅ (07-11 `585b2dc`) |
| M0-4 | `nexa-tree` 이식 + 테스트(★ 플래그십 코어) | P0 | 중 | M0-3 | ✅ (07-11 `370b56f` — 21 green+벤치 1 ignored) |
| M0-5 | `nexa-app` Win32 창 스켈레톤(클래스·메시지 루프·WM_PAINT·DPI) + 맥 스텁 | P0 | 중 | M0-1 | ✅ (07-11 `34b5649` — windows 타깃 check green) |
| M0-6 | CI — core(ubuntu/mac test)·windows(build/test·예산 B2 게이트) | P0 | 소 | M0-5 | ✅ (07-11 `3b2ddf8` — 러너 검증은 push 후) |
| M0-7 | GDI 렌더 스파이크(가시 N행 텍스트·더블 버퍼) | P1 | 중 | M0-5 | ✅ (07-11 `90a9243` — 100k 행·휠/키 스크롤·DPI, windows check/clippy green) |
| M0-8 | **게이트 실측**: 빈 창 RSS·exe 크기·임포트 테이블(Windows) → journal 기록·`0.1.0` 태그 | P0 | 소 | M0-7 | ✅ (07-11 — B1 13.22MB·B2 0.20MB·B3 인박스만, CI B3 게이트화) |

## §2. M1 — 뷰어 코어 (★)

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M1-1 | `nexa-gui` 크레이트 분리(위젯 trait·무효화 rect·입력 라우팅·테마 토큰) | P0 | 중 | M0 | ✅ (07-11 `63ff331`·`80cbbef` — 플랫폼 중립·테스트 17, 원본 docs/39 토큰 차용) |
| M1-2 | **ADR-0002 렌더링 확정** — GDI vs DirectWrite GDI interop 스파이크 비교(품질·RSS·속도) | P0 | 중 | M0-7 | ✅ (07-12 — DW interop 채택: 벤치 −28%·RSS +4.1MB 예산 내, [07](07-adr-0002-rendering.md)) |
| M1-3 | 가상화 파일 리스트 초안(nexa-tree 배선·세로 스크롤·행 그리기) | P0 | 대 | M1-1·2 | ✅ (07-12 `199d67a`·`856ced5` — 실 FS 표시·클릭 펼침·레이아웃 캐시 1,673µs·GDI 제거) |
| M1-4 | 컬럼 시스템(5컬럼·헤더 정렬 3상태·다중열·리사이즈·가로 스크롤) — 원본 docs/24 | P1 | 중 | M1-3 | ✅ (07-12 `57374d5`·`47a9d28` — 트리밍·TZ 날짜 포함, 벤치 1,813µs) |
| M1-5 | 인라인 펼침 + 교차폴더 다중 선택 + 러버밴드 — 원본 docs/07 | P0 | 중 | M1-3 | ✅ (07-12 `df012bb`·`67e784b` — 마커/본문 분리·Ctrl/Shift/Ctrl+A·밴드·캐럿·sel_bg) |
| M1-6 | 키보드 네비 전체(캐럿·범위·Ctrl+Space·→/←·Alt+화살표) + 타입어헤드 — 원본 docs/32 | P1 | 중 | M1-5 | ✅ (07-12 `48ed980`·`e5a2386` — 캐럿 네비·버퍼/cycle/HUD. Alt+화살표는 M1-8로) |
| M1-7 | 셸 아이콘(LRU 캐시+로딩 큐 — 원본 A-4 교훈) | P1 | 중 | M1-3 | ✅ (07-12 `033182d`·`0bffc72` — RSS 28MB ⚠ M2-8 트림 감시) |
| M1-8 | 네비게이션(뒤로/앞으로/위로·히스토리·마우스 X버튼)·숨김/점 토글 | P1 | 소~중 | M1-3 | ✅ (07-12 `3bd09df`·`8148510` — 더블클릭/Enter 진입·Alt+화살표·Ctrl+H/. 포함) |
| M1-9 | 게이트: 100k 첫 렌더 <150ms·스크롤 60fps·RSS | P0 | 소 | M1-3~8 | ✅ (07-12 `a7ff9df` — 115ms·2.1ms/프레임·10k RSS 27.87MB → `0.2.0`) |

## §3. M2 — 셸 골격·상주

| ID | 항목 | 우선 | 규모 | 상태 |
|---|---|---|---|---|
| M2-1 | 계층 경로 바(세그먼트·편집·자동완성 — 원본 docs/27·PATH-SUG) | P1 | 중 | ✅ α (07-12 `c2eddd6`·`356f9b9` — 세그먼트·편집. 자동완성·오버플로=β 후속) |
| M2-2 | 듀얼 패널 + 패널별 탭(드래그·잠금/고정·세션) — 원본 F20·탭 UX | P0 | 대 | ✅ (07-12 `fff90ed`·`6d8e2c0` — Panel 추출·스플리터·Ctrl+T/W/Tab. 드래그·잠금·세션=후속/M2-5) |
| M2-3 | 메뉴 바·도구 모음·상태바(커스텀 드로잉) | P1 | 중 | ✅ (07-12 `f146a7c`·`cf7fb49` — 드롭다운 오버레이·CMD 단일화·체크 동기) |
| M2-4 | 테마 시스템(라이트/다크/시스템 — 원본 docs/39 토큰 차용) | P1 | 중 | ✅ (07-12 `3bc8283` — OS 추종·F6 순환·DWM 타이틀바. 영속=M2-5) |
| M2-5 | 설정/세션 영속(`data\` — 원본 docs/34·40·43 차용) | P0 | 중 | ✅ (07-12 — key=value 텍스트(crate 0)·원자 쓰기·기동 로드/종료 저장. 주기 저장·창 위치=후속) |
| M2-6 | i18n(내장 en/ko + `data\lang\` 오버라이드 — 원본 docs/42 포맷 호환) | P2 | 중 | ✅ (07-12 — properties `.lang`·키 단위 오버라이드·폴백 체인(현재→en→키)·동적 전환(언어 라디오·재시작 불요)·system=OS 추종·`lang` 영속. 컬럼 폭 리셋=α 한계. 우클릭 앱 고유 항목은 M3-4에서 동일 경로 적용) |
| M2-7 | **IME(한글) 1차**(WM_IME_*·조합 창 위치) · **UIA 1차**(리스트/트리 패턴) | P1 | 대 | ✅ (07-13 — IME: 조합 창 캐럿 배치(경로바 편집). UIA: 스냅샷 프로바이더(List/ListItem·SelectionItem·FocusChanged), .NET UIA 클라이언트 검증. 조합 인라인·패턴 완성·구조 이벤트=M5-3) |
| M2-8 | 상주 규율(유휴 트림·백그라운드 0%) + 게이트: 상주 RSS ≤30MB | P0 | 중 | ✅ (07-12 — 유휴 60s·최소화 트림(DW+HICON 해제·작업집합 반납·지연 재적재)·자니터 자기 소거. 실측 활성 26.9→트림 0.21~2.9MB 게이트 통과. 메모리 압박 구독·arena 회수·soak=β 후속) |

## §4. M3 — 파일 조작

| ID | 항목 | 우선 | 규모 | 상태 |
|---|---|---|---|---|
| M3-1 | `nexa-ops` 전송 엔진(복사/이동·진행률·충돌·취소) — 원본 TransferPathsInto 스펙 | P0 | 대 | ✅ (07-13 — rlib 신설·transfer 단일 경로(같은 폴더 규칙·충돌 순차·4MB 진행·취소·개별 격리·fast path)+Ctrl+C/X/V 배선·워커·세대 가드·Esc 취소·양쪽 재로드. α: 충돌=건너뜀(확인 모달 후속)·진행 창 없음·내부 클립보드만) |
| M3-2 | 삭제(휴지통 IFileOperation/완전)·이름변경·새로 만들기 | P0 | 중 | ✅ (07-13 — Del=휴지통(SHFileOperationW α — IFileOperation은 M3-3과 재검토)·Shift+Del=완전(확인창)·F2 인라인 리네임(rows 오버레이)·Ctrl+Shift+N/메뉴 새 폴더·파일(즉시 리네임). 리네임 IME 창 위치=α 한계) |
| M3-3 | **Undo/Redo**(OperationHistory 이식 — 휴지통 복원 포함) | P0 | 중 | ✅ (07-13 — history 모듈(스택 2개·실패 소실=무결성)+연산 4종(Move/Copy/Rename/Create)·Ctrl+Z/Y·Ctrl+Shift+Z·편집 메뉴·전송/이름변경/새로 만들기 undo 기록·휴지통 복원(셸 undelete)·완전 삭제는 undo 불가. 실 휴지통 왕복 통합 테스트 통과·exe 0.58MB·RSS 25.51MB·B3 통과(ole32). α: 메뉴 활성 표시 없음·다중 버전 최초 일치 1건) |
| M3-4 | 셸 컨텍스트 메뉴(IContextMenu+고유 병합 — 원본 ADR-0005 계승 = [ADR-0003](08-adr-0003-shell-context-menu.md)) | P0 | 대 | ✅ (07-13 — 행 우클릭=셸 메뉴(HMENU 호스팅·IContextMenu2/3 포워딩은 자기 wndproc — comctl32 불요)·빈 영역=배경 메뉴(CreateViewObject·새로 만들기)·고유 병합 0x8000+(완전 삭제·붙여넣기·Undo/Redo)·delete/rename/paste 동사 가로채기(undo·인라인·전송 합류)·Apps/Shift+F10. 크레이트 분리 대신 앱 모듈(shellmenu.rs — recycle.rs 동일 계층). 후속(M5): 커스텀 레지스트리 §7·Checksum·VerbReplacement·교차 부모) |
| M3-5 | 클립보드 상호운용(CF_HDROP 읽기/쓰기) · OLE DnD(IDropTarget/DoDragDrop) | P1 | 대 | ✅ (07-13 — OS 클립보드 단일 출처(내부 클립보드 제거·탐색기↔앱 Ctrl+C/X/V·잘라내기 1회성·왕복 테스트)·DnD 수신(IDropTarget — 볼륨별 기본+Ctrl/Shift·자기/하위 금지·최적화 이동 NONE 반환)·발신(SHCreateDataObject+DoDragDrop — 행 누름+임계 이동·MOVE여도 원본 미삭제 안전 방향). B3 무변. 실기 QA 대기. α: spring-load hover·드롭 하이라이트 후속) |
| M3-6 | watcher(무간섭 갱신 — 선택·캐럿·스크롤 보존, 원본 NAV-UPFOCUS) | P1 | 중 | ✅ (07-13 — ReadDirectoryChangesW 비재귀(패널 현재 폴더)·300ms 디바운스·무간섭 재로드(펼침·선택·캐럿·스크롤 보존)·편집/전송 중 지연·세대 가드·실패=F5 폴백. 실기: 외부 생성/삭제 자동 반영. α: 펼친 하위 폴더 비감시(재귀 금지 규약)) |

## §5. M4 — 하단 패널

| ID | 항목 | 우선 | 규모 | 상태 |
|---|---|---|---|---|
| M4-1 | 하단 도크(정보 뷰·토글·세션) | P1 | 중 | ✅ (07-13 — InfoDock 위젯(패널별 1개 = 원본 대원칙)·정보 뷰(원본 DockInfo: 다중=개수/단일=이름·종류·크기·수정·경로/없음=현재 폴더)·Ctrl+`/보기 메뉴 토글·높이 드래그(비율 0.15~0.5)·settings 영속(dock·dock_ratio). 실기 QA 대기) |
| M4-2 | 내장 미리보기(텍스트·이미지 — WIC 디코딩) | P1 | 중 | ✅ (07-13 — **내장**(DR-7 플러그인 비이관): 도크 종류 스트립(정보\|미리보기 클릭 전환)·텍스트(16KB·UTF-8 lossy·이진 판정)·이미지(WIC Fant 스케일·비율 유지·8건 캐시·CoCreateInstance 지연=B3 무변)·draw_image 프리미티브. 독립 예제 examples/preview_image.rs. 실기 QA 대기) |
| M4-3 | `nexa-term` ConPTY + VT 스크린(원본 VtScreen·docs/37 이식) | P1 | 대 | ✅ (07-14 — **nexa-term rlib**(VtScreen: SGR 16/256/트루컬러·CSI 커서/지우기/삽입삭제/DECSTBM·스크롤백 800·전각 셀·테스트 9)·ConPTY 세션(pwsh→cmd 폴백·읽기 스레드 UTF-8 경계·EXIT 통지·세대 가드)·도크 [터미널] 종류. **+ QA 시리즈**: 셀 단위 렌더(한글 열 정렬)·드래그 선택(엣지 자동 스크롤)·스크롤백 휠·Ctrl+C/V·세로바 캐럿(깜빡임)·Backspace=DEL·term_font. **M4 마감 `0.5.0`**(B1 터미널 상주 5.07MB) — 잔여는 X-3) |

## §6. M5 — 마감·릴리스

| ID | 항목 | 우선 | 규모 | 상태 |
|---|---|---|---|---|
| M5-1 | 퀵 런처·일괄 이름변경 등 원본 잔여 패리티(원본 docs/25·44) | P2 | 대 | ✅ (07-15 — ① 퀵 런처 바: 외부 프로그램 버튼·%path% 치환·settings.cfg 영속(`launcherN=`)·VS Code 시드·보기 메뉴 토글 ② 일괄 이름변경 α: nexa-ops::batch_rename(치환→대소문자→삽입→연번·충돌 4종)+다이얼로그(실시간 미리보기·충돌 차단)+MoveBatchOp 트랜잭션 undo·Ctrl+Shift+R. 실기 QA 대기. 후속: 런처 UI CRUD·exe 아이콘 / 정규식·날짜·프리셋(β)) |
| M5-2 | 릴리스 파이프라인(태그→단일 exe 자동 첨부·예산 CI 게이트) | P0 | 중 | ✅ (07-15 — release.yml: 태그 push→test+B2/B3 게이트→NexaDir-\<버전\>-win-x64.exe를 GitHub Release 자동 첨부. **첫 태그 실행 검증 완료**: `0.6.0` Release 생성·exe 첨부 확인) |
| M5-3 | 접근성·IME 마감 · 서명 결정(원본 PKG-4 공동) | P1 | 중 | ✅ (07-15 — UIA SelectionItem 실동작(WM_APP_UIA_SELECT)·구조 변경 이벤트(ChildrenInvalidated)·리네임 인라인 IME 조합 창 배치·서명=무서명 유지 확정(DR-3 갱신 — 원본 인증서 확보 시 release.yml 서명 단계). 실기 QA 대기. α: 동수 개명 구조 이벤트 미포착·IME 조합 인라인 렌더는 조합 창 방식 유지) |

## §7. 추가 항목 (append-only)

<!-- 예: | X-N | 항목 | 우선 | 규모 | 의존 | 상태 | -->

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| X-27 | **툴바 내비 버튼 mouseover 배경색**(사용자 요청 07-20): 경로 바(breadcrumb) 세그먼트는 이동 가능한 폴더에 마우스를 올리면 배경색(hover)이 바뀌는데, **이전/다음/상위 폴더 이동 버튼**([toolbar](../crates/nexa-app/src/win.rs) 내비 버튼)은 mouseover 시 배경색 변화가 없음 → 동일한 hover 배경색을 내비 버튼에도 적용해 일관성 확보. **착수 전 경로 바 세그먼트 hover 색 SSOT 확인**(Style hover/sel_bg 계열)해 동일 토큰 재사용·비활성(히스토리 끝) 버튼은 hover 제외 | P3 | 소 | — | ✅ (07-20 — SSOT=`theme.sel_bg` 확인, Toolbar hover 통일[내비·도구 모음·런처 공통]·비활성 제외. 실기 QA 대기) |
| X-26 | **SosomLab·제품 홈페이지 + About 화면**(사용자 요청 07-20): ① **SosomLab 조직 홈페이지**(SosomLab 소개·제품 목록 랜딩) ② **Nexa Dir 제품 홈페이지**(기능·스크린샷·다운로드[Release 링크]·설치 안내) ③ **앱 내 About 화면**(간단한 제품 소개·버전·SosomLab/제품 홈페이지 링크·라이선스[PolyForm NC]·저작권) — 도움말 메뉴에 배선. 설계 방향 — 홈페이지는 정적 사이트(GitHub Pages 후보·저장소/호스팅 결정 필요)·About는 앱 모달(Nx 컨트롤·기존 다이얼로그 규약). 착수 시 배치·기술 스택 확정 | P2 | 중 | — | 🚧 ③ About ✅(07-20 — about.rs·도움말 메뉴·버전/링크/라이선스/저작권, 링크는 홈페이지 확정 전 GitHub) · ①② 홈페이지 = 설계 대기 |
| X-25 | **일괄 이름변경 — 조상·자손 동시 선택 처리**(사용자 QA 07-20): 트리에서 상위 폴더와 그 하위 폴더/파일을 함께 선택하면 적용이 깨짐 — [win.rs `open_bulk_rename`](../crates/nexa-app/src/win.rs) 순차 `rename` 루프가 부모 폴더를 먼저 개명하면 자손의 저장된 `old_path`가 디스크에서 소멸(선택 순서 = `selected_paths()` 삽입 순서라 비결정적). 부모 우선 시 자손 rename **실패**, 자손 우선 시 겉보기 성공이나 done 쌍이 stale → **undo·`rename_expanded` 경로 추적 손상**. [conflicts()](../crates/nexa-ops/src/batch_rename.rs)는 부모 문자열 동등만 봐 이 관계 미감지. **1차(감지 후 차단)**: 선택 집합의 조상-자손 관계 검출 → 자손 항목을 새 `Conflict` 종류(예: `Nested`)로 표기·적용 제외·미리보기 사유 표시. **2차(경로 rebase 완전 지원 — 차후)**: 얕은 것부터 정렬 적용 + 각 rename 성공 후 남은 pairs의 `old_path` 접두사 치환(`rename_expanded` 로직 재사용)·실제 결과 경로로 done 기록 → undo까지 정합 | P1 | 중 | M5-1 | 🚧 1차 ✅(07-20 — `Conflict::Nested` 경로 접두 검출[개명 예정 조상만]·미리보기 사유·적용 차단·테스트) · 2차 rebase = 대기 |
| X-24 | **좌측 도크(디렉토리 트리 내비게이터)**(사용자 설계 등록 07-19): 좌측 세로 도크에 **전체 디렉토리 트리** 표시 — 직관적/구조적 탐색 전용, 행 클릭 = **현재(활성) 탭의 위치 이동**(파일 패널 뷰와 독립·원본 재발명 금지 규약에 따라 착수 전 원본 트리 사이드바 확인). 설계 방향 — ① 명명: 도크 일반화(하단 도크 `dock.bottom`·좌측 도크 `dock.left` — 07-19 용어 체계) ② 레이아웃: 전고 좌측 밴드(하단 도크와 직교·스플리터 독립 비율 영속) ③ 데이터: nexa-tree 재사용(디렉토리만 필터 스트림) ④ 세션: 표시/폭/펼침 영속 | P2 | 대 | — | ☐ 설계 등록 |
| X-1 | **QA 확인 대상: Apps 키/Shift+F10** — 캐럿 행 앵커 위치에 셸 메뉴 표시(M3-4 S2). 나머지 M3-4 QA 5건은 07-13 통과 | P2 | 소 | M3-4 | ☐ QA 대기 |
| X-2 | **미리보기 플러그인 — 런타임 WASM(wasmi)**(시임·매핑 = [ADR-0004](09-adr-0004-preview-plugins.md) 07-14 · **런타임 = [ADR-0005](25-adr-0005-wasm-plugins.md) 07-26 재개정**, DR-7) — ~~S1 공급자 시임~~ ✅ · ~~S2 starlark 도입~~ ✅(0.14.2 — 원장·B2 +4.24MB 실측) · ~~S3 1차~~ ✅(file 핸들·read_text·disp_width·설정 preview_map — **07-26 사용자 지시**: 1차 내장 러스트 뷰어 폐기 → Starlark 재설계 + **독립 미리보기 창 F3**(기준 캔버스) + **samples/markdown-viewer**(markdown.star md+Mermaid) + 가이드 [24](24-plugin-dev-guide.md). `feat/starlark-plugin` 실기 QA 후 병합) · ~~실행 격리 상한~~ ✅(07-26 — 300ms/500ms·연료·힙) · ~~Mermaid 이미지 렌더~~ ✅(07-26 — render_svg/is_dark·인라인 이미지 마커·flowchart SVG) · ~~런타임 wasmi 전환~~ ✅(**07-27 — [ADR-0005](25-adr-0005-wasm-plugins.md)**: Starlark 코어 제거[revert 가능]·wasmi 1.1[B2 +1.68MB 실측·fuel 격리]·markdown-viewer-wasm 80KB 참조 플러그인[GitHub 태그 렌더+Mermaid SVG]·가이드 24 개정 — `feat/wasmi-plugin`) → 잔여: **다중 매치 공급자 콤보**(도크 우상단·선택 영속)·핫 리로드·**완전 비동기 미리보기**(워커+로딩 표시)·sequence SVG·exif.wasm 예시 | P1 | 대 | M4-2 | 🚧 런타임 전환 ✅ |
| X-4 | **펼침 상태 유지 — 원본 F18 이식**(사용자 요청 07-14: 하위 진입→상위 복귀 시 다른 폴더[A000]의 펼침이 소실) | P1 | 중 | M3-6 | ✅ (07-14 — 탭별 영속 Expanded 집합[BTreeMap 부모 우선]·경계 동기[펼침=등재·접힘=말소·비가시 보존=재펼침 복원]·리네임 접두사 치환·**세션 영속**(panelN.expM·상한 200/탭). 테스트 153 green — 실기 QA 대기) |
| X-5 | **터미널 마우스 프로토콜 전달**(Zellij 탭 클릭 등) | P2 | 중 | M4-3 | ✅ (07-14 — VtScreen DECSET 1000/1002/1003/1006 추적·SGR `ESC[<b;x;y M/m` 전달[클릭/우클릭/드래그/휠]·Shift=로컬 우회. 레거시 비SGR 인코딩=α 미지원) |
| X-6 | **도크 레이아웃 재구성**(파일/정보 가로 분리·독립 좌/우 스플리터) | P1 | 중 | M4-1 | ✅ (07-14 — 원본 BottomLeftCol/Splitter/RightCol 이식: 전폭 밴드·dock_split 독립 비율(영속)·스플리터 3종 독립·panel_at_pt 라우팅. 실기 QA 대기) |
| X-7 | **설정 창 전면 확장** | P3 | 중 | S6 | ✅ (07-14 — VS Code식 검색+카테고리+레지스트리·dir2 영속 설정 전부. **09-04 본문 스크롤 재구축**[컨테이너 분리·자식 이동·트랙패드 픽셀·오버레이 바 — journal 09-04]. 잔여: 재시작 필요 확인창[원본 PREF-9]·터미널 wrap/cols[X-3]·다중 폰트 슬롯은 해당 기능 도입 시) |
| X-8 | **설정 창 VS Code식 완성**(X-7 후속, 사용자 요청 07-15) — 즉시 적용(저장 버튼 제거·WM_APP_PREFS_APPLY 동기 통지·EDIT=포커스 이탈 시)·리사이즈 창(WS_THICKFRAME·최소=기본 크기) | P3 | 소 | X-7 | ✅ (07-15 — 실기 QA 대기) |
| X-9 | **설정 창 원본 UI 재현**(사용자 요청 07-15 — 원본 스크린샷) — 사이드바(검색+카테고리 오너드로 하이라이트)+섹션 제목+라벨 일체형 체크박스+라디오 그룹(테마/언어)+터미널 카테고리 | P3 | 중 | X-8 | ✅ (07-15 — 실기 QA 대기) |
| X-3 | **터미널 설정**(원본 Settings §Terminal/Fonts 패리티): ① `term_wrap`(word wrap, 원본 NoWrap 기본 true) ② `term_cols`(80~1000, 원본 MaxColumns 240 — 가로 스크롤 동반) 잔여. ~~③ term_font~~ ✅ ~~④ term_font_size~~ ✅ ~~⑤ 명시 폴백 체인~~ ✅(07-14 — IDWriteFontFallbackBuilder·쉼표 목록·설정 창 노출) | P2 | 중 | M4-3 | ✅ (07-15 — ①term_wrap[기본 켜짐=현행 유지·원본 NoWrap 기본과 차이 명시] ②term_cols 80~1000 기본 240+가로 스크롤[Shift+휠·가로 휠·view_x]·설정 창 노출·영속. 실기 QA 대기) |
| X-10 | **설정 창 VS Code 심화**(사용자 분석 요청 07-15 — 스크린샷 기준): ~~① 검색 시 좌측 카테고리 매치 필터~~ ✅(07-15 — 계층 트리 개편+라벨/상세 매치 필터+드릴다운+검색 유지 탐색+✕ 지우개. **매치 수 "(N)" 표기는 잔여**) ② 다중 토큰 AND 매칭+결과 수 표기 ③ 항목 **설명 문장** 렌더(제목 아래) ④ **수정됨 표시**(기본값과 다른 항목 좌측 세로 바+기본값 복원) ⑤ 제목 "카테고리: 항목" 접두 규약 | P3 | 중 | X-9 | ✅ (07-15 — ①매치 수 "(N)" ②다중 토큰 AND+결과 수("N개 일치") ③설명 문장(회색·`.desc` 키 9종) ④수정됨 좌측 accent 바(기본값 복원만 잔여) ⑤전역 검색 "카테고리: 항목" 접두. 실기 QA 대기) |
| X-12 | **폰트 슬롯 확장**(사용자 요청 07-15 — 원본 Fonts 스크린샷): 기본(메뉴·탭·경로바·설명·도크)·파일 목록(+폴더 볼드)·상태바·파일 헤더(볼드/이탤릭 데코) — DwBackend 다중 TextFormat 슬롯 + 설정 노출(이름+크기 1줄). 콘솔(터미널→글꼴 카테고리 이동)·대화상자 기존 | P2 | 대 | X-7 | ✅ (07-16 — 잔여 β: 우클릭 메뉴 폰트 실적용[HMENU=OS 규약 — 자체 그리기 전환 시]·설치 글꼴 콤보) |
| X-13 | **런처 CRUD UI**(사용자 요청 07-15): 설정 창에서 exe/명령 추가·삭제·**순서 정렬**·**구분자 삽입**·**아이콘 지정**. 1/2 완료(07-16 브랜치 `feat/x13-launcher-crud` `0888677` — LauncherItem.icon `launcherN.icon` 별도 키 직렬화[인자 내 `\|` 보존 규약 유지]·하위호환·바 아이콘 오버라이드). 2/2 = 설정 창 편집기(목록·추가/구분선/삭제/▲▼/변경) 잔여 | P2 | 중 | M5-1 | 🚧 1/2 |
| X-15 | **오프라인 라이선스 인증**(사용자 요청 07-16): **유출 방지 + 발급 용이**한 오프라인 활성화 설계·개발. 설계 방향 — ① 라이선스 = 서명된 텍스트 파일(exe 옆 `license.key` — 포터블 DR-3 부합): 페이로드(이름·이메일·에디션·발급일·만료·기기 바인딩 옵션[HWID 해시]) + **ECDSA P-256 서명** ② 앱 내 검증 = **Windows CNG(bcrypt.dll — OS 인박스 DLL)** → 외부 crate 0·B3 게이트 유지, 공개키만 exe에 임베드(개인키 유출 경로 원천 차단) ③ 발급 = 오프라인 CLI 서명 도구(별도 크레이트 `nexa-lic` — 배포 제외, 개인키는 발급 PC 로컬 보관) ④ 위변조 = 서명 검증 실패 시 미등록 동작. 착수 시 ADR 작성 | P2 | 대 | — | ☐ 설계 등록 |
| X-16 | **최적화 패스**(사용자 요청 07-16): 1차 완료(07-16 — redundant clone 8건·workspace lints·**row_ref 셀 핫패스 무클론**·**DW 레이아웃 캐시 무할당 조회**·**아이콘 틱 무효화 rect 축소**·mouse_xy/Invalidations 정리, 170 green). **잔여 백로그**(점검 도출 — 건별 슬라이스): ① 모달 공통화 modal.rs(3중 복제 ~150줄) ② Settings↔PrefValues↔State 매핑 단일화(4곳 사본) ③ win.rs 분할(theme/termdock/fileops/contextmenu) ④ 플랫폼 중립 모듈 lib 승격(panel/config/source/i18n/pathinput/nav ~2,400줄) ⑤ launcher 시드 로직 분리 ⑥ index_of_path O(E×V) ⑦ paint rcPaint 존중 | P2 | 대 | — | 🚧 1차 ✅ |
| X-23 | **일괄 이름 변경 카드 재편**(사용자 요청 07-17 — PF 카드 UI 참조): 동작별(Replace Text/RegEx·Insert·Case·Number·Date) **GroupCard 카드**로 다이얼로그 재구성. 토대 = **Nx 컨트롤 세트 완성**(07-17): NxGroupCard(타이틀 자식 배치·통지 투과)·NxComboBox(✓ 팝업)·NxCheckBox·NxTextBox(포커스 링)·NxIconButton(shape 투명)·NxButton(기본/Default/Disabled)·NxSegmented(SegOpts — 라운드/간격)·NxSpin(독립 글상자+분리 버튼·min/max 비활성) + 규약 3종(**Nx 명명** `Nexa.Nx*`·**공통 자동 높이**[버튼·세그먼트 = 컴팩트 글꼴+4]·**AA = DrawCtx 백엔드만**[gdipctx = 유일 GDI+ 접점·Style.behind 블렌드]). NxDropList 은퇴(콤보 통일). 검증 갤러리 ctldemo(임시 🃏). **재편 1차 ✅(07-17)**: 카드 스택 = 파이프라인(± 추가/삭제·마지막 1장 불가·실시간 미리보기·Text/RegEx 분리·Padding 콤보·Date `${}` 포맷+? 도움말·프리셋 카드 복원). **2차 ✅(07-18 `feat/nx-grid`)**: **NxGrid(ctl 14호)** 미리보기(Apply/Before/After·리사이즈·행별 제외 토글·**오버레이 스크롤바 세로/가로**·**행 선택 파일 목록 규약**·GridOpts[무헤더/지브라/⊖]) + **프리셋 v2 macOS 시안**([…⌄]=[프리셋들/─/Save/Edit]+건수 라벨 동일 행·Save 라운드 팝업 기본 이름 전체 선택·Edit 지브라 목록 행별 ⊖ 스테이징 삭제) + **하단 행 재편**(`feat/bulk-bottom-bar` — Apply→"이름 변경" NxButton Default·우하단 [취소][이름 변경]·프리셋 하단 좌측·그리드 전고·GridOpts.row_h = 파일 목록 행 높이 동일). 잔여 β: 카드 재배치(▲▼ 대체 — 카드 다수 스크롤 ✅ 07-18) | P2 | 중 | X-22 | 🚧 2차 ✅ |
| X-22 | **일괄 이름 변경 v2 — PF 패리티**(사용자 분석 주도 07-17, 설계 = [22-batch-rename-v2.md](22-batch-rename-v2.md)): ① Apply to 스코프 4종(전 동작 공통) ② Replace Mode(First/Last/Entire) ③ **Add Date 신설**(수정/생성일·토큰 포맷·위치·감싸기) ④ Insert/Number/Date 공용 임의 위치 모델 ⑤ Number Prefix/Suffix 감싸기 ⑥ 미리보기 ✓ 마커+건수. 슬라이스 S1(코어)/S2(ctl 컴포넌트)/S3(UI) | P2 | 대 | M5-1 | ✅ (07-17 — ctl segmented/spin/droplist 신설[판매용 추상화]·Date 엔진·v1 프리셋 호환·실기 검증. QA 1차: droplist owner 승격 크래시+설정 즉시 영속 `fix/x22-qa-droplist` 수리) |
| X-21 | **shell: 특수 폴더 스킴**(사용자 요청 07-17): 경로 바 `shell:이름` 전체 지원 — SHParseDisplayName 위임(KnownFolders·GUID 자동 커버)·가상 폴더는 원문 격리. 잔여 β: 가상 폴더(휴지통 등) 탐색은 X-17 가상 루트 모델 확장 시 | P2 | 소 | — | ✅ (07-17) |
| X-20 | **패널/정보 모드**(사용자 요청 07-16 — 원본 FR-C1 단일↔듀얼): 싱글/듀얼 패널(우 패널 숨김 — 상태 보존·복귀 원복)·싱글/듀얼 정보(전폭 공유 도크 — 활성 추종). 싱글 패널 = 싱글 정보 고정(선호값 보존·복귀 원복). 잔여 β: 공유 도크 터미널 활성 추종(현재 좌 고정)·정보 모드 라디오 비활성 표시(메뉴 enabled 미지원) | P1 | 중 | X-6 | ✅ (07-16 — 실기 QA 대기) |
| X-18 | **배포 2채널**(사용자 요청 07-16 — DR-3 개정): 포터블 최소파일 설계 명문화 + **설치형 exe**(Inno Setup·사용자별 설치 기본) + release.yml 2종 첨부 + **data_dir 쓰기 폴백**(`%LOCALAPPDATA%\NexaDir\data`). 설계 [21-distribution.md](21-distribution.md). 실기 검증(21 §6 체크리스트)은 다음 버전 태그에서 | P1 | 중 | M5-2 | ✅ (07-16 — 태그 실검증 대기) |
| X-19 | **보기 모드 3종**(사용자 요청 07-16 — 스크린샷 확정): 타일(아이콘 32+이름/보조 줄 그리드·내 PC 용량 바)·일반 폴더(평탄 리스트)·트리 폴더(기존). 내 PC 드라이브 컬럼(전체/여유) 자동 전환. ~~탭별 모드~~ ✅(07-16 — 탭별 상태·새 탭 계승·세션 영속·도구 모음 라디오 3버튼·update_status 동기). 잔여 β: 타일 인라인 이름변경·아이콘 보기(3크기 — 사용자 보류) | P1 | 대 | X-17 | ✅ (07-16 — 177 green·실기 QA 대기) |
| X-17 | **가상 최상위 "내 PC"**(사용자 요청 07-16 — 스크린샷: 탐색기 "내 PC"): 드라이브 루트(C:\)에서 **상위 이동(Alt+↑) 시 가상 드라이브 목록 뷰**로 진입해 **드라이브 간 이동** 가능하게. 설계 방향 — ① 가상 루트 노드(실제 FS 경로 아님 — nexa-vfs 가상 소스 or 패널 특수 모드) ② 드라이브 열거 = GetLogicalDrives+GetDriveTypeW+GetVolumeInformationW+GetDiskFreeSpaceExW(볼륨명·종류·용량/여유 — 탐색기식 사용량 바 렌더는 β) ③ 경로 바 최상위 세그먼트("내 PC")·경로 표기 규약 ④ 드라이브 행 Enter/더블클릭 = 그 드라이브 루트로 navigate. **착수 전 원본(../nexa-dir) 드라이브 뷰 구현 여부 확인**(재발명 금지 규약) | P1 | 중 | — | ✅ (07-16 — 원본 미구현 확인·센티널 `::PC::` 설계·G-7 선택·i18n 표시·테스트 3건. 볼륨명·용량 바 데코=β 잔여) |
| X-14 | **커스텀 우클릭 메뉴 설정**(사용자 요청 07-15 — 원본 ADR-0005 §7 선언 레지스트리): 고유 병합 항목(완전 삭제·붙여넣기·Undo/Redo·경로/이름 복사 등)별 **표시 체크박스 + ▲▼ 순서** 설정 | P2 | 중 | M3-4 | ☐ |
| X-11 | **원본 패리티 갭 잔여** — 목록 SSOT = [19-parity-gap.md](19-parity-gap.md) §1(G-1~14: .lnk·Checksum·spring-load·탭 핀/패널 간 이동·F13-1 위로 시 선택·글꼴 슬롯·재시작 확인창 등) | P2~3 | 대 | — | ☐ (건별 착수) |
| X-28 | **탭 바 UX 확장**(사용자 요청 07-20): ① 탭 우클릭 메뉴 **New Tab**(탭 닫기 위·Ctrl+T 동일 경로) ② **멀티라인 탭**(폭 초과 줄바꿈 — 줄 수는 paint 측정 `lines()`+`take_lines_changed()`→호스트 재레이아웃·탭 바 높이 `tab_h×lines`) ③ **상하 드래그 이동**(x 중간점 스냅 규칙을 교차 줄에 일반화 — 아래 줄=항상 뒤 인덱스) | P2 | 중 | M2-2 | ✅ (07-20 — 실기 QA 대기) |
| X-29 | **도크 텍스트 문자 단위 선택 복사**(사용자 요청 07-20 — 미리보기 보완): 정보/미리보기 텍스트 드래그 = **(라인,문자) 영역 선택**(edit.rs 오프셋 캐시 규약·폭 초과 측정 상한)·부분 하이라이트·Ctrl+C 복사(다중 라인 `\r\n`). 단순 클릭=선택 없음(종전 라인 선택에서 변경) | P2 | 소 | M4-2 | ✅ (07-20 — 실기 QA 대기) |
| X-30 | **전송 진행 UX 개편**(사용자 요청 07-21 — 5건): ① 진행 창 "1 B / 1 B" 오표기 수정([dialog.rs](../crates/nexa-app/src/dialog.rs) `set_done` 1/1 강제 → 실제 바이트 유지) ② **설정 `transfer_close_ms`**(0~10000ms·기본 2000 — prefs "파일 전송" 카테고리, **0=진행 창 미표시**) ③ 용량 **B~TB 적응** + `진행/전체 (pct%) · 파일 {cur}/{count}` 정보 줄(바 바로 위) ④ **항목별 크기 비례 세그먼트 바**(완료 accent·진행 부분 채움·건너뜀 회색·실패 적색·경계선 — nexa-ops `Event` 프로토콜[Plan/ItemStart/Bytes/ItemEnd] 확장) ⑤ **전송 중 대상 잠금**(`in_flight` 경로+하위 — 더블클릭/Enter/Alt+↓ 실행 차단[비프+제목줄]·드래그 발신 제외) ⑥ QA 후속(07-21): **DEL 휴지통 삭제 워커 이관 + 낙관적 숨김**(SHFileOperationW = 셸 고정 비용 판단 — UI 3~4초 멈춤 해소·행 즉시 화면 제거[nexa-tree `remove_paths`]·실패 시 재로드 원복)·세그먼트 **최소 3px 보장**(작은 파일 구간 소멸 수정)·**파일별 5색 순환 팔레트**·닫기 시간 **ms 단위 개정**(`transfer_close_ms`)·설정 창 "View _Sort" 표기 수정(SS_NOPREFIX/DT_NOPREFIX) | P1 | 중 | M3-1 | ✅ (07-21 — main 병합) |
| X-31 | **일본어 내장 언어팩**(사용자 요청 07-21): `ja.lang`(en 기준 전 키 번역 — Windows 일본어 UI 용어 관례) + [i18n.rs](../crates/nexa-app/src/i18n.rs) `BUILTIN_JA`/`discover` 등록(내장 3언어 en·ko·ja — 메뉴/설정 라디오 자동 노출·`data\lang` 키 단위 오버라이드·OS `ja-JP` 시스템 추종)·파리티 테스트 전 언어 일반화 | P2 | 소 | M2-6 | ✅ (07-21) |
| X-32 | **클립보드·DnD UX 보완 4종**(사용자 요청 07-22): ① Ctrl+X 잘라내기 **흐림 표시**(OS 클립보드 미러 `CUT_MARKS`·`WM_CLIPBOARDUPDATE` 단일 길목·`RowSource::is_ghosted` — 외부 탐색기 잘라내기 포함) ② Ctrl+V **선택 대상 안 붙여넣기**(`paste_dest` — 폴더 1개 선택 시 그 안, 파일 1개 선택 시 그 파일의 폴더[07-22 QA 보완]) ③ DnD **엣지 자동 스크롤**(상/하단 한 행 존) ④ DnD **호버 대기 3초 = 탭 전환/접힌 폴더 펼침**(설정 `dnd_hover_ms` 기본 3000·200~10000·lang 3종). **표 누락분 소급 등록**(07-24 확인 — 브랜치 `feat/clipboard-dnd-ux`로 07-22 병합 완료, DEVLOG·BRANCHES에는 기록되어 있었음) | P2 | 중 | M3-4 | ✅ (07-22 — main 병합 ff `ec8d727`·209 green) |
| X-33 | **macOS·Linux 확장 검토**(사용자 요청 07-24): 크로스 플랫폼 확장 타당성을 실측 기반으로 정리 → [23 검토서](23-cross-platform-feasibility.md) 신설. 계측 결과 = 전체 40,041 LOC 중 **중립 40.2%**(맥 네이티브 147 테스트 green·`nexa-app` 스텁 빌드 성공) / Windows 결합 59.8%. 핵심 = 화면 로직이 이미 `nexa-gui`(`DrawCtx` 의존)에 있어 재사용 가능하나, **`ctl` 18종 7,139 LOC(HWND 골격 종속)와 OS 통합 3K LOC가 재작성 대상**이며 **DR-1·DR-2·DR-8이 문안 그대로는 Linux에서 성립 불가**(ADR-0005 개정 필요). **다음 액션 = 결정이 아니라 맥 렌더 스파이크**(M0-7 방식 — "rows.rs 무수정으로 맥 창에 파일 목록 렌더 가능한가" + 예산 실측). 사용자 결정 5건은 검토서 §7 | P3 | 대 | — | 📐 검토 완료 (07-24) — 착수 여부 = 사용자 결정 대기 |
| X-34 | **항목 우클릭 "새로 만들기" + 생성 후 인라인 리네임**(사용자 요청 07-27 — 3건): ① 파일/폴더 **항목** 메뉴에도 새로 만들기(파일=부모·폴더=자신 — 빈 영역 우클릭과 동일 동작·다중 선택 숨김) ② **전체 ShellNew 템플릿**(폴더만이 아니라 파일 템플릿도 — CLSID_NewMenu 직접 호스팅·전용 대역 0x7000·ACTIVE Vec 포워딩·라벨 앱 언어 `ctx.new`·설정 row `new` 키) ③ **생성 후 리네임 모드 진입**(셸 invoke 전후 폴더 diff → `Outcome::Created` → `focus_created_and_rename` 공용화[Ctrl+Shift+N과 동일 경로]·접힌 폴더 hover_expand·배경 메뉴 휴리스틱 포함) ④ QA 1차 픽스: 템플릿 주 메뉴 평탄 삽입 → `MenuHost` 선별 라우팅(INITMENUPOPUP=HMENU 소유자·DRAW/MEASUREITEM=itemID 대역) | P2 | 중 | M3-4 | ✅ (07-27 — main 병합) |
| X-36 | **클라우드 동기화 폴더 내 PC 노출**(사용자 요청 08-01 — [26 검토서](26-cloud-integration-study.md) §2): OneDrive 개인/비즈니스(레지스트리 `Accounts\*`)·Google Drive(DriveFS 마운트/볼륨 라벨)·Dropbox(`info.json`)·iCloud 탐지 → **`::PC::` 뷰 "클라우드" 섹션**(절대 경로 join-대체 규약 재사용·실존 프로브·다계정 = 키/JSON 열거 자연 지원). 선행 단위 = **Entry 표시명≠경로 분리**(.lnk display_name 전례). β: 플레이스홀더 미리보기 방어(`RECALL_ON_DATA_ACCESS`)·상태 컬럼·용량. crate 0·B2/B3 무변. **착수 전 원본 대조(맥) 필요** | P1 | 중 | X-17 | ✅ (08-02 — `feat/x36-cloud-connections` **main ff 병합·`0.13.0` 배포**: MenuBar 하위 메뉴·vfs `target`/`extra_roots`·cloud.rs 탐지·`cloudN` 직렬화·Cloud 메뉴[바로 가기/온라인 보기/URL 복사/연결 해제/연결 추가하기]·This PC 우클릭 연결. 254 green·3.44MB. 실기 QA 통과[3사 연결·탐색]·매뉴얼 [기능-클라우드](wiki/기능-클라우드.md). 잔여 β = 플레이스홀더 미리보기 방어·상태 컬럼·§2-5 pin 일괄·섹션 분리 정렬·원본 대조[맥]) |
| X-37 | **직접 클라우드 클라이언트 = 기능 플러그인**(사용자 요청 08-01 — [26 검토서](26-cloud-integration-study.md) §3): 플러그인 종류 `kind: cloud`(nx_meta v2·하위호환) + 공급자 ABI(`nx_list`/`nx_stat`/`nx_read`…) + 호스트 임포트 `nx_http`(WinHTTP — **B3 +winhttp.dll**)·`nx_secret_*`(DPAPI) + OAuth2 PKCE 루프백 + **연결 모델**(`cloudN.*` — 클라우드별 2+계정·토큰은 호스트만 소유[헤더 주입 — 플러그인 비노출]) + `nexa_vfs::Provider` 승격(`::CLOUD:<id>::` 센티널·**1차 읽기 전용**). 전제 = **ADR-0006**(DR-7 종류 확장·DR-2 B3 개정) 승인 → OneDrive(Graph) 스파이크 → Dropbox → Google Drive(scope 전략). 사용자 결정 5건 = 검토서 §6 | P2 | 대 | X-36·X-2 | 🚧 **구현 완료 — 잔여는 배포 선행 조건**(코드 작업 없음) (08-01~02 — 사용자가 OAuth 직접 연결 채택. [ADR-0006](27-adr-0006-cloud-oauth.md) 신설·**DR-2 개정**(B3 +winhttp/crypt32/ws2_32/bcrypt)·`oauth.rs`[PKCE·루프백·WinHTTP 교환/refresh]·`secret.rs`[DPAPI]·**Connect Cloud 메뉴**[브라우저 열기/URL 복사/취소]·client_id 사용자 제공. 248 green·3.38MB·B3 통과. **1~13차 완료** — 인증·탐색·다운로드·쓰기·표시명/폴더 재귀·3사 완성·Google client_secret 동봉·진행 표시(폴더 열기 배지 + 전송 창 재사용·바이트 단위·취소)·**계정 간 복사**·[홈] 버튼·컬럼 폭 동기·Dropbox 실사용 5건 픽스(scope write·리디렉션 정확 일치·계정 조회 POST·헤더 ASCII·유니코드 이스케이프 해석). 254 green·3.44MB·B3 통과. **잔여**(①~③ = **배포 인원 한도** — 3사 등록은 완료, 본인 사용은 무관. 근거 = [ADR-0006 §2-4-1](27-adr-0006-cloud-oauth.md) 08-02 점검) = ① **Dropbox 프로덕션 승인**(현재 Development 1/500 — **50명 연결 시 2주 내 신청 안 하면 신규 연결 동결**. 심사 무료·수일 → 세 건 중 **가장 쉽고 효과가 큼**) ② **Entra 게시자 확인** — 개인 MS 계정은 이미 무제한이나 **조직 계정은 동의 차단**(2020-11-08 이후 멀티테넌트 앱 정책·`Files.ReadWrite`가 대상). 요건 = CPP 계정 + **커스텀 도메인 DNS 검증**(도메인 보유 선행). 미해결 시 조직 사용자는 **동기화 폴더 Link로 우회 가능** ③ Google **CASA Tier 2**(수개월·연 $500~4,500) — **프로덕션 게시로도 100명 상한이 유지**되고 그 100명은 프로젝트 수명 누적·리셋 불가. 소규모면 프로덕션 게시로 7일 만료만 제거하는 절충 가능 ④ 온디맨드 파리티([26](26-cloud-integration-study.md) §2-5 β1~β3 — 기능 잔여) ⑤ 로컬 undo 불가(서비스 휴지통 이용). 플러그인화는 ABI 실측 후) |
| X-38 | **7-Zip 드롭 수신 — 지연 렌더링 소스 대응**(사용자 QA 08-02): 7-Zip 등 압축 관리자는 드래그 중 추출 전 임시 폴더만 광고하고 **드롭 확정 후 GetData(CF_HDROP) 재조회가 실제 추출을 트리거** → [dnd.rs](../crates/nexa-app/src/dnd.rs) Drop 재조회(1차) + 소스의 임시 폴더 삭제와 비동기 전송의 경쟁을 **Drop 반환 전 같은 볼륨 rename 스테이징 확보**로 차단(2차 — 지연 렌더링 시그니처에만 발동). 잔여 β: 7-Zip 작업 폴더가 %TEMP%와 다른 볼륨인 구성은 rename 불가로 경쟁 잔존 · `IDataObjectAsyncCapability` 정식 핸드셰이크 미구현 | P1 | 소 | M3-4 | ✅ (08-02 — main 병합·실기 QA 통과[단일/다중/폴더]) |
| X-39 | **보기 옵션 적용 범위 + 패널 간 탭 DnD**(사용자 요청 08-02 — 4건 증분): ① 폴더 우선 정렬 툴바 토글(show 3번째·시안 D) ② 숨김/Dot/폴더우선 **적용 범위 3택**(`view_scope` 전체/좌-우 패널[기본]/활성 탭 — **값의 SSOT는 탭**·세션 `panel{i}.views` 탭당 3비트·update_status 활성 탭 추종·stale 수렴·툴팁 "라벨 — 범위") ③ **패널 간 탭 DnD**(반대 패널 탭 바 = 즉시 교차 이동+드래그 이양 "미리 보기 = 실이동"·**ESC 원위치 복귀**[값 채택은 커밋 유예 = 무손실]·마지막 탭 이동 불가) | P1 | 대 | X-28 | ✅ (08-02 — main 병합·보기 옵션/설정 QA 통과. 잔여 β: 탭 **Ctrl 복제 드래그**·드래그 중 시각 피드백[고스트/삽입선]) |
| X-40 | **외부·클라우드 변경 무갱신 해소**(사용자 QA 08-11 — "갱신 버튼을 눌러야 한다"): 원인 4건 = ① `WM_ACTIVATEAPP` 핸들러 **부재**(활성화 갱신 계기 없음) ② **통지가 오지 않는 경로에 폴백 부재**(RDCW는 로컬 NTFS 규약 — 클라우드 가상 드라이브·네트워크는 구독 성공해도 무통지. watcher가 **죽은 게 아니라 조용해** 07-31 생존 치유의 사각지대) ③ **디바운스 무한 연기**(`SetTimer` 재무장이 만료를 미뤄 동기화가 끝날 때까지 재로드 0) ④ 죽은 watcher 치유가 `update_status` 의존(유휴 중 죽으면 계기 없음). 해결 = **[fsprobe.rs](../crates/nexa-app/src/fsprobe.rs)**(폴더 mtime O(1) + 상한 4096 내 1회 열거 서명·전 플랫폼) + `refresh_on_return`(활성화·최소화 복원) + `TIMER_FSPOLL` 3s(**창 활성 시에만** — 유휴 백그라운드 0% 유지·틱마다 `sync_watchers`) + `WATCH_MAX_MS` 1s 연장 상한. 잔여 β: **`::CLOUD:` API 탭은 폴링 대상 아님**(네트워크 비용 — 갱신은 F5·탐색) · 4096 초과 폴더는 **내용만 바뀐 변경**을 프로브가 못 잡음(폴더 mtime만·watcher/F5 몫) · 펼친 하위 폴더는 프로브 비대상(루트만) | P1 | 중 | M3-6·X-35 | 🚧 **`0.15.0` 배포 완료 — 클라우드 실검증 대기**(main 병합·106 green·비Windows check·B2 3.66MB·B3 통과. **로컬 NTFS는 기존 watcher로도 잡히므로 스모크는 무회귀 확인일 뿐** — OneDrive/Google Drive/Dropbox 동기화 폴더·네트워크 경로가 진짜 검증 대상. 사용자 매뉴얼 = [기능-파일-조작](wiki/기능-파일-조작.md) "폴더 자동 반영") |
| X-41 | **도구 모음·퀵 런처 바 크기 조정**(사용자 요청 08-11 — **실물 QA 3라운드**): 시안 3택 → C안(셀 24·아이콘 16, `0.15.0` 배포) → 실물 보고 **"24는 충분치 않다, 32 정도"**(둘 다 32·아이콘 24) → **"빠른 실행은 이전 크기가 낫다"+"도구 모음은 중간"** → **확정 = 도구 모음 28(아이콘 20)·퀵 런처 24(아이콘 16)**. 구조 변경 = **두 바의 높이 분리**(`launch_row_h` 신설 — 종전엔 `launch_h = tool_h`라 한쪽만 못 키웠다). 아이콘은 [chrome.rs](../crates/nexa-gui/src/widgets/chrome.rs) `b.h-8` 산식이 따라오고 20·16 **둘 다 래스터 버킷과 정확히 일치**. **dw.rs 24 버킷 신설**(2라운드에서 24를 20 버킷으로 늘려 그리던 것 발견 — 최종값에선 미사용이나 **고DPI 125%에서 사용**. 종전 잔여 β를 앞당겨 해소) · 런처 아이콘 원본은 `L|`(라지 32px)로 바꿨다가 **스몰 16px로 원복** | P2 | 소 | X-27 | ✅ (08-11 — main 직커밋. `0.15.0`이 1라운드 값[셀 24]으로 나간 뒤 확정값 28/24를 **`0.16.0`으로 재배포**) |
| X-35 | **휴지통 삭제 실패 무통지 해소**(사용자 보고 07-27 — 열린 파일 삭제 실패 시 행만 재출현·무통지. 설계 확정 = [journal 07-27](journal/2026-07-27.md)): **레이어드 P+B** — ① 사전 잠금 프로브(파일 `CreateFileW(DELETE)` µs 판정·폴더는 자신만) ② 잠긴 항목 = 모달로 묻고 선택([건너뛰고 삭제]/[취소]·숨김 제외) ③ 워커 후 `exists()` diff 백스톱 ④ 실패 모달 [다시 시도](실패분 재진입)+실패 항목 자동 선택·캐럿 ⑤ 성공분만 부분 undo(원본 규약 복원 — D3) ⑥ watcher `pending_delete` 가드(D4) ⑦ lang `del.partialFail`·잠김 키. 후속 재검토: IFileOperation 전환(항목별 HRESULT·진행률) · **QA 1차 보강**: 펼친 하위 폴더 감시(`watch_dirs` 루트+가시 펼침 폴더 상한 64·diff 재구독 — M3-6 α 해소) | P1 | 중 | M3-2 | ✅ (07-27 — main 병합) |
| X-42 | **가상 파일 붙여넣기 — RDP·Outlook·압축 폴더 폴백**(사용자 보고 08-23 — 맥 RDP 세션에서 복사한 파일이 탐색기에는 붙여넣기 되고 앱에서는 안 됨): 원인 = 읽기측이 **CF_HDROP 단일 포맷**인데 원격 소스는 실경로가 없어 rdpclip이 **가상 파일 2종**(`FileGroupDescriptorW` + `FileContents` IStream 지연 스트리밍 — CFSTR_* MS 규약)으로 게시(Outlook 첨부·탐색기 zip 내부·MTP 동일). 구현 = [clipboard.rs](../crates/nexa-app/src/clipboard.rs) `has_virtual_files`/`extract_virtual_files`(OleGetClipboard → IDataObject — FileContents가 **lindex 지정**을 요구해 원시 API 불가·경로 탈출 기각·항목별 격리·충돌 " (2)") + [win.rs](../crates/nexa-app/src/win.rs) `paste_virtual` 4개소 폴백(Ctrl+V·배경 paste·고유·pasteInto — **실경로가 있으면 전송 엔진 undo 경로 우선**). 테스트 2 신설(파싱 + 가짜 IDataObject 종단 — 실 클립보드 비접촉). **2차 완결(08-23 `3e8854c`+`2d6dbdf`)**: ⓐ DnD 가상 수신(Outlook 첨부·zip 내부·MTP — Drop 반환 전 동기 추출. RDP는 프로토콜상 드래그 없음) ⓑ **워커화**(계획·스트림 확보만 UI — `CoMarshalInterThreadInterfaceInStream`으로 워커 MTA에·out-of-proc는 원 프로세스 직결 프록시. 진행 창·취소 = 클라우드 전송 공용 상태 재사용) ⓒ undo(`VPasteOp` — 휴지통 왕복 역방향·세 경로 기록) ⓓ 클라우드 대상(스테이징 추출 → X-37 업로드 경로 재사용). 잔여 β: DnD 드롭의 `IDataObjectAsyncCapability` 비동기화(X-38 β 겸용 — 현재 Drop 동기는 소스 생존 규약) | P1 | 중 | M3-5 | ✅ 2차 (08-23 — main 직커밋·115 green·B2 3.70MB·B3 무변. **실기 QA 대기**: 맥 RDP 대용량·취소·Ctrl+Z·Outlook/zip 드래그·클라우드 붙여넣기) |
| X-43 | **빈 폴더 펼침 글리프 억제**(사용자 요청 08-23): 현재 보기(숨김/점 필터)로 보여줄 자식이 없는 폴더는 트리 ▸ 글리프를 그리지 않음 — 보기 옵션이 내용을 드러내면 복귀. 설정 `hide_empty_glyph`(기본 1 — "파일 목록" 체크박스·lang 3종). 판정 = 열거 완료 폴더 트리 실측([nexa-tree](../crates/nexa-tree/src/lib.rs) `loaded_child_count`) · 미열거 = `read_dir` 조기 종료 프로브(NodeId 캐시·캐시 수명=소스 수명·열기 실패=글리프 유지 — 클라우드 보호). [RowItem.is_dir](../crates/nexa-gui/src/widgets/rows.rs) 신설(marker와 분리 — 빈 폴더도 폴더 서식 유지·`has_children` 의미 불변으로 watch_dirs 부작용 차단). **β(08-23 `2430bde`)**: 느린 경로(UNC·원격 드라이브·광학)는 프로브 생략 = 글리프 유지(`probe_skip_slow` — GetDriveType 1회·페인트 60fps 우선). 잔여: 실측상 필요 시 비동기 프로브(아이콘 로딩 큐 A-4 패턴) | P2 | 중 | M1-5 | ✅ 1차+β (08-23 — main 직커밋. **실기 QA 대기**) |
| X-44 | **감시 파이프라인 보완**(08-23 전수 조사 — "간혹 F5 필요" 유실 시나리오 8건 중 실행분. 상세 [journal 08-23](journal/2026-08-23.md)): 사용자 확정 원칙 = **활성화 순간 보이는 영역(트리 펼침 포함) 갱신 + 성능 깎는 추가 폴링 배제**. ① **탭 활성화 = 갱신 계기**(`Tab.stale` — 전환·닫기로 드러난 탭을 update_status가 무간섭 재열람. 유력 원인 1위) ② **프로브 기준선 즉시 수립**(열거~틱 0~3초 갭 삼킴 제거 — update_status·reload_both·디바운스 만료 공용 헬퍼) ③ **fsprobe 서명 강화**(항목별 (크기,mtime) SplitMix 접기 — 같은 크기·비최신 mtime 덮어쓰기 포착·비용 동일) ④ **뷰포트 펼침 폴더 mtime O(1) 점검**(기존 틱 편승 — `visible_expanded_dirs`·`rows.viewport()`) + **앱 활성화 = 보이는 영역 재열람**(프로브 게이트 폐지) ⑤ WM_DEVICECHANGE(USB → 내 PC 뷰 재로드) ⑥ 소실 폴더 조상 폴백. **라운드로빈 2단계 프로브는 사용자 지시로 배제**(추가 폴링). **2차(08-23 `3155915` — 사용자 QA)**: **뷰포트에 보이는 접힌 폴더도 감시·스윕 합류**(`watch_dirs`+`viewport_dirs` — 비활성 중 접힌 빈 폴더 안 파일 생성이 글리프에 반영 안 되던 비대칭 해소. 이벤트 기반 = 상시 비용 0). **3차(08-23 `ce6c0ee` — 사용자 확정)**: **비활성(보이는 창)에서도 폴링 유지**(감속 10s·정지는 최소화만) — 원격 생성분은 동기화가 내려오기 전까지 디스크에 없어 통지가 원리적으로 없고, 내려오는 시점에 비활성이면 낡던 것(디스크 실측으로 확정 — "활성화하면 생김" = 동기화 완료+재열람). **4차(08-23 `2bc8dba` — 실측 확정)**: 뷰포트 폴더 점검을 mtime → **프로브 서명(상한 내 열거 fold)** 승격 — 실측으로 **플레이스홀더 생성은 부모 mtime도 RDCW 통지도 안 남김**(삭제는 통지·FSW 로그 대조)이 확정돼 열거가 유일한 검출 경로. 침묵 재현 테스트(SetFileTime 원복) 계약화·비활성 유지 캡처로 글리프 표시 검증. **5차(08-23 — 사용자 확정 "이벤트로 즉시")**: [shellnotify.rs](../crates/nexa-app/src/shellnotify.rs) 신설 — **셸 변경 통지 구독**(SHChangeNotifyRegister ShellLevel|NewDelivery·DISKEVENTS·재귀 = OneDrive가 탐색기에 알리는 채널 그대로·shell32 인박스 B3 무변). 패널 루트 추종 재등록(sync_watchers)·수신 = 디바운스 합류·**스윕 보험 강등**(비활성 10s→30s) = 이벤트 1선+얇은 보험 2선. 잔여 β: 스크롤 시 감시 diff 재구독 churn 관찰 · 뷰포트 스윕의 SMB 체감 · USN 저널(비특권 읽기)은 셸 통지로 부족한 재현 시의 다음 카드 | P1 | 중 | X-40 | ✅ 5차 (08-23 — main 직커밋·116 green. **실기 QA**: 클라우드 생성 → 로컬 반영 순간 비활성에서 즉시 표시·장기 관찰) |
| X-45 | **항상 맨 위에 표시 토글**(사용자 요청 08-23): View 메뉴 체크 항목 + 툴바 panel 블록 버튼(압정 SVG)이 `CMD_TOGGLE_TOPMOST` 공유 — `SetWindowPos` TOPMOST 밴드 전환(활성화·위치·크기 불변)·즉시 적용·체크 동기·설정 `always_on_top` 영속(기동 복원)·기존 toolbar_order엔 `ontop` 보충 삽입. lang 3종 | P2 | 소 | M2-3 | ✅ (08-23 — main 직커밋·115 green·B2 3.71MB·B3 통과·재시작 반영) |
| X-46 | **압축 파일 미리보기**(사용자 요청 08-24 — 그리드·별도 창·내장+플러그인·암호 비기록·확장 용이): **압축을 풀지 않고 목록만** 읽는 계층을 [nexa-vfs/archive](../crates/nexa-vfs/src/archive/)에 신설 — 포맷 레지스트리(**새 포맷 = 파일 1개 + FORMATS 한 줄**) + 내장 리더 ZIP(Zip64·AES/ZipCrypto 표시·SFX 델타 보정·CD 암호화=암호 요구·26개 확장자)·TAR(ustar/GNU/PAX)·CAB·RAR 5/4·7z(헤더가 LZMA = NeedsCodec 안내·AES 코더 시 암호)·단일 스트림(gz/bz2/xz/zst/lz4/lz/z). 표시 = 하단 도크 요약 + **그리드 창**([archivewnd](../crates/nexa-app/src/archivewnd.rs) — NxGrid 재사용으로 파일 그리드 규약 계승·8열·수치 정렬·Ctrl+C TSV·Esc). **암호** = [Secret](../crates/nexa-core/src/secret.rs)(Debug 마스킹·Drop 소거)+마스킹 모달([pwprompt](../crates/nexa-app/src/pwprompt.rs)) — **저장 경로 자체가 없음**·세션 메모리 한정·경유 버퍼/EDIT 잔상 소거. **플러그인 ABI v2**(nx_meta 4번째 줄 = 능력 선언·nx_archive·file_size/read_at/password 임포트) + 참조 구현 [samples/archive-viewer-wasm](../samples/archive-viewer-wasm/)(ISO 9660/ar/cpio — 31KB). 설계 SSOT = [28](28-archive-preview.md). **동봉 배포(08-24 2차)**: [scripts/build-plugins.ps1](../scripts/build-plugins.ps1)(빌드 단일 출처·CI 편입) + 탐색 경로 2원화(`data\plugins` → `<exe>\plugins`·같은 id는 사용자분 우선) + 포터블 zip 동봉·설치형 `{{app}}\plugins`·**NexaDir-Plugins-<ver>.zip** 신규 자산 ([21 §5-2](21-distribution.md)·[18 §3-1](18-build-and-test.md)). 잔여 β: 항목 내용 미리보기(inflate 필요)·아카이브 폴더처럼 탐색(VFS 공급자)·7z 평문 헤더·RAR4 유니코드 이름·그리드 트리 보기 | P1 | 중 | X-2 | ✅ (08-24 — `feat/x46-archive-preview` 6커밋·319 green(워크스페이스)·clippy 0·B2 3.83MB·B3 21종 무변. **실기 QA 대기**) |
| X-47 | **오버레이 스크롤바 확산**(09-04 설정 창 본문에 첫 구현 — 반투명·스크롤 중만 표시·단계 페이드·호버 시 두꺼움·드래그 트랙): NxGrid([ctl/grid.rs](../crates/nexa-app/src/ctl/grid.rs))·NexaCardHost([bulkrename.rs](../crates/nexa-app/src/bulkrename.rs))는 불투명·호버 없음, **메인 파일 목록([rows.rs](../crates/nexa-gui/src/widgets/rows.rs))은 스크롤바 자체가 없음** → 같은 방식으로 통일할지 | P3 | 중 | X-7 | 🔶 1/2 (09-04 — **메인 파일 목록 세로+가로 적용**[rows.rs·`request_tick`·`fill_round_rect_alpha`]. 잔여 = NxGrid·카드 호스트 확산 — 사용자 결정) |
| X-48 | **글꼴 폴백 체인 실적용 — 두부 방지**(사용자 요청 09-04): 쉼표 목록 규약이 터미널(X-3)에서만 살아 있고 UI 슬롯·대화상자·미리보기는 **1순위조차 미적용**이던 실측 → [fontchain.rs](../crates/nexa-app/src/fontchain.rs)(설치된 첫 패밀리·폴백 목록·GDI 글리프 런 폴백 `GdiChain`) · DW 슬롯 3종 `IDWriteFontFallback` · 미리보기 창 런 폴백 · 대화상자/제목 1순위 정정. 한계: 네이티브 컨트롤(STATIC/EDIT)은 OS 글꼴 연결 위임·비BMP는 1순위 유지 | P2 | 중 | X-3·X-12 | ✅ (09-04 구현 — **실기 QA 대기**: 체인에 기호/이모지 글꼴 추가 후 파일 목록·미리보기 두부 소멸 확인) |
| X-49 | **터미널 테마 — 라이트 팔레트·스킴 15종·선택자**(사용자 요청 09-04): 라이트/다크가 같은 Campbell이던 것 → 셀 색 **기호화**([nexa-term](../crates/nexa-term/src/lib.rs) `TermPalette`/`SCHEMES`/`resolve_scheme` — 렌더 시 해석 = 테마 전환 즉시 스크롤백 재도장) · 라이트 기본 **GitHub Light**(16색 ≥3:1 테스트) · 다크 9·라이트 6 · 설정 `term_theme`(system/dark/light/스킴 id — 다크 앱 + 라이트 스킴 허용)·`term_theme_dark`·`term_theme_light`(모르는 id = 해석 시 폴백) · 설정 창 `Kind::Select` 콤보 3개(터미널 카테고리 하단) | P2 | 중 | M4-3·X-7 | ✅ (09-04 구현·미배포 — QA 1차 구성 승인 · 선택 하이라이트 블렌드 시도 → **QA 2차 사용자 원복 = 반전 확정**(항목별 색이 블록으로 드러남). QA 3차 "모두 일반 색" = 도구 셸 `NO_COLOR` 상속(코드 무관) → 해소·pwsh 색 확인. QA 4차 = 밝은 팔레트 무색 셀 반전을 accent 블록+흰 글자로(색 셀·다크 불변). **실기 QA 잔여**: 라이트 선택 블록·F6 즉시 재색칠·다크 앱+라이트 스킴·콤보 즉시 반영/영속. 잔여 β: 사용자 정의 스킴 파일·View 메뉴 노출) |
| X-50 | **터미널 복사 서식 — HTML·RTF 동시 게시 + 설정 4택**(사용자 요청 09-04 — WT "클립보드에 복사할 텍스트 형식"): [nexa-term](../crates/nexa-term/src/lib.rs) `get_runs`·`export::{to_html,cf_html,to_rtf}` · `clipboard::write_text_html_rtf` · 설정 `term_copy_format`(기본 text/html/rtf/both)·설정 창 드롭다운(QA 1차 라디오→콤보) · Ctrl+C = 현재 팔레트·글꼴 | P3 | 중 | X-49 | ✅ (09-04 구현·미배포 — **실기 QA 대기**: both 설정 → Word/Outlook 붙여넣기 색·글꼴 유지·메모장 평문·기본은 평문만. 잔여 β: 우클릭 복사 메뉴·복사 시 후행 공백 옵션) |
| X-51 | **정규 점검 하네스 수립**(사용자 요청 09-04 — 기능 보완·성능·다수 파일·설정·플러그인·보안·커버리지·펜테스팅형): [29](29-audit-checklist.md)(순서·항목·기준·회차 기록) · [`scripts/audit.ps1`](../scripts/audit.ps1)(테스트·clippy·비Windows·B-2/B-3·PE 완화·매니페스트·열거/VT 벤치·유휴 실측) · `nexa-vfs/examples/audit_enum`·`nexa-term/examples/audit_vt` · 적대적 입력 테스트 X-1(VT)·X-2(설정) · cargo-llvm-cov | P2 | 대 | — | ✅ (09-04 1차 실측·조사 6영역 완료 — 발견 27건 = X-52~X-57. 회차마다 29 §1~§8 보완) |
| X-52 | **파일 조작 HIGH 묶음**(점검 #1·2·12·13·14·7): 덮어쓰기가 대상 선삭제 → 임시 이름 후 교체 · 전송 중 드롭 조용히 폐기(스테이징 회수 없음) → 큐잉/명시 거부 · 하위 트리 한 파일 실패 = 폴더 중단 + 정션 처리 · 항목마다 PostMessage+Vec 클론 O(N²) · 계획 단계 트리 2회·취소 불가 · `reload_both` 양 패널 동기 재열거+O(S×V) 선택 복원 · 오류 목록 표시 없음 | P1 | 대 | — | 🚧 (점검 1차 발견 — 착수 대기) |
| X-53 | **설정·세션 저장 HIGH 묶음**(점검 #6·7·8·19): `save()` 옛 파일 선삭제·fsync 없음·임시명 고정 → 원자적+pid 임시명 · 변경마다 UI 스레드 전체 직렬화·무변경 저장·`apply_prefs` 전체 무효화 → dirty 비교+디바운스 · 세션 복원 창 생성 전 동기 → 활성 탭만 즉시 · `cloud_client_secret_*` 직렬화 누락 · WM_ENDSESSION · 다중 인스턴스 · 비UTF-8 전체 초기화 · `col_layout` dirty 누락 | P1 | 중 | X-16 ② | 🚧 (점검 1차 발견) |
| X-54 | **렌더/타이머 HIGH 묶음**(점검 #9·10·11·20): `paint()` `rcPaint` 무시 · 캐럿 타이머 비활성/최소화 미해제 · `sync_watchers` 가시 행 String 클론(`update_status` 31곳) · `index_of_path` O(S×V) → 해시 · 프리뷰 `update_status`마다 재파싱 · 이미지 캐시 히트 복사 · 레이아웃 캐시 LRU · `term_cell_w` 매 프레임 · FSPOLL 뷰포트 스윕 UNC 미제외 · #24 최소화 재무장 | P1 | 중 | — | 🚧 (점검 1차 발견) |
| X-55 | **보안 하드닝**(점검 #4·5·15·16·17·22): ConPTY `pwsh.exe` 전체 경로(lpApplicationName) · 매니페스트(asInvoker·longPathAware·DPI) · `/DEPENDENTLOADFLAG:0x800`·`-C control-flow-guard`·`/CETCOMPAT`·`overflow-checks` · WM_APP 포인터 메시지 쿠키 · 클라우드 임시 폴더 유일성+MOTW+정리 · CSPRNG 실패 = 오류 · 리디렉션 정책 · DPAPI 엔트로피 · 완화 정책 · audit.ps1에 DependentLoadFlags/CET/임포트∖KnownDLLs 판정 추가. **정책 결정**: 코드 서명·출처 증명·소스 하드코딩 OAuth 시크릿 | P1 | 중 | — | 🚧 (점검 1차 발견 — 플래그 4줄은 즉시 가능) |
| X-56 | **플러그인 런타임 보강**(점검 #3·18): 벽시계 타임아웃 + 호스트 임포트 연료 과금 + 워커 이동 · OUT_CAP vs 샘플 20k(≈1.2MB) 정합·오류 문구 · 로드 오류 표시/로그 · 능력별 링커(`password`는 archive만) · 비활성 플러그인 미실행 · 서킷 브레이커 · dist 드리프트 CI diff · 잘림 플래그 · 임시 BMP 상한/정리 · 링커 캐시 · A22/A23 스톱워치 테스트 · 동봉본 SHA-256 매니페스트 | P2 | 중 | X-2 | 🚧 (점검 1차 발견) |
| X-57 | **최근 기능 잔여 결함**(점검 #25·26·27 — X-40~X-50 회귀 점검): Ctrl+C 실패 시 선택 유지+신호 · `TextRun` faint · 복사 글꼴 `first_installed` · Select 콤보 미지 값 폴백 기록 · 오버레이 바 DPI 스케일·키보드 스크롤 flash·WM_MOUSELEAVE 호버 해제·테스트 · 가상 붙여넣기 실패 신호 · `sanitize_rel` 예약 장치명 · 압축 창 다크 스타일·WM_QUIT·빈 선택 Ctrl+C·목록 워커 · fontchain 현지화 이름·캐시 갱신 · fsprobe 이름 해시·SCAN_CAP 진동 | P2 | 중 | X-46~X-50 | 🚧 (점검 1차 발견) |
