# 개발 여정

**2026-07-11 시작**. 단 5일(07-11~07-15)에 M0~M5를 마감하고 `0.6.0`을 발행했으며, 이후 포스트 M5 UX 고도화로 **`0.13.0`**(08-02)까지 왔습니다. 각 마일스톤은 "완료 = 테스트 green + 예산 게이트 통과"로만 인정됩니다.

> 예산 게이트가 무엇인지, DR/ADR이 무엇인지는 **[설계 결정](설계-결정)** 문서를 참고하세요.

---

## 타임라인

### M0 — 기반·게이트 → `0.1.0` (07-11)

프로젝트 발족. 원본 스택 한계 실측 → 올 러스트 재구축 결정 후 설계 문서 세트를 확정했습니다. 워크스페이스 스캐폴딩(정적 CRT·LTO·panic abort) → **nexa-core/vfs/tree 원본 이식**(rlib 직접 링크로 FFI/ABI 소멸) → Win32 창 스켈레톤 → GDI 렌더 스파이크(더블 버퍼·100k 행 중 가시 행만).

- **게이트 실측**: 빈 창 유휴 RSS **13.22MB** · exe **0.20MB** · 임포트 6종 전부 인박스.
- 원본 60MB+ → 13MB로 스택 전환의 정당성을 첫날 증명.
- 특기: **맥에서 windows 타깃 `cargo check` green** — WinUI 시절 불가능하던 UI 코드 사전 타입 검증.

### M1 — 뷰어 코어 → `0.2.0` (07-12)

원본 뷰어 축을 순차 재현. `nexa-gui` 분리 → **ADR-0002 렌더링 확정**(DirectWrite interop, GDI 대비 −28%) → 가상화 파일 리스트 → 5컬럼 시스템(정렬 3상태·Shift 다중열) → **★ 플래그십**(삼각형=펼침 vs 본문=선택 분리·**폴더 교차 다중 선택**·러버밴드) → 키보드 네비 + 타입어헤드 → 셸 아이콘(LRU 캐시) → 네비게이션(히스토리·숨김 토글).

- **게이트**: 100k 첫 렌더 **115ms** · 스크롤 2,098µs/프레임(60fps 예산의 13%) · 10k 유휴 27.87MB.

### M2 — 셸 골격 → `0.3.0` (07-13)

탐색기 크롬 완성. 경로 바(브레드크럼 + 편집) → **듀얼 패널 + 패널별 탭** → 메뉴/도구/상태바(**커스텀 드로잉·자식 HWND 0**) → 테마 시스템(시스템/라이트/다크·DWM 다크 타이틀바) → 설정/세션 영속 → i18n(en/ko·동적 전환) → **상주 규율**(유휴 트림: 활성 26.9MB → 유휴 0.21MB) → IME/UIA 1차.

- **게이트**: 듀얼·탭4 상주 **26.9MB** ≤30 통과.

### M3 — 파일 조작 → `0.4.0` (07-13)

"뷰어 → 탐색기" 정체성 완성. 전송 엔진(순번 복제·충돌·4MB 청크·취소) → 삭제/이름변경/새로 만들기 → **Undo/Redo**(휴지통 복원 포함) → **셸 컨텍스트 메뉴**(comctl32 회피) → 클립보드 상호운용 + **OLE 드래그앤드롭** → 폴더 watcher(무간섭 재로드).

- **게이트**: 활성 32.57 → 300초 후 **6.29MB** ≤30.

### M4 — 하단 도크 → `0.5.0` (07-14)

하단 패널 3종. 정보 뷰 → **내장 미리보기**(텍스트·WIC 이미지) → **ConPTY 터미널**(VT 파서·SGR·스크롤백·pwsh→cmd 폴백). 이어 실기 QA로 이동 프리즈 2건을 근본 해소(12,009ms → 4ms).

- **게이트**: 터미널 상주 최악 케이스에서 300초 후 **5.07MB** ≤30.

### M5 — 마감·릴리스 → `0.6.0` (07-15)

퀵 런처 바 → 일괄 이름 변경 α(원본 스펙 **최초 구현**) → **릴리스 파이프라인**(태그 push → 테스트 → 예산 게이트 → GitHub Release 자동 첨부) → 접근성·IME 마감 + **무서명 유지 확정**.

- **게이트**: 유휴 RSS 중앙값 **16.86MB** · exe **0.90MB**.

### 07-16 ~ 07-19 — UX 고도화 → `0.7.0`

마감 이후 사용자 주도 UX 심화. **배포 2채널**(포터블 + Inno Setup 설치형) → 가상 최상위 "내 PC" → 보기 모드 3종(타일/일반/트리) → 폰트 슬롯 → 패널/정보 싱글·듀얼 모드 → **일괄 이름 변경 v2**(카드 스택 파이프라인·정규식·날짜 토큰) → **Nx 커스텀 컨트롤 라이브러리**(14종·GDI+ AA 백엔드) → **SVG 아이콘 파이프라인**(서브셋 파서·9종 전부 SVG) → 컬럼 드래그 재배열·auto-fit → 하단 도크 버튼·용어 체계 확정.

- **게이트**: exe **1.40MB** · 임포트 인박스 17종.

### 07-20 ~ 07-22 — 상호작용 다듬기 → `0.8.1` · `0.9.0` · `0.10.0` · `0.11.0`

탭 바 UX(우클릭 New Tab·멀티라인 탭·상하 드래그) → 하단 도크 텍스트 **문자 단위 선택 복사** → About 창 → **전송 진행 UX 개편**(항목별 크기 비례 세그먼트 바·전송 중 대상 잠금) → **일본어 내장 언어팩**(en·ko·ja) → 클립보드·DnD 4종(Ctrl+X 흐림 표시·선택 대상 안 붙여넣기·엣지 자동 스크롤·호버 3초 전환). 이 구간에 **Chocolatey·winget 배포 채널 등록**과 이 위키가 만들어졌습니다.

### 07-26 ~ 07-27 — 플러그인과 안전한 삭제 → `0.12.0`

미리보기 **플러그인 시스템**을 구축하고, 실제로 만들어 본 뒤 런타임을 **Starlark → wasmi로 교체**했습니다(ADR-0005 — `.wasm` 하나가 모든 OS에서 동일 동작). 참조 구현 markdown-viewer-wasm은 GitHub 스타일 마크다운과 **Mermaid 다이어그램을 이미지 수준으로** 렌더합니다. 이어 항목 우클릭 **"새로 만들기"**(셸 템플릿 전체 + 생성 후 인라인 리네임)와 **휴지통 삭제 실패 무통지 해소**(사전 잠금 프로브 + 사후 diff 백스톱·성공분만 부분 undo).

### 07-31 — 안 보이던 곳 손보기

**전 소스 안정성 감사 7건** — 그중 watcher가 버퍼 넘침을 죽음으로 오판하던 결함은 "OneDrive 폴더가 가끔 갱신되지 않는다"는 오랜 증상의 유력한 원인이었습니다. 함께 conpty 종료 대기 핸들 경쟁·완료 통지 유실로 인한 상태 고착도 해소. 브라우저가 Release exe 다운로드를 차단하는 문제에는 **zip 자산 + `SHA256SUMS.txt`**로 대응했습니다(무서명 exe의 평판 필터 — 서명 경로는 재조사 결과 MS Store 외에 해법이 없다는 결론).

### 08-01 ~ 08-02 — 클라우드 → `0.13.0`

가장 큰 기능 추가. **Link**(설치된 동기화 폴더를 "내 PC"에 얹기 — 네트워크 0)와 **Connect**(앱이 OneDrive·Google Drive·Dropbox에 직접 로그인 — OAuth2 PKCE·토큰 DPAPI 보관)를 모두 구현했습니다. 인증만 되던 상태에서 탐색·다운로드·업로드·**계정 간 복사**까지 이어졌고, 그 과정의 실사용 QA 17건이 전부 진짜 결함이었습니다(이진 파일이 UTF-8 변환으로 손상되던 것·로딩이 끝나지 않던 5가지 원인·한글 이름이 헤더 ASCII 제약에 걸리던 것 등). 이 구간에 **비Windows CI가 07-27부터 붉었던 것**도 발견해 복구했습니다.

- **게이트**: exe **3.43MB**(포터블 자산) · 임포트 인박스 **21종** · 254 테스트 green.

---

## 예산 실측 이력 — 원본 대비 1/4 이하

| 태그 | 날짜 | B1 유휴 RSS | B2 exe | B3 임포트 |
| --- | --- | --- | --- | --- |
| **0.1.0** (M0) | 07-11 | **13.22MB** | 0.20MB | 6종 인박스 |
| **0.2.0** (M1) | 07-12 | 10k 유휴 27.87MB | ~0.3MB | 통과 |
| **0.3.0** (M2) | 07-13 | 상주 26.9MB · 트림 0.21MB | — | 통과 |
| **0.4.0** (M3) | 07-13 | 활성 32.57 → 300s 6.29MB | 0.60MB | 인박스만 |
| **0.5.0** (M4) | 07-14 | 300s 5.07MB (터미널 상주) | 0.73MB | 인박스만 |
| **0.6.0** (M5) | 07-15 | 중앙값 16.86MB | 0.90MB | 인박스 15종 |
| **0.7.0** (UX) | 07-19 | 트림 후 2.68MB | **1.40MB** | 인박스 **17종** |
| **0.11.0** (UX) | 07-22 | — | 1.50MB | 통과 |
| **0.12.0** (플러그인) | 07-27 | — | **3.35MB** | 통과(wasmi는 순수 러스트 — DLL 무증가) |
| **0.13.0** (클라우드) | 08-02 | — | **3.43MB** | 인박스 **21종** |

> **교훈 사례**: B3 게이트가 실제로 작동했습니다. M1-7에서 `shell32` 신규 임포트를, 07-19에서 `gdiplus.dll`·`shlwapi.dll` 누락을, 08-01에는 클라우드 작업이 들여온 `bcrypt.dll`을 로컬/CI 게이트가 검출했습니다(마지막 것은 "PKCE 해시는 이미 있는 `bcryptprimitives`로 되겠지" 하는 예상을 게이트 실행이 뒤집은 사례입니다).
>
> exe가 0.2MB → 3.4MB로 성장한 배경은 세 번의 도약입니다: UX 기능 누적(SVG 파이프라인·GDI+ 백엔드·컨트롤 라이브러리) → **플러그인 런타임 wasmi 내장(+1.68MB)** → 클라우드(+0.26MB). 10MB 게이트에는 여전히 6.5MB 이상 여유가 있습니다.
>
> B1(유휴 RSS)은 M5 마감(07-15) 실측이 최신값이며, 이후 추가된 기능에 대한 재실측은 아직 하지 않았습니다.
