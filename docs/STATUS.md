# STATUS — Nexa Dir 진행 현황

> **갱신: 2026-07-21 7차 (KST)** — **릴리스 `0.9.0` 배포 완료(GitHub
> 전용 — 사용자 요청)**: 보류 방침을 **choco push만**으로 축소(새
> Release는 대기 중 0.8.1 심사에 무영향·winget=수동 PR) — release.yml
> choco push에 `vars.CHOCO_PUSH` 스위치(기본 꺼짐, 승인 후 변수 등록으로
> 재개). **실행 검증**: 게이트 B2·B3 통과·choco push **skipped**·Release
> 자산 2종(포터블 1.48MB·설치형 2.6MB) 첨부. 0.9.0 = 0.8.1 이후
> 전체(X-25 1차·X-26 ③·X-27·X-28·X-29·X-30 전송 진행 UX+QA 시리즈·
> X-31 일본어 언어팩). [journal/2026-07-21.md](journal/2026-07-21.md).
>
> **직전(07-21 6차)** — **X-31 일본어 내장 언어팩(사용자
> 요청)**: `ja.lang`(en 기준 전 키 번역·Windows 일본어 UI 용어 관례) +
> i18n 내장 등록(en·ko·ja — 메뉴/설정 라디오 자동 노출·`data\lang`
> 오버라이드·OS `ja-JP` 시스템 추종). 파리티 테스트 전 언어 일반화.
> 61 green·clippy 0·exe 1.56MB.
> [journal/2026-07-21.md](journal/2026-07-21.md).
>
> **직전(07-21 5차)** — **X-30 마감 — main 병합**
> (`feat/transfer-progress-ux` 7커밋): 전송 진행 UX 개편 전체(1B 오표기
> 수정·B~TB 단위·파일 {cur}/{count}·세그먼트 바[최소 3px·5색 순환 팔레트]·
> `transfer_close_ms` 설정[0=창 미표시]·전송 중 대상 잠금) + QA 시리즈
> (DEL 휴지통 삭제 **워커 이관 + 낙관적 숨김**[셸 API 고정 비용 판단 —
> 화면 즉시 제거·실패 시 재로드 원복]·설정 창 "View _Sort" 표기 =
> `SS_NOPREFIX`/`DT_NOPREFIX`). 전 테스트 green·clippy 0·릴리즈 실측 exe
> 1.5MB. 릴리스 태그는 choco 승인 대기 방침 유지(07-21 1차).
> [journal/2026-07-21.md](journal/2026-07-21.md).
>
> **직전(07-21 4차)** — **배포 채널 상태 실측 점검**: ①
> **Chocolatey 진전** — `nexa-dir`·`nexa-dir.portable` 0.8.1 자동 검사 3종
> **완료**(Validation/Verification **Passing**, Package Scan **Flagged Note**
> = 참고성 플래그·차단 아님) → `awaiting moderation`. 07-19 기록의 "3종
> Pending"은 정정됨. ② **winget 변동 없음** — #404528·#404533 모두 `OPEN`,
> `Azure-Pipeline-Passed` 라벨 유지·07-19 14:07 이후 무변동(신규 기여자
> 재실행 권한 없음 → 모더레이터 검토 대기). **양 채널 우리 측 조치 불요.**
> [journal/2026-07-21.md](journal/2026-07-21.md).
>
> **직전(07-21 3차)** — **X-30 QA 후속 3건(사용자)**: ①
> **DEL 휴지통 삭제 3~4초 UI 멈춤 해소** — `SHFileOperationW` UI 동기
> 호출이 원인, 워커 스레드 이관(`WM_APP_DELETE` 완료 통지 — undo·재로드
> 유지, "삭제 중..." 표시) ② **세그먼트 4파일→3구간 수정** — 전 항목
> **최소 3px 보장**(작은 파일 구간 반올림 소멸) ③ **파일별 진행색 5색
> 순환 팔레트**(파랑·초록·주황·보라·분홍 — 인덱스 % 5). 전송 창 닫기
> 시간 **단위 초→ms 개정**(`transfer_close_ms` 0~10000·기본 2000) 포함.
> [journal/2026-07-21.md](journal/2026-07-21.md).
>
> **직전(07-21 2차)** — **X-30 전송 진행 UX 개편(사용자 요청
> 5건 — `feat/transfer-progress-ux`)**: ① 완료 창 **"1 B / 1 B" 오표기
> 수정**(`set_done` 1/1 강제 → 실제 바이트 유지) ② **설정
> `transfer_close_ms`**(0~10000ms·기본 2000, prefs "파일 전송" 카테고리 —
> **0=진행 창 미표시**) ③ 용량 B~TB 적응 + `진행/전체 (pct%) · 파일
> {cur}/{count}` ④ **항목별 크기 비례 세그먼트 바**(nexa-ops `Event`
> 프로토콜 확장 — 완료/진행/건너뜀/실패 상태색) ⑤ **전송 중 대상
> 잠금**(실행/드래그 차단). 전 테스트 green·clippy 0 — **실기 QA 후 병합
> 대기**. [journal/2026-07-21.md](journal/2026-07-21.md).
>
> **직전(07-21 1차)** — **릴리스 배포 보류 방침(사용자)**: Chocolatey
> 모더레이션 **승인 전까지 새 버전 태그·Release 배포 보류**(대기 중 이중 큐
> 회피 — 첫 승인 후 후속 버전은 심사 가속). 실기 QA는 로컬 릴리즈 빌드로
> 계속. [journal/2026-07-21.md](journal/2026-07-21.md).
>
> **직전(07-20 2차)** — **탭 UX·미리보기 선택 + 백로그 구현(사용자
> 요청 5건)**: ① **X-28 탭 바 UX** — 우클릭 메뉴 New Tab(탭 닫기 위) +
> **멀티라인 탭**(폭 초과 줄바꿈 — 줄 수 = paint 측정 → 재레이아웃 1프레임
> 수렴·탭 바 높이 tab_h×lines) + **상하 드래그 이동**(x 중간점 스냅을 교차
> 줄에 일반화) ② **X-29 도크 텍스트 문자 단위 선택 복사**(정보/미리보기 —
> edit.rs 오프셋 캐시 규약·Ctrl+C 무변·단순 클릭=선택 없음) ③ **X-27 ✅**
> 툴바 hover=`sel_bg` 통일 ④ **X-25 1차 ✅** `Conflict::Nested`(개명 예정
> 조상 경로 접두 검출 — 자손 표기·적용 차단, 2차 rebase 후속) ⑤ **X-26 ③
> About 창 ✅**(about.rs — dialog.rs 규약·도움말 메뉴 신설·링크 3종은 홈페이지
> 확정 전 GitHub, ①② 홈페이지 = 설계 대기). 전 테스트 green·clippy 0·
> 릴리즈 실측 exe **1.45MB**. 실기 QA 대기.
> [TODO §7](TODO.md)·[journal/2026-07-20.md](journal/2026-07-20.md).
>
> **직전(07-20 1차)** — **백로그 등록 3건(문서만·코드 무변)**: ①
> **X-25 일괄 이름변경 조상·자손 동시 선택 처리** — 코드 확인 결과 결함 재현
> (상위 폴더+하위 항목 동시 선택 시 적용 루프가 부모 우선 개명하면 자손 경로
> 소멸 → 실패, 자손 우선이면 undo/경로 추적 손상). 대응 = 1차 감지 후 차단
> (`Nested` 충돌)·2차 경로 rebase 완전 지원(차후). ② **X-26 SosomLab·제품
> 홈페이지 + 앱 About 화면**. ③ **X-27 툴바 내비 버튼 mouseover 배경색**(경로 바
> 세그먼트 hover 색 재사용). [TODO §7](TODO.md)·[journal/2026-07-20.md](journal/2026-07-20.md).
>
> **직전(07-19 6차)** — **제품명 정리 + 배포 채널 검토**: ①
> **제품명 "2" 제거** — 저장소만 nexa-dir2, 그 외(창 클래스 NexaDirMain·
> 데이터 경로 NexaDir[마이그레이션]·산출물·문서·위키) 전부 nexa-dir/Nexa Dir.
> ② **배포 채널 4종 큐 대기(우리 조치 불요)**: winget `SosomLab.NexaDir`(#404528)·
> `.Portable`(#404533) = **Azure 검증 통과**, partiallySucceeded는 **MS 내부
> Guardian 인프라 경고**(매니페스트 무관)·신규 기여자는 재실행 권한 없어
> **모더레이터 검토 대기**. Chocolatey `nexa-dir`·`nexa-dir.portable` =
> 이메일 수정 repush 후 **자동 검사 3종 Pending → 모더레이션 대기**. 승인까지
> 수일~2주.
>
> **직전(07-19 5차)** — **배포 채널 확장 + `0.8.1`**: ①
> **winget** — `SosomLab.NexaDir`(설치형)·`SosomLab.NexaDir.Portable`
> 매니페스트 PR 제출(winget-pkgs#404528 외) ② **Chocolatey** —
> `nexa-dir`(설치형·CI 자동)·`nexa-dir.portable`(수동 pack-and-push)
> 0.8.1 게시 완료, **모더레이션 대기**(자동 검사 3종 → 사람 검토 —
> 승인까지 며칠~2주) ③ 릴리스 `0.8.1` = Chocolatey 최초 게시용 코드
> 무변 승격(자산 NexaDir-0.8.1-*).
>
> **직전(07-19 4차)** — **릴리스 `0.8.0`**: ① **GitHub Wiki
> 신설**(소개·기능 매뉴얼·개발자 매뉴얼 19페이지 — 소스 = docs/wiki) ②
> **다크모드 툴바 아이콘**(잉크 = 테마 본문색·`-dark` 전용 에셋 11종·svg
> 파서 루트 색 상속·4배 슈퍼샘플) ③ **exe 리소스 아이콘/버전정보**
> (build.rs + rc.exe — 작업표시줄 고정 빈 아이콘 해소) ④ **배포명 "Nexa
> Dir"**(창 제목·설치형·산출물 NexaDir-* — 내부 식별자·데이터 경로 불변)
> ⑤ 게이트: B2 1.43MB·B3 인박스 17종.
>
> **직전(07-19 3차)** — **릴리스 `0.7.0`**: B3 원장 수리
> (gdiplus·shlwapi 인박스 등재 — GDI+/SVG 파이프라인분·main CI 적색 해소)·
> 게이트 실측(B2 1.40MB ≤ 10·B3 통과)·@app 0.7.0·태그 push → GitHub
> Release 자동 첨부(포터블+설치형).
>
> **직전(07-19 2차)** — ① **하단 도크 툴바 버튼**(사용자
> SVG — 듀얼 파일 패널 옆·순서 병합 = 누락 자식 **정의 위치 삽입**·도크
> 꺼짐 = 정보 패널 토글 비활성) ② **UI 용어 체계 확정**: 파일 패널/File
> Panel · **하단 도크**/Bottom Dock(사용자 명명) · 정보 패널/Info Pane —
> 영어 라벨 Title Case("Dual File Panels")·"넓이→너비" 오타 수정(ko/en
> 31키) ③ **좌측 도크 설계 등록**(TODO X-24 — 디렉토리 트리 내비게이터·
> 클릭 = 활성 탭 이동) ④ 편집 트리 **레이아웃 확정**(표시 체크 = 1열
> 고정·셰브론 = 체크 뒤·하위 없는 그룹 생략) + 그룹 해제 시 하위 체크
> 비활성·펼침/닫음 트리 ⑤ 드라이브 루트 탭 = "D:" 표기 ⑥ **하단 도크
> 기본 표시**(현 실행 설정 승격 — 도구모음 순서는 기본값과 이미 일치).
> 전체 green·clippy 0.
>
> **직전(07-19 1차)** — ① **툴바 전면 개편**: 아이콘 9종 **SVG 단일
> 파이프라인**(svg.rs 서브셋 파서[rect/circle/line/polyline/path M·L·H·V·C·A·
> Z/text·요소별 색/채움/굵기 오버라이드]·gdipctx 래스터·PNG 기구 폐지)·
> 패널 듀얼↔싱글/정보 패널 **토글 버튼**·컬럼 동기(SYNC)·**툴팁 i18n**·
> 도구 모음 26px·기본 순서 = refresh|panel|view|show|settings.
> ② **순서/표시 편집 공통 창**(ordereditor + NxOrderTree[ctl 15호]):
> 도구모음/파일 컬럼/컨텍스트 메뉴 각각 별도 창(prefs [편집…]·우클릭 팝업
> 경유) — 그룹/자식 이동 규칙·Shift 다중(형제 한정)·**드래그 이동**(고스트·
> 미리보기·자동 스크롤·ESC)·**키보드 내비**·체크 = 표시 여부(name 잠금)·
> 오버레이 스크롤. ③ **컬럼**: 헤더 **드래그 재배열**(고스트+미리보기+ESC)·
> **경계 더블클릭 auto-fit**(상한 설정)·표시/순서 세션 영속(`panel{i}.cols`)·
> colsync 연동(off = 독립·재켜짐 = 포커스 기준). ④ 컨텍스트 메뉴 항목
> 표시/순서 설정(`ctx_menu_order`). 실기 QA 전 항목·전체 green·clippy 0·
> exe 1.32MB·유휴 RSS 22.8MB(게이트 통과).
>
> **직전(07-18)** — ① **NxGrid(ctl 14호)**: 컬럼 리사이즈·체크 마크
> 열·**오버레이 스크롤바 세로/가로**(macOS — 얇은 썸 페이드↔드래그 시 일반 바,
> 픽셀 실측)·**행 선택 파일 목록 규약**(Shift/Ctrl/Ctrl+방향키+Space/Ctrl+A·
> 포커스 프레임·NXGR_SELCHANGE)·**GridOpts**(무헤더·지브라·외곽선·
> Mark::Check/⊖ — "셀 데이터화" 확장 계약) ② **user32 AV 진범 채록**(빈 셀
> 빈 Vec→DrawTextW — 런타임 이분) ③ **X-23 2차**: 미리보기 = NxGrid(Apply
> 행별 제외 토글+건수 연동) + **프리셋 v2 macOS 시안**([…⌄] 메뉴
> [프리셋들/─/Save/Edit]+건수 라벨 동일 행 · Save = 무캐션 **DWM 라운드
> 팝업**[기본 이름 전체 선택·우하단 취소/OK] · Edit = 지브라 목록 행별 빨간
> ⊖ **스테이징 삭제**[OK 확정/취소 폐기]) ④ NxMenuButton 구분선 ·
> NxTextBox EM_SETSEL 위임 · Style.danger ⑤ **하단 행 재편**(`feat/bulk-bottom-bar`):
> Apply → **"이름 변경(Rename)" NxButton Default**(NXBTN_SETENABLE 활성 —
> idle 회색/변경 시 파랑) · 우하단 [취소][이름 변경] 실측 정렬 · 프리셋
> [… ⌄]+건수 = 하단 좌측(구 Apply 자리) · 미리보기 그리드 전고 확장 ·
> **GridOpts.row_h**(파일 목록 20px @96dpi 동일 산식 — 행 높이 통일).
> ⑥ **Tab 내비·3단 체크·헤더 전체 토글**(`feat/bulk-tab-tristate`):
> IsDialogMessage+CONTROLPARENT+WANTARROWS(Tab 배치 순서)·NxCheckBox
> CheckMode 2단/3단(부분 = 흐릿한 ✓)·그리드 헤더 체크(전체 토글 NXGR_ROW_ALL).
> ⑦ **NxGrid 깜빡임 제거**(`fix/grid-flicker`): WM_PAINT 더블버퍼+erase 배제.
> ⑧ **NxGrid 다중열 정렬**(`feat/nxgrid-sort` — 원본 docs/23 §4 이식):
> 3상태 순환·Shift 다중키·▲▼ 앞/①② 뒤·비교 = 호스트(NXGR_SORT). 같은 묶음에서
> **ExtDot 이중 점 진범 픽스**·동작 **6종 확정**(이동/확장자 카드 제거)·
> NxSegmented 화살표. ⑨ **Segoe MDL2 글리프 통일**(`feat/mdl2-glyphs`·
> `feat/mdl2-disclosure`): 내비 ←→↑ = E72B/E72A/E74A·디스클로저 ▶▼ =
> E76C/E70D(9 DIP)·세그먼트 화살표 = E72A/E72B · **내비 활성 상태**
> (이전/다음 = 히스토리·상위 = This PC 비활성, 흐린 글리프 식별).
> 실기 왕복 QA 완료. 테스트 187 green·clippy 0.
> ⑩ **ctl 재사용 리팩터 + 전수 검토**(`refactor/ctl-base`): **base.rs**
> 신설로 14개 Nx 컨트롤 생명주기 보일러플레이트 통합(state/attach/drop_state/
> notify/register_class — 순 −107줄)·GDI 자원 **RAII화**(icon_font·bg_brush =
> Drop). **전수 검토**: GDI 누수 0·grid 더블버퍼 정상·**잠재 크래시 1건 수정**
> (segmented 빈 텍스트→DrawTextW 댕글링)·나눗셈/캐스트/인덱스/라우팅/내비 활성
> 안전. 후속(보류): 팝업 3종 골격 계층화(안전 우선).
> ⑪ **후반 묶음(07-18)**: 원본 앱 아이콘 4종 보관 + 리네임 **± raster 12종**
> 제작·교체(NxIconButton 이미지 모드 — gdipctx decode_png·`ImageFit`
> Native/Stretch) · **docs/ctl 판매 매뉴얼**(허브+overview Quick Start+13문서
> 함수/프로퍼티 레퍼런스) · **카드 스크롤 뷰포트**(NexaCardHost — 그리드
> 오버레이 바·하단 바 상시·kind별 높이 fitting·하단 5px) · 정렬 순번 ①②
> **상시 표시**(트리거는 Shift 전용 재확정) · **컬럼 넓이 동기화** 설정(보기
> 메뉴 4종 하단·기본 on·영속·off=패널 독립·탭=패널 상속·세션 `panel{i}.colw`
> [탭별 확장 예약]·DPI 리셋 이후 복원 픽스) · Dual/Single file panel(s) 라벨 ·
> **설정 트리 MDL2 셰브론 통일**. 실기 QA 각 수행. 테스트 188 green.
> 다음: X-23 잔여 β(카드 재배치) · X-13 2/2 · X-16 백로그.
>
> (이전 07-17) — ① **X-22 일괄 이름 변경 v2**(PF 6동작 패리티 —
> 코어 재작성[스코프·Mode·임의 위치·감싸기·**Date 엔진**·v1 프리셋 호환] +
> **ctl 3종 신설**[segmented/spin/droplist — 판매용 추상화] + UI 재구성[✓ 미리보기
> +건수]) ② **X-21 shell: 특수 폴더**(SHParseDisplayName 위임) ③ X-20 QA 2건
> (스플리터 음수 rect 정규화·0-rect 도크 히트) ④ **X-22 QA**: droplist **owner
> 승격 크래시**(팝업 USERDATA로 해소·fontbox 타이머 누수 동일 수리) + 메뉴 명령
> **설정 즉시 영속**(persist_settings — 비정상 종료 유실 방지).
> ⑤ **Nx 컨트롤 세트 완성**(X-23 토대 — macOS 시안, 사용자 QA 연속 반영):
> NxGroupCard(타이틀 자식 배치·통지 투과)·NxComboBox(✓ 팝업)·NxCheckBox·
> NxTextBox(포커스 accent 링)·NxIconButton(shape 투명·크기 = 체크박스)·
> **NxButton**(기본/Default[accent]/Disabled)·**NxSegmented**(SegOpts —
> 라운드/스퀘어·간격 기본 0·선택 accent 필)·**NxSpin**(독립 라운드 글상자·
> 숫자 우측 정렬·분리 버튼 블록·min/max 비활성). NxDropList 은퇴(콤보 통일).
> ⑥ **렌더 규약**: **AA = DrawCtx 백엔드만**(GdipCtx = 유일 GDI+ 접점 —
> 인박스·크레이트 0·D2D 교체 대비)·1비트 리전 클립 폐기 → `Style.behind`
> 블렌드·텍스트 = GDI/DW 유지·높이 규약(공통 자동 + 버튼/세그 컴팩트).
> 검증 갤러리 ctldemo(임시 🃏 — 카드 3종·**수평 배치 샘플 행**).
> 테스트 186 green·clippy 0·exe 1.20MB. 다음: X-23 카드 재편(사용자 주도
> 탐색) · X-13 2/2 · X-16 백로그.
>
> (이전 07-16) — 대규모 하루: 병합 브랜치 14개 + 등재 4건(X-15~X-20).
> ① **X-16 최적화 1차**(핫패스 무클론 `row_ref`·DW 캐시 무할당·아이콘 틱 rect 축소 —
> 백로그 7건 등재) ② **X-18 배포 2채널**(DR-3 개정 — 포터블+설치형 Inno Setup·
> data_dir 폴백·release.yml 2종 첨부, [21](21-distribution.md)) ③ **X-17 가상 최상위
> "내 PC"**(센티널 `::PC::` — 드라이브 간 이동·G-7 선택) ④ **X-19 보기 모드 3종**
> (타일[아이콘 32·내 PC 용량 바]/일반/트리 — **탭별 상태**·도구 모음 라디오 3버튼·
> 세션 영속) ⑤ **X-12 폰트 슬롯**(기본/콘솔/우클릭[저장만 — HMENU 규약]/상태바/
> 파일 목록+폴더 굵게/헤더 굵게·이탤릭 — DwBackend 스타일 맵·select_font·설정
> 이름+크기 1줄) ⑥ **ctl 커스텀 컨트롤 라이브러리 신설**: 1호 searchbox(내장 ✕·
> 세로 중앙 — z-순서 진범 구조 해소)·2호 **fontbox**(설치 글꼴 드롭다운[자기 글꼴
> 렌더]·키보드/마우스·콤마 선입력 시만 체인[아니면 교체]·Enter 확정[FBM_HAS_DROP
> 펌프 통과]·캐럿 맨 뒤) ⑦ QA 시리즈: 설정 그룹 페이지 위계·rebuild 재진입 공백
> 진범·크기 콤보+Enter 즉시 적용·토글 배경 단순화 등.
> ⑧ **X-20 싱글/듀얼 패널·정보 모드**(원본 FR-C1): 싱글 패널 = 우 패널 레이아웃만
> 숨김(**상태 보존·복귀 원복**)·싱글 정보 = 전폭 공유 도크(활성 패널 추종) —
> **선호값/효과 분리**로 "싱글 패널 = 싱글 정보 고정·듀얼 복귀 시 원복" 규칙.
> ⑨ fontbox QA 마무리(교체 규칙·Enter 확정 FBM_HAS_DROP·캐럿 맨 뒤).
> 테스트 179 green·clippy 0·B2 1.14MB. 실기 QA 대기(설치형은 다음 태그).
> 다음: X-13 2/2(런처 편집기 — 1/2 브랜치 보류) · X-16 백로그 · X-14 · X-15 · X-11.
>
> (이전 07-15) — **M5 마감 → `0.6.0` 릴리스 발행**(첫 태그 실행 검증 완료
> — Release 자동 생성·exe 첨부 확인) 후 **보완 배치 + 설정 창 개편 3연타**(사용자 QA):
> ① **M5-1 보완**(`feat/m5-ui-polish`) — 런처 **exe 셸 아이콘 16×16 정사각**+그룹
> 구분선(`launcherN=-`)·도구 모음 토글 시각(배경+accent)·**일괄 이름변경 순서형
> 파이프라인**(RenameOp 목록 — **정규식**[regex-lite 원장]·구간 이동·확장자 변경·블록
> ▲▼✕·**프리셋 저장/불러오기** `data\renames\*.cfg`)
> ② **설정 창 전면 개편**(`feat/prefs-tree` → `fix/prefs-tree-search` →
> `fix/prefs-tree-drill`) — 사이드바 = **계층 트리 단일 컴포넌트**(오너드로 LISTBOX·
> 일반/파일 목록/하단 패널)·**그룹 = 하위 메뉴 링크 페이지**(세부는 leaf 선택 시)·
> **검색 = 라벨/상세 매치 트리 필터 + 유지 탐색**(leaf=매치 상세만)·✕ 지우개 — 전부
> 자동화 실기 검증(WM 주입·컨트롤 덤프)
> ③ **원본 패리티 갭 문서화**([19-parity-gap](19-parity-gap.md) — G-1~14·초과 항목).
> **07-15 후반 배치 4건**: ④ **X-10 완결**(트리 매치 수 "(N)"·다중 토큰 AND·"N개
> 일치"·설명 문장·수정됨 accent 바·"카테고리: 항목" 접두 — 기본값 복원만 잔여)
> ⑤ **X-3 완결**(term_wrap·term_cols 80~1000+가로 스크롤) ⑥ **쉬운 갭**(G-7 위로 시
> 떠난 폴더 선택[+상단/중단/하단 배치 설정]·G-12 webp·G-13 폴더 우선 정렬 옵션·
> **대소문자 구분 정렬**·런처 **pwsh·cmd 시드 v2**[1회 마이그레이션]).
> **07-15 심야 배치**: ⑦ **설정 배치 2**(전송 완료 [닫기 (N)] 카운트다운 버튼·탭
> 더블클릭 동작[+**탭 고정** 신규]·타입어헤드 옵션 전면[범위·리셋·3×3 피커·체크 3종]
> +QA 5건[진행 창 중첩·공백 라우팅·파일 목록 분할+휠 스크롤·리셋 표시]) ⑧ **세션
> 디바운스 자동 저장**(탭 변경→1초 코얼레싱 flush — 설계 문서
> [20](20-session-coalescing.md)).
> 테스트 168 green·clippy 0·B2 **1.06MB**. 실기 QA 대기.
> 다음: X-11 패리티 갭 건별 · X-2 Starlark · X-12 폰트 슬롯 · X-13 런처 CRUD ·
> X-14 우클릭 메뉴 설정 · X-10 기본값 복원.

## 1. 확정된 결정 ([10](10-decision-record.md))

| # | 영역 | 결정 |
| --- | --- | --- |
| DR-1 | 스택 | **올 러스트 단일 바이너리** — Win32(windows-rs)+커스텀 드로잉 · ADR-0001 Accepted |
| DR-2 | 예산 | 유휴 RSS ≤30MB · exe ≤10MB · 임포트=OS 인박스만 — **병합 게이트** |
| DR-3 | 배포 | **개정(07-16)**: 포터블 단일 exe **기본** + 설치형 exe **보조** 2채널(`data\` 영속·쓰기 불가 시 LOCALAPPDATA 폴백 — [21](21-distribution.md)) |
| DR-4 | 코어 | 원본 nexa-core/vfs/tree **rlib 이식**(FFI 폐지) |
| DR-5 | UX | 원본 M1 기능 패리티·디자인 규약 계승 |
| DR-6 | 라이선스 | PolyForm NC + 의존성 퍼미시브 온리 |
| DR-7 | 플러그인 | .NET SDK 비이관 — 내장 미리보기 대체 |
| DR-8 | 외부 crate | 기본 0 지향, 건별 원장 기록(`windows` 승인) |

## 2. 예산 실측 현황 (DR-2)

| 항목 | 예산 | 최신 실측 | 시점 |
| --- | --- | --- | --- |
| B1 유휴 RSS | ≤30MB | **16.86MB**(중앙값, 3회 18/16.86/4.12 — **M5 마감 실측**: 10k·도크 정보 뷰·런처 바·유휴 300s. 활성 ~36→트림 직후 2.6MB 후 재상승 편차 큼 — 최저 4.12는 M4 수준, 재상승 원인 관찰은 β arena 회수와 공동 과제). M4 터미널 상주 실측 5.07MB(07-14) | 07-15 실기 |
| B2 exe 크기 | ≤10MB | **1.45MB** (X-28 멀티라인 탭·X-29 도크 선택·About 창 포함 — 0.8.1 1.43MB 대비 +0.02) | 07-20 실기 |
| P1 100k 첫 렌더 | <150ms | **115ms**(중앙값, 열거 포함 — 10k는 42ms) | 07-12 실기 |
| P2 스크롤 | 60fps(<16.7ms) | **2.1ms/프레임**(100k·200프레임 벤치) | 07-12 실기 |
| B3 임포트 DLL | OS 인박스만 | **통과** — 기존 + dwrite·combase·ole32·bcryptprimitives·shell32 (`scripts/budget-b3.ps1` 단일 출처) | 07-13 실기 |

## 3. 마일스톤 (상세 [MILESTONES](MILESTONES.md))

- **M0** 기반·게이트 ✅ (`0.1.0`) — 설계 문서·스캐폴딩·코어 3크레이트 이식·Win32 창·렌더 스파이크·CI·게이트 실측.
- **M1** 뷰어(★플래그십) ✅ (`0.2.0`) — 전 항목 완료 + 게이트 통과(100k 115ms·60fps·RSS).
- **M2** 셸 골격 ✅ (`0.3.0`) — 경로 바·듀얼/탭·크롬·테마·설정/세션·i18n·IME/UIA 1차·상주 규율(게이트: 듀얼·탭4 26.9MB ≤30).
- **M3** 파일 조작 ✅ (`0.4.0`) — 전송·삭제/이름변경/새로 만들기·Undo/Redo(휴지통 복원)·셸 컨텍스트 메뉴·OS 클립보드/OLE DnD·watcher + 탐색기 클릭/편집 시맨틱(게이트: 10k 유휴 300s **6.29MB** ≤30).
- **M4** 하단 패널 ✅ (`0.5.0`) — 도크·정보 뷰·미리보기·ConPTY 터미널(+상호작용 QA 시리즈)·프리즈 2건 근본 해소(게이트: 10k+터미널 상주 유휴 300s **5.07MB** ≤30).
- **M5** 마감 ✅ (`0.6.0` 발행) — M5-1(퀵 런처 바·일괄 이름변경 α+파이프라인 확장) · M5-2(릴리스 파이프라인 — 첫 태그 실행 검증 완료) · M5-3(UIA/IME 마감·서명=무서명 확정). 실기 QA 잔여.

## 4. 개발 모델 ([11](11-dev-environment.md))

- 맥 = 일상 개발(코어 test + **windows 타깃 cargo check로 UI 코드까지 타입 검증**) · Windows PC/CI = 실행·QA·예산 실측.

## 5. 다음 단계

1. ~~M0~~ ✅ `0.1.0` · ~~M1~~ ✅ `0.2.0` · ~~M2~~ ✅ `0.3.0` · ~~M3~~ ✅ `0.4.0` · ~~M4~~ ✅ `0.5.0`.
2. ~~M5 마감~~ ✅(07-15) — ~~`0.6.0` 태그·M5-2 첫 실행 검증~~ ✅(Release 발행·exe 첨부 확인). 실기 QA 잔여.
3. 백로그: **X-11 원본 패리티 갭 건별**([19](19-parity-gap.md) — G-7·12·13 해소) · X-2 Starlark 플러그인 · ~~X-3~~ ✅(07-15) · ~~X-10~~ ✅(07-15 — 기본값 복원만 잔여) · X-1 Apps 키 QA.
