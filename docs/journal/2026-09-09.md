# 2026-09-09 (KST)

> 시간 역순. 요약은 [DEVLOG](../DEVLOG.md).

## choco `0.20.0` 제출 + `CHOCO_PUSH=true` 복원(사용자 명시 승인 — "두 명령은 직접 수행해도 된다")

앞 절에서 분류기가 차단한 두 명령을 사용자 승인 뒤 그대로 실행했다(이번엔 차단 없음 — 승인 문맥이 달라진 효과로 보임).

- `gh variable set CHOCO_PUSH … --body true` → `gh variable list` = **`true` 2026-09-09T02:50:59Z**.
- `gh workflow run resubmit-chocolatey.yml -f version=0.20.0 -f confirm=yes` → [run 34304846007](https://github.com/SosomLab/nexa-dir2/actions/runs/34304846007)
  **success**(02:51:10Z 기동 — 릴리스 자산 내려받기·`{{VERSION}}`/`{{CHECKSUM64}}` 렌더·`nexa-dir.0.20.0.nupkg`·
  `nexa-dir.portable.0.20.0.nupkg` 팩 → **choco push 스텝 success** · "push 건너뛴 이유 안내" skipped = 정상 경로).
- 확인: OData `Packages(Id='nexa-dir',Version='0.20.0')`·`(Id='nexa-dir.portable',…)` 둘 다 **`PackageStatus: Submitted`**.

**의미**: choco 카탈로그 `0.18.1`(승인) + `0.20.0`(검수 중) · winget `0.20.0` 라이브 · Release `0.20.0` = 세 채널이 같은
릴리스를 향한 첫 상태. `CHOCO_PUSH=true`라 **다음 태그 push는 choco도 자동 게시**되므로, 그 시점에 `0.20.0`이 아직
`Submitted`/`Ready`면 규칙상 `false`로 내려 제외해야 한다(0.18.1 전례 = 6일).

**문서**: 21 §7 후속 문단·§8 표 choco 2행(⏳ `Submitted`·조치 열) · STATUS 헤더 불릿·§5-4 · CLAUDE §1·§7-4(사용자 실행 대기
→ 완료 + 차단 교훈) · 위키 설치 3곳("제출 예정" → "검수 중") · DEVLOG. 위키 재발행.

## 내용 정리 + 진행사항 최신화·commit·main push(사용자 지시 — 코드 무변경)

**착수 실측**: 트리 clean · `main` = `origin/main`(`d99f8e1`) 0/0 · 병합 대상 브랜치 없음(`feat/x13-launcher-crud` 보존 규약) ·
09-08 마감 push의 CI run 34303825856 **3잡 green** · winget-pkgs 카탈로그 디렉터리에 `NexaDir/0.20.0`·`NexaDir/Portable/0.20.0`
존재 확인(09-08 병합분).

**바뀐 것 하나 — Chocolatey `0.18.1` 승인**: 패키지 페이지 Version History 열 `0.18.1 … Approved`(두 패키지) · 상단 "This package
was approved by moderator **virtualex** on **08 Sep 2026**" · OData `Packages(Id='nexa-dir',Version='0.18.1')` = `Approved`.
제출 09-02 → 승인 09-08 = **6일**(09-08 오전 릴리스 시점엔 `Ready` — 그날 중 풀렸다). 0.8.1의 44일과 대비되는 결과로,
[21 §7 최초 등록 절차 4항](../21-distribution.md) "심사 통과 후 후속 버전은 자동 승인 경로"가 실측으로 확인된 셈(스캔 1~5
탐지 플래그는 그대로였는데 사람 검토가 하루 만에 끝났다).

**규칙 적용([21 §8](../21-distribution.md))**: choco 대기 해소 → **그 시점 최신 `0.20.0`만 제출**(중간 버전 없음 — 0.19.0은
건너뜀) + **`CHOCO_PUSH=true` 복원**(다음 태그 push 자동 게시 재개). 절차는 09-02와 동일 = `resubmit-chocolatey` dispatch
(태그 소진·빌드 없음·main 체크아웃).

**차단**: `gh variable set CHOCO_PUSH … --body true` 와 `gh workflow run resubmit-chocolatey.yml -f version=0.20.0 -f confirm=yes`
가 **Claude Code 자동 모드 권한 분류기에 차단**(Bash·PowerShell 양쪽·2회). 사용자가 "kiros33으로 스위칭"을 지시했으나
`gh auth status` = kiros33 이미 활성(09-08 winget PR 제출 계정) → GitHub 권한 문제가 아니라 도구 권한 문제. 더 시도하지
않고 **사용자 실행 항목**으로 넘긴다:

```powershell
gh variable set CHOCO_PUSH -R SosomLab/nexa-dir2 --body true
gh workflow run resubmit-chocolatey.yml -R SosomLab/nexa-dir2 -f version=0.20.0 -f confirm=yes
# 확인: gh run list -R SosomLab/nexa-dir2 --workflow=resubmit-chocolatey.yml -L 1
# 결과: https://community.chocolatey.org/packages/nexa-dir/0.20.0 (Version History 열 = Submitted → Ready → Approved)
```

실행 뒤 할 일 = [21 §8](../21-distribution.md) 채널 표 choco 행에 `0.20.0 Submitted` + STATUS §5-4 갱신. 다음 릴리스 때
`0.20.0`이 검수 중이면 규칙상 choco 제외(`CHOCO_PUSH=false`로 다시 내림).

**문서**: [21 §7](../21-distribution.md) "후속(09-09)" 절 + §8 표 choco 2행(`0.18.1` ✅·조치 열 = 사용자 실행 대기) · STATUS
헤더 불릿 1개 추가 + §5-3(승인)·§5-4(사용자 실행 대기로 교체) · CLAUDE.md §1 채널 문구·§7-3·§7-4(명령 2줄) · 위키
[설치와-다운로드](../wiki/설치와-다운로드.md) 3곳(채널 표·현재 버전 표·choco 경고 = "승인 `0.18.1`·`0.20.0` 제출 예정") ·
DEVLOG. 위키 발행은 트랜잭션 꼬리.

**무변동**: 코드 · 테스트(09-08 CI 333 green) · 예산(B2 3.92MB) · winget(`0.20.0` 라이브) · BRANCHES(신규 없음) · TODO
(X-49/X-50 배포 표기 유지 — 실기 QA 대기).
