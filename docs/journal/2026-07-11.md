# 2026-07-11 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST)** — 실제 착수는 첫 커밋 이전.

## M0-8 게이트 실측 · `0.1.0` (main)

> **M0 완료 게이트** — Windows 실기(Windows 11 Pro 26200, x64)에서 DR-2 예산 3종 실측. docs/18 §5 절차.

- **실측 결과(전 게이트 통과)**:
  - **B1 유휴 RSS**: 릴리스 exe 기동(100k 합성 행 창) → 유휴 약 95초 → `Get-Process` WorkingSet64 10초 간격 3회 = 13.24/13.22/13.22MB → **중앙값 13.22MB ≤ 30MB** (Private Bytes 1.70MB — 대부분 공유 페이지).
  - **B2 exe 크기**: `target/release/nexa-app.exe` = 214,528 bytes = **0.20MB ≤ 10MB** (정적 CRT·lto fat·strip).
  - **B3 임포트**: dumpbin /imports = user32·kernel32·gdi32·ntdll·oleaut32·api-ms-win-core-synch-l1-2-0 — **전부 OS 인박스**.
  - Windows 실기 테스트: workspace 26 green(벤치 1 ignored) — 맥 check와 동일 결과. 렌더 스파이크 창 실행 확인(M0-7 실기 검증 겸).
- **CI B3 게이트 강화**: 목록 가시화 → **화이트리스트 fail 게이트**(kernel32·user32·gdi32·ntdll·oleaut32 + `api-ms-win-*` 외 발견 시 throw). dumpbin 탐색을 `Program Files*`(x86 포함)로 확장 — 로컬 BuildTools가 x86 경로였던 실측 반영.
- M0 전 항목 완료 → **`0.1.0` 태그**. 다음: M1-1(`nexa-gui` 분리)·M1-2(ADR-0002 렌더링 비교).

## M0-7 GDI 렌더 스파이크 (`feat/m0-render-spike`)

> 렌더 모델(docs/01 §3) 실증: 창 1개·더블 버퍼·**가시 행만** 그리기 — M1 가상 리스트(M1-3)의 전제 검증.

- **M0-7 렌더 스파이크**(`90a9243`): `win.rs` 확장 —
  - **더블 버퍼**: 메모리 DC+비트맵을 창 상태에 캐시, 클라이언트 크기 불일치 시에만 재생성. `WM_ERASEBKGND` → 1(깜빡임 제거), 화면 반영은 `BitBlt` 1회.
  - **가시 행만**: 합성 `TOTAL_ROWS=100_000` 중 `scroll_row`부터 `클라이언트 높이/행 높이`개만 `ExtTextOutW(ETO_OPAQUE)` — 행 배경(교대 음영)+텍스트를 호출 1회로. 프레임 비용은 창 높이에만 비례(100k 체급은 M1-9 게이트와 동일).
  - **스크롤**: 휠(`WHEEL_DELTA` 잔여 누적 — 트랙패드 분수 노치 대응·3행/노치) + 키보드 ↑↓·PgUp/PgDn·Home/End(키보드 우선 DR-5). 클램프 `[0, TOTAL-가시행]`.
  - **DPI**: `WM_DPICHANGED`에서 폰트(Segoe UI 9pt)·행 높이(20px@96dpi) 재계산 + 권고 rect `SetWindowPos`.
  - **수명**: 창 상태(Box)는 `GWLP_USERDATA` — `WM_NCCREATE` 생성, `WM_NCDESTROY`에서 GDI(폰트·브러시·백버퍼) 해제 후 drop. 다크 팔레트는 M2 테마 전까지 하드코딩.
  - windows feature `Win32_UI_Input_KeyboardAndMouse` 추가(VK_*). **검증(맥)**: windows 타깃 `cargo check`·`clippy` green(경고 0) · `cargo test` 26 green · fmt. 실행 확인은 M0-8(Windows 실기)과 병행.
- **권한 병합**(`8c5f7f8`): `Bash(git -C:*)`가 인자 매칭 안 됨 → `Bash(git -C *)` 추가(병합 편집). 사용자 재강조: settings.json **덮어쓰기 절대 금지** — 영구 메모리에도 기록.

## M0 스캐폴딩·코어 이식·Win32 스켈레톤 (`feat/m0-scaffold`)

> 첫 개발 브랜치. TODO M0-1~6 순차, 단위=커밋 1개.

- **M0-1 워크스페이스**(`391e4bb`): 루트 Cargo.toml(resolver 2·workspace.package 0.1.0)·rust-toolchain(stable)·릴리스 프로파일(opt3·**lto fat·panic abort·strip**)·`.cargo/config.toml`(**+crt-static** x64/arm64) · nexa-app 스텁.
- **M0-2~4 코어 이식**(`38f92ff`·`585b2dc`·`370b56f`): 원본 `core/crates/{nexa-core,nexa-vfs,nexa-tree}`를 **그대로 rlib 이식**(cdylib/nexa-interop 미이식 — FFI/ABI v7 계층 소멸, DR-4). `cargo test` — tree 21 green+10만 노드 벤치 1 ignored(원본 동일).
- **M0-5 Win32 창 스켈레톤**(`34b5649`): `windows` crate 0.62.2(cfg(windows) 타깃 의존) — RegisterClassW·CreateWindowExW·GetMessageW 루프·WM_PAINT(FillRect+DrawTextW)·`SetProcessDpiAwarenessContext(PerMonitorV2)`. 비-Windows는 스텁 main. **검증(맥)**: `cargo check --target x86_64-pc-windows-msvc` green — 유일 수정은 `COLOR_WINDOW` 위치(WindowsAndMessaging→**Gdi**). 릴리스 빌드는 `windows_subsystem = "windows"`.
- **M0-6 CI**(`3b2ddf8`): core(ubuntu·macos `cargo test`) + windows(test·release 빌드·**예산 B2 게이트**[exe>10MB fail]·임포트 DLL 가시화·아티팩트). 러너 검증은 push 후.
- **권한 사고 복구 ×2**(`7282ad4`·`cb8e1db` 외): IDE "Allow always"가 세션 시작 스냅샷으로 settings.json 전체 덮어씀(사용자 확인) → git 커밋본과 세션 추가분 병합 복원 + rust 전 명령(`cargo:*`·`rustup:*`·rustfmt·rustdoc)·python(python·pip·pytest·uv) 자동 허용. **재발 방지**: 광범위 규칙으로 팝업 자체 제거, 파일은 병합만, 덮이면 `git restore`.

## 설계 문서 세트 (`docs/foundation`)

> 사용자 요청: "문서 작성 기준·형태는 nexa-dir을 그대로 차용, 개발 방향 설계 + 마일스톤 + 순차 TODO, 이후 첫 개발 착수. 큰 단위=브랜치, 세부 기능=커밋, push는 명시 요청 시."

- **원본 분석**(대화 선행): 원본은 Rust 코어(cdylib, ABI v7) + WinUI 3(C#/.NET8). 실측 한계 — 유휴 RSS 60MB+(CLR+WinUI 하한), self-contained ≈64MB, 단일 exe는 XAML/PRI 결함(BUG-010)으로 고위험. 무게중심이 C#(FileOps·셸·터미널·설정)에 있음을 확인.
- **방향 확정**: unmanaged **올 러스트 + Win32(windows-rs) + 커스텀 드로잉**(GDI→DirectWrite interop). 코어 3크레이트는 rlib 직접 링크(FFI 폐지). 후보 비교(Slint=라이선스 위반·egui=예산/IME·C++=이점 없음·Delphi=자산 폐기)는 [06](../06-adr-0001-stack.md).
- **예산 게이트 신설(DR-2)**: B1 RSS ≤30MB(목표 20MB급)·B2 exe ≤10MB·B3 임포트=OS 인박스만 — 마일스톤 병합 게이트. 원본의 "사후 최적화로 60MB 도달" 교훈의 역전.
- **문서 산출**: 00 비전(경쟁 분석 포터블 관점 — 사용자 요청으로 One Commander 추가)·05 요구(FR 패리티 맵·NFR 예산)·01 아키텍처(크레이트 8개 계획·렌더링/스레딩/영속)·06 ADR-0001(Accepted)·10 DR-1~8+crate 원장·11 환경(맥 check 모델)·12 단일 exe 패키징·15 방법론(원본 계승+예산 규율)·18 빌드 SSOT(예산 측정 절차)·02 로드맵 M0~M5·MILESTONES·TODO(M0-1~M5-3 순차 백로그)·README(문서 홈)·STATUS·DEVLOG·BRANCHES·CLAUDE.md.
- **커밋**: `8f528de`(00·05) → `079fa34`(01·06) → `47597e4`(10·11·12·15·18) → `506fa4e`(02·MILESTONES·TODO) → 운영 문서(이 커밋).

## 저장소 초기화 · 권한 · 최초 push

- main 루트 커밋 `dffc8f9`: .gitignore·LICENSE(PolyForm NC, 원본 계승)·`.claude/settings.json`(dev 명령 자동 허용 — 원본 규약 차용, 파괴적 명령은 ask).
- 권한 파일이 세션 자동 기록으로 덮여 사용자 추가분과 충돌 → 와일드카드 목록으로 병합 정리(`43a0989`). **규율: 이 파일은 이후 덮어쓰기 금지, 병합(추가)만.**
- 사용자 요청으로 원격 `SosomLab/nexa-dir2`에 main·docs/foundation 최초 push. 이후 push는 명시 요청 시에만.
