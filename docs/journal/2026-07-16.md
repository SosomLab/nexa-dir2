# 2026-07-16 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 최적화 패스 X-16 (`refactor/optimization-pass`)

> **사용자 요청**: 최적화 브랜치 — 전체 코드 점검으로 효율 개선 + 모듈 구성·라이브러리
> 도출 대상 추출. 병행 요청: 오프라인 라이선스 인증 설계 TODO 등록(X-15),
> 가상 최상위 "내 PC"(X-17), 포터블/설치형 배포 구성(별도 브랜치 후속).

- **점검 방법**: 병렬 분석 3축 — ① win.rs(4,500줄) 구조·중복 ② GUI/트리 핫패스 할당
  ③ 크레이트 경계·라이브러리 후보. 결론: 크레이트 의존 그래프는 깨끗(순환 없음·Win32는
  nexa-app 한정·외부 crate는 regex-lite 1개), 죽은 코드 없음, 정렬 비교는 이미 무할당.
  핵심 병목은 **렌더 핫패스의 셀별 힙 할당**과 **전창 무효화 증폭**.
- **수행(커밋 5건, 전부 테스트 green)**:
  1. `6f2c247` redundant clone 8건 제거(clippy::redundant_clone — panel/win).
  2. `964321a` `[workspace.lints]` 신설(전 크레이트 상속 — 린트 기준 일원화) +
     nexa-gui widgets 파사드 정리(ScrollAlign 재수출·심층 경로 임포트 제거).
  3. `c49cffb` **셀/아이콘 렌더 핫패스**: `Tree::row_ref`(이름 빌림 경량 뷰) 신설 —
     `TreeSource::cell`(4열)·`icon`의 통 VisibleRow 클론(행당 힙 String ~5회 낭비,
     가시 40행×2패널 ≈ 프레임당 수백 할당) 제거. 회귀 테스트 추가.
  4. `88d9c73` **DirectWrite 레이아웃 캐시**: 튜플 키 `(String,i32)`가 히트 시에도
     `to_owned()` 할당 → **중첩 맵(폭→텍스트)**으로 `&str` 무할당 조회. 아이콘 포맷
     캐시는 제어문자 접두사 `format!` 대신 **음수 폭 네임스페이스**로 분리.
  5. `529b3ac`+`dc254fa` **전창 무효화 축소**: 셸 아이콘 틱(80ms)/도착 통지가
     `InvalidateRect(None)`로 초기 로딩 중 초당 ~12회 전창 재도장 유발 → 파일 목록
     본문 2개+런처 바 rect만 무효화. `mouse_xy` 헬퍼(7개 핸들러 중복 제거),
     apply_prefs의 버려지는 Invalidations 4건 → 공유 수집기.
- **도출·잔여(X-16 백로그 — TODO 기록)**: 대형 구조 개편은 별도 슬라이스로.
  ① **모달 공통화 modal.rs**(클래스 등록·메시지 펌프·center_over가 dialog/prefs/
  bulkrename에 3중 복제 — ~150줄) ② **설정 매핑 단일화**(Settings↔PrefValues↔State
  필드 목록이 4곳 삼중사본 — 새 설정마다 4파일 수정 위험) ③ **termdock.rs/theme.rs/
  fileops.rs 추출**(win.rs 분할 — term_paint too_many_arguments가 신호) ④ **플랫폼
  중립 모듈 lib 승격**(panel/config/source/i18n/pathinput/nav ~2,400줄 — bin에 갇혀
  재사용·통합 테스트 불가, bulk_rename식 UI/로직 분리 모델) ⑤ launcher.rs 시드 로직
  분리(테스트 가능화) ⑥ `index_of_path` O(E×V) 개선 ⑦ paint의 rcPaint 존중.
- 170 green·clippy 0.

## X-13 런처 CRUD 1/2 (`feat/x13-launcher-crud` — 병합 보류)

> "다음 진행"으로 착수 — 도중 최적화 요청으로 브랜치 보존 후 전환.

- `0888677` LauncherItem.icon 필드 + **`launcherN.icon` 별도 키 직렬화**(4필드 인라인
  대신 — 인자 안 `|` 마지막 필드 보존 규약과 충돌 회피)·구 3필드 하위호환·
  build_launcherbar 아이콘 소스 오버라이드. 2/2 = 설정 창 편집기(목록·추가/구분선/
  삭제/▲▼/변경) 잔여.

## 할 일 등록 (사용자 요청)

- **X-15 오프라인 라이선스 인증**: 서명된 `license.key`(ECDSA P-256) + 검증은
  **Windows CNG(bcrypt.dll 인박스)** = 외부 crate 0·B3 유지, 공개키만 임베드(개인키
  유출 원천 차단), 발급 = 오프라인 CLI 서명 도구(`nexa-lic` — 배포 제외). 착수 시 ADR.
- **X-17 가상 최상위 "내 PC"**: C:\에서 Alt+↑ → 드라이브 목록 뷰(볼륨명·용량) →
  드라이브 간 이동. 착수 전 원본 드라이브 뷰 구현 확인.
