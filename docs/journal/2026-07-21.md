# 2026-07-21 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 휴지통 삭제 지연 — 원인 판단 + 낙관적 숨김 (사용자 QA 연속)

- **사용자 판단 확인**: "휴지통 관련 기능 호출이 오래 걸리는 게 근본 원인" —
  **동의**. 근거: ① Shift+Del(완전 삭제 = `std::fs`)는 즉시 처리(사용자
  실측) ② 워커 이관 후에도 파일이 실제로 사라지기까지 수 초 = API 자체
  소요 ③ `SHFileOperationW`(휴지통 이동)는 파일 크기와 무관한 셸 고정
  비용 — $Recycle.Bin `$I`(메타)/`$R`(본체) 생성·볼륨별 휴지통 설정/용량
  확인·SHChangeNotify 브로드캐스트·셸 확장/AV 후킹. 환경 요인(휴지통에
  항목 다수·OneDrive 폴더·실시간 검사)이 크면 3~4초도 정상 범위.
- **우리 코드로 API를 빠르게 만들 수는 없음**(대안 IFileOperation도 동일
  셸 경로) → **탐색기 동일 UX = 낙관적 숨김** 채택: DEL 즉시 대상 행을
  화면에서 제거하고 워커가 뒤에서 휴지통 이동을 마친다.
  - [nexa-tree `remove_paths`](../../crates/nexa-tree/src/lib.rs) 신설 —
    경로(+펼친 하위) 노드를 roots/visible/children/선택에서 제외(arena
    유지 = 잔여 id 안정·FS 무변). 테스트 1건(파일/폴더 하위/무매치).
  - [panel `hide_paths`](../../crates/nexa-app/src/panel.rs) → do_delete가
    양 패널에 즉시 적용. **실패 시 완료 재로드가 원복**(정직 복구).
  - 워커에 COM STA 초기화 추가(셸 확장 대비 — SHFileOperation 권고).
- 진단 안내(환경 측): 휴지통 비우기 후 재시도·대상 폴더가 OneDrive 동기화
  경로인지·셸 확장(TortoiseGit 등)/실시간 AV 개입 여부 확인.

## X-30 QA 후속 3건 (사용자 — `feat/transfer-progress-ux` 연속)

- **DEL 휴지통 삭제 3~4초 UI 멈춤**(사용자 QA — 소파일 4개도 재현): 원인 =
  [`do_delete`](../../crates/nexa-app/src/win.rs)가 `SHFileOperationW`(휴지통
  이동)를 **UI 스레드에서 동기 호출** — 셸 네임스페이스·휴지통 메타데이터
  갱신 비용으로 파일 크기와 무관하게 수 초 소요. 수정 = **워커 스레드
  이관**(전송과 동일 규약): `pending_delete`(동시 1잡 가드·대상 보관) → 워커
  `SHFileOperationW` → `WM_APP_DELETE`(0x800D) 완료 통지에서 undo 기록·결과
  노트·재로드. 진행 중 제목줄 `del.progress`("삭제 중..."). 완전 삭제
  (Shift+Del)는 std fs 즉시라 동기 유지. 히스토리 undo/redo 경로의 동기
  호출(`recycle_delete_one` 등)은 빈도가 낮아 유지(후속 검토).
- **세그먼트 4파일이 3구간으로 보임**(사용자 QA): 작은 파일 구간이 크기
  비례 반올림으로 1px 미만이 되어 소멸(경계선 2개만 표시). 수정 = **전
  항목 최소 3px 보장** — 부족분은 가장 넓은 구간에서 1px씩 차감(공간 부족
  시 생략·0바이트 항목도 자기 구간 확보).
- **파일별 진행색 순환 팔레트**(사용자 요청): 완료/진행 채움색을 항목
  인덱스 % 5 순환 — 파랑(기존 accent)·초록·주황·보라·분홍(`SEG_PALETTE`,
  BGR). 건너뜀 회색·실패 적색은 상태색 유지. 인접 구간이 항상 다른 색이라
  경계 식별이 쉬워짐.

## X-30 전송 진행 UX 개편 (사용자 요청 5건 — `feat/transfer-progress-ux`)

사용자 요청: ① 진행 창 시간을 설정 화면에서 직접 변경(+0=창 미표시) ② "1 B"
용량 오표기 확인 ③ 전송 미완료 대상 실행/이동 제한 ④ 용량 단위 적응 +
진행/전체·파일 수 표시 ⑤ 전체 바를 파일별 비율로 구분해 어느 파일이
완료/진행 중인지 보이게.

- **"1 B / 1 B" 오표기 원인**([dialog.rs](../../crates/nexa-app/src/dialog.rs)
  `set_done`): 완료 표시가 바 100%를 만들려고 `done=1/total=1`로 **실제
  바이트를 덮어써** 완료 순간 "1 B / 1 B (100%)"가 됨(진행 중에는 정상 —
  사용자 스크린샷 재현 일치). 수정 = 완료 시 실제 값 유지(취소·실패 시 부분
  진행도 정직 표기). 0바이트 전송(빈 파일)은 완료 단계에서만 100% 간주.
- **nexa-ops 이벤트 프로토콜**([lib.rs](../../crates/nexa-ops/src/lib.rs)):
  `on_progress(Progress, &Path)` → **`on_event(Event)`** 확장 — `Plan{sizes,
  total_bytes}`(시작 전 1회 — 항목별 크기·총합 선통지) · `ItemStart{index,
  dest}`(실제 대상 경로 — 잠금용) · `Bytes(Progress)` · `ItemEnd{index,
  status(Done/Skipped/Failed)}`. 테스트 2건 추가(계획/시작/종결 검증·항목별
  건너뜀 보고) — 총 35 green.
- **진행 창 개편**(dialog.rs): ① 용량 표기 **B/KB/MB/GB/TB 적응**(`fmt_bytes`
  TB 추가) ② 정보 줄(바 바로 위) = `진행 / 전체 (pct%) · 파일 {cur}/{count}`
  (단일/다중 모두 파일 수 표시 — `ops.fileCount`) ③ **세그먼트 바**
  (`paint_segments`): 항목별 크기 비례 구간을 누적 경계로 분할(오차 비누적),
  상태색 = 완료 accent·진행 accent 부분 채움·건너뜀 회색·실패 적색·대기 배경
  + 구간 경계선(3px 미만 생략). 계획 미도착/512항목 초과 = 단색 폴백.
- **설정 `transfer_close_ms`**(0~10000ms, 기본 2000 = 기존 하드코딩 2초
  승격 — **단위 초→ms 개정, 사용자 요청 07-21 2차**): 완료 후 진행 창 닫기
  대기. 닫힘 시점 = 정확히 ms(카운트다운 첫 틱 = ms 나머지 간격, 이후 1초
  주기 재무장 — 버튼 [닫기 (N)] 표시는 올림 초). **0 = 진행 창 자체를
  표시하지 않음**(전송은 수행·제목줄 % 유지). 구 키 `transfer_close_secs`는
  ×1000 이행 파싱. [config.rs](../../crates/nexa-app/src/config.rs)
  직렬화/파싱/왕복 테스트 + [prefs.rs](../../crates/nexa-app/src/prefs.rs)
  신설 카테고리 **파일 목록 > 파일 전송**(Number 행) + 즉시 적용 배선.
- **전송 중 대상 잠금**([win.rs](../../crates/nexa-app/src/win.rs)):
  `TransferShared.in_flight`(현재 쓰는 대상 경로 — ItemStart 기록/ItemEnd
  해제) 기준 `transfer_blocks`(`is_same_or_sub` — 폴더 재귀 포함). ①
  더블클릭/Enter/Alt+↓ 실행 = `guarded_shell_open` 게이트(차단 시 경고 비프
  + 제목줄 `ops.itemBusy`) ② OLE 드래그 발신 대상에서 제외. 앱 내 재전송은
  기존 동시 1잡 규칙이 이미 차단. `Win32_System_Diagnostics_Debug` 피처
  추가(MessageBeep — user32 인박스, B3 무영향).
- 워커 배선: `TransferShared.items`(Mutex<Vec<SegItem>>)에 Plan/Bytes/End
  반영(항목별 done = 전체 누적 − 항목 시작 시점 기준), ItemStart는 상태만
  기록(게시 생략 — 다항목 소파일 메시지 절감). i18n ko/en 5키 추가.
- 검증: 전 워크스페이스 테스트 green(nexa-ops 35 포함)·clippy 0·fmt. 실기
  QA(대용량 복사·다항목 세그먼트·0초 설정·잠금 동작)는 로컬 릴리즈 빌드로
  진행 예정. 병합 전 — 브랜치 유지(사용자 QA 후 병합).

## 릴리스 배포 보류 방침 확정 (사용자)

- **Chocolatey 모더레이션 승인 전까지 새 버전 태그 부여·GitHub Release 배포를
  하지 않는다**(사용자 방침). 현재 `nexa-dir`/`nexa-dir.portable` 0.8.1이
  모더레이션 대기 중 — 대기 중 새 버전을 push하면 두 버전이 각자 큐에 쌓여
  심사만 복잡해지는 것을 회피(07-20 검토 결론과 일치: 첫 승인 후 후속 버전은
  자동 검사 통과 시 심사가 빨라짐).
- 로컬 `cargo build --release`는 실기 QA용으로 계속 사용(태그/배포와 무관).
  07-20 구현분(X-28·X-29·X-27·X-25 1차·X-26 ③)의 실기 QA는 로컬 빌드
  (exe 1.45MB)로 진행, 릴리스 반영은 choco 승인 후 다음 버전 태그에서.
- winget PR 2건(모더레이터 검토)·Chocolatey 2건(모더레이션) 모두 외부 큐
  대기 — 우리 측 추가 조치 없음(07-19 검토 유지).
