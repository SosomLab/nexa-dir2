# 2026-07-21 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 배포 채널 등록 상태 실측 점검 (사용자 요청) — Chocolatey 자동 검사 3종 통과

저장소 최신화(`e7e9f20`) 후 winget·Chocolatey 등록 진행 상태를 **실제 조회**로 점검.

### Chocolatey — 진전 있음 (Pending → 검사 완료·모더레이터 대기)

`nexa-dir`·`nexa-dir.portable` 0.8.1 양쪽 동일:

| 검사 | 07-19 기록 | 07-21 실측 |
| --- | --- | --- |
| Validation Testing | Pending | **Passing** |
| Verification Testing | Pending | **Passing** |
| Package Scan | Pending | **Flagged Note** |
| 종합 | 모더레이션 대기 | `awaiting moderation` |

- 07-19 문서의 "자동 검사 3종 Pending"은 **현재 실제와 불일치** — 3종 모두
  완료됐다. 본 기록으로 정정.
- Package Scan `Flagged Note`는 **실패가 아니라 참고성 플래그**(사람 검토 시
  참고 항목) — 재제출·수정 불요.
- 참고: 미승인 패키지는 community feed OData(`/api/v2/Packages()`)에 노출되지
  않아 조회 시 빈 결과 — 상태 확인은 패키지 페이지(`/packages/<id>/<ver>`)로.

### winget — 변동 없음 (07-19 14:07 이후 정지)

- [#404528](https://github.com/microsoft/winget-pkgs/pull/404528) `SosomLab.NexaDir` 0.8.1 ·
  [#404533](https://github.com/microsoft/winget-pkgs/pull/404533) `.Portable` 0.8.1 — 둘 다 `OPEN`.
- 라벨(양쪽 동일): `Azure-Pipeline-Passed`·`Policy-Test-1.2`·`New-Package`·
  `Validation-Guide`. CLA 체크 SUCCESS.
- 07-19 시도한 `@wingetbot run` 재실행은 `Commenter does not have sufficient
  privileges`로 거부, 이후 신규 코멘트 없음 — **신규 기여자 권한 없음 →
  모더레이터 검토 대기**(07-19 결론 유지).

### 결론

양 채널 모두 순수 외부 큐 대기 — **우리 측 조치 없음**. 아래 릴리스 배포 보류
방침도 그대로 유효(승인 후 07-20 구현분을 다음 버전 태그로 묶음).

## 릴리스 배포 보류 방침 확정 (사용자)

- **Chocolatey 모더레이션 승인 전까지 새 버전 태그 부여·GitHub Release 배포를
  하지 않는다**(사용자 방침). 현재 `nexa-dir`/`nexa-dir.portable` 0.8.1이
  모더레이션 대기 중 — 대기 중 새 버전을 push하면 두 버전이 각자 큐에 쌓여
  심사만 복잡해지는 것을 회피(07-20 검토 결론과 일치: 첫 승인 후 후속 버전은
  자동 검사 통과 시 심사가 빨라짐).
- 로컬 `cargo build --release`는 실기 QA용으로 계속 사용(태그/배포와 무관).
  07-20 구현분(X-28·X-29·X-27·X-25 1차·X-26 ③)의 실기 QA는 로컬 빌드
  (exe 1.45MB)로 진행, 릴리스 반영은 choco 승인 후 다음 버전 태그에서.
- winget PR 2건(모더레이터 검토)·Chocolatey 2건(모더레이션) 모두 외부 큐
  대기 — 우리 측 추가 조치 없음(07-19 검토 유지).
