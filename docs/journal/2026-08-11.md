# 2026-08-11 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 저장소 최신화 + 배포 채널 상태 확인 (사용자 지시 — main 직커밋)

08-02 6차 이후 9일 만의 점검. **코드 작업 없음** — 확인과 기록만.

### 저장소

- 트리 clean · `git fetch --all --prune --tags` 무변 · **main ↔ origin/main 0/0**.
  최신 커밋 `fedc6ea`(08-02) · 최신 태그·릴리스 **`0.14.0`** · `Cargo.toml` `0.14.0`.
  즉 **최신화할 대상이 없었다** — 9일간 커밋·릴리스 무변동(사용자 QA 대기 구간).

### winget — 심사 대기 2건이 **둘 다 병합**됐다 (08-06)

08-02 점검에서 "모더레이터 waiver 대기"로 남겨 둔 두 PR이 **같은 날 30분 간격으로**
병합됐다. 우리 측 조치 없이 해소된 것이 예측대로였다.

| PR | 대상 | 병합 | 최종 라벨 |
| --- | --- | --- | --- |
| [#410978](https://github.com/microsoft/winget-pkgs/pull/410978) | `SosomLab.NexaDir.Portable` 0.13.0 | **2026-08-06 00:16 UTC** | `Moderator-Approved` · **`Waived-Policy-Test-1.2`** · `Validation-Completed` · `Publish-Pipeline-Succeeded` |
| [#404528](https://github.com/microsoft/winget-pkgs/pull/404528) | `SosomLab.NexaDir`(설치형) 0.8.1 **최초 등록** | **2026-08-06 00:46 UTC** | `Moderator-Approved` · `Validation-Completed` · `Publish-Pipeline-Succeeded`(`Policy-Test-1.2`·`Validation-Guide` 제거됨) |

- **Portable = 0.11/0.12와 같은 패턴 재확인** — 모더레이터가 `Policy-Test-1.2`를 떼고
  `Waived-Policy-Test-1.2`를 붙인 뒤 승인. 이번엔 waiver까지 **5일**(07-31 제출분은 1일).
- **설치형은 07-19 제출 → 08-06 병합 = 18일**. 07-31의 `Needs-Author-Feedback`
  (`DisplayVersion` 제거 요구) 대응이 마지막 병목이었고, 그 뒤 policy 라벨은 **waiver 라벨
  없이 그냥 제거**된 채 승인됐다(포터블과 처리 방식이 달랐다).
- **카탈로그 실측**(`winget search SosomLab`) — 두 채널 모두 **라이브**:

  ```
  Nexa Dir             SosomLab.NexaDir            0.8.1   winget
  Nexa Dir (Portable)  SosomLab.NexaDir.Portable   0.13.0  winget
  ```

  `winget show --versions` = Portable **0.8.1 / 0.11.0 / 0.12.0 / 0.13.0** · 설치형 **0.8.1**.

- **∴ winget은 2채널 전부 배포 완료.** 남은 것은 심사가 아니라 **버전 격차**:
  - Portable **0.13.0 → 0.14.0**: 08-02에 "0.13.0 PR이 OPEN이라 중복 회피"로 보류했던
    조건이 **해소**됐다. 지금 제출 가능.
  - 설치형 **0.8.1 → 0.14.0**: 6버전 뒤짐. 첫 등록이 워낙 오래 걸려 그동안 후속 제출을
    하지 않았다. 최초 등록이 끝났으므로 이후 버전 업데이트는 통상 경로(수 시간~1일).

### Chocolatey — 무변동 (07-20 이후 **22일** 무이벤트)

패키지 페이지 실측(OData 피드는 미승인분을 반환하지 않음 — 08-02와 동일 방법).

| 패키지 | 버전 | 상태 | 마지막 이벤트 |
| --- | --- | --- | --- |
| [`nexa-dir`](https://community.chocolatey.org/packages/nexa-dir) | 0.8.1 | **Ready = awaiting moderation**("in moderation and has not yet been approved") | `chocolatey-ops` **2026-07-20 02:22** — 스캔 Flagged(검출 1~5 = 승인 차단 아님) |
| [`nexa-dir.portable`](https://community.chocolatey.org/packages/nexa-dir.portable) | 0.8.1 | 동일 | `chocolatey-ops` **2026-07-20 02:21** — 동일 |

- 자동 3단계(Validation·Verification·Scan)는 07-19~20에 이미 끝났고, **사람 검토만 22일째
  안 붙었다**. 모더레이터 코멘트·라벨 변화 0.
- 공개 피드(`FindPackagesById`) 두 ID 모두 여전히 **빈 결과** = 미승인 방증.
- **`CHOCO_PUSH` 실측 정정**: `gh variable list --repo SosomLab/nexa-dir2` = **exit 0 · 빈
  목록**. 08-02 기록의 "변수는 토큰 권한으로 못 읽어 스텝 결과로 확증"은 **이제 직접 조회
  가능**하며, 결과는 **변수 미등록**(07-21 보류 방침 그대로). 승인 시 여기에
  `CHOCO_PUSH=true`를 등록하면 후속 버전부터 자동 재개된다(코드 무변).
- 선택지는 08-02와 같다 — **기다리거나**, 패키지 페이지에 **무서명 Rust exe 오탐 소명
  코멘트**를 남기거나. 후자는 우리가 할 수 있는 유일한 능동 조치지만 의무는 아니다.

### 문서 반영

[21 §8](../21-distribution.md) 채널 표·제출 이력 · [STATUS](../STATUS.md) 최상단 ·
[DEVLOG](../DEVLOG.md) · [CLAUDE.md](../../CLAUDE.md) §7 — **"심사 대기 4건"이
"choco 2건"으로 줄었다**는 사실을 네 문서 모두에 반영.
