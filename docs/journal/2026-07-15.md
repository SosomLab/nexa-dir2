# 2026-07-15 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 릴리스 파이프라인 (`feat/m5-release-pipeline`) — GitHub Releases 자동 배포 (M5-2)

> **사용자 지시**: "브랜치 생성 후 GitHub를 통해서 release 가능하도록 배포 작업".

- **`.github/workflows/release.yml` 신설**: 버전 태그 push(`0.5.0` 형식·`v` 접두사 허용)
  → windows-latest에서 `cargo test`+release 빌드 → **예산 게이트**(B2 ≤10MB·B3 임포트
  화이트리스트 — CI와 동일 스크립트) 통과 필수 → `NexaDir2-<버전>-win-x64.exe` 개명
  (포터블 단일 exe — DR-3) → **GitHub Release 자동 생성**(자동 노트)+exe 첨부.
  `workflow_dispatch` 수동 실행=게이트+아티팩트까지(Release는 태그에서만).
- 릴리스 절차(docs/18 §5 — SSOT 갱신 동일 커밋): `git tag 0.6.0 && git push origin 0.6.0`.
- 기존 §5 예산 측정 → **§6 재번호**(ci.yml·release.yml·budget-b3.ps1 참조 동기).
- M5-2 완료 처리(TODO §M5) — 첫 태그 실행 검증 대기.

## UX QA 6차 (main 직커밋) — 설정 창 원본 UI 재현(X-9)·컬럼 짝 리사이즈·도크 텍스트 복사

> **사용자 QA/요청 4건**: ① 설정 창을 원본 스크린샷(VS Code식 사이드바+섹션) 형태로
> ② 컬럼 경계에서 커서 변경 + 좌우 컬럼 동시 조절 ③ 터미널 포커스 시 탭 바도 비활성
> ④ Info/Preview 내용 드래그·복사. + release 빌드 요청.

- **설정 창 원본 UI 재현(X-9 — 원본 docs/40 §8 스크린샷)**: 좌측 사이드바(검색창 상단
  +카테고리 목록 **오너드로 선택 하이라이트**) + 세로 구분선 + 우측 **섹션 제목**(세미
  볼드 +5pt)·**라벨 일체형 체크박스**·**라디오 그룹**(테마 System/Light/Dark·언어) ·
  [EDIT]+우측 라벨(글꼴·크기). 카테고리 = 모양/글꼴/파일 목록/**터미널**(신설 키
  pref.cat.terminal — 터미널 글꼴·크기 이동)/도크/언어. 창 배경 白(COLOR_WINDOW)·
  WM_CTLCOLOR로 라벨 배경 일치·검색 플레이스홀더(EM_SETCUEBANNER). 기본 760×560.
  Cargo 피처 `Win32_UI_Controls` 추가(DRAWITEMSTRUCT 바인딩 — 신규 DLL 없음).
- **컬럼 짝 리사이즈**(②): 경계 드래그 시 **좌+우 컬럼 동시 조절**(총폭 보존 — 양쪽
  min_width로 이동량 클램프, 이웃이 고정/마지막이면 기존 단독 조절). `resize_hot`
  공개 + WM_SETCURSOR에 경계 IDC_SIZEWE 분기(드래그 중 유지).
- **터미널 포커스 시 패널 전체 비활성**(③): sync_focus_visuals에서 탭 바·리스트 모두
  `활성∧터미널비포커스`로 통일(set_list_focused를 set_focused로 재통합). 캐럿 행
  테두리도 비활성 시 text_dim(accent 잔존 해소).
- **도크 텍스트 선택·복사**(④): InfoDock에 **라인 단위 드래그 선택**(내용 영역
  MouseDown→Move 확장·Up 후 유지, sel_bg 하이라이트) + `selected_text`. 내용/종류
  변경·도크 밖 클릭·반대 패널 클릭 시 해제. **Ctrl+C 우선순위**: 리네임 편집 > 터미널
  > **도크 선택** > 파일 복사. 이미지 미리보기는 제외(라인 없음).
- 검증: clippy 0·테스트 158 green. 실기 QA 대기(release 빌드 안내 포함).

## 포커스 영역 단위 강조 재설계 (main 직커밋) — 터미널 포커스까지 반영

> **사용자 QA(스크린샷)**: 좌측 파일 선택 후 우측/터미널 클릭 시에도 양쪽 선택색이
> 동일 · 하단 터미널 스트립([Terminal]+[→])도 양쪽 다 accent — 활성 영역 식별 불가.
> 패널 단위(UX QA 5차 ①②)를 **실제 키 포커스 영역 단위**로 확장.

- **`sync_focus_visuals`(win.rs)**: 탭 바 accent=활성 패널 · **리스트 선택색=활성
  패널이면서 `term_focus=None`**(터미널 포커스 중엔 활성 패널 리스트도 비활성 색) ·
  **도크 스트립(활성 종류 라벨·[→])=그 패널 터미널이 키 포커스일 때만 accent**.
  호출: set_active·WM_LBUTTONDOWN 종단(클릭으로 확정된 포커스)·WM_KEYDOWN 낡은
  터미널 포커스 해제·WM_NCCREATE(세션 복원 반영).
- `Panel::set_focused`(탭 바)와 `set_list_focused`(rows 전파+새 탭 상속) 분리,
  도크 강조는 호스트가 `dock.set_focused`로 직접 동기(InfoDock 기본값 false).
- **WM_CAPTURECHANGED 방어**: 캡처 강탈로 WM_LBUTTONUP이 안 오면 스플리터류 드래그
  플래그(accent)가 잔존 → 캡처 상실 시 3종 플래그 일괄 해제+재도장.
- 검증: clippy 0·테스트 158 green·빌드 통과. 실기 QA 대기.
  (참고: 직전 스크린샷 재현 3건[선택색·[→]·스플리터 잔존]은 수정 전 구버전 exe —
  재빌드 후 확인 필요. Copy as name 위치는 80985bc에 반영됨.)

## UX QA 5차 (main 직커밋) — 비활성 패널 구분·스플리터 잔존·이름 복사 위치·설정 창 X-8

> **사용자 QA/요청 6건**: ① 좌측 선택 후 우측 클릭 시 양쪽 선택 하이라이트가 동일해
> 활성 패널 식별 불가 → 비활성 쪽 색 구분 ② 듀얼 터미널에서 비활성 패널의 [→](폴더
> 이동) 버튼도 accent 파랑 유지 → 비활성 표시 ③ 스플리터 드래그 후 마우스를 떼어도
> 파란 강조가 남음 ④ Copy as name을 Copy as path 바로 밑으로 ⑤ 설정 창을 VS Code
> 방식으로 ⑥ 설정 창 크기 조절.

- **비활성 패널 선택색 구분**(①② — 탐색기 관례): 테마 토큰 `sel_bg_inactive` 신설
  (다크 0x2C313A·라이트 0xE6E9EE — 무채색 블렌드). `VirtualRows`/`InfoDock`에
  `set_focused` 추가, `Panel::set_focused`가 tabbar와 함께 **전 탭 rows·dock**에 전파
  (새 탭·복제 탭은 패널 focused 상속). 도크 스트립 활성 종류 라벨과 [→] 버튼도
  비활성 패널에선 `sel_bg_inactive`(accent 대신). 기동 시 복원된 활성 패널 인덱스
  반영(기존: 무조건 우측만 비활성 처리 — `active=1` 복원 시 불일치).
- **스플리터 강조 잔존 수정**(③): WM_LBUTTONUP에서 `dock_split_drag`(도크 좌/우)·
  `dock_drag`(도크 높이)는 플래그만 끄고 재도장을 안 해 accent가 남음(파일 좌/우만
  invalidate) → 두 경로 모두 해제 시 InvalidateRect.
- **이름 복사 위치**(④ — 원본 PR#10): `CustomItem.after_id` 신설 — 셸 메뉴 병합 시
  지정 ID **바로 아래 InsertMenuW**(제자리 대체된 "경로 복사"[CTX_COPY_PATH] 앵커),
  대상 부재 시 하단 고유 섹션 폴백. Copy as name이 Copy as path 바로 밑에 위치.
- **설정 창 VS Code식 완성 + 리사이즈**(⑤⑥ — X-8, X-7 후속):
  - **즉시 적용**: [저장]/[취소] 제거(pref.save/cancel 키 말소) — 체크박스·테마/언어
    순환=클릭 즉시, 글꼴/크기 EDIT=**포커스 이탈(EN_KILLFOCUS) 시** 적용(키 입력마다
    DWrite 백엔드 재생성 방지). 통지 = `WM_APP_PREFS_APPLY`(0x8006) **동기 SendMessage**
    (포인터는 통지 동안만 유효 — 수신 즉시 복사). 호스트 적용 로직을 `apply_prefs`로
    분리(실시간·닫기 공용, 멱등 — 테마는 무변경 시 생략). 닫기=X, 닫기 시 최종 값
    한 번 더 적용·영속(미이탈 편집 값 수거).
  - **리사이즈**: WS_THICKFRAME+WS_MAXIMIZEBOX(`PREFS_STYLE`), WM_SIZE에서 검색창 폭·
    편집 컨트롤 폭(잔여 폭) 재배치, WM_GETMINMAXINFO 최소 크기=기본 클라이언트 크기.
- 검증: `cargo check` clean(경고 0)·테스트 158 green·빌드 통과. 실기 QA 대기.
