# 2026-07-27 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 로컬 브랜치 정리 (사용자 지시 — 확인 후 선별 삭제)

- **지시**: "불필요한 브랜치는 정리" → 삭제는 파괴적 작업이라 **내용 확인 후 선별**.
- **삭제**: `feat/md-preview`(tip `6f47512`·7커밋) — 내장 러스트 md/mermaid 뷰어
  1차 시도. 사용자가 07-26에 **폐기 지시**했고, 파서·레이아웃 자산은
  `samples/markdown-viewer-wasm/src/{lib,mm}.rs`로 **이식 완료**(중복) → 삭제.
- **보존**: `feat/x13-launcher-crud`(tip `0888677`·1커밋) — **삭제 보류**.
  근거: `launcherN.icon` 오버라이드 직렬화가 **main에 미반영된 고유 작업**이고
  ([TODO](../TODO.md) X-13 "1/2 완료"가 이 브랜치를 참조), 삭제 시 유실된다.
  322커밋 뒤처져 있어 X-13 2/2 착수 시 rebase 후 병합 예정 — [BRANCHES](../BRANCHES.md)
  상단에 보존 사유 명기.
- 삭제 SHA는 BRANCHES 표에 기록(단기 reflog 복구 가능).

## 플러그인 개발 문서 정리·가이드 보강 (사용자 요청 — main 직커밋 `bd938bf`)

- **요구**: wasm 플러그인 **개발 방법·프로젝트 구성·개발자 가이드·적용 방법** 정리.
- **점검 결과**: 문서 체계(가이드 24·ADR-0005·샘플 README·wiki)는 이미 존재하나
  가이드가 116줄로 압축돼 **외부 개발자가 처음부터 따라할 최소 예제와 적용 절차가
  부재** → 보강(문서 전용·코드 무변경).
- **보강 내용**([24](../24-plugin-dev-guide.md) 116→193줄):
  - **§1-1 프로젝트 구성** — 디렉터리 골격 + `Cargo.toml` 필수 4항목 주석
    (cdylib / `[workspace]` 분리 / opt-level="z"+lto / panic="abort" = 트랩 격리).
  - **§1-2 최소 예제 전체 코드** — `.log`/`.ini` 줄 번호 뷰어(약 30줄·**19KB**).
    **문서 수록 전 실제 검증**: wasm32 컴파일 → wasmi 런타임 `load_dir`/
    `run_preview`로 메타 파싱·`read_text`·제목 태그 렌더까지 동작 확인(임시 테스트
    후 제거 — 트리 clean).
  - **§2 ABI** = export 2종 표(반환·내용·`memory` 자동 export·`id` 개명 금지).
  - **§4 빌드→적용→확인 3단 재구성** — 산출물 명명(`-`→`_`)·**파일명 = 로드 순서**·
    포터블/설치형 복사 위치 표·설정 창 체크·`preview_map` 강제 지정·오류 1줄 격리.
- 실측: preview:: 5 green 유지·트리 clean.

## `.star` → `.wasm` 표기 정정 (사용자 QA — main 직커밋 `ddc632a`)

- **발단**: wasmi 전환 후에도 **설정 창에 노출되는 문구**가 `.star`로 남아 있음을
  사용자가 발견(런타임·코드는 전환 완료·표시 문자열만 잔존).
- **정정 범위**(현행 표면 전체 — 실제 사용자/개발자가 보는 곳):
  ① **lang 3종**(ko/en/ja) `pref.plugins.desc`/`empty` = `data\plugins\*.wasm`·
     ".wasm 파일을 복사" ② **wiki 개발-플러그인 = wasmi 판 재작성**(개념·왜 WASM
     [크로스플랫폼·격리·+1.68MB 실측]·동작 범위·계약 요약·설정·빌드/배포·로드맵) ·
     **기능-설정** 표 · **설계-결정** 플러그인 절(계획 단계 → 동작 중) ③ **ADR-0004
     상단**에 "런타임은 ADR-0005로 정정 — 본문 `.star` 서술은 구판" 명시.
- **보존**(규약 §2-2·§2-5): journal·DEVLOG·STATUS 과거 차수·BRANCHES의 `.star`
  언급은 그 시점 사실 기록이라 무수정. ADR-0004 본문도 삭제 대신 정정 표기.
- 실측: **217 green**(i18n 파리티 포함)·clippy 0·release exe 3.18MB 재빌드.
