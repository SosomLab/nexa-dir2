# 2026-08-23 (토)

> 시간 역순 — 최신이 위. 요약은 [DEVLOG](../DEVLOG.md).

## X-44 4차 — 뷰포트 폴더 점검을 mtime → 프로브 서명으로 승격 (`2bc8dba` — 실측 확정·검증 완료)

**재보고**: 탐색기에는 `22222\문서.docx`가 보이는데 앱에는 여전히 글리프가 없다.
3차(비활성 폴링 유지) 후에도 재현 — **이번엔 파일이 로컬에 실재**했다.

- **실측 3종이 원인을 확정**: ① 파일은 22:37:19부터 디스크에 존재(온라인 전용
  플레이스홀더 — ARCHIVE|SPARSE|REPARSE|OFFLINE|RECALL_ON_DATA_ACCESS) ② **부모
  폴더(22222) mtime 불변**(22:36:11 그대로 — 플레이스홀더 생성이 디렉터리 mtime을
  안 건드린다) ③ **RDCW(FSW) 생성 이벤트 없음**(같은 파일의 삭제는 22:36:12에
  이벤트 발생 — 감시 로그로 대조 확정). ∴ **CfCreatePlaceholders는 mtime도 통지도
  남기지 않고, 새 자식은 열거(read_dir)만이 본다** — mtime 1단계 스윕(2차)과
  watcher(2차)가 이 케이스에 원리적으로 무력했던 이유.
- **수정**: 뷰포트 폴더(펼침+접힘) 점검을 **루트 프로브와 같은 fsprobe 서명**(상한
  SCAN_CAP 내 열거 count/fold)으로 승격 — `sub_probe`가 DirSig 맵으로. 비용 =
  화면에 보이는 폴더 수(~수십)로 유계·기존 틱 편승(활성 3s/비활성 10s·신규 타이머 0).
- **신규 테스트**: 부모 mtime을 SetFileTime으로 되돌려 침묵을 재현 — count/fold가
  자식 추가를 잡는 것을 계약화(cfg(windows)).
- **검증**: 116 green·clippy 0·비Windows check. 재빌드·재시작 후 **비활성 유지
  캡처로 글리프 표시 확인**(로컬 실재 파일 기준 즉시 표시 ✔ — 이후 침묵 생성분은
  틱 주기 내 표시). 부수 관찰: 타이틀에 "Token exchange failed (/Benthic)" —
  클라우드 연결 1건의 토큰 갱신 실패(본 건과 무관 — 재로그인 대상, 사용자 안내).

## X-44 3차 — 비활성(보이는 창) 프로브 폴링 유지 (`ce6c0ee` — 사용자 확정·재시작 반영)

**재보고**(스크린샷 3장): 클라우드 웹에서 직접 만든 파일이 비활성 앱에 안 뜨고(빈 폴더
글리프도), 활성화하는 순간 갱신되며 등장. 펼쳐진 `111111`의 `문서.docx`도 동일.
"비활성 상태에서도 뜨게 해 달라."

- **디스크 실측이 원인을 갈랐다**: 생성 수 분 후에도 `문서.docx`가 **로컬에 존재하지
  않았다**(재열거로도 무변 — SharePoint 라이브러리의 하향 동기화 지연). 즉 ① 원격
  생성분은 **내려오기 전까지 디스크에 없어 어떤 감시로도 뜰 수 없고** ② 내려오는
  시점에 앱이 비활성이면 폴링(뷰포트 스윕·루트 프로브)이 꺼져 있어 활성화 전까지
  낡았다. "활성화하면 생긴다"는 체감은 **그 사이 동기화가 완료됐고 활성화 재열람이
  드러낸 것**. 삭제가 비활성에서도 반영되던 비대칭 = 삭제는 기존 플레이스홀더의
  실제 FS 삭제라 watcher 통지가 난다.
- **수정**: `WM_ACTIVATEAPP` 비활성 시 `KillTimer` 대신 **감속 재무장**
  (`FSPOLL_IDLE_MS` 10s — 활성 3s). 옆 창에서 작업하며 보이는 앱은 계속 갱신되고,
  **정지는 최소화에서만**(안 보이면 무의미 — 유휴 백그라운드 0% 규율 유지. X-40의
  "창 활성일 때만" 규정을 사용자 확정으로 개정). O(1) stat 스윕 + 상한 열거라 비용
  무시 수준 — "성능 깎는 폴링 배제" 원칙과 합치.
- **부수 실측 준비**: 파일이 실제로 내려오는 순간 RDCW(FSW) 통지가 나는지 백그라운드
  감시 로그를 걸어 둠 — 통지가 나면 watcher만으로도 즉시 반영(폴링은 보험), 안 나면
  (플레이스홀더 생성 무통지) 이번 폴링 유지가 필수 경로였음이 확정된다.
- 검증: 115 green·clippy 0·비Windows check. 재빌드·앱 재시작(pid 반영).

## X-45 항상 맨 위에 표시 토글 (`8071` 계열 — 사용자 요청·앱 재시작으로 즉시 반영)

**요청**: "항상 맨 위에 표시"를 토글로 — 툴바 버튼 + View 메뉴(직전 검증 실험에서
TOPMOST로 창을 올려 관찰한 것이 계기로 보임).

- **구현**: `CMD_TOGGLE_TOPMOST` 단일 명령을 View 메뉴 체크 항목(런처 토글 아래)과
  툴바 **panel 블록 5번째 버튼**(압정 SVG `always-on-top.svg` — 순수 스트로크 =
  다크 전용판 불요)이 공유. 동작 = `SetWindowPos` TOPMOST/NOTOPMOST 밴드 전환
  (`SWP_NOACTIVATE|NOMOVE|NOSIZE` — 활성화·위치·크기 불변). 토글 즉시 적용 +
  메뉴/툴바 체크 동기 + **설정 영속**(`always_on_top` — 기동 시 복원, 앱 토글 영속
  관례 계승). 기존 사용자의 `toolbar_order`에는 파서가 `ontop` 키를 **보충 삽입**
  (foldersfirst 08-02 전례 — 순서 편집 창에도 자동 노출). lang 3종
  `menu.view.alwaysOnTop`.
- build_menus/build_toolbar 시그니처에 파라미터 추가(호출부 11곳) — 인자 구조체화는
  X-16 ② 후보로 유지.
- **검증**: 115 green(순서 보충·설정 왕복 테스트 갱신)·clippy 0·비Windows check·
  **B2 3.71MB·B3 통과**. 사용자 지시로 앱 정상 종료(세션 저장)→재빌드→재실행 —
  실행 중 exe에 오늘 작업 전부(X-42 2차·X-43 β·X-44 1·2차·X-45) 반영.

## X-44 2차 실측 검증 — "여전히 글리프 안 뜸" 재보고의 원인 분리 (코드 무변경)

**재보고**: 새 빌드(19:07 exe·19:08 실행 — 실측으로 최신 확인)로도 클라우드에서 22222에
파일을 만들면 글리프가 안 뜬다. **실측 실험**으로 앱 체인과 동기화를 분리했다.

- **방법**: 실행 중인 앱 창을 **활성화 없이**(SWP_NOACTIVATE·일시 TOPMOST) 앞으로 올려
  화면 캡처로 관찰 — 비활성 전제를 깨지 않고 글리프 상태를 눈으로 확인. 그 상태에서
  22222 안에 **로컬로** 파일 생성/삭제(동기화 개입 배제).
- **결과**: 생성 후 ~1.8초 내 **비활성 상태 그대로 ▸ 글리프 출현**, 삭제 후 소멸 —
  **X-44 2차 체인(뷰포트 접힌 폴더 watcher → 디바운스 → 무간섭 재열람 → 프로브 재판정)
  은 양방향 모두 정상**. 캡처 3장으로 확증.
- **∴ 재보고의 유력 원인 = 앱이 아니라 클라우드→로컬 동기화 시점**: SharePoint 문서
  라이브러리의 OneDrive 동기화는 변경 열거 기반이라 수초~수분 지연이 있을 수 있고,
  로컬에 파일이 내려오기 전에는 어떤 감시로도 뜰 수 없다. **다음 QA 요령** = SharePoint
  에 만든 뒤 로컬 폴더(탐색기)에 파일이 실제로 내려온 시점 기준으로 글리프를 확인.
  내려왔는데도 안 뜨는 재현이 잡히면 그때 다시 앱 쪽을 판다(동기화 엔진의 플레이스홀더
  생성이 RDCW를 우회하는 케이스 — 현재 실측으로는 부정됨).
- **사용자 지시로 앱 재시작**: WM_CLOSE 정상 종료(세션 저장) → 무변경 확인 빌드 →
  재실행(pid 4444 — 실험용 TOPMOST도 해제). **투명성 기록**: 실험 파일
  `글리프테스트-로컬.txt`를 동기화 폴더(22222)에 수 초간 생성했다 삭제 — 라이브러리에
  잠깐 업로드됐다 지워졌을 수 있다(사이트 휴지통에 흔적 가능).

## X-42 2차 — 가상 파일 ⓐ DnD 수신·ⓑ 워커화·ⓒ undo·ⓓ 클라우드 (`3e8854c`+`2d6dbdf` — 실기 QA 대기)

1차의 4개 잔여를 전부 소화. 코어(`extract_virtual_from` — 1차 때 분리해 둔 재사용 지점)
는 그대로 두고 진입 경로·실행 방식·마감(undo)을 확장했다.

- **ⓐ DnD 수신**(`3e8854c`): DragEnter에서 CF_HDROP 부재 + `FileGroupDescriptorW`
  광고면 가상 모드(`DropTarget.virtual_src`) — 항상 복사 수신(원본 삭제 불가 소스·실경로
  없어 자기/하위 검사 무의미), Drop **반환 전** 동기 추출(소스가 DoDragDrop 종료 후
  스트림을 무효화할 수 있다 — 7-Zip 스테이징의 "반환 전 확보" 규약). 수혜 = **Outlook
  첨부·탐색기 zip 내부·MTP 끌어놓기**. **정정**: 앞서 "RDP DnD도 열린다"고 했으나
  RDP는 프로토콜상 파일 드래그가 없다(클립보드 리디렉션만) — 비대상.
- **ⓑ 워커화**(`2d6dbdf`): UI 스레드는 **계획까지만**(디스크립터 파싱·" (2)" 대상 계획·
  항목별 GetData 스트림 확보) — IStream을 `CoMarshalInterThreadInterfaceInStream`으로
  마샬링해 워커(MTA)에 넘긴다. **out-of-proc 소스(rdpclip)는 언마샬 프록시가 원
  프로세스로 직접 이어져** Read(실제 바이트 전송)가 UI 펌프에 의존하지 않는다 —
  대용량 RDP 붙여넣기 중 창 반응 유지. HGLOBAL 소스는 계획 시점 즉시 복사. 진행 창·
  취소·세그먼트 바·`in_flight` 실행 잠금은 **클라우드 전송 공용 상태(TransferShared)
  재사용**(08-01 "같은 진행 창" 전례 — 동시 1개·바쁘면 동기 폴백). 완료 =
  `WM_APP_VPASTE`. 취소 시 쓰다 만 파일 소거·잔여 Skipped·**마샬 패킷은 취소여도
  1회 소비**(누수 방지). Cargo 기능 `Win32_System_Com_Marshal` 추가(ole32 — B3 무변).
  DnD 드롭은 소스 생존 규약 때문에 동기 유지(AsyncCapability 핸드셰이크 = X-38 β 겸용
  후속).
- **ⓒ undo**: `VPasteOp` 신설 — 원본 경로가 없어 재복사(redo)가 불가하므로
  DeleteBatchOp의 **역방향**으로: undo = 생성물 휴지통 삭제 · redo = 셸 undelete 복원.
  워커 완료·동기 폴백·DnD 드롭 세 경로 모두 기록(존재하는 최상위만·전량 실패 = 무기록).
- **ⓓ 클라우드 대상**: 종전 무동작 가드를 해제 — `%TEMP%\NexaDir\vpaste-*` 스테이징에
  동기 추출 후 **기존 업로드 경로 재사용**(start_transfer 클라우드 분기 = X-37
  Upload/UploadTree — 계정 간 복사의 임시 폴더 경유 전례). RDP에서 복사한 파일을
  OneDrive 탭에 바로 붙여넣기 가능.
- **검증**: 워커 종단 테스트 신설(실 클립보드 비접촉 — 300KB 다중 청크·크기 미상
  EOF·세그먼트 상태·진행 바이트) + 판정 게이트 정/부 2단언. **115 green(app)·78(gui)·
  clippy 0·비Windows check·B2 3.70MB·B3 통과(신규 DLL 0)**.
- **실기 QA**(사용자): RDP 대용량 붙여넣기 중 창 반응·진행 창·취소 · Ctrl+Z(휴지통
  왕복) · Outlook 첨부/zip 내부 드래그 · 클라우드 탭 붙여넣기(업로드 연계).

## X-44 2차 — 뷰포트의 접힌 폴더도 감시·스윕 (`3155915` — 사용자 QA 즉시 대응)

**보고**: 비활성 중 클라우드에서 접힌 빈 폴더(`22222`) 안에 파일이 생겨도 글리프가 안
생기고(앱 클릭 = 활성화해야 출현), **펼쳐 뒀던** 폴더에서의 삭제는 비활성 중에도 정상
반영되는 비대칭. 요청 = "눈에 보이는 폴더까지 갱신/추적 대상으로".

- **원인**: watcher 대상이 "루트 + 가시 **펼침** 폴더"뿐 — 삭제가 됐던 건 그 시점에
  그 폴더가 펼쳐져(감시돼) 있었기 때문. **접힌 폴더 내부 변경은 부모 폴더의 비재귀
  통지로 신뢰성 있게 오지 않고**, 비활성 중엔 프로브 폴링(뷰포트 mtime 스윕 포함)도
  꺼져 있어 계기가 0이었다.
- **수정**: `watch_dirs`가 **뷰포트에 보이는 접힌 폴더를 감시 목록에 합류**(상한 64
  공유·중복 제거) — 이벤트 기반이라 상시 비용 0·비활성 중에도 동작(사용자가 확인한
  "비활성 중 바로바로 갱신" 경로 그대로). `visible_expanded_dirs`는
  `viewport_dirs(expanded_only)`로 일반화 — 활성 시 mtime O(1) 스윕도 접힌 폴더 포함.
- 부수 비용: 스크롤로 뷰포트가 바뀌면 감시 diff 재구독이 따라온다(기존 diff 로직이
  유지분은 무비용 — 폭주 시 스레드 churn은 관찰 대상).

## X-43 β — 느린 경로 프로브 생략 (`2430bde`)

빈 폴더 글리프 판정의 미열거 폴더 프로브가 페인트 경로 동기 실행이라 **UNC·원격
드라이브·광학**에서는 폴더당 수십 ms~수 초(타임아웃)로 프레임을 잡을 수 있다 →
`probe_skip_slow`(UNC 접두·GetDriveType REMOTE/CDROM)로 **프로브를 생략하고 글리프
유지**(안전 방향 — 퇴행은 "빈 폴더인데 ▸ 표시"뿐). 판정은 문자열+GetDriveType 1회·
NodeId 캐시로 폴더당 1회. 리무버블(USB)은 로컬 FS라 프로브 유지·비Windows 무생략.
후속(필요시) = 비동기 프로브(아이콘 로딩 큐 A-4 패턴).

## X-44 간헐 무갱신 보완 (`a80d2dc` — main 직커밋, 실기 QA 대기)

전수 조사(아래 항목)의 유실 시나리오를 구현으로 보완. **사용자 확정 원칙 2가지**가
설계를 갈랐다 — ① "노후된 비활성 탭·Deactivated 앱이 **활성화되는 순간, 보이는
영역(트리 모드 펼침 포함)은 모두 갱신**한다" ② "성능을 깎아 먹을 빈도의 감시·폴링은
배제한다". 또 하나의 제약: **비활성 중에도 watcher 경로로 즉시 갱신되는 현행 동작은
잘 쓰고 있다** — 그 경로는 손대지 않고 새는 곳만 메꾼다.

- **S1 탭 활성화 = 갱신 계기**(유력 원인 1위): `Tab.stale` 신설 — `switch_tab`·
  `close_tab`(이웃 드러남)이 표시하고 update_status 길목(`active_tab_stale` 확장)이
  **무간섭 재열람**으로 소거. 비활성 탭은 watcher·프로브 비대상이라 그동안의 외부
  변경이 어디에도 안 잡혀 있었다 — 활성화 순간이 유일하게 올바른 계기.
  기존 테스트 1건이 구 의미론("전환=비stale")을 단언하고 있어 함께 갱신.
- **S2 기준선 즉시 수립**: 종전엔 프로브 기준선을 **다음 폴링 틱**(0~3s 뒤)에 "경로
  변경 = 기준선만" 분기로 세워, 열거~틱 사이의 외부 변경이 영영 삼켜졌다(동기화가
  활발한 폴더에 진입하는 순간이 정확히 이 창) → update_status에서 경로가 바뀐 패널만
  즉시 수립(가상 루트·클라우드 제외) + `reload_both` 끝에 재수립(전송·삭제 완료 후
  1회 여분 재로드가 있던 기존 잔결함도 소거) + 디바운스 만료 경로도 공용 헬퍼로 통일.
- **S3 서명 강화(비용 동일)**: `DirSig`의 `bytes 합 + 최신 mtime(max)`이 **같은 크기
  덮어쓰기인데 그 파일이 폴더 내 최신이 아닌 경우**(동기화 클라이언트가 서버 시각으로
  mtime을 찍는 전형)를 못 잡았다 → 항목별 (크기, mtime ns) SplitMix 혼합의 순서 무관
  접기(`fold`). 같은 열거 1회 비용. 신규 테스트(최신 파일을 따로 둬 max로는 안 잡히는
  형상 재현).
- **"보이는 영역은 모두 갱신" 원칙 점검**: 재열람 자체는 펼침 복원까지 재열거하므로
  **계기만 있으면** 보이는 트리 전체가 새로워진다. 계기의 구멍 = 프로브가 루트만 보고
  watcher 상한(64) 밖 펼침 폴더가 무감시 → **뷰포트에 보이는 펼침 폴더의 mtime O(1)
  점검**을 기존 3s 틱에 편승(`Panel::visible_expanded_dirs` + `rows.viewport()` 신설 —
  read_dir 없음·신규 타이머 0 = 성능 원칙). 하위 추가·삭제·이름 변경은 그 폴더 mtime이
  바뀌어 잡히고, 내용만 바뀐 덮어쓰기는 watcher(상한 내)·활성화 재열람 몫.
  **앱 활성화(refresh_on_return)는 프로브 게이트를 버리고 보이는 영역 재열람으로 개정**
  (서명이 못 잡는 변경도 복귀 시점엔 확실히 반영 — 활성화는 저빈도라 비용 무시 수준).
- **S5 WM_DEVICECHANGE**: 핸들러 부재로 내 PC 뷰가 USB 삽입/제거를 F5 전까지 몰랐다 →
  DBT_DEVICEARRIVAL/REMOVECOMPLETE 시 가상 루트 패널만 디바운스 재로드.
- **S8 소실 폴더 조상 폴백**: reopen 실패 시 조용히 return → 유령 목록 + watcher 죽은
  채 재구독 연속 실패 + **F5도 같은 경로라 무효** 고착 → 최근접 존재 조상으로
  navigate(클라우드 센티널·가상 루트는 경로 문법이 달라 비대상).
- **배제(사용자 지시)**: 라운드로빈 2단계 프로브(구 제안 ⑤) — 추가 폴링 빈도라 제외.
  탭 전환 DirSig 게이트도 불채택(무간섭 재열람이 이미 저비용·확실).
- **검증**: 신규 테스트 4(stale 계기·조상 폴백·뷰포트 목록·같은 크기 덮어쓰기) —
  **114 green(app)·78(gui)·clippy 0·비Windows check·B2 3.69MB(별도 target 실측)·B3
  무변**. **실기 QA**(사용자): 탭 전환 순간 목록 최신화 · 알트탭 복귀 시 갱신 ·
  USB 삽입/제거 반영 · 보고 있던 폴더 삭제 시 조상 이동 · 동기화 중 갱신 누락 재발
  여부(장기 관찰). 앱 종료 후 재빌드 필요(실행 중 exe는 X-41까지).

## 내용 정리 + 진행사항 최신화 + main push (사용자 지시 — 하루 마감)

**착수 실측**: 트리 clean · main이 origin보다 **4커밋 앞**(오늘 작업분 — 전부 main
직커밋) · **병합할 브랜치 없음**(로컬 브랜치는 `feat/x13-launcher-crud`뿐 — X-13 2/2
착수 전 보존 규약이라 대상 아님). 정리 대상은 문서 어긋남이 아니라 **외부 채널의
진행**이었다.

- **winget 0.16.0 PR 2건이 둘 다 병합돼 있었다**(원천 실측 — PR 라벨 JSON +
  `winget show --versions`): ① Portable [#415214](https://github.com/microsoft/winget-pkgs/pull/415214)
  **제출 당일(08-11 13:10 UTC) 병합** — waiver 없이 `Moderator-Approved`, 역대 최단
  ② 설치형 [#415215](https://github.com/microsoft/winget-pkgs/pull/415215) **08-14 병합**
  (`Waived-Policy-Test-1.2` 경유 — **버전 업데이트도 최초 등록과 같은 waiver 경로를
  탄다**는 확인). 카탈로그 실측 = Portable 0.8.1/0.11.0/0.12.0/0.13.0/**0.16.0** ·
  설치형 0.8.1/**0.16.0** → **winget 두 채널이 처음으로 최신 릴리스와 완전 동기**.
  심사 대기는 choco 2건(0.8.1)만 남았고, 다음 릴리스는 [21 §8 제출 규칙](../21-distribution.md)대로
  **winget 제출(대기 없음)·choco 제외(대기 중)**.
- **choco 무변동**(패키지 페이지 실측): 두 패키지 모두 0.8.1 **flagged 유지**(07-20
  스캔 플래그 후 무이벤트 — 사람 검토 미착수 34일째).
- **낡은 "심사 중" 표기 정정 4곳**: CLAUDE.md §1·§7-3 / STATUS §5-3(+choco 재확인
  날짜) / MILESTONES 포스트 M5 행 / [21 §8](../21-distribution.md) 채널 표(재점검
  날짜 08-23·병합 경위 노트·제출 이력 2곳에 병합 결과 append). **위키는 무수정** —
  설치 페이지가 카탈로그 버전을 명시하지 않는 서술이라(08-11 정리의 결과) winget
  병합으로 낡아진 곳이 없고, choco "심사 중"은 여전히 사실. 발행할 것도 없다.
- **그 외 어긋남 없음 확인**: STATUS 166줄("한 장" 규약 내) · 오늘 신설 문서 없음
  (docs/README 색인 무변) · X-42/X-43/X-44/제출 규칙은 각 트랜잭션에서 이미 기록.
  억지로 만들 일이 없으면 없다고 적는다(08-11 규약).
- **push**(사용자 지시): main 직커밋 5건(feat 2·docs 3 — 이 정리 커밋 포함) —
  태그 없음(공개 Release 미발생·별도 승인 불요).

## 파일 변경 감시 파이프라인 전수 조사 (사용자 보고 — "간혹 F5를 눌러야 파일이 보인다"·코드 무변경)

**보고**: 원인 특정 불가한 간헐 무갱신. 이벤트 감시가 전 경로를 커버하는지 전수 조사 지시.
watcher.rs·fsprobe.rs·panel.rs·win.rs(타이머·디바운스·세대 가드)·자기 조작 재로드 경로
전부를 실코드로 점검했다(탐사 에이전트 — 계기별 커버 범위 지도 + 유실 시나리오 8건).

- **유력 원인 톱 3** — ① **S1 백그라운드 탭 낡음**(가장 유력): 탭 전환은 재열거하지
  않고(panel.rs switch_tab — 주석으로 자인) 비활성 탭 루트는 watcher·프로브 둘 다
  비대상. 전환 후 프로브는 경로 변경을 "기준선만 재설정"으로 처리해(win.rs
  `TIMER_FSPOLL` 틱) **전환 전 변경을 영영 삼킨다**. 이후 그 폴더에 새 변경이 오면
  저절로 고쳐지므로 정확히 "간혹·재현 불가"로 체감된다 ② **S2 열거→구독/기준선 갭**:
  폴더 진입/복귀 직후 0~3초 창(열거는 끝났고 watcher 구독은 update_status·프로브
  기준선은 다음 3s 틱)의 외부 변경은 어느 계기에도 안 잡힌다 — 동기화가 활발한
  폴더에 진입하는 순간이 정확히 이 창 ③ **S3+S4 프로브 서명 한계**(통지 무음 경로
  한정): 같은 크기 덮어쓰기인데 그 파일 mtime이 폴더 내 최신(max)이 아닌 경우
  count·bytes·newest 전부 불변 = 서명 무변 · 4096 초과 폴더 내용 변경 · **펼친
  하위 폴더**(watcher 상한 64 초과분 + 프로브는 루트만 = 겹치지 않는 사각).
- **그 외**: `::PC::` 뷰는 **WM_DEVICECHANGE 핸들러 부재**(USB 삽입/제거 무반영) ·
  `::CLOUD:` 탭은 자동 갱신 계기 0(F5뿐 — 알려진 한계) · 죽은 watcher의 마지막 통지
  세대 폐기(희귀) · 경로바/리네임 편집 방치 시 디바운스 무기 연기(전송 중엔 양 패널) ·
  보고 있는 폴더 소실 시 reopen 조용히 실패 = F5도 무효(탐색으로만 탈출).
- **건전 확인**: 디바운스·WATCH_MAX 경계, 세대 가드(A-1), 무간섭 재로드는 유실 경로
  없음(로컬 열거는 UI 스레드 동기라 낡은 결과가 새 결과를 덮을 수 없다).
- **보완 제안**(작은 것부터 — [TODO](../TODO.md) X-44 등재·착수는 사용자 결정):
  ① 탭 전환 시 재열거 1회(또는 비활성화 시점 DirSig 비교 게이트) ② 프로브 기준선을
  열거 직후 즉시 수립(폴링 틱 대기 제거) ③ fsprobe 서명을 newest(max) 대신 항목별
  (mtime,size) 해시 누적으로 강화 ④ WM_DEVICECHANGE 처리 ⑤ 펼침 폴더 라운드로빈
  프로브 ⑥ 소실 폴더 조상 폴백.

## X-43 빈 폴더 펼침 글리프 억제 (`b1a04ca` — main 직커밋, 실기 QA 대기)

**보고**: 우측 패널 `…\04. 분석&설계 관리\11111`처럼 **현재 보기로 보여줄 내용이 없는
폴더**(숨김/점 파일만 있거나 완전히 빔)에도 ▸ 글리프가 떠서 펼쳐도 아무것도 없다 →
글리프를 숨기고, 보기 옵션 변경으로 내용이 드러나면 복귀. **설정 토글(기본 = 숨김)**.

- **판정 이원화**: 열거 완료(펼쳤던) 폴더 = 트리 실측(`nexa-tree
  loaded_child_count` — 필터는 열거 시 적용되므로 children이 곧 답) · 미열거 폴더 =
  `read_dir` **조기 종료 프로브**(트리와 같은 필터 규칙 적용·`NodeId` 캐시로 폴더당
  IO 1회). **캐시 수명 = 소스 수명** — 재로드·탐색·필터 변경은 전부 새 소스라 무효화
  코드가 따로 없다. 열기 실패(권한·클라우드 센티널)는 **글리프 유지**(모르면 숨기지
  않는다 — 클라우드 폴더가 억제로 못 펼치게 되는 사고 방지).
- **marker ≠ is_dir 분리**(nexa-gui): `RowItem.is_dir` 신설. rows.rs가 marker를 폴더
  판정 프록시로 쓰던 3곳(리네임 확장자 선택·트리/타일 폴더 굵게)을 is_dir로 교체 —
  글리프가 억제된 빈 폴더도 폴더 서식을 유지한다. Flat 보기 비굵게는 현행 유지.
  `nexa-tree has_children` 의미는 **불변** — `sync_expanded`(X-4 펼침 등재/말소)·
  `watch_dirs`(감시 대상)·클라우드 행 판정이 "폴더 여부"로 소비하므로 트리 층에서
  바꾸면 **빈 폴더가 감시에서 빠지는** 부작용이 났다(설계 검토에서 차단).
- **전파**: 설정 `hide_empty_glyph`(기본 1) — NavCtx에 실어 `open_source` 관문에서
  적용(모든 열거 경로 커버: 탐색·재로드·새 탭·복제·세션 복원) + 설정 창 토글은 전 탭
  즉시(`Panel::set_hide_empty_glyph`). 설정 창 "파일 목록" 체크박스·lang 3종(en/ko/ja).
- **함정**: clippy `then(||)`→`then_some`.
- **검증**: 신규 테스트 2(필터 기준 억제·복귀·is_dir 유지 / 열거 완료 실측 경로) —
  **110 green(app)·78(gui)·36(tree)·clippy 0·비Windows check 통과·B2 3.67MB(별도
  target 실측 — 실행 중 exe가 빌드 잠금)·B3 무변**(std::fs만). 성능: 프로브는 가시
  행만·캐시 1회·조기 종료라 스크롤 체감 없음 예상 — 느린 네트워크 경로 실측은 QA에서.
- **실기 QA**(사용자): ① `11111` 같은 빈/숨김만 폴더 글리프 사라짐 ② 점·숨김 표시
  토글 시 글리프 복귀 ③ 설정 체크 해제 = 현행 복귀 ④ 네트워크/동기화 폴더 스크롤
  체감 ⑤ 빈 폴더에 파일 생성 시(watcher 재로드) 글리프 출현. **앱 종료 후 재빌드 필요**
  (현재 실행 중인 exe는 X-42까지만 포함).

## 릴리스 채널 제출 규칙 명문화 (사용자 지시 — 릴리스마다 반영)

**지시**: winget·choco의 배포 요청 상태를 확인해 **대기 중인 버전이 없으면 릴리스 후
그 버전을 제출**하고, **대기 중인 버전이 있으면 그 채널은 제외**하도록 규칙화하고
릴리스마다 반영할 것.

- 이 판단은 08-02("0.13.0 PR OPEN이라 0.14.0 보류")·08-11("0.15.0을 건너뛰고
  0.16.0으로 한 번만")에서 **건별로 두 번 내린 것과 같은 결론**이라, 개별 판단을
  **상시 규칙으로 승격**한 것. [21 §8 "릴리스 시 채널 제출 규칙"](../21-distribution.md)
  신설 + CLAUDE.md §7 채널 항목에 규칙 링크.
- **채널별 "대기 중" 판정 원천**: winget = 우리가 제출한 PR이 OPEN인가(같은 패키지에
  PR이 겹치면 안 된다 — 병합/닫힘 후 제출) · Chocolatey = 모더레이션 미승인 버전이
  있는가(현재 0.8.1 두 패키지가 07-20 스캔 플래그로 잠김 → **계속 제외** — 기존
  `CHOCO_PUSH` 게이트·사용자 지시와 합치. 승인되면 그 시점 최신 버전부터 재개).
- **제외했다가 재개할 때는 중간 버전을 건너뛰고 최신 버전만** 제출한다(winget은
  중간 버전 생략 무방 — 08-11 확립).
- 세션 메모리에도 등재(`release-channel-submit-rule`) — 릴리스 절차를 밟는 세션이
  규칙을 놓치지 않도록.

## X-42 가상 파일 붙여넣기 — RDP·Outlook·압축 폴더 폴백 (`227ed4c` — main 직커밋, 실기 QA 대기)

**보고**: 맥 → 윈도우 RDP 세션에서 맥 파일을 복사해 붙여넣으면 **탐색기는 되는데
앱에서는 안 된다**. 기능 확인 + 보완 검토 요청 → 검토 후 사용자 승인으로 구현.

### 원인 — RDP 파일 클립보드는 CF_HDROP이 아니다

- 앱 읽기측(clipboard.rs `has_files`/`read_file_list`)이 **CF_HDROP(실경로 목록)
  단일 포맷**이었다(M3-5 규약 — 탐색기↔앱 상호운용에는 충분했다).
- 맥에서 복사한 파일은 **원격 Windows 디스크에 존재하지 않는다** — rdpclip은
  경로 대신 **가상 파일 2종**을 게시한다: `FileGroupDescriptorW`(항목 목록·이름·
  크기·속성) + `FileContents`(항목별 내용 — 붙여넣는 순간 RDP 채널로 지연
  스트리밍). 탐색기는 이 MS 규약(CFSTR_FILEDESCRIPTORW/CFSTR_FILECONTENTS)을
  지원해서 됐고, 우리는 `has_files()` false → 메뉴 비활성/Ctrl+V 무반응.
- **같은 규약을 쓰는 소스가 더 있다**: Outlook 첨부 복사·탐색기 zip 내부 항목·
  MTP 기기(폰) — 전부 이번 폴백으로 함께 열린다. X-38(7-Zip DnD 지연 렌더링)과
  친척이지만 다른 층위다 — 7-Zip은 CF_HDROP을 **늦게** 만들고, RDP는 CF_HDROP을
  **아예 안** 만든다.

### 구현 (1차 슬라이스 — 동기 추출)

- **판정**: `has_files()` = CF_HDROP **또는** FileGroupDescriptorW(신설
  `has_virtual_files()`) — 붙여넣기 메뉴가 가상 파일에도 활성화된다.
  `sync_cut_marks`는 `read_file_list()`가 None을 돌려줘 빈 집합으로 수렴(무해).
- **추출**(`extract_virtual_files` → `extract_virtual_from` 본체 분리):
  `OleGetClipboard()` → `IDataObject`. 원시 `GetClipboardData`로는 안 되는 이유 =
  FileContents는 **lindex(항목 인덱스)** 지정이 필요해 FORMATETC를 받는 OLE
  경로만 가능. 디스크립터(HGLOBAL) 파싱 → 항목별 `GetData(FileContents,
  lindex=i, TYMED_ISTREAM|TYMED_HGLOBAL)` — rdpclip·Outlook은 IStream(64KiB
  루프·크기 미상은 EOF까지), 일부 소스는 HGLOBAL(크기 힌트로 여분 패딩 방어).
- **안전장치**: 디스크립터 이름은 **신뢰 불가 외부 데이터** — `..`·절대 경로·
  드라이브 문자는 항목째 기각(경로 탈출 방지) · cItems가 실제 버퍼보다 커도 담긴
  만큼만(손상 방어) · 항목별 실패 격리(부분 성공 허용 — 전송 엔진 규약 동일) ·
  최상위 이름 충돌은 `nexa_ops::unique_dest` " (2)"(하위 항목은 같은 매핑 추종·
  자식이 부모 디렉터리 항목보다 먼저 와도 성립).
- **배선**(win.rs `paste_virtual`): Ctrl+V·배경 메뉴 paste 동사·고유 붙여넣기·
  pasteInto 4개소 — **CF_HDROP이 있으면 기존 전송 엔진(undo 포함)이 우선**,
  없을 때만 폴백. 클라우드 대상은 무동작(로컬 FS 기록 불가 — 업로드 연계는 후속).
  완료 시 양쪽 재로드.
- **함정(빌드에서 발견)**: `FILEDESCRIPTORW`는 **packed 구조체** — 배열 필드
  참조가 E0793(정렬 UB). 값 복사(`let buf = fd.cFileName`)로 우회.

### 검증

- **테스트 2건 신설**(총 110 green): ① 디스크립터 파싱 — 한글 이름·하위 경로·
  크기 미상·탈출 2종 기각·cItems 과대 방어 ② **종단** — `#[implement(IDataObject)]`
  가짜 소스(rdpclip 형상: 디스크립터 HGLOBAL + SHCreateMemStream)로 실 클립보드
  비접촉 추출: 파일 내용·하위 폴더·64KiB 초과(100KB) 스트림·기존 파일 무손상·
  " (2)" 충돌까지. `extract_virtual_from` 분리가 이 테스트를 가능하게 했고,
  후속 DnD 수신 재사용 지점이기도 하다.
- clippy `-D warnings` 0 · 비Windows check 통과(warning 14건은 svg 모듈 기존
  dead-code — 변경분은 전부 `#[cfg(windows)]` 안) · **B2 3.67MB**(release 실측
  3,667,456 B) · **B3 통과 — 신규 DLL 0**(OleGetClipboard는 ole32 — M3-5부터 사용).
- **실기 QA = 사용자 몫**(맥 RDP 환경 필요): 맥 Finder 복사 → 앱 붙여넣기(단일·
  다중·폴더·대용량·한글 이름). Outlook 첨부·zip 내부 항목 붙여넣기도 같은 경로.

### 잔여 (후속 슬라이스 — TODO X-42 β)

- **워커화**: OleGetClipboard의 IDataObject는 STA 구속이라 현재 동기 추출(대기
  커서만) — 대용량은 완료까지 창이 멈춘다. RDP 채널이 느려 체감 큼 → 스트림
  마샬링(CoMarshalInterThreadInterfaceInStream) + 진행 창 연계.
- **undo 미기록**(원본 경로가 없어 전송 엔진 밖) — "생성 파일 삭제"형 undo 검토.
- **DnD 수신**(dnd.rs `hdrop_paths`도 CF_HDROP 전용) — `extract_virtual_from`
  재사용으로 RDP·Outlook 드래그 앤 드롭도 열 수 있다.
- 위키 [기능-파일-조작](../wiki/기능-파일-조작.md) 갱신은 릴리스 트랜잭션에서.
