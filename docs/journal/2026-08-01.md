# 2026-08-01 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## winget 설치형 #404528 모더레이터 피드백 처리 (사용자 승인 — 07-31 점검 후속)

- **조치**: [#404528](https://github.com/microsoft/winget-pkgs/pull/404528)
  `Needs-Author-Feedback`(07-31 부착 — "`DisplayVersion`이 `PackageVersion`과 동일값,
  제거하라") 대응 — 포크 `kiros33/winget-pkgs` 브랜치 `SosomLab.NexaDir-0.8.1`의
  installer.yaml에서 `AppsAndFeaturesEntries.DisplayVersion: 0.8.1` 1줄 제거
  (**커밋 `7e49694`**, gh contents API — 계정 kiros33 전환 후 원복, 07-27 전례 동일).
- PR 코멘트로 조치 회신([#issuecomment-5144288306](https://github.com/microsoft/winget-pkgs/pull/404528#issuecomment-5144288306)) —
  author 응답+push가 `Needs-Author-Feedback` 해제 트리거(봇 재처리 대기).
- **로컬 사본 동기**: `packaging/winget/0.8.1/SosomLab.NexaDir.installer.yaml` 동일 제거.
  (0.11.0·0.12.0 Portable 매니페스트에는 `DisplayVersion` 자체가 없음 — 확산 불요.
  다음 설치형 버전 매니페스트 작성 시에도 넣지 않는다.)
- **잔여**: 검증 파이프라인 재실행 + `Policy-Test-1.2` waiver 판단은 모더레이터 몫 —
  author 쪽 블로커는 해소. 상태 추적만.
