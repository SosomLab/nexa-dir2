# 2026-07-12 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST)** — 실제 착수는 첫 커밋 이전.

## 경로바 밀착 (`feat/m2-navbar-flush`, 사용자 지시)

- 4px "구분"의 정체 확인: 의도한 색이 아니라 **어떤 위젯도 칠하지 않는 틈**(백버퍼 미도색 검정 노출).
- `799819f`: 경로바를 네비 버튼 끝에 바로 이어 붙여 틈 제거.

## 네비 바 간격 폴리시 (`feat/m2-navbar-polish`, 사용자 지시)

- **사용자 지시**: [←][→][↑]는 간격 없이 붙이고, ↑ 옆 경로바는 4px 여유만 두고 배치.
- `7d947df`: Toolbar 고정 버튼 폭 모드(`with_button_width` — 연속 배치·글리프 중앙 정렬) 신설.
  패널 네비 바 = 3×26px 연속 + 4px(@96dpi, DPI 비례) + 경로 바. 전역 도구 모음도 버튼 간 간격 제거.

## 패널별 네비 버튼 (`feat/m2-panel-navbar`, 사용자 지시)

> **사용자 지시**: 원본처럼 이전/다음/상위 버튼을 **각 패널의 경로바 옆**에 두고 그 패널의 활성 탭에 동작하게.
> (M2-3의 전역 도구 모음만으로는 원본 docs/20 §2 "네비게이션 바(패널별)" 미충족 — 지적 타당.)

- `1f97534`: Panel에 `navbtns: Toolbar` 재사용 배치 — 네비 바 행 = **[←][→][↑](96px) + 경로 바(잔여)**,
  탭 바로 하단(원본 §2 배치). 버튼 = **그 패널의 활성 탭 히스토리** 대상(drain_actions에서 BTN_* 수거).
  전역 도구 모음(활성 패널 대상)은 원본에도 있으므로 공존 유지. 테스트 97 green.

## M2-4 테마 시스템 — 시스템/라이트/다크 (`feat/m2-theme`)

> 원본 docs/39 §3 이식. 팔레트 토큰(M1-1 기이식) 위에 **모드 계층**만 추가 — 전 위젯이 토큰만 참조하므로
> 전환 = 실효 테마 재해석 + 전체 재그리기(런타임 스타일 시스템 불요).

- **구현**(`3bc8283`): `ThemeMode{System·Light·Dark}` — System은 레지스트리 `AppsUseLightTheme` 추종,
  `WM_SETTINGCHANGE`에서 재해석(OS 테마 실시간 반영) · 보기 메뉴 **테마 라디오**(체크 동기) + **F6 순환** ·
  타이틀바 `DWMWA_USE_IMMERSIVE_DARK_MODE` 동기. 시작 기본 = 다크(DR-5), 모드 영속은 M2-5.
- **B3**: dwmapi(DWM)·advapi32(RegGetValueW) 임포트 추가 — 로컬 게이트 사전 검증·화이트리스트 갱신.
- **실기(픽셀 검증)**: panel_bg_alt 픽셀 — 다크 `#1F242B` → F6 라이트 `#F5F7FA` → F6 시스템(OS 라이트
  추종 확인) → F6 다크 복원. 메뉴 라디오 경로는 mouse_event 플레이크로 키보드(F6) 검증 대체(로그로
  앱 정상 확인 — M2-3 QA 교훈과 동일 원인).
- 후속: 모드 영속(M2-5) · 테마팩/밀도(원본 §5 — M5) · 행 선택 알파 브러시 토큰화.

## M2-3 메뉴 바·도구 모음·상태바 (`feat/m2-chrome`)

> 원본 docs/20 §1·§2의 크롬 영역 — 전면 커스텀 드로잉(자식 HWND 0 유지).

- **위젯 3종**(`f146a7c`): `MenuBar` — 드롭다운을 **오버레이**로(창 1개 원칙: 콘텐츠 위에 마지막에 그림),
  제목 토글·열린 상태 hover 메뉴 전환·명령 id 통지·**체크 표시**(토글 동기)·단축키 힌트·구분선·외부 클릭/Esc 닫기.
  `Toolbar` — 글리프 버튼(←→↑⟳)+명령 통지. `StatusBar` — 좌/우 텍스트(변경 시에만 무효화). 테스트 6.
- **배선**(`cf7fb49`): 레이아웃 = 메뉴/도구/[좌║우]/상태바 적층 · **명령 체계(CMD_*)** — 메뉴(파일: 새 탭·탭
  닫기·종료 / 보기: 숨김·점 ✓·새로 고침 F5)와 도구(활성 패널 네비)가 같은 run_command 사용, Ctrl+H/.도 경유해
  **메뉴 체크와 상시 동기**. 메뉴 열림 시 마우스 전용 라우팅(오버레이 아래 hover 차단). 상태바 = [좌/우]
  항목/선택/탭 · 필터 상태·평균 페인트.
- **실기**: 메뉴 "보기" → 숨김 토글 **62→44행** · 도구 ↑ → C:\Users 이동 · Esc/외부 클릭 닫힘. exe 0.40MB·B3 통과.
  **QA 교훈**: mouse_event 자동화에서 클릭 간 대기 부족 시 입력 유실(플레이크) — 클릭 전 250ms·다운/업 50ms 간격
  필요. 무반응처럼 보였던 1차 스모크는 이것이 원인(디버그 로그로 격리, 앱 정상).
- 후속: 도움말 메뉴·명령 팔레트(M5)·도구 모음 뷰 전환 버튼(뷰 모드 도입 시)·상태바 잡 진행률(M3).

## M2-2 듀얼 패널 + 패널별 탭 (`feat/m2-panels`)

> 원본 docs/20(레이아웃)·F20(패널별 탭) 이식 — M2 최대 항목(P0). 드래그 재배열·잠금·세션 복원은 후속(M2-5 연계).

- **TabBar 위젯**(`fff90ed`): 도메인 비종속 — `TabAction{Switch·Close·New}` 통지. 활성 탭 = 패널 배경+상단
  accent 2px(**비활성 패널은 border 색** — 활성 패널 시각화), 닫기 ×(마지막 탭 불가)·[+]. 테스트 3.
- **Panel 추출**(`6d8e2c0`): `panel.rs`(플랫폼 중립) — 탭 바+경로 바+리스트 적층(§1) · 탭 = 독립 뷰+**독립
  히스토리**(§3) · 탭 관리·네비게이션 전부 win.rs에서 이동 · `drain_actions`(위젯 동작 수거). 테스트 4
  (적층 레이아웃·개폐 ≥1 유지·**탭별 독립 히스토리**·실패 시 위치 유지).
- **win.rs**: 듀얼 배치(스플리터 드래그·비율·MIN_PANEL 200·드래그 중 accent) · 활성 패널(클릭/**Tab 전환** —
  커맨더 규약) · **Ctrl+T/W/Ctrl+Tab** · 키보드·휠 = 활성 패널 · 타이틀 = `[좌/우] 경로 [행·탭 n/m]`.
- **실기**: Tab 전환 [좌]→[우] · Ctrl+T 탭 2/2 · 탭2만 `.android` 진입(**탭별 독립 확인**) · Ctrl+Tab 순환
  (탭1 = 원 경로 유지) · Ctrl+W 닫기 후 활성 조정 · 좌 패널 복귀 시 원 상태. **듀얼 RSS 25.1MB**(B1 내) ·
  exe 0.38MB·B3 통과. 테스트 90 green(tabbar 3·panel 4)·clippy 0.
- 후속: 탭 드래그 재배열·잠금/고정 · 우 패널 숨김 토글(단일↔듀얼) · 세션 저장/복원(M2-5) · 휠 = 커서 아래 패널.

## M2-1 계층 경로 바 α (`feat/m2-pathbar`) — M2 착수

> 원본 NexaPathBar(docs/27) α 범위 이식. **네비게이션 비종속**(§3: 위젯은 통지만·이동은 호스트) 원칙 유지.

- **위젯**(`c2eddd6`): `pathbar.rs` — `split_path` 로컬 FS 세그먼터(드라이브 `C:`→`C:\`) · 브레드크럼(세그먼트
  클릭=이동 통지 `take_navigation()`·**현재 세그먼트 비활성**·hover 강조는 클릭 가능만·하단 경계선) ·
  **편집 모드**(우클릭 진입·타이핑/BS·**Enter 제출·Esc/포커스아웃 취소 복귀** §2·accent 테두리·끝 커서).
  세그먼트 히트 범위는 페인트 시 캐시(측정은 DrawCtx 한정). `InputEvent::RightDown` 신설. 테스트 5.
- **배선**(`356f9b9`): 상단 바+리스트 레이아웃(`layout` — WM_SIZE·DPI 공용) · 좌클릭 y-라우팅(편집 중 클릭=
  포커스아웃 취소) · 편집 중 키/문자 분기(경로 입력은 공백 허용) · `apply_source`에서 이동 완료 시 `set_path`.
- **실기**(ClientToScreen 좌표 자동화): "C:" 세그먼트 클릭 → `C:\`(23행) · 우클릭 편집 + `C:\Users\kiros33`+Enter →
  복귀(62행) · 정상 종료. exe 0.36MB·B3 통과. 테스트 83 green(pathbar 5)·clippy 0.
- α 범위 밖(후속 β/γ): 오버플로 `…`·UNC·**자동완성(PATH-SUG)**·형제 `▾` 드롭다운·드롭 타깃·경로 복사 메뉴.

## M1-9 게이트 실측 · M1 완료 `0.2.0` (`feat/m1-gate`)

> **M1 완료 게이트** — 원본 AC5(NFR-P1/P2) + DR-2 예산. Windows 실기·릴리스 빌드.

- **계측**(`a7ff9df`): run() 진입 시각 앵커 → 첫 WM_PAINT 완료 = "첫렌더 Nms" 타이틀 표시(벤치 리셋과 무관 보존).
- **실측 결과(전 게이트 통과)** — 픽스처: %TEMP% 실제 파일 100k(플랫)·10k:
  - **100k 첫 렌더**: 106/115/115ms → **중앙값 115ms < 150ms** ✓ (Tree::open 열거 100k 포함. 10k는 42ms)
  - **스크롤 60fps**: F3 200프레임 벤치 **2,098µs/프레임** — 16.7ms 예산의 13% ✓
  - **B1 유휴 RSS**: 10k 폴더 로드(docs/18 §5 기준) 유휴 60s 3회 중앙값 **27.87MB ≤ 30MB** ✓
  - 참고 관측: 100k 스트레스 로드 시 53.2MB(예산 외 — 노드당 ~250B, arena 회수는 원본 A-3 후속과 동일 과제)
  - B2 exe 0.35MB · B3 인박스만(기측정) ✓
- **M1 뷰어 코어 완료** → `0.2.0` 태그. 원본 M1 패리티 중 뷰어 축(리스트·컬럼·정렬·교차 선택·러버밴드·키보드·
  타입어헤드·아이콘·네비게이션) 달성. 다음: M2 셸 골격(경로 바·듀얼 패널/탭·메뉴·테마·설정·상주 규율).

## M1-8 네비게이션 — 히스토리·진입·위로·필터 토글 (`feat/m1-navigation`)

> 뷰어를 "탐색기"로 — 진입/이동. 파일 실행(ShellExecute)·경로 바는 후속(M3·M2-1).

- **위젯**(`3bd09df`): `replace_source`(스크롤·캐럿·타입어헤드 리셋, **컬럼·정렬 유지+새 소스 재적용** —
  원본 PanelView.SortKeys 지속 규약)·`row_at`/`marker_hit` 공개(더블클릭 진입 vs 마커 펼침 구분).
- **앱**(`8148510`): `nav.rs` History(push 시 앞으로 절단·동일 경로 무시·replace — 순수 테스트 3) ·
  **더블클릭(CS_DBLCLKS)/Enter = 캐럿 폴더 진입** · **Alt+←/→/↑**(WM_SYSKEYDOWN)·**마우스 X버튼** =
  뒤로/앞으로/위로 · **Ctrl+H/Ctrl+.** = 숨김/점 파일 토글(`Tree::open_filtered` 재열기 — 히스토리 무이동).
  열기 실패(권한 등) 시 히스토리 위치 복원.
- **실측**(릴리스): Enter 진입 `C:\Users\kiros33` → `.android`(2행) → Alt+↑ 위로 → Alt+← 뒤로 —
  **타이틀 경로 왕복 확인**. Ctrl+H 재열기 정상. RSS 24.5MB·exe 0.35MB·B3 통과. 테스트 80 green(nav 3·gui 1)·clippy 0.
- 유보: Alt+화살표 중 Alt+→ 실기 확인은 뒤로 이후만 유효(정상) · 파일 Enter=실행(M3-4 셸 연동) ·
  필터 기본값 영속(M2-5) · 경로 바(M2-1).

## M1-7 셸 아이콘 — LRU 캐시 + 속도 제한 로딩 큐 (`feat/m1-icons`)

> 원본 A-4(`IconKey`+`ShellIconCache`) 이식 — "빠른 스크롤 시 셸 호출 폭주 → 크래시" 교훈의 속도 제한 모델.

- **위젯**(`033182d`): 아이콘 어휘 추상 — `RowSource::icon → (키, 힌트)`(기본 None)·`DrawCtx::draw_icon`(기본 no-op).
  트리 셀 = 마커·들여쓰기·**아이콘(16px)**·이름.
- **캐시**(`0bffc72`): `icons.rs` — `icon_key`(폴더="dir"·확장자 공유·**파일별 고유 아이콘**[exe·lnk·ico·cur·msi·scr]=
  경로 키, 원본 IconKey 이식) + `IconStore<T>`(**LRU 256**·키 dedupe 큐·틱 배치 — 제네릭이라 전 플랫폼 테스트) +
  `shell::ShellIcons`(SHGetFileInfoW — 확장자/폴더는 `SHGFI_USEFILEATTRIBUTES`로 **파일 접근 없이** 타입 아이콘,
  exe류만 실경로. 축출·drop 시 DestroyIcon). 배선: `DwCtx::draw_icon`(DrawIconEx→메모리 DC)·**WM_TIMER 80ms 틱당
  4개 로드**(원본 MaxConcurrent·TickMs 계승)·로드 완료 시 재그리기. 미로드 행은 공백 → 틱 후 채워짐.
- **B3 워크플로 검증**: `shell32.dll` 신규 임포트를 **push 전 로컬 게이트가 검출**(2차 red의 재발 방지책이 작동) —
  근거와 함께 화이트리스트 갱신.
- **실측**(릴리스·70행): 벤치 **2,547µs/프레임**(DrawIconEx 포함) · **유휴 RSS 27.95MB** — shell32/아이콘 인프라
  로딩으로 **+10MB 점프, B1(≤30MB) 내지만 여유 2MB**. ⚠ M2-8(유휴 트림·EmptyWorkingSet 자니터, 원본 A-4 후속과
  동일)의 최우선 감시 항목으로 지정. exe 0.34MB. 테스트 74 green(icons 7)·clippy 0.

## M1-6 키보드 네비 전체 + 타입어헤드 (`feat/m1-keyboard`)

> 원본 docs/32(사용자 확정 규약) 이식 — 코어 `find_prefix`(A/B/C)는 기이식, 이번 작업 = 버퍼·캐럿 네비·배선.

- **캐럿 키보드 네비**(`48ed980`): ↑↓·PgUp/PgDn·Home/End = **캐럿 이동 + 단일 선택 + 스크롤 추적**(탐색기 규약,
  기존 "스크롤만"에서 승격) · Shift+이동 = anchor 범위 · Ctrl+이동 = 캐럿만 · **Space/Ctrl+Space = 선택 토글**
  (docs/32 §7 결정 1 — Space는 타입어헤드 제외) · **→ = 펼침/이미 펼침이면 첫 자식, ← = 접힘/부모로**(docs/07 §8 완성).
- **타입어헤드**: `typeahead.rs` — 버퍼 누적·**1000ms 타임아웃**·**반복 단일키 cycle**(누적 대신 다음 매치)·Backspace
  (원본 `TypeAheadBuffer` 이식, **시각 주입**으로 순수 로직·전 플랫폼 테스트). 범위 = **가시 스트림 위치상대+wrap(C)**
  — `RowSource::find_prefix` → 코어 위임(A/B 설정 노출은 M2). 매치 = 단일 선택+캐럿+스크롤. **HUD** = 본문 좌하단
  플로팅 배지(§7-A — 레이아웃 불변·"찾기: 버퍼"·accent 테두리), `tick()`(win.rs WM_TIMER 250ms) 타임아웃 소거.
- **배선**(`e5a2386`): WM_CHAR(제어문자·Space·**Ctrl 조합 제외** — Ctrl+H=0x08 충돌 방지)·`now_ms` 주입(Instant 기반) ·
  WM_KEYDOWN 수식키(GetKeyState)·WM_TIMER·`InputEvent::Key{key,shift,ctrl}`/`Char` 개편.
- **실측**(릴리스·62행): 캐럿 이동/타입어헤드 점프 정상 · **Shift+End = "선택 61"** · RSS 17.8MB·exe 0.33MB·B3 통과.
  **QA 교훈**: WScript SendKeys는 확장 키(END·화살표) 전송 시 **Shift를 일시 해제**(NumLock 조작 포함) —
  Shift 조합 검증은 `keybd_event`/SendInput으로. 디버그 로그로 원인 격리 후 도구 특성으로 판명(앱 정상).
- 검증: `cargo test --workspace` 69 green(신규 typeahead 4·gui 8·source 1) · clippy 0.
- 후속 유보: A/B 범위·타임아웃 설정화(M2 설정 시스템)·한글 초성/IME 미리보기(원본 §9와 동일)·Alt+화살표(M1-8 네비게이션).

## M1-5 ★ 인라인 펼침 + 교차폴더 다중 선택 + 러버밴드 (`feat/m1-select`)

> **플래그십**(원본 docs/07 — 제1 차별화). 코어 선택 모델(OrderedSet·anchor·교차 선택)은 M0 이식분 그대로,
> 이번 작업 = 위젯 선택 UX + 배선.

- **위젯**(`df012bb`): 클릭 시맨틱 분리 — **삼각형(마커 존) = 인라인 펼침**(§1-1) vs **본문 = 선택**(§1-2).
  `SelectOp{Single·Toggle(Ctrl)·RangeTo(Shift)}`를 `RowSource`에 위임 · **러버밴드**(빈 본문 영역 드래그 —
  세로 교차 행 `select_span`·accent 외곽선·시작 시 선택 해제) · `Ctrl+A`(SelectAll)·`→/←`(캐럿 행 펼침/접힘 §8,
  부모 이동은 M1-6) · 선택 행 배경 `sel_bg` 토큰 신설(원본 알파 브러시 → 사전 블렌드) · 캐럿 1px accent 테두리.
- **배선**(`67e784b`): `TreeSource` — 코어 `select/select_range/select_all_visible`에 위임(러버밴드 span =
  Single+range 재사용). **교차폴더 선택 테스트**(AC2: dirA 자식 + 루트 파일 동시 선택 → `selected_paths` 2건).
  win.rs — MK_CONTROL·`GetKeyState` Ctrl+A·VK_RIGHT/LEFT·타이틀 선택 수.
- **실측**(릴리스·62행): SendKeys Ctrl+A → 타이틀 "선택 62" 반영 · 하이라이트 포함 벤치 **1,540µs/프레임** ·
  RSS 18.20MB · exe 0.31MB · B3 통과. 테스트 60 green(신규 gui 4·source 1)·clippy 0.
- 후속 유보: 더블클릭 진입(M1-8 네비게이션)·Shift+↑↓ 범위·캐럿 id 기반화(M1-6)·드래그 DnD(M3-5).

## M1-4 컬럼 시스템 (`feat/m1-columns`)

> 원본 docs/23 스펙 이식 — 코어 정렬(COL-2a `SortSpec`)은 이미 이식돼 있어 **UI 측만** 신규.

- **위젯**(`57374d5`): `columns.rs` `Column{key·title·width·min_width·align·sortable·resizable}`(컬럼=데이터, 의미는
  `RowSource`에 위임 §1) · 헤더 행(본문 위 고정·컬럼 경계선·`header_bg`) · **정렬 3상태 순환**(단순 클릭=단일 리셋,
  ▲→▼→없음) + **Shift 다중열**(기존 정렬 ≥1일 때 추가/방향 순환/제거 — COL-3) · 표시 규약 = **화살표 이름 앞·순번
  원문자 이름 뒤**(§4 사용자 확정) · **드래그 리사이즈**(경계 핸들 [right-6,right+2)·min_width) · **가로 스크롤**
  (HWheel·총폭 클램프). `RowSource` += `cell(row,key)`·`set_sort(keys)`, `InputEvent` += HWheel·MouseMove/Up·shift,
  `DrawCtx` += `text_width`(우측 정렬용). 컬럼 미설정 시 M1-3 단일 컬럼 경로 유지. **테스트 23 green**(3상태·다중열
  추가/제거·리사이즈 min·가로 클램프·헤더 라벨·우측 정렬 x좌표).
- **배선**(`47a9d28`): `source.rs` 5컬럼 셀 값 — 확장자·크기(human)·수정한 날짜(**civil_from_days 순수 구현** + TZ
  오프셋 주입, 외부 crate 0 유지)·종류(폴더/EXT 파일/바로가기) + `set_sort`→`nexa_tree::SortSpec`(폴더 우선 유지).
  `dw.rs` **말줄임표 트리밍**(`CreateEllipsisTrimmingSign`, 컬럼 폭 초과 셀)·캐시 키에 폭 포함·`text_width`.
  `win.rs` 5컬럼(dpi 스케일)·마우스 캡처(리사이즈)·Shift+휠/WM_MOUSEHWHEEL·`GetTimeZoneInformation`
  (feature `Win32_System_Time`).
- **실측**(릴리스·62행): F3 벤치 **1,813µs/프레임**(M1-3 1,673 대비 +8% — 5컬럼 비용 미미) · RSS 18.14MB ·
  exe 0.31MB · **B3 로컬 게이트 통과**(push 전 검증 — 신규 임포트 없음). 테스트 55 green·clippy 0.
- 초안 유보(후속): DPI 변경 시 사용자 리사이즈 폭 보존 문제(현재 미재스케일) · 컬럼 설정 모달(COL-4)·헤더 드래그
  순서 변경 — 원본 §6은 M2+.

## M1-3 가상화 파일 리스트 초안 (`feat/m1-virtual-list`)

> **실제 파일시스템 첫 표시** — nexa-tree 가시 노드 평면 스트림을 `RowSource`에 배선 + ADR-0002 §5 이행.

- **위젯 확장**(`199d67a`): `RowSource`를 `RowItem{text·depth·marker}` 기반으로 — 들여쓰기(`indent_w`)·
  펼침 마커(▸/▾, text_dim)·`InputEvent::MouseDown` 신설·클릭=행 활성화(`toggle`, 구조 변경 시 재클램프+전체 무효화).
  nexa-gui 테스트 20 green.
- **배선·이행**(`856ced5`): `source.rs` `TreeSource`(평면 스트림→RowItem 투영·클릭=expand/collapse, 플랫폼 중립 테스트 2) ·
  win.rs 실 디렉터리 로드(argv[1]→%USERPROFILE%→C:\, **초안=동기 열거** — 백그라운드 열거는 M2 상주 규율) ·
  WM_LBUTTONDOWN 라우팅 · 타이틀=경로/행수/평균 페인트 µs. **ADR-0002 §5 이행**: `gdi.rs`·F2 전환 삭제(DW 단일 경로),
  `dw.rs` 텍스트 키 레이아웃 캐시(NO_WRAP·좌정렬이라 폭 무관, DPI 변경 시 flush, cap 4096).
- **실측**(Windows 실기·릴리스): 실 트리(62행) F3 벤치 **1,673µs/프레임** — 캐시로 M1-2 스파이크(4,373µs) 대비 −62%.
  유휴 RSS **18.17MB** · exe **0.27MB** — 전 게이트 내. 기동·클릭 펼침/접힘·WM_CLOSE 정상.
- 검증: `cargo test --workspace` 48 green(신규 gui 4·source 2) · clippy 0 · fmt.

## CI B3 red 2차 · 게이트 스크립트 단일화 (main 직행)

- M1-3 push의 CI가 또 B3 fail — `bcryptprimitives.dll`(레이아웃 캐시 `HashMap`의 RandomState가
  BCryptGenRandom 사용, OS 인박스). 같은 유형 재발이라 **화이트리스트를 `scripts/budget-b3.ps1`로
  단일화**(CI·로컬 공용) — push 전 로컬 실행으로 사전 검증 가능. 로컬 통과 확인 후 push.

## CI B3 게이트 red 복구 (main 직행)

- M1-2 병합 push의 CI windows job이 **B3 화이트리스트 게이트에서 fail** — ADR-0002로 늘어난 임포트
  `dwrite.dll`·`combase.dll`(둘 다 OS 인박스)이 목록에 없었음. 로컬 dumpbin 재실측으로 확인 후
  화이트리스트 갱신(`5a613b3`) → CI green 복구. **교훈**: 인박스 DLL이 늘어나는 변경은 ci.yml·docs/18을
  같은 커밋에서 갱신(docs/18 §4에 규약 명기). M1-2에서 ADR 문서에만 적고 게이트를 안 돌린 것이 원인.

## M1-2 ADR-0002 렌더링 확정 (`feat/m1-adr0002-render`)

> GDI vs DirectWrite GDI interop을 **같은 창에서 실측 비교** 후 확정 — [07 ADR-0002](../07-adr-0002-rendering.md) Accepted.

- **DirectWrite interop 백엔드**(`dw.rs`): `IDWriteBitmapRenderTarget`(자체 메모리 DC = 더블 버퍼) +
  `IDWriteTextLayout` → 커스텀 `IDWriteTextRenderer`(`#[implement]` COM 콜백) → `DrawGlyphRun(COLORREF)`.
  배경은 동일 DC에 GDI ETO_OPAQUE 채우기 — `nexa_gui::DrawCtx` 어휘 변경 없이 구현(추상 검증).
  `windows-core` 직접 의존 추가(매크로 전개 경로, DR-8 원장 기록) · feature `Win32_Graphics_DirectWrite`.
- **비교 하네스**(`win.rs`): F2 = 백엔드 전환 · F3 = 200프레임 스크롤 벤치(동기 UpdateWindow) ·
  타이틀바에 평균 페인트 µs 표시 · `NEXA_RENDER=gdi|dw` 강제. 실측은 WScript.Shell SendKeys로 자동화.
- **실측**(릴리스·1200×800·100k 행·Windows 11 실기): 벤치 **GDI 6,072µs vs DW 4,373µs(−28%)** —
  행당 TextLayout 생성 포함(캐시 미적용 상한치)에도 DW가 빠름. 유휴 RSS 13.30 → **17.40MB(+4.1MB)** ·
  exe 0.21 → **0.23MB** — 전 게이트 내.
- **확정**: DirectWrite GDI interop 채택(속도·한글 폴백 일관성·TextLayout 확장성[트리밍·히트테스트 → M1-4~6]).
  기본 백엔드 DW 전환(스모크: RSS 17.07MB). GDI 경로·F2/F3은 육안 비교용 유지 → **M1-3에서 제거** +
  가시 행 레이아웃 캐시 도입 예정. docs/01 §3·docs/10(ADR 색인·crate 원장) 현행화.
