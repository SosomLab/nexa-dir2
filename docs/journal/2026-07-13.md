# 2026-07-13 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST)** — 실제 착수는 첫 커밋 이전.

## 느린 재클릭 리네임·필드 정렬 (main 직커밋 `7765cf9`·`4cc7e3d`)

- **기선택 항목 느린 재클릭 = 이름 바꾸기**(탐색기 관례, 사용자 지시): 직전 클릭과 같은
  항목(패널+경로 기준)이고 **간격 ≥ 1s**일 때만 진입 — 짧은 간격은 더블클릭 시도로 무시
  (빠른 2연타는 WM_LBUTTONDBLCLK 분기로 자연 방어). 진입 시 OLE 드래그 후보 미설정.
- **리네임 필드 텍스트 정렬**: 편집 진입 시 테두리+여백만큼 이름이 우측으로 밀리던 것 —
  필드를 텍스트 왼쪽으로 3px 확장해 편집 텍스트 x = 일반 표시 x(밀림 없음).

## 편집기 3차·행 히트영역 수정 (main 직커밋 `a60447e`)

> 실기 QA 3차: ① 리네임 뒷부분(.ico) 잘림·깨짐 ② 편집 텍스트 드래그가 1자 선택 후 고착
> ③ 미선택 행 드래그가 파일 DnD로 동작 ④ 종류 컬럼 오른쪽 공간을 빈 영역으로 원함.

- **① 런 분할 그리기 자체가 원인**(백엔드 말줄임 트리밍·이음새) → `DrawCtx::text`
  (배경 없는 텍스트, dw 구현) 신설 — 선택 하이라이트=fill_rect + **텍스트 1회 그리기**.
- **② 행 프레스마다 OLE 파일 드래그 후보(drag_press)** 가 걸려 모달 DoDragDrop이 마우스를
  삼킴(release 미전달=선택 고착) → **기선택 행만 DnD 후보**·리네임 중 제외.
- **③④ 원본 B-4 완전 이식**: 미선택 행 드래그=러버밴드(클릭만=단일 선택 동일) ·
  **행 히트영역=컬럼 총폭**(종류 오른쪽 = 빈 본문: 선택 하이라이트 제외·클릭=해제·
  드래그=밴드·우클릭=배경 메뉴·캐럿 테두리도 컬럼 폭까지).

## 편집기 캐럿 2차 결함 수정 — 런 지움·커닝 오차·드래그 선택 (main 직커밋 `25bce68`)

> 실기 QA 2차: ① 리네임 진입 시 이름부 소실(".png"만 보임) ② 캐럿 표시 위치 ≠ 실제 편집
> 위치(클릭 후 Backspace가 다른 문자 삭제) ③ 드래그 선택 안 됨.

- **① 원인**: `text_opaque`는 **clip 전체를 bg로 선채움** — 선택 하이라이트용 3개 런에 같은
  필드 rect를 clip으로 넘겨 마지막 런이 앞 런들을 지움. → 필드 bg 1회 선채움 + **런 자신의
  x 구간만 clip**으로 전달.
- **② 원인**: 문자별 폭 합산(개별 `text_width`)이 전체 문자열 레이아웃 커닝과 어긋나 캐럿
  x·클릭 매핑이 누적 오차로 밀림. → 문자 경계 오프셋을 **접두사 폭**으로 측정(레이아웃과
  동일 커닝. 편집 필드는 입력 시에만 페인트라 비용 수용).
- **③**: `EditState` click(캐럿+시작점)→drag(범위 확장)→release(무이동=선택 해제) —
  경로바·리네임 배선(panel MouseUp 라우팅 추가). 좌측 잘림 시 첫 가시 문자 경계 절단
  (렌더러 좌측 클립 비보장). 테스트 +1(드래그 범위·단순 클릭 구분) — 총 136 green.
- 경로바 브레드크럼 밀도 변경(사용자 지시): 구분자 `›`→`\`·세그먼트 여백 2px(`98a460d`).

## UX 결함 수정 — 재로드 무간섭 갱신·편집기 캐럿 모델 (`fix/ux-reload-editor`)

> 실기 QA(사용자, 07-13) 6건: ① 우측 삭제 시 좌측 펼침 폴더 닫힘 ② 우측 새 폴더 시 좌측 선택
> 취소 ③ 경로바 편집 진입 시 전체 선택 원함·Ctrl+A/방향키 미동작 ④ 리네임도 동일 ⑤ 클릭 캐럿
> 이동 불가 ⑥ 긴 경로가 앞 기준으로 잘림(+캐럿을 세로바로).

- **S1 — 재로드 무간섭 갱신**(①② — 원본 NAV-UPFOCUS 계승·M3-6 선행): `reopen_filtered`가
  경로 기준 스냅샷(펼침[가시 순=부모 우선]·선택·캐럿·스크롤) → 재열기 → 복원(expand_path·
  select·restore_view). 소실 항목 개별 무시. rows `source_mut`/`restore_view`·source `tree_mut`
  신설. 통합 테스트(외부 변경 후 보존+새 항목 반영).
- **S2 — 편집기 캐럿·선택 모델**(③④⑤ — `edit.rs` 신설, 경로바·리네임 공용 `EditState`):
  캐럿 이동(←→/Home/End·Shift 선택)·Ctrl+A·Delete·선택 대체 입력·**세로바 캐럿**(`_` 대체)·
  선택 하이라이트·**클릭 캐럿 배치**(paint가 문자 경계 오프셋 캐시 → 최근접 경계·필드 밖
  클릭=취소 유지). 경로바 진입=**전체 선택**·리네임=파일 **이름부**/폴더 전체 선택(탐색기
  관례). win.rs 편집 키 라우팅(`edit_key_of`)·IME는 캐럿 앞 텍스트 기준.
- **S3 — 경로바 끝 정렬 오버플로**(⑥ — 원본 breadcrumb 끝 정렬 계승): 편집 필드=캐럿 가시
  좌측 잘림(캐럿=끝이면 끝 정렬) · 브레드크럼=뒤(최근 폴더)부터 역산해 앞 세그먼트 `…` 생략.
- **검증**: 워크스페이스 **135 green**(edit 6·pathbar/rows 시맨틱 갱신·reopen 통합) ·
  clippy 0 · B3 통과 · 릴리스 재기동. 실기 QA 대기(재로드 보존·편집 UX 6건 재확인).

## M3-5 QA 결함 수정 — 발신 CF_HDROP·충돌 확인창 (`fix/m3-5-dnd-conflicts`)

> 실기 QA(사용자, 07-13): ✅ 탐색기→앱 붙여넣기·앱→탐색기 붙여넣기(탐색기 측 덮어쓰기 창 포함)·
> 탐색기→앱 DnD·드롭 후 Undo. ❌ ① 앱→탐색기 드래그 🚫 ② 패널 간 드래그 안 됨 ③ 재붙여넣기
> 시 확인 없이 건너뜀/취소 표기.

- **①② 원인 = 발신 데이터 객체에 CF_HDROP 부재**: `SHCreateDataObject`(절대 PIDL)는 셸
  IDList 포맷만 내고 CF_HDROP을 렌더링하지 않음 → 탐색기도, CF_HDROP만 읽는 자기 수신부도
  거부(동일 원인). **수정**: CF_HDROP을 직접 제공하는 최소 `IDataObject`(FileListDataObject —
  GetData/QueryGetData/EnumFormatEtc=SHCreateStdEnumFmtEtc·나머지 E_NOTIMPL) 자체 구현.
  DROPFILES 빌더는 `hglobal_file_list`로 클립보드 쓰기와 공용화. 부수 효과: 교차폴더 선택
  드래그 지원(같은 부모 제약 소멸).
- **③ 원인 = M3-1 α 충돌 정책(무조건 건너뜀) 노출**. **수정**: 원본 TRANSFER-ENGINE 규약대로
  **충돌 항목만 순차 확인창**(예=덮어쓰기·아니오=건너뜀·취소=전체 중단, MB_YESNOCANCEL).
  워커 스레드 모달(자체 메시지 루프 — UI는 진행 표시 유지), i18n 문구는 UI 스레드 선확정
  (`ops.overwrite`/`Title`, `\n` 이스케이프). M3-1 α 한계 1건 해소.

## M3-5 클립보드 상호운용·OLE DnD (`feat/m3-clipboard-dnd`)

> 원본 대응: `MainWindow.PasteFromOsClipboardAsync`/`OsClipboardIsCutAsync`(OS 클립보드 읽기측) ·
> `OleDropTarget.cs`(DND-EXT) · `OnRowDragStarting`(발신) + docs/33 **B-14dnd**(디스크별 기본
> 동작·Ctrl/Shift 표준 수정키). 원본은 WinUI DataPackage/StorageItems — dir2는 **Win32 원시
> 포맷(CF_HDROP·DROPFILES·Preferred DropEffect)과 OLE COM으로 재구현**(관리 런타임 0).

- **S1/S2 — OS 클립보드**(`clipboard.rs`): CF_HDROP(DROPFILES 헤더+wide 이중 NUL) +
  "Preferred DropEffect"(DWORD — 잘라내기 판정) 읽기/쓰기. **원본 대비 개선: 내부 클립보드
  제거, OS 클립보드 단일 출처** — 탐색기↔앱 양방향(Ctrl+C/X/V) 완전 상호운용·이중 상태 소거.
  잘라내기=붙여넣기 후 비움(탐색기 관례). 셸 메뉴 paste 가로채기·폴더에 붙여넣기도 동일 경로.
  열림 가드(Drop=CloseClipboard)·실패 시 HGLOBAL 해제. **실 클립보드 왕복 통합 테스트**
  (비ASCII 경로·cut effect, `#[ignore]` 수동) 통과.
- **S3 — OLE DnD 수신**(`dnd.rs`): `#[implement(IDropTarget)]`+`RegisterDragDrop` —
  DragEnter에서 CF_HDROP 1회 추출, 좌표별 대상 판정(폴더 행=그 폴더·그 외=패널 루트)은
  win.rs 훅 2개로 State 접근 격리. 연산=**Ctrl 복사·Shift 이동·기본 같은 볼륨 이동/다른
  볼륨 복사**(B-14dnd)·자기/하위 드롭 금지. **최적화 이동 규약: 이동 드롭은 DROPEFFECT_NONE
  반환** — 소스(탐색기)의 원본 삭제와 비동기 전송 경쟁 차단(교차 볼륨 이동 중 소실 방지).
- **S4 — DnD 발신**: 행 누름(drag_press)+임계 이동(SM_CX/CYDRAG) → `SHCreateDataObject`
  (셸 표준 IDataObject — CF_HDROP 등 셸 구성)+`IDropSource`(Esc 취소·버튼 해제 드롭) →
  `DoDragDrop(COPY|MOVE)`. MOVE 반환이어도 **원본 미삭제**(α — 비최적화 대상은 복사로 남는
  안전 방향·탐색기는 최적화 이동 수행). 앱 내부 패널 간 드래그도 동일 OLE 왕복(S3 수신 합류).
- **검증**: 테스트 전부 green·clippy 0 · 실 클립보드 왕복 통과 · B3 통과(**임포트 무변** —
  StructuredStorage 피처는 컴파일 게이트일 뿐) · 기동 스모크(세션 3탭 복원 32.6~33.7MB).
  **RSS 관측 메모**: 무입력 80s 후에도 32.6MB(트림 미발동) — 사용자 PC 사용 중이라 창 위
  마우스 이동이 활동으로 기록됐을 가능성(측정 오염) + OleInitialize 상주분(+α) 가능성 병존.
  **B1 정식 실측(10k·유휴 5분 프로토콜)은 M3-6/M3 마감 게이트에서 재확인** — OLE 오버헤드가
  실재하면 트림 대상에 OLE 캐시 검토. **DnD 실기 QA 대기**: 탐색기→앱 드롭(복사/이동 커서·
  Ctrl/Shift)·앱→탐색기 드래그·앱 내부 패널 간·탐색기↔앱 Ctrl+C/V 왕복.
- **α 한계(후속)**: 드래그 중 폴더 hover 진입(spring-load — 원본 B-15h)·드롭 하이라이트
  시각 피드백·"Performed DropEffect" SetData(소스 뷰 갱신 힌트) 미구현.

## 도구 모음 정리 — 새로고침만 유지 (`fix/toolbar-refresh-only`, 사용자 지시)

- **보고**: 전역 도구 모음의 **이전/다음 버튼 오동작**(사용자 QA 중 발견).
- **조치**: 네비게이션은 **패널별 네비 바**([←][→][↑] — M2 `feat/m2-panel-navbar`, 정상 동작)가
  전담하므로 전역 도구 모음의 이전/다음/위로를 **제거**하고 ⟳(새로고침)만 유지 — 오동작 경로
  자체를 소거(중복 UI 제거). 원인 규명은 불요 판정(제거로 종결).
- **검증**: clippy 0 · 테스트 green · 릴리스 재빌드·재기동(QA 인스턴스는 WM_CLOSE 정상 종료 —
  세션 저장 경로 경유 후 새 빌드로 복원) · **사용자 실기 확인**(07-13 — 버튼 동작 확인, 탭 3/3
  세션 복원 스크린샷).

## M3-4 셸 컨텍스트 메뉴 — IContextMenu 호스팅·고유 병합 (`feat/m3-shellmenu`, ADR-0003)

> 원본 이식: `Nexa.App/ShellContextMenu.cs` + [원본 docs/38 ADR-0005](../../../nexa-dir/docs/38-adr-0005-shell-context-menu.md).
> dir2 결정 기록: [docs/08 ADR-0003](../08-adr-0003-shell-context-menu.md) — 계승 + 올 러스트 특이점.

- **ADR-0003 신설**: 원본 A안(클래식 HMENU+병합) 계승. dir2 특이점 4가지 — ① 자기 wndproc 보유
  → **comctl32 서브클래스 불요**(WM_INITMENUPOPUP/DRAWITEM/MEASUREITEM/MENUCHAR를 thread_local
  활성 IContextMenu2/3로 직접 포워딩, 신규 임포트 DLL 0) ② windows-rs 제공 COM 인터페이스(수동
  vtable 선언 0 — 원본은 ~100줄 ComImport) ③ HRESULT Result 격리(in-proc 확장 SEH 크래시는
  원본 동일 수용) ④ 클래식 룩 이질감 수용(원본 사용자 결정 계승).
- **S1 — 행 우클릭**(`shellmenu.rs`): 경로→`SHParseDisplayName` full PIDL→`SHBindToParent`
  부모 IShellFolder+child→`GetUIObjectOf(IContextMenu)`→`CreatePopupMenu`+`QueryContextMenu`
  (1~0x7FFF·Shift=CMF_EXTENDEDVERBS)→`TrackPopupMenuEx`(TPM_RETURNCMD)→`InvokeCommand`
  (CMIC_MASK_UNICODE|PTINVOKE·lpVerb=MAKEINTRESOURCE). **동사 가로채기**(원본 verbInterceptor
  — 콜백 대신 `Outcome::Verb` 반환): delete=`do_delete`(undo 기록 합류·Shift 열림=완전 삭제),
  rename=인라인 리네임(M3-2). COM 국소 초기화(recycle.rs 동일)·PIDL CoTaskMemFree·모달 메뉴
  펌프 동안 **State 참조 차단**(wndproc 재진입 안전 — 2단계 추출 패턴). rows 위젯 우클릭 선택
  규약(탐색기: 미선택 행=단독 선택·선택 행=유지·빈 영역=해제)+테스트 1.
- **S2 — 고유 병합·키보드**: `CustomItem`(0x8000+ 대역·MF_GRAYED) 구분자 아래 병합 —
  **완전 삭제**(항상)·**폴더에 붙여넣기**(단일 폴더 대상+내부 클립보드 有 — 잘라내기 1회성 유지·
  전송 엔진 합류=undo 기록). **Apps 키·Shift+F10** = 캐럿 행 앵커(`rows.row_anchor` 신설·
  ClientToScreen)에 표시. 셸 제공 동사는 중복 추가 안 함(원본 규약).
- **S3 — 빈 영역 배경 메뉴**(원본 ADR-0005 S2): 폴더 PIDL→데스크톱 `BindToObject`→
  **`CreateViewObject(IID_IContextMenu)`** — 새로 만들기 서브메뉴·정렬·속성 등 탐색기 빈 영역
  동일. 공용 `run_menu()`로 항목/배경 흐름 통합. 고유 병합 = **Undo/Redo**(원본 빈영역 메뉴
  계승 — 설명 라벨 `ctx.undoOf`·can_undo 활성 상태). **paste 동사 가로채기** → 내부 클립보드
  전송(OS 클립보드 CF_HDROP은 M3-5). `rows.in_body()` 신설(헤더 제외 빈 본문 판정).
- **검증**: 워크스페이스 테스트 **127 green**(gui 52 — 우클릭 선택 신규 포함) · clippy 0 · fmt ·
  release exe **0.60MB**(B2) · **B3 통과**(임포트 무변 — comctl32 회피 확인) · 실기 스모크
  (기동·RSS 26MB·정상 종료).
- **실기 QA(사용자, 07-13) 5/6 통과**: ① 배경 메뉴 새 폴더 생성 ② 파일/폴더 행 우클릭 각각 동작
  ③ **보내기 하위 메뉴 동작**(IContextMenu2/3 wndproc 포워딩 검증) ④ 휴지통 이동→복원(delete
  가로채기+undo 합류) ⑤ 빈영역 메뉴 Undo/Redo. **잔여: Apps/Shift+F10 캐럿 앵커 표시**
  ([TODO §7 X-1](../TODO.md) QA 대기 등재).
- **α 한계(후속 M5)**: 커스텀 항목 레지스트리/설정 사용자화(원본 §7)·Checksum 서브메뉴·
  VerbReplacement(copyaspath 제자리 대체)·교차 부모 선택(현재 캐럿 부모 기준 축소)·
  마커 존 우클릭 시 캐럿 불이동(기존 선택 기준 메뉴). **nexa-shell 크레이트 분리 대신 앱 모듈**
  채택(~370줄·COM 의존 얇음 — recycle.rs 동일 계층, docs/01의 크레이트 계획은 규모 도달 시 재검토).

## M3-3 Undo/Redo — OperationHistory 이식·휴지통 복원 (`feat/m3-undo`)

> 원본 이식: `Nexa.ViewModels/OperationHistory.cs`(+ `OperationHistoryTests.cs`) — B-13u,
> docs/33 §Undo/Redo. 휴지통 복원은 `Nexa.App/RecycleBin.cs`(S2 — 셸 undelete 동사).

- **nexa-ops `history` 모듈**(플랫폼 중립): `ReversibleOp` trait + `OperationHistory`(undo/redo
  스택 2개·새 push 시 redo 무효화·상한 100·**실패 연산은 스택에서 소실**=무결성 우선, 원본 규약).
  연산 4종 — `MoveBatchOp`(역이동)·`CopyBatchOp`(사본 삭제 주입)·`RenameOp`(new↔old)·
  `CreateOp`(생성물 삭제 주입/재생성 주입). 오류는 **`OpError`로 구조화**(i18n 문구는 앱 책임 —
  원본의 Localizer 직접 호출을 계층 분리). 테스트 9 이식(스택 규약 5 + 연산 왕복 4).
- **앱 배선**(win.rs): `State.history`(세션 한정) + push 3곳 — **전송 완료**(Move/CopyBatchOp,
  `Outcome.transferred` 수행 쌍만·취소돼도 수행분 기록)·**이름변경**(RenameOp, 동일 이름 무동작
  제외)·**새로 만들기**(CreateOp, undo=휴지통·redo=재생성 주입). **Ctrl+Z=실행 취소·Ctrl+Y/
  Ctrl+Shift+Z=다시 실행** + **편집 메뉴**(Undo/Redo). 실행 후 항상 양쪽 재로드·결과는 타이틀
  노트(성공=설명/실패=OpError→i18n 사유). i18n en/ko: undo/redo·history.*·op.*Count·rename.done.
- **휴지통 복원**(`recycle.rs` — 원본 RecycleBin.cs): 휴지통 셸 폴더 열거 → "원래 위치+이름" 매칭
  (정확 일치·확장자 숨김 폴백) → 셸 **`undelete` 동사**(탐색기 Ctrl+Z와 동일). COM 초기화 국소
  수행·PIDL/STRRET 수동 해제. **원본과 차이**: `StrRetToBufW`(shlwapi) 대신 STRRET 직접 파싱 —
  **신규 임포트 DLL 0 목표**(단, `ole32.dll`은 CoInitialize/CoTaskMemFree로 신규 — OS 인박스라
  DR-2 준수, B3 화이트리스트 등재). `DeleteBatchOp`(앱 계층 — 셸 COM 의존): undo=복원·redo=재삭제,
  Del(휴지통) 경로에 push. **완전 삭제는 undo 불가**(설계상 제외 — 확인창 방어, 원본 동일).
- **검증**: 워크스페이스 test 전부 green(ops 20·history 9 포함) · **실 휴지통 왕복 통합 테스트**
  (삭제→복원→원위치·내용 무손상, `#[ignore]` 수동) 로컬 통과 · release exe **0.58MB**(B2) ·
  기동 RSS **25.51MB**(B1) · **B3 통과**(ole32 등재).
- **α 한계(후속)**: 편집 메뉴 Undo/Redo **활성/비활성 표시 없음**(위젯 enabled 미지원 — 없으면
  타이틀 노트로 알림) · 다중 버전 휴지통 항목은 경로당 최초 일치 1건(삭제 시각 비교=후속) ·
  IFileOperation 미채용(SHFileOperationW + 셸 undelete로 충분 — 원본 문서의 재검토 결론).

## M3-2 삭제·이름변경·새로 만들기 (`feat/m3-fileops`)

> 원본 이식: `MainWindow.DeletePaths`(Del=휴지통 무확인·Shift+Del=완전+확인창 방어·개별 격리) ·
> B-6 인라인 리네임 · BG-N1/N2 새로 만들기("생성 후 즉시 인라인 이름변경" = RevealAndRename).

- **nexa-ops 확장**: `delete_permanent`(재귀·무동작 격리)·`rename`(제자리 — 트림·빈/구분자 오류·
  동일 이름 무동작·충돌 AlreadyExists)·`create_new_dir/file`(unique_dest 재사용 — "새 폴더 (2)"·
  "새 파일 (2).txt"). 테스트 +2(규칙·순번/재귀 삭제).
- **gui: 인라인 이름변경 편집기**(`VirtualRows.rename`) — begin(캐럿 이동+스크롤)·문자/Backspace·
  submit(행, 새 이름 반환 — 실행은 호스트)·cancel. 편집 중 **키 네비 차단·타입어헤드 대신 버퍼**·
  외부 클릭=취소. 필드 = 트리 컬럼 이름부 오버레이(깊이·아이콘 오프셋 반영, 4변 accent). 테스트 +1.
- **앱 배선**: **F2**=캐럿 행 리네임 · **Del**=휴지통(`SHFileOperationW` FO_DELETE+FOF_ALLOWUNDO —
  COM 초기화 불요·인박스라 α 채택, 원본 문서의 IFileOperation은 M3-3 휴지통 복원과 함께 재검토) ·
  **Shift+Del**=완전 삭제(`MessageBoxW` 확인창, 기본=취소) · **Ctrl+Shift+N/파일 메뉴**=새 폴더·
  새 파일(생성→재로드→`index_of_path`로 행 찾아 즉시 리네임). 대상 = 선택 or 캐럿(원본
  KeyboardTargets). 조작 후 양쪽 재로드(`reload_both` 공용화). 이름변경 중 스페이스는 버퍼로.
- **실기**(픽스처 a/b.txt): Ctrl+Shift+N→"MyDir" 입력+Enter → **생성+리네임 완료** · Del →
  **휴지통 삭제** · Shift+Del→Esc → **잔존(확인창 방어)** · Shift+Del→Y → **완전 삭제**(타이틀
  "완전 삭제: 1개 삭제"). 테스트 **116 green**·clippy 0·B2 0.56MB·B3 무변.
- **α 한계(후속)**: 리네임 필드 IME 조합 창 위치 미배치(결과 문자열 수신은 정상 — 경로바만 배치) ·
  휴지통 삭제 undo 기록 없음(M3-3 OperationHistory) · 새 바로가기(원본 N3)=M5.

## M3-1 nexa-ops 전송 엔진 — 복사/이동·진행률·충돌·취소 (`feat/m3-ops-transfer`, M3 착수)

> 원본 이식: docs/33 **TRANSFER-ENGINE**("진입 방식은 달라도 최종 복사/이동은 단일 엔진
> `TransferPathsInto`") + `Nexa.ViewModels/FileOps.cs`(순수 I/O). DR-4 계승 — 새 rlib `nexa-ops`.

- **`nexa-ops` 크레이트 신설**(플랫폼 중립·crate 0): FileOps 이식 — `unique_dest`(" (2)"… 순번,
  폴더는 확장자 분리 안 함)·`size_of`(재귀·실패 격리)·`same_volume`(루트 비교·실패 시 보수적)·
  `copy/move_onto_with_progress`(4MB 청크 바이트 진행·동일 볼륨 rename fast path·순환 이동 금지) +
  **`transfer()` 단일 경로**: 같은 폴더 규칙(이동=무동작·복사=순번 복제)·충돌 항목만 순차
  `resolve`(Overwrite/Skip)·취소(AtomicBool)·**항목 실패 개별 격리**·Outcome(전송 쌍은 M3-3 Undo용).
  원본과 차이(안전 개선): 취소/실패 시 **부분 복사 파일 정리**(원본은 잔존). 테스트 8(순번 명명·
  같은 폴더 규칙·재귀 복사·fast path·충돌 순차·순환 격리·진행 총량·취소 정리).
- **앱 배선**: 내부 클립보드(원본 FileClipboard — 경로+모드) `Ctrl+C/X/V`(잘라내기는 1회성) →
  **워커 스레드** 전송(UI 무블록) + `WM_APP_TRANSFER` PostMessage 통지(**세대 가드** — 원본 A-1
  계승, 낡은 워커 무시) → 타이틀 진행 %·완료 시 **양쪽 패널 재로드**(원본 규약)·결과 요약(전송·
  건너뜀·실패·취소, i18n ops.* 키). **Esc = 취소**. 공유 상태는 원자/뮤텍스만(워커의 State 접근 금지).
- **실기**: 픽스처 3파일 — Ctrl+A→C→V(같은 폴더) → **6개 항목·" (2)" 순번 복제·"전송 3"** /
  Ctrl+A→X→V(같은 폴더) → **무동작·"전송 0 · 건너뜀 6"**(제자리 이동 규칙). 테스트 113 green·
  clippy 0·B2 0.54MB·B3 무변.
- **α 한계(후속)**: 충돌 정책 = 전부 건너뜀(덮어쓰기 **확인 모달** 자리 — resolve 콜백에 배선만 하면
  됨)·진행 **창** 없음(타이틀 % — 원본은 진행 창)·클립보드는 내부 전용(OS CF_HDROP=M3-5)·
  DnD 진입점=M3-5·watcher 정밀 갱신=M3-6.

## M2-7 IME(한글) 1차·UIA 1차 — M2 완료 `0.3.0` (`feat/m2-ime-uia`)

> 원본 NFR-A1(접근성: 키보드·스크린리더) 대응 1차. 원본(WinUI)은 플랫폼이 제공하던 것을
> 커스텀 드로잉 스택에서 직접 구현. 마감(조합 인라인 표시·패턴 완성)은 M5-3.

### IME(한글) 1차 — 조합 창 캐럿 배치

- `WM_IME_STARTCOMPOSITION`/`WM_IME_COMPOSITION`에서 `ImmSetCompositionWindow(CFS_POINT)` —
  대상 = **편집 중 경로바**(활성 패널 우선), 캐럿 x = 필드 pad + `text_width(버퍼)`(끝 고정 α 모델).
  결과 문자열은 DefWindowProc의 WM_IME_CHAR→WM_CHAR 변환으로 **기존 문자 경로 그대로 수신**.
- `PathBar::edit_info()` 신설(버퍼·필드 rect·pad 노출). `Win32_UI_Input_Ime` feature + B3에
  imm32.dll 등재.
- **실기**: 편집 모드 진입(우클릭) → WM_IME_* 주입 + 한글 SendKeys("가나다") → 생존·Esc 복귀·정상
  종료 확인. 실제 한국어 IME 조합 창 위치는 수동 확인 항목(자동화 불가 — IME 활성화 스크립팅 제약).

### UIA 1차 — 리스트/항목 프로바이더 (`uia.rs`)

- **스냅샷 모델**: UIA 콜백은 임의(MTA) 스레드에서 오므로 창 상태 직접 접근 금지 —
  WM_GETOBJECT/포커스 이벤트 시점에 UI 스레드에서 **가시 행 스냅샷**(`Snap`: 이름·선택·캐럿·화면
  rect)을 떠 `Arc`로 프로바이더에 담는다(불변 = 스레드 안전).
- `ListProvider`(fragment root, ControlType=List, Name=활성 경로) + `RowProvider`(ListItem,
  Name=파일명, HasKeyboardFocus=캐럿, **SelectionItemPattern**(IsSelected 읽기)) — Navigate
  (부모/형제/첫·끝 자식)·GetFocus(캐럿 행)·ElementProviderFromPoint(y→행)·런타임 id(전역 행 인덱스).
- **캐럿 통지**: `update_status` 경유 캐럿 변경 감지 → `UiaRaiseAutomationEvent(FocusChanged)`
  (스크린리더가 행 이름을 읽는 트리거). `UiaClientsAreListening` 가드 — 무클라이언트 시 비용 0.
- VARIANT는 0.62 raw 유니온이라 수동 구성(v_str/v_i4/v_bool — 소유권은 호출자 VariantClear).
  feature 4종(Com·Ole·Variant·Accessibility) + B3에 uiautomationcore.dll 등재.
- **실기(UIA 클라이언트 검증)**: .NET System.Windows.Automation으로 조회 — **리스트
  Name="C:\Users\kiros33\Nextcloud"·가시 항목 14·이름(Audios|Books|Company)·IsSelected 읽기 정상**.
- **1차 한계**: 스냅샷 정적(구조 변경 이벤트 미발행 — 다음 WM_GETOBJECT가 새 트리)·행 rect는
  헤더 근사·선택 조작(Select/Add/Remove) no-op·비활성 패널 미노출. → M5-3 마감.

### M2 완료 판정 → `0.3.0`

- M2-1 α~M2-8 전 항목 ✅. **게이트**(02-roadmap: 상주 시나리오 탭 4·듀얼 RSS ≤30MB):
  세션 복원(듀얼·탭 4/4) 활성 26.9MB + M2-8 트림(0.21~2.9MB) — **통과**.
- 테스트 105 green·clippy 0·fmt·B2 0.50MB·B3 통과(imm32·uiautomationcore 등재).
