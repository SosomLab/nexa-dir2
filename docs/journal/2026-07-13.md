# 2026-07-13 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST)** — 실제 착수는 첫 커밋 이전.

## M3-1 nexa-ops 전송 엔진 — 복사/이동·진행률·충돌·취소 (`feat/m3-ops-transfer`, M3 착수)

> 원본 이식: docs/33 **TRANSFER-ENGINE**("진입 방식은 달라도 최종 복사/이동은 단일 엔진
> `TransferPathsInto`") + `Nexa.ViewModels/FileOps.cs`(순수 I/O). DR-4 계승 — 새 rlib `nexa-ops`.

- **`nexa-ops` 크레이트 신설**(플랫폼 중립·crate 0): FileOps 이식 — `unique_dest`(" (2)"… 순번,
  폴더는 확장자 분리 안 함)·`size_of`(재귀·실패 격리)·`same_volume`(루트 비교·실패 시 보수적)·
  `copy/move_onto_with_progress`(4MB 청크 바이트 진행·동일 볼륨 rename fast path·순환 이동 금지) +
  **`transfer()` 단일 경로**: 같은 폴더 규칙(이동=무동작·복사=순번 복제)·충돌 항목만 순차
  `resolve`(Overwrite/Skip)·취소(AtomicBool)·**항목 실패 개별 격리**·Outcome(전송 쌍은 M3-3 Undo용).
  원본과 차이(안전 개선): 취소/실패 시 **부분 복사 파일 정리**(원본은 잔존). 테스트 8(순번 명명·
  같은 폴더 규칙·재귀 복사·fast path·충돌 순차·순환 격리·진행 총량·취소 정리).
- **앱 배선**: 내부 클립보드(원본 FileClipboard — 경로+모드) `Ctrl+C/X/V`(잘라내기는 1회성) →
  **워커 스레드** 전송(UI 무블록) + `WM_APP_TRANSFER` PostMessage 통지(**세대 가드** — 원본 A-1
  계승, 낡은 워커 무시) → 타이틀 진행 %·완료 시 **양쪽 패널 재로드**(원본 규약)·결과 요약(전송·
  건너뜀·실패·취소, i18n ops.* 키). **Esc = 취소**. 공유 상태는 원자/뮤텍스만(워커의 State 접근 금지).
- **실기**: 픽스처 3파일 — Ctrl+A→C→V(같은 폴더) → **6개 항목·" (2)" 순번 복제·"전송 3"** /
  Ctrl+A→X→V(같은 폴더) → **무동작·"전송 0 · 건너뜀 6"**(제자리 이동 규칙). 테스트 113 green·
  clippy 0·B2 0.54MB·B3 무변.
- **α 한계(후속)**: 충돌 정책 = 전부 건너뜀(덮어쓰기 **확인 모달** 자리 — resolve 콜백에 배선만 하면
  됨)·진행 **창** 없음(타이틀 % — 원본은 진행 창)·클립보드는 내부 전용(OS CF_HDROP=M3-5)·
  DnD 진입점=M3-5·watcher 정밀 갱신=M3-6.

## M2-7 IME(한글) 1차·UIA 1차 — M2 완료 `0.3.0` (`feat/m2-ime-uia`)

> 원본 NFR-A1(접근성: 키보드·스크린리더) 대응 1차. 원본(WinUI)은 플랫폼이 제공하던 것을
> 커스텀 드로잉 스택에서 직접 구현. 마감(조합 인라인 표시·패턴 완성)은 M5-3.

### IME(한글) 1차 — 조합 창 캐럿 배치

- `WM_IME_STARTCOMPOSITION`/`WM_IME_COMPOSITION`에서 `ImmSetCompositionWindow(CFS_POINT)` —
  대상 = **편집 중 경로바**(활성 패널 우선), 캐럿 x = 필드 pad + `text_width(버퍼)`(끝 고정 α 모델).
  결과 문자열은 DefWindowProc의 WM_IME_CHAR→WM_CHAR 변환으로 **기존 문자 경로 그대로 수신**.
- `PathBar::edit_info()` 신설(버퍼·필드 rect·pad 노출). `Win32_UI_Input_Ime` feature + B3에
  imm32.dll 등재.
- **실기**: 편집 모드 진입(우클릭) → WM_IME_* 주입 + 한글 SendKeys("가나다") → 생존·Esc 복귀·정상
  종료 확인. 실제 한국어 IME 조합 창 위치는 수동 확인 항목(자동화 불가 — IME 활성화 스크립팅 제약).

### UIA 1차 — 리스트/항목 프로바이더 (`uia.rs`)

- **스냅샷 모델**: UIA 콜백은 임의(MTA) 스레드에서 오므로 창 상태 직접 접근 금지 —
  WM_GETOBJECT/포커스 이벤트 시점에 UI 스레드에서 **가시 행 스냅샷**(`Snap`: 이름·선택·캐럿·화면
  rect)을 떠 `Arc`로 프로바이더에 담는다(불변 = 스레드 안전).
- `ListProvider`(fragment root, ControlType=List, Name=활성 경로) + `RowProvider`(ListItem,
  Name=파일명, HasKeyboardFocus=캐럿, **SelectionItemPattern**(IsSelected 읽기)) — Navigate
  (부모/형제/첫·끝 자식)·GetFocus(캐럿 행)·ElementProviderFromPoint(y→행)·런타임 id(전역 행 인덱스).
- **캐럿 통지**: `update_status` 경유 캐럿 변경 감지 → `UiaRaiseAutomationEvent(FocusChanged)`
  (스크린리더가 행 이름을 읽는 트리거). `UiaClientsAreListening` 가드 — 무클라이언트 시 비용 0.
- VARIANT는 0.62 raw 유니온이라 수동 구성(v_str/v_i4/v_bool — 소유권은 호출자 VariantClear).
  feature 4종(Com·Ole·Variant·Accessibility) + B3에 uiautomationcore.dll 등재.
- **실기(UIA 클라이언트 검증)**: .NET System.Windows.Automation으로 조회 — **리스트
  Name="C:\Users\kiros33\Nextcloud"·가시 항목 14·이름(Audios|Books|Company)·IsSelected 읽기 정상**.
- **1차 한계**: 스냅샷 정적(구조 변경 이벤트 미발행 — 다음 WM_GETOBJECT가 새 트리)·행 rect는
  헤더 근사·선택 조작(Select/Add/Remove) no-op·비활성 패널 미노출. → M5-3 마감.

### M2 완료 판정 → `0.3.0`

- M2-1 α~M2-8 전 항목 ✅. **게이트**(02-roadmap: 상주 시나리오 탭 4·듀얼 RSS ≤30MB):
  세션 복원(듀얼·탭 4/4) 활성 26.9MB + M2-8 트림(0.21~2.9MB) — **통과**.
- 테스트 105 green·clippy 0·fmt·B2 0.50MB·B3 통과(imm32·uiautomationcore 등재).
