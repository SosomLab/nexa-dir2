# 2026-07-31 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## ZIP 자산 + SHA256SUMS 추가 (사용자 요청 — `feat/dist-zip-assets`)

### 배경 — 사용자 다운로드 차단 보고

브라우저에서 [Release 0.12.0](https://github.com/SosomLab/nexa-dir2/releases/tag/0.12.0)
포터블 exe를 받으려다 **보안 차단**. 원인은 무서명(DR-3) exe에 대한
**브라우저 SmartScreen 다운로드 평판 필터** — 서명이 없으면 게시자 평판이 쌓이지
않고, 파일 해시 평판은 버전마다 0에서 다시 시작한다.

### 조사 — 인증 경로 재확인 (기존 기록 2건 정정)

Microsoft 공식 문서([code-signing-options](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options)
2026-04 · [smartscreen-reputation](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)
2026-05) 기준으로 **[12 §4](../12-packaging-single-exe.md)의 전제 2건이 무효**임을 확인:

| 경로 | 현행 사실 | 판정 |
| --- | --- | --- |
| **EV 인증서** | **2024년에 SmartScreen 즉시 통과 폐지** — 지금은 OV와 동일하게 평판 축적 | EV 프리미엄($400+/y) 지불 근거 소멸 |
| **Azure Artifact Signing**(구 Trusted Signing) | 조직 = 미국·캐나다·EU·영국 / 개인 = 미국·캐나다 **지역 제한** | **한국은 법인·개인 모두 불가** |
| **SignPath Foundation**(무료 OSS 서명) | "OSI-approved license **without commercial dual-licensing**" 필수 | PolyForm NC(DR-6) + 상업 라이선스 정책([13](../13-licensing.md)) **이중 결격** |
| **Microsoft Store(MSIX)** | Microsoft 재서명 · 인증서 불요 · **경고 없음** · 등록비 2026년 폐지 | 유일한 무료 완전 해소 경로 — 별건 검토 |
| **OV 인증서** | 연 $150~300 + HSM 필수 · 경고는 남고 **버전 간 평판 승계**만 획득 | 한국에서 유일한 유료 선택지 |

→ 서명은 어느 경로든 **초기 경고를 없애지 못한다**(Store 제외). 무비용 즉효책이
필요하다는 판단.

### 결정 — zip 자산 **추가**(사용자 지시: 기존 Release 구성 유지)

| 단계 | zip 효과 |
| --- | --- |
| 브라우저 다운로드 차단(실제 증상) | ✅ **해소** — exe 확장자 평판 필터를 타지 않음 |
| 실행 시 SmartScreen 경고 | ❌ 남음 — Explorer·WinRAR·7-Zip 22.00+ 는 압축 해제 시 MOTW 전파 |
| Defender 검사 · Smart App Control | ❌ 무관 |

- 기존 exe 자산은 **그대로 유지** — winget·choco 매니페스트가 URL+SHA256으로
  직접 참조하므로 교체하면 3채널이 동시에 깨진다. zip은 덧붙이기만.
- **포터블 zip 우선 안내**. 설치형 zip은 압축을 풀어도 UAC "알 수 없는 게시자"
  프롬프트가 추가되고 설치 프로그램 자체가 Defender 휴리스틱에 더 잘 걸린다.

### 구현 (release.yml — 스텝 2개 신설)

- **`ZIP 자산 추가`**: `README.txt`(무서명 경고 예고 + [추가 정보]>[실행] 안내 +
  `Get-FileHash` 대조법 · 한/영 · UTF-8 BOM) 생성 → `Compress-Archive`로
  `NexaDir-<버전>-win-x64.zip`(포터블+README) ·
  `NexaDir-Setup-<버전>.zip`(설치형+README) 2종.
- **`SHA256SUMS.txt`**: 자산 4종(exe 2 + zip 2) 체크섬 — README가 안내하는 대조본.
  ascii 인코딩·`<hash>  <파일명>` 표준 형식.
- `upload-artifact` path + `action-gh-release` files에 `NexaDir-*.zip`·
  `SHA256SUMS.txt` 추가. **태그 push에서만 동작**(`if: github.ref_type == 'tag'`).
- 결과 Release 자산 = **exe 2 + zip 2 + SHA256SUMS.txt = 5종**.

### 함정 — YAML 블록 스칼라 × PowerShell here-string

`run: |` 안에서 here-string(`@" ... "@`)은 **종료자가 컬럼 0**이어야 해 YAML 들여쓰기
블록을 이탈시킨다(파서 오류 14건). **문자열 배열 + `Set-Content`** 로 우회 — 같은
결과에 들여쓰기 유지. 동일 함정이 재발할 수 있어 워크플로에 주석으로 남김.

### 검증 (로컬 dry-run — CI 태그 push 전)

- YAML 파싱 통과(스텝 14개 정상 인식).
- 더미 exe 2개로 스텝 본문 실행 → zip 2종 생성, 내부 엔트리
  `NexaDir-0.12.0-win-x64.exe, README.txt` / `NexaDir-Setup-0.12.0.exe, README.txt` 확인.
- README.txt 선두 3바이트 `EF BB BF`(UTF-8 BOM — 메모장 한글 정상) 확인.
- SHA256SUMS.txt 4행·소문자 해시·파일명만(경로 제거) 확인.
- **실 검증은 다음 버전 태그 push에서** — [21 §6](../21-distribution.md) 체크리스트 추가.

### 잔여

- 다음 릴리스 후 **브라우저에서 zip 다운로드가 실제로 차단되지 않는지** 사용자 실기 확인.
- Microsoft Store(MSIX) 채널은 별건 — [00-vision](../00-vision.md)의 MSIX 배제 전제
  (비용·서명 필요)가 무너졌으므로 재검토 대상. 착수는 사용자 결정 대기.
