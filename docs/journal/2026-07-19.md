# 2026-07-19 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 순서 편집 키보드 내비(사용자 확정) (main 직커밋)

> **사용자**: 키보드로 대상을 옮겨 다니기 — 화살표 = 단일 선택 이동·
> Shift+화살표 = 다중 선택.

- ordertree `key_move`(↑/↓ 단일 선택 + 가시 스크롤)·`key_extend`(Shift —
  같은 레벨·부모 형제로만 확장, 클릭 Shift 규약 동일)·`key_toggle`(Space =
  체크)·+Ctrl+↑/↓ = 순서 이동(▲▼ 동일). ordereditor WM_KEYDOWN 라우팅
  (tree 핸들 복사 — 중첩 차용 회피)·SetFocus. 실기: ↓ 이동·Shift+↓ 확장.

## 순서 편집 창 고도화 — 드래그 이동·스크롤·▲▼ 비활성·SVG 버튼 (main 직커밋)

> **사용자 요청 4건**: ① ▲▼ = 제공 SVG 이미지(16px·선 = 기본 색) ② 선택
> 없으면 ▲▼ 비활성 ③ 트리에서 **드래그로 이동**(자기 소속 밖 금지·컬럼
> 이동식 고스트 박스·긴 내용은 가장자리 자동 스크롤·스크롤바 = 파일 목록
> 그리드 차용) ④ (ESC = 드래그 취소 — 컬럼 규약 일치).

- **iconbutton `create_svg`**(gdipctx `svg_to_image` — svg_to_hicon에서
  비트맵 렌더부 분리): 2배 래스터 → Stretch AA. 잉크 = style.text(기본 색)·
  비활성 = 알파 38%. assets/ui/arrow-up·down.svg.
- **NxOrderTree 전면 개정**: 세로 스크롤(휠 + 우측 오버레이 썸 5px/드래그
  8px — 그리드 차용) · **블록 드래그**(선택 형제 + 그룹 자식 포함 — 임계
  5px·같은 부모 한정 스왑 미리보기·고스트 박스[첫 라벨 (+N)·accent 테두리]·
  가장자리 자동 스크롤 타이머·ESC cancel_drag) — 확정 시 **뷰 원상 복원 후
  delta(형제 칸)만 통지**(NXOT_DRAGMOVE·take_drag_delta — 모델·뷰 정합).
- **ordereditor**: 트리 12행 캡(초과 = 스크롤)·▲▼ 초기 비활성 + 선택 동기
  (NXIB_SETENABLE)·드래그 delta = 기존 단일 스텝 이동 재사용·ESC = 드래그
  취소 우선/없으면 닫기.
- 실기: 초기 비활성→선택 활성·드래그 중 고스트+미리보기·확정 반영·원복.
  green·clippy 0.

## 드래그 고스트·우클릭 팝업·기본 순서 승격 (main 직커밋)

> **사용자 요청 5건**: ① 컬럼 드래그 시 헤더 모양 고스트가 커서 추종(세로
> 고정)·스냅 = 옮겨진 미리보기·ESC 취소 ② Context 설정 = 별도 서브메뉴
> ③ 도구모음 빈 영역 우클릭 = 설정 팝업 ④ 컬럼 우클릭 = 순서 설정 팝업
> ⑤ 현재 도구모음 순서를 기본값으로.

- **드래그 UX**: 활성 드래그 = **라이브 미리보기**(스냅 위치로 즉시 재배열
  — 확정 MouseUp·`ESC = orig 복원`[`cancel_col_drag` — win ESC 체인 선두
  소비]) + **고스트 헤더**(배경/테두리/라벨 복제·커서 x 추종·헤더 행 고정).
  인디케이터 라인은 미리보기로 대체.
- **Context 서브메뉴**: prefs 트리 `filelist` 아래 `ctxmenu` 리프 신설 —
  컨텍스트 메뉴 편집 항목 이동(pref.cat.ctxmenu).
- **우클릭 팝업**(`show_bar_popup` — TrackPopupMenu·지연 실행 재진입 규약):
  도구모음 빈 영역 = [도구 모음 순서…][설정…] · 컬럼 헤더 = [파일 컬럼…].
  main이 ordereditor 소유자가 되는 경로 신설(WM_APP_EDIT_TOOLBAR/COLS +
  WM_APP_ORDER_EDIT 수신 — 툴바 재구성·컬럼 적용+동기). prefs 스펙 빌더
  공용화(toolbar/col_editor_spec).
- **기본 순서 승격**: TOOLBAR_BLOCKS = refresh|panel|view|show|settings
  (사용자 배치 그대로 — 병합 보충 순서 포함, 테스트 갱신).
- 실기: 고스트/ESC 원복·팝업 2종 캡처. green·clippy 0.

## 설정 편집기 3종 = 공통 별도 창 + 컬럼 기능 완성 (`feat/order-editors`)

> **사용자 요청 3건**: ① 컬럼 헤더 **드래그 재배열**(탭과 동일 UX — 경계
> 근처 드롭) + 설정에서 컬럼 표시/순서 + colsync 연동(off = 좌우 독립·재켜짐
> = 포커스 기준) ② **컬럼·툴바·Context menu 설정 = 각각 별도 창**(설정에서
> 호출) — 공통점은 어댑터 방식으로 ③ 마무리 후 push.

- **공통 프레임워크**: config 제네릭 순서 문법
  `블록[자식:vis,…]`(`parse/serialize_order_with` + OrderDefs SSOT —
  COLUMN_BLOCKS·CTXMENU_BLOCKS 신설, 툴바는 호환 래퍼) ·
  **NxOrderTree 체크 열**(표시 토글 — NXOT_TOGGLE·take_toggled/checks) ·
  **ordereditor.rs** 신설 = 공통 모달 편집 창(EditorSpec 어댑터: defs·
  라벨 fn·체크 여부·평면 여부·잠금 key — 이동/토글/재선택/실시간 통지
  [WM_APP_ORDER_EDIT] 공통 구현).
- **prefs 재구성**: 인라인 툴바 트리 폐지 → `Kind::OrderDialog`(캡션 +
  [편집…] 버튼) 3항목: 도구 모음 순서(Appearance)·파일 컬럼·컨텍스트 메뉴
  (View & Sort). 변경 = 편집 창에서 실시간 수신 → apply_now(X-8).
- **컬럼**: rows 헤더 라벨 드래그(임계 5px — 무드래그 = 정렬 클릭[MouseUp
  확정으로 이동·테스트 click 헬퍼 개정]·삽입 인디케이터 accent 2px) →
  take_col_reordered → 패널 탭 상속 + colsync on 시 반대 패널 동기.
  표시/순서 편집 = 포커스 패널 적용(name 잠금·전부 숨김 방지). 세션
  `panel{i}.cols=cols[name:1,…]`(레이아웃 — 폭보다 먼저 복원).
- **컨텍스트 메뉴**: Settings.ctx_menu_order — 행/배경 고유 항목의 표시·
  순서를 메뉴 구성 시 반영(셸 동사 비대상·copyName의 after_id 고정 유지).
- 실기 QA: 편집 창 3종 열기/이동/토글·툴바 라이브 스왑·**Kind 숨김 → 헤더
  즉시 소멸·Ext 순서 이동**·헤더 드래그 재배열(Ext↔Size 왕복)·원복.
  196+ green·clippy 0.

## 도구 모음 순서 설정 (`feat/toolbar-order`)

> **사용자 요청**: 설정에서 도구모음 순서 변경 — 트리 표시, 그룹 선택 =
> 통째 이동·자식 선택 = 그룹 안 이동(위/아래 이미지 버튼), Shift 연속
> 다중 선택만(레벨 혼합 차단), 다중 선택 일괄 이동.

- **NxOrderTree(ctl 15호)** 신설: (라벨, 레벨) 행·부모 = 순서 유도.
  클릭 = 단일·Shift = **같은 레벨·같은 부모 형제 범위만**(사이 다른 레벨
  미포함 — 혼합 차단). NXOT_SELCHANGE 통지·set_rows/selection API.
  NxIconButton `Up`/`Down` 셰브론 변형 추가.
- **모델/영속**: config `TOOLBAR_BLOCKS`(5블록: panel[toggle,info,colsync]/
  view[tree,flat,tiles]/refresh/settings/show[hidden,dot]) +
  `parse/serialize_toolbar_order`(미지 토큰 제거·누락 보충 = 전방 호환,
  테스트 3종) + `Settings.toolbar_order`.
- **build_toolbar 재작성**: 순서 문자열 주도(블록 사이 구분선 자동) —
  전 호출부 order 인자. prefs 적용 = 즉시 set_buttons 재구성(X-8 라이브).
- **prefs `Kind::ToolbarOrder`**: 캡션 + 트리 + ▲▼ — 이동 = 모델 재배열 →
  트리 재구성·이동분 재선택 → apply_now(툴바 실시간 반영).
- 실기 QA: 그룹 통째 ↑(툴바 라이브 스왑)·자식 2개 Shift 연속 ↓(그룹 내
  한정)·레벨 혼합 Shift 무시·settings.cfg 영속·기본 복원. 64 green·clippy 0.

## 컬럼 경계 더블클릭 auto-fit (main 직커밋)

> **사용자 요청**: 컬럼 크기 조절 가능한 경계 더블클릭 = 콘텐츠 길이
> auto-fit + 최대 폭 제한(설정에서 조정).

- **rows**: `autofit_col_at`(리사이즈 핸들 히트) · `autofit_texts`(가시 행 +
  헤더 텍스트·부가 폭[들여쓰기·아이콘·패딩 — paint_tree_cell 배치 재현]) ·
  `set_col_width`(min 클램프·col_resized 마크).
- **win**: WM_LBUTTONDBLCLK 헤더 핸들 분기 → `autofit_column`(DwCtx
  text_width List 폰트 실측 최대값·상한 = `col_autofit_max`×DPI) →
  기존 `sync_col_widths` 전파(탭 상속·좌우 동기 규약 그대로).
- **설정**: `Settings.col_autofit_max`(기본 400px·50~2000 클램프·직렬화
  왕복 테스트) + prefs 파일 목록 카테고리 Number 항목 + en/ko 키.
- 실기: Name 경계 더블클릭 → 305→130px 콘텐츠 맞춤 확인. 61 green·clippy 0.

## 듀얼 정보 패널 토글 버튼 (main 직커밋)

> **사용자 제공 SVG**: 정보 패널 토글(사각 + 상하 분할 점선 + i 글리프 —
> i 기둥은 요소별 stroke-width 3.4).

- **요소별 `stroke-width` 오버라이드**(`Element.width` — 부재 = 루트 상속)
  파서/렌더러 확장. 테스트 +1(60 green).
- **CMD_INFO_TOGGLE(65)**: 기존 CMD_INFO_SINGLE/DUAL 핸들러 arm 합류
  (현재 모드 반전·메뉴 라디오 유지·변경 시 툴바 set_buttons 재구성).
  버튼 = 패널 토글 옆·켜짐 = 듀얼 정보·**싱글 패널에선 비활성**(정보 싱글
  고정 규칙과 일치). 툴팁 = infoDual/infoSingle(현재 상태). 실기 캡처.

## 토글 표시 재원복 — 블렌드 배경 + 검정 유지 (main 직커밋, 5차 최종)

> **사용자 재확정**: 파랑 필 배경은 원복(accent 38% 블렌드), 토글 시에도
> 아이콘은 흰색 전환 없이 **검정 유지**.

- chrome 켜짐 배경 = accent 38% 블렌드 복원·`#on` 분기 제거. icons
  `-on` 규칙·SVG_INK_ACCENT·dw `#on` 매핑 제거(비활성 `#dis`만 유지).
  실기 캡처. 59 green·clippy 0.

## 토글 표시 기준 개정 — 파랑 필 + 흰 선 / 기본 검정 (main 직커밋)

> **사용자 시안(macOS 스타일)**: 선택 = 파란 필 배경 + 흰 아이콘, 기본 =
> 배경 없음 + 검정 아이콘.

- 잉크 4차 개정: 기본 `#000000` · 켜짐 흰색(`-on`) · 비활성 검정 알파 38%.
  켜짐 배경 = **고정 `#0A7AFF`**(chrome `CHECKED_BG` — 흰 선 대비 보장,
  직전 accent 38% 블렌드 폐지). 켜짐 `#on` 키는 **모든 checked 아이콘
  버튼에 무조건 적용**(accent 플래그·빌더 제거). 실기 캡처(켜짐 5버튼
  파랑 필+흰 선·기본 검정) 확인.

## 토글 켜짐 배경 = accent 38% 블렌드 (main 직커밋)

> **사용자 요청**: 켜짐 배경이 도구 모음 배경과 유사해 식별 곤란 —
> 잘 보이는 배경색으로.

- 진단: sel_bg(라이트 `#D8E8FF`)와 chrome_bg(`#EEF1F5`)가 근사 명도.
- Toolbar 켜짐 배경 = **chrome_bg→accent 38% 블렌드**(라이트 ≈ `#ABCAF9`·
  다크 ≈ `#2A4A7A` — 테마 자동 적응, 07-16 "sel_bg 충분" 확정을 개정).
  실기 캡처(켜짐 5버튼 뚜렷) 확인.

## settings SVG 교체 — 전 아이콘 SVG 완결·PNG 기구 폐지 (main 직커밋)

> **사용자 제공 SVG**: 설정 = 톱니(원호 다수 — A 서브셋 내) + 중심 원.

- `settings.svg` 9호 등록 — **툴바 아이콘 9종 전부 SVG**. PNG 버킷 기구
  폐지: `emb!` 매크로·`EMBEDDED` 테이블·gdipctx `png_to_hicon` 제거,
  make_embedded = SVG 단일 경로. settings PNG 3종 삭제(assets/toolbar에
  PNG 0). README 전면 개정(SVG 규격·서브셋·매핑표).
- exe 1.32MB 유지. 59 green·clippy 0. 실기 캡처.

## panel-toggle 켜짐 전체 파랑 — 반픽셀 AA 정렬 픽스 (main 직커밋)

> **사용자 보고**: 켜짐 시 전체 선이 #3D8BFF로 보여야 함 — 실측 결과 색은
> 이미 accent였으나 **테두리가 16px 래스터에서 반픽셀 경계에 걸려 AA로
> 뿌옇게(회청색) 보임**(10배 확대·픽셀 실측으로 진단).

- `panel-toggle.svg` rect 1..31 → **2..30**(16px에서 정수 픽셀 정렬) —
  테두리·분할선 모두 선명한 #3D8BFF. 파일 교체만.

## refresh SVG 교체 — 원호(A) 명령 지원 (main 직커밋)

> **사용자 제공 SVG**: 새로고침 = 315° 원호 + 화살촉 — `A`(arc) 경로
> 명령 첫 사용이라 파서 확장.

- **svg.rs `Seg::Arc`**: SVG F.6.5 엔드포인트→중심 변환을 파싱 시 수행
  (**원형 한정** — rx=ry·회전 무시, 타원 호 = 전체 무효). 반지름 부족 시
  규약대로 확대·fa/fs 플래그로 방향 판정. 테스트 +1(중심 16,16·r13·
  -45°→-315° 검증, 59 green).
- **gdipctx**: `GdipAddPathArc`(현재점 연속 — 이음선 자동)·끝점 갱신.
- `refresh.svg` 7호 등록·PNG 3종 제거. 남은 PNG = settings 1종. 실기 캡처.

## SVG 잉크 3차 — 어두운 회색 (main 직커밋)

> **사용자 요청**: 전체 SVG 선 색을 더 진하게(어두운 회색).

- `SVG_INK` `#40464E` → **`#2B3036`**(DIM 알파 38% 동반). 캡처 확인 —
  PNG 잔여 2종(refresh·settings `#6E747C`)과 대비 커짐(SVG 전환 시 해소).

## SVG 규격 통일 — 32vb·여백 1·stroke 2 (main 직커밋)

> **사용자 요청 2건**: ① 기본 SVG 모든 선을 view-flat 색으로 —
> 원인 = 색이 아니라 **panel-toggle 선 굵기(1.5/32)가 얇아 AA로 흐려 보임**
> → 굵기 2로 통일 ② dotfiles·hidden = view-flat과 동일 크기·꽉 차게 —
> 16vb 판을 **32vb·여백 1·stroke 2 규격**으로 환산(×2).

- 전 SVG 아이콘 규격 통일: 32 viewBox·콘텐츠 1..31·stroke 2·currentColor.
  실기 캡처(선 농도 균일) 확인.

## 아이콘 최대 크기 일괄 + 도구 모음 26px (main 직커밋)

> **사용자 요청 4건**: ① colsync 최대 크기 ② view flat/tiles/tree 최대
> 크기 ③ 도구 모음 위/아래 1px 확장(16px 아이콘 여유) ④ panel-toggle
> 최대 크기 + **켜짐 = 전체 선 파란색**.

- colsync: 화살표 전폭(0.5..31.5)·SYNC 10.5 시도 → **32px 폭 초과로 "SYN"
  잘림 발굴** → 9.5 확정. view 3종·panel-toggle = 여백 1px 최대 판(1.25~
  1.36배). flat 실자산 테스트 좌표 갱신.
- 도구 모음 높이 24→**26px**(s(26)) + 아이콘 여백 4→5px(`b.h-10`) —
  아이콘 16px 유지·상하 1px 여유(런처 바도 동일 높이).
- panel-toggle 켜짐 = **`-on` 접미 일반 규칙(전체 accent 재렌더)**로 회귀 —
  전용 panel-toggle-on.svg 폐지(사용자 재확정: 전체 파란색). 실기 듀얼
  (전체 블루)↔싱글 왕복 캡처. 58 green·clippy 0.

## dotfiles SVG v2 교체 (main 직커밋)

> **사용자 제공 SVG**: DOT 텍스트 → **채움 점**(circle fill 오버라이드) —
> 16 viewBox(16px 1:1 정합·0.5 픽셀 경계 정렬 시안).

- `dotfiles.svg` 교체만(파이프라인 변경 없음 — Element.fill이 흡수).
  실기 캡처.

## hidden SVG 교체 (main 직커밋)

> **사용자 제공 SVG**: 숨김 파일 토글 = 접힘 파일 + "HIDDEN" 텍스트
> (dotfiles와 동일 계열 시안).

- `hidden.svg` 7호 등록·PNG 3종 제거. 파이프라인 변경 없음. 실기 캡처.
- **v2 교체(사용자 제공)**: HIDDEN 텍스트 → **채움 H 글자 패스**(요소
  `fill="currentColor" stroke="none"`) — **요소별 채움 오버라이드**
  (`Element.fill`: none=스트로크·색=채움·부재=루트 상속) 파서/렌더러 확장.
  테스트 +1(58 green). 실기 캡처.
- **v3 교체(사용자 제공)**: dotfiles v2와 동일 계열 **16 viewBox 픽셀 정합**
  판(파일 윤곽 0.5 경계 + H 확대) — 파일 교체만. 실기 캡처.

## 패널 토글 단일 버튼 + 요소 색 오버라이드 (main 직커밋)

> **사용자 재확정 3건**: ① 듀얼/싱글 별도 아이콘 폐지 — **단일 토글 버튼**
> ② 켜지면 선을 푸른계열로 ③ **border 색은 이전과 동일**(선만 accent).
> 토글 시안 SVG 제공(사각 + 세로 분할선).

- **CMD_PANEL_TOGGLE(64)** 신설 — 클릭 = 현재 모드 반전(기존
  SINGLE/DUAL 핸들러 arm 합류·메뉴 라디오는 유지). 상태 변경 시 툴바
  `set_buttons` 재구성(checked·accent·colsync 활성·툴팁[현재 모드명] 일괄).
- **svg.rs `Element{op, color}`**: 요소 `stroke`/`fill`의 `#RRGGBB`를 색
  오버라이드로 파싱(currentColor/부재 = 잉크·알파는 잉크 상속 — 비활성
  흐림 호환). 테스트 +1(57).
- **chrome `accent_checked`** 빌더 → 켜짐 시 `#on` 키(dw `-on` 매핑) →
  icons: **정확한 이름의 상태 전용 SVG 우선**(panel-toggle-on.svg — 테두리
  잉크·선 #3D8BFF), 없으면 원본을 accent 잉크로 재렌더.
- panel-dual/single/dual-disabled PNG 9종 제거. 실기: 듀얼(블루 선·checked)
  ↔ 싱글(회색·SYNC 흐림) 왕복 캡처. 197 green·clippy 0.

## dotfiles SVG 교체 (main 직커밋)

> **사용자 제공 SVG**: 닷파일 토글 = 접힘 모서리 파일 윤곽(스트로크) +
> "DOT" 텍스트(채움) — 혼합 모드 첫 사례(루트 스트로크 + 텍스트 채움).

- `dotfiles.svg` 5호 등록·PNG 3종 제거. 파서/렌더러 변경 없음(text 상시
  채움 규약이 per-element `fill`/`stroke=none`을 자연 흡수). 실기 캡처.

## colsync SVG 교체 — 파서 fill·text 확장 (main 직커밋)

> **사용자 제공 SVG**: 컬럼 동기화 = 채움 양방향 화살표 + "SYNC" 텍스트 —
> 기존 스트로크 서브셋 밖이라 파이프라인 확장.

- **svg.rs 확장**: 루트 `fill` 모드(색 지정 = 채움·none/부재 = 스트로크) +
  `<text>` 요소(x/y[베이스라인]/font-size/font-weight/text-anchor=middle —
  본문은 태그 뒤 조각에서 수집). 테스트 +2(56 green).
- **gdipctx**: 채움 = `GdipFillPath`(도형)·텍스트 = `GdipAddPathString`
  아웃라인 패스(Arial 고정·중앙 앵커 = StringFormat center·베이스라인 근사).
- **icons**: `colsync` SVG 4호 등록 + **`-disabled` 변형 = 원본 SVG를 알파
  38% 잉크(SVG_INK_DIM)로 렌더** — disabled PNG 관례 폐지. colsync PNG
  6종(활성 3+비활성 3) 제거. 실기: 활성 SYNC·싱글 모드 흐림 캡처 검증.

## SVG 잉크 진하게 (main 직커밋)

> **사용자 요청 2단계**: "조금 더 진하게" → "더 진하게".

- `SVG_INK` `#6E747C` → `#5A6068`(1차) → **`#40464E`**(확정) — SVG 아이콘
  (보기 3종)만 적용, PNG 세트는 기존 톤 유지(차후 SVG 전환 시 통일).
  픽셀 실측·캡처 검증.

## 트리 보기 SVG 전환 — 보기 3종 SVG 완결 (main 직커밋)

> **사용자 제공 SVG**: 트리 보기(부모 사각+긴 줄·└ 엘보 자식 2 — V/H 경로).

- `view-tree.svg` 보관·`EMBEDDED_SVG` 3호 등록, PNG 3종 제거 — **보기 모드
  3종(트리/플랫/타일) 전부 SVG 단독**. 릴리스 재빌드·실기 캡처 확인.
  194 green·clippy 0.
- **v2 교체(사용자 제공)**: 자식을 우측 열로 옮기고 엘보·연결선 분리(└ +
  대시 + 사각) — 파일 교체만으로 반영(SVG 파이프라인 검증 사례). 재빌드·캡처.
- **v3 재교체(사용자 제공)**: 가지 구조를 단일 세로줄 + 가로 가지 2개
  (`M7 13V25H18` + `M7 16H18`)로 정리 — 역시 파일 교체만. 재빌드·캡처.

## 타일 보기 SVG 전환 (main 직커밋)

> **사용자 제공 SVG**: 타일 보기(2×2 라운드 사각, 32 viewBox·2px 스트로크)
> — "svg 적용, 불필요한 파일은 제거".

- `view-tiles.svg` 보관·`EMBEDDED_SVG` 등록(2호), PNG 3종 제거(view-flat과
  동일 규약 — SVG = 전 크기 즉석 래스터라 버킷 불필요).
- 릴리스 재빌드·실기 캡처 확인. 194 green·clippy 0.
