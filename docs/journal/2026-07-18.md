# 2026-07-18 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 도구 모음 16×16 이미지화 + 패널 모드·컬럼 동기 버튼 (`feat/toolbar-icons`)

> **사용자 요청 5건**: ① 보기 모드 이름 3종 변경(Tree/Flat/Tile view)
> ② 도구 모음 = 16×16 이미지 — **큰 이미지 기반으로 생성해 도입**
> ③ refresh = 300° 호 + 화살촉 방향 정확히 ④ view-tree = 타일과 동일 점유로
> 최대 확대 ⑤ 듀얼/싱글 패널 라디오 + 컬럼 동기화 버튼 추가 — **싱글 상태에선
> 듀얼·컬럼 동기 선택 불가**.

- **라벨**: en `Tree view/Flat view/Tile view` · ko `트리 보기/플랫 보기/타일 보기`.
- **아이콘 12종**([assets/toolbar](../../crates/nexa-app/assets/toolbar/README.md)):
  64px GDI+ 벡터 드로잉 → 16/20/32px(100/125/200% DPI 버킷) 다운스케일.
  잉크 `#6E747C` 단일(라이트/다크 겸용) · **비활성 = 알파 38% 변형**(색 아닌
  투명도 — 라이트 테마에서 비활성이 더 진해 보이던 v1 결함 QA로 발굴·교체).
- **렌더 경로**: `include_bytes!` 임베드(icons.rs `EMBEDDED`, 키
  `emb:<이름>:<크기>`) → gdipctx `png_to_hicon`(GDI+ = 유일 접점 규약 준수,
  `GdipCreateHICONFromBitmap`) → 기존 HICON 캐시(get_or_request **동기 분기** —
  큐/워커 미경유, LRU 축출 시 재생성). dw.rs draw_icon이 크기→버킷 매핑과
  비활성 `#dis`→`-disabled` 해석. Toolbar 아이콘 분기(정사각 셀)는 런처 경로
  재사용 — 글리프는 미로드 폴백.
- **패널 그룹**: [듀얼][싱글][컬럼동기] 라디오/토글 신설(기존
  CMD_PANEL_DUAL/SINGLE·CMD_COLW_SYNC 핸들러에 툴바 checked/enabled 동기 추가).
  싱글 모드 = 듀얼·컬럼동기 **비활성**(알파 흐림·클릭 무시 — 듀얼 복귀는 View
  메뉴 경로 유지). ToolButton `enable` 빌더 신설.
- 실기 QA: 임베드 렌더·라디오 전환·싱글 비활성 2종 클릭 무시·colsync 토글
  on/off·설정 영속 왕복 전부 캡처 검증. 187 green·clippy 0.
## 디스클로저 MDL2 + 내비 버튼 활성 상태 (`feat/mdl2-disclosure`)

> **사용자 요청 2건**: ① 폴더 닫힘/열림 마커도 원본 글리프로 변경 ② 내비
> 이전/다음 = **사용 가능할 때만 클릭**·불가 시 식별 가능한 disable 표시,
> 상위 = **This PC에서 비활성 인식**.

- **디스클로저**: rows.rs Marker glyph = ▸/▾ → **MDL2 E76C(닫힘)/E70D(열림)**,
  마커 그리기 = text_opaque → **glyph_opaque**(셀 배경 선도장 유지) ·
  DwBackend `mdl2_small_format`(9 DIP — 원본 FontSize 9 규약) 신설,
  셰브론 코드포인트(E70D/E70E/E76B/E76C)는 소형으로 라우팅.
- **내비 활성**: ToolButton.enabled 신설(비활성 = **text_dim 흐린 글리프** ·
  hover 강조 없음 · 클릭 무시) + Toolbar::set_enabled. Panel::sync_chrome에서
  동기 — 이전/다음 = History can_back/can_forward · 상위 = `is_virtual_root`
  (This PC) 제외. sync_chrome은 생성·탐색·탭 전환 전 경로에서 호출돼 항상 최신.
- 실기: 초기 ←→ 흐림·↑ 진함 → 상위 1회 후 ← 활성 → **내 PC에서 ↑ 흐림** ·
  트리 보기 닫힘 ▶/펼침 ▼/자식 닫힘 동시 렌더. 187 green·clippy 0.

## Segoe MDL2 글리프 통일 — 세그먼트 화살표·내비 버튼 (`feat/mdl2-glyphs`)

> **사용자 요청·확인 3건**: ① 원본 내비 버튼 화살표의 정체 확인 — **Segoe MDL2
> Assets 글리프**(Back U+E72B·Forward U+E72A·Up U+E74A, NavBtnStyle 11px)
> ② 현재 앱 세그먼트("→ abc/← abc")와 **폴더 이동 이전/다음/상위 버튼**에 적용
> ③ 원본 폴더 디스클로저 글리프 확인 — ChevronRight U+E76C(닫힘)/
> ChevronDown U+E70D(열림), 9pt·투명도 0.8(참고: 종류 아이콘 폴더 U+E8B7·
> 링크 U+E71B·문서 U+E8A5).

- **내비 버튼**: DwBackend에 `mdl2_format`(Segoe MDL2 Assets 11 DIP — 인박스)
  신설, glyph_opaque가 **U+E700 대역 PUA를 자동 라우팅**(기타 글리프 ⟳✕→ 등은
  기존 icon_format 유지). panel.rs nav_buttons = E72B/E72A/E74A.
- **NxSegmented**: 직전 벡터 화살표 렌더 폐기 → "→ /← " 접두 = **MDL2
  E72A/E72B 글리프**(컨트롤 소유 icon_font — 본문 글꼴-3 크기·WM_DESTROY 해제),
  [글리프][5px][텍스트] 실측 중앙 정렬 유지.
- 실기 캡처: 내비 행 ←→↑ MDL2 렌더·Insert 카드 세그먼트 동일 스타일.
  187 green·clippy 0.

## 설정 ⚙ 톱니바퀴 + 갤러리 버튼 숨김 (`fix/prefs-gear-icon`)

> **사용자 요청 2건**: ① 설정 아이콘이 꽃 모양 — 톱니바퀴로 ② 샘플 갤러리 숨김.

- 툴바 설정 글리프 U+2699(⚙ — 본문 폰트에서 꽃처럼 렌더) → **MDL2 Settings
  U+E713**. 이 과정에서 함정 2건 수리: ① `text_width`가 본문 폰트 기준이라
  PUA 폭 0(버튼 셀 붕괴) — MDL2 포맷 측정 라우팅 추가 ② Toolbar 일반(글리프
  폭) 분기가 `text_opaque`(본문 폰트 → tofu) — **PUA 글리프면 glyph_opaque**
  로 렌더(내비 고정 폭과 동일 경로). 실기 캡처 톱니 확인.
- ctl 갤러리 🃏 툴바 버튼 숨김(개발 검증 = 0x8009 주입 경로만 유지).

## 설정 트리 라벨 정렬 = 파일뷰 규약 (`fix/prefs-tree-align`)

> **사용자 요청**: 하위 유무와 관계없이 왼쪽 정렬 일치(하위 있는 1레벨과
> 없는 1레벨의 정렬이 달라 혼동).

- 마커 존을 **고정 폭 상시 예약**(파일뷰 마커 존 규약 동일) — 그룹만 셰브론을
  그리고, leaf(예: "탭")도 같은 들여쓰기로 라벨 x 일치. 실기 캡처 확인.

## 설정 트리 디스클로저 = MDL2 통일 (`fix/prefs-mdl2-marker`)

> **사용자 요청**: 설정에서도 파일 목록과 동일한 확장/축소 폰트 사용.

- 설정 사이드바 트리의 그룹 마커(텍스트 ▸/▾ — 본문 폰트)를 **파일 목록과
  동일한 Segoe MDL2 셰브론**(E76C 접힘/E70D 펼침·9px `icon_font`)으로 교체 —
  마커를 라벨 문자열에서 분리해 별도 폰트로 렌더(검색 중 = 강제 펼침 표시
  유지). icon_font는 생성/파괴 수명 관리. 실기 캡처(일반·파일 목록·하단 패널
  그룹 ∨ 렌더). clippy 0.

## 카드 스크롤·Ctrl 다중 정렬·컬럼 폭 동기화 (`feat/card-scroll`)

> **사용자 요청 6건**: ① 카드 다수 시 그리드 오버레이 방식 스크롤 + 하단
> 바(프리셋·버튼) 상시 유지 ② kind별 카드 높이 fitting ③ 하단 바 5px 하향
> ④ Ctrl+헤더 = 다중 정렬 + 순번 원문자 상시 ⑤ Dual/Single file panel(s)
> 라벨 ⑥ "컬럼 넓이 동기화" 설정(기본 on·off=패널 독립·탭=패널 상속·세션
> 탭 레벨 직렬화·확장성).

- **카드 스크롤(NexaCardHost)**: 뷰포트 창(폼 영역 = 우측 그리드와 동일
  상~하단) + 오버레이 썸(그리드 규약 — 페이드↔드래그 일반 바)·휠 라우팅
  (br_proc — 카드 EDIT 버블 포함)·통지/CTLCOLOR 투과·CONTROLPARENT·
  + 추가 시 가시 보정. 카드 높이 = KIND_LAST_ROW fitting(마지막 컨트롤 기준
  하단 공백 제거·kind 변경 즉시 재배치). ctl() 재귀 탐색(2단 깊이).
- **다중 정렬**: 순번 원문자(①②) = **정렬 시작부터 상시**(단일 = ①) —
  테스트 개정. 트리거는 Ctrl 병행 시도 후 **Shift 전용으로 복원**(사용자
  재확정 — 혼동. 원본 docs/23 §4 규약 유지, `fix/sort-shift-only`).
- **컬럼 폭 동기화**: 보기 메뉴 모드 4종 하단 체크(기본 on·settings 영속).
  rows 리사이즈 → take_col_resized 폴링 → 패널 전 탭 상속 + on이면 반대
  패널 동기. 세션 `panel{i}.colw`(modes 동일 레벨 — 탭별 `colw{j}` 확장
  예약). **복원은 DPI set_metrics 리셋 이후**(pending_colw — 진범: 시작 시
  기본 폭으로 덮임). 실기: on 동기·off 독립·재시작 유지 3종 검증.
- 188 green·clippy 0.

## ImageFit 표시 모드 + ctl 문서 세트 (`feat/imagebtn-fit`)

> **사용자 요청 2건**: ① 이미지 버튼 표시 모드(원본 크기 자동 맞춤/컨트롤
> 맞춤 늘림) ② 컨트롤별 설명 문서 + 내비게이션 문서로 엮어 관리.

- **NxIconButton `ImageFit`**: `Native`(원본 픽셀 크기 그대로 중앙 배치) /
  `Stretch`(컨트롤 맞춤 바이큐빅 — 기본). gdipctx `image_size`
  (GdipGetImageWidth/Height) 추가·`create_image`에 fit 인자.
  bulkrename ± = Stretch(현행 유지).
- **ctl 문서 세트**: `docs/ctl/` 신설 — **README(내비게이션 허브: 14종 색인·
  호수·클래스명)** + overview(명명·판매 추상화·AA·Style·base·함정 원장) +
  컨트롤별 13문서(searchbox·fontbox·segmented·spin·groupcard·combobox·
  checkbox·textbox·iconbutton·button·label·menubutton·grid — 각 모양/동작·
  API·메시지 계약 표·원장 주의). docs/README ②설계 표에 연결.
  계약 SSOT = 소스 모듈 doc — 문서는 사용자/판매 관점 요약.

## 아이콘 보관 + 카드 ± 버튼 raster 교체 (`feat/rename-btn-images`)

> **사용자 요청 연속**: ① 원본 앱 아이콘 모두 가져와 보관 ② 리네임 ＋/−
> 작은 이미지 제작(+비활성) ③ 카드 ± 버튼을 그 이미지로 교체.

- **아이콘 보관**: 원본 `../nexa-dir/Assets/AppIcon` 4종(다크/라이트 × ico +
  1024 PNG)을 `crates/nexa-app/assets/`에 — 기존 nexa-dir.ico(다크·해시 동일)
  외 3종 추가·README. 디자인 원본(.psd/.svg)은 원본에도 없어 raster가 SSOT.
- **리네임 ± 이미지 생성**(사용자 선택 = 채운 원+흰 기호·색 구분): 추가 ＋ =
  초록 `#33B24E`·삭제 − = 빨강 `#FF3B30`(Style.danger)·비활성 = 회색
  `#A4A8AC`(Style.border). 각 16/20/32px·PNG·투명·AA — 활성 6+비활성 6 =
  12종(`assets/rename/`).
- **버튼 교체**(NxIconButton 이미지 모드): gdipctx에 `decode_png`
  (SHCreateMemStream→GdipLoadImageFromStream)·`dispose_image`·`draw_image`
  (바이큐빅 스케일) 추가 — **GDI+ 유일 접점 규약 유지**. NxIconButton
  `create_image`(active/disabled PNG) 신설, enabled 전환 시 raster 교체,
  GpImage = IbState Drop RAII. bulkrename 32px include_bytes! 임베드(포터블
  단일 exe)·make_card ± 교체. 실기 렌더 확인(초록 ＋·빨강 −). 187 green.

## ctl 재사용 리팩터 + 전수 검토 (`refactor/ctl-base`)

> **사용자 요청**: 새 코드 재사용성 강화·유사 컨트롤 통폐합/계층화·전체 코드
> 성능/보안/기능 점검.

- **base.rs 신설(재사용 계층)**: 14개 Nx 컨트롤이 문자 그대로 반복하던 4대
  생명주기 패턴을 추출 — `state<T>`/`attach_state<T>`/`drop_state<T>`
  (GWLP_USERDATA 상태 박스)·`notify(hwnd, code)`(WM_COMMAND 재발행 —
  button click·checkbox toggle·combobox/segmented/spin/grid/fontbox notify
  6종 통합)·`register_class`(창 클래스 1회 등록). 전 컨트롤 마이그레이션.
  순 −107줄(보일러플레이트 14블록 제거).
- **GDI 자원 RAII화**: segmented icon_font·textbox bg_brush를 `Drop` 구현으로
  이관(WM_DESTROY 수동 해제 → 박스 회수 시 자동 — 누락 방지). 팝업 컨트롤은
  close_drop 정리 후 drop_state 위임.
- **전수 검토 결과**: ① **GDI 자원 전부 짝 맞음**(pen/brush/font/region/DC —
  gdipctx 오류 분기까지 개별 해제·region은 SetWindowRgn 시스템 소유·grid
  더블버퍼 bmp/DC 정상 해제) — **누수 0** ② **잠재 크래시 1건 수정**:
  segmented 화살표 라벨의 `rest` 빈 문자열 시 빈 `Vec<u16>`→DrawTextW
  (07-18 grid 크래시와 동일 댕글링 포인터 AA 클래스) — 빈 문자열 가드 추가
  ③ 나눗셈 분모 `.max(1)`·음수→usize 캐스트 없음·bulkrename 정렬 인덱스
  안전·MDL2 라우팅 통제 입력만·내비 활성 우회 이중 가드 — 문제 없음.
  clippy 0·187 green·전 컨트롤 회귀 스모크 통과.
- **후속 계층화 후보**(미실행 — 별도 슬라이스): 팝업 소유 3종
  (combobox/menubutton/fontbox)의 "NOACTIVATE 팝업 + 바깥클릭 타이머 +
  owner USERDATA" 공통 골격 추출(X-16 백로그 ① 모달 공통화와 함께). 팝업
  owner는 과거 크래시 진앙이라 신중 이관 필요 — 현 단계는 안전 우선 보류.

## 다중열 정렬·6종 확정·ExtDot 픽스·화살표 렌더 (`feat/nxgrid-sort`)

> **사용자 요청 5건**: ① 이전 프로젝트 파일 그리드의 **다중열 정렬** 설계 분석·
> 반영 ② 동작 이름 6종 정확 표기 ③ 그 외 동작 제거 ④ Extension with dot
> 삽입 시 이중 점(`faaa052.INS.txt`) 확인 + **전수 점검** ⑤ 세그먼트 좌우
> 화살표 = 끝이 큰 긴 화살표(원본 내비 버튼 참고).

- **설계 분석 결과**: 원본 docs/23-column-system.md §4(3상태 순환·Shift 다중·
  ▲▼ 앞/①② 뒤·비교 = 도메인) — **메인 파일 목록에는 기이식 완료**
  (rows.rs apply_sort/header_label → TreeSource::set_sort → Tree::set_sort).
  반영 대상 = **NxGrid**: 동일 규약 이식 — apply_sort(단순 = 단일 3상태·
  Shift+기존 ≥1 = 추가/순환/제거)·header_label(글리프+order_badge 재사용
  [nexa-gui pub 승격])·**비교 = 호스트**(NXGR_SORT 통지 + sort_spec 조회).
  bulkrename = **표시 순서만 재배열**(order 맵 — items/excluded 인덱스 고정,
  행/헤더 토글 order 경유·대소문자 무시 안정 정렬). 실기: ▼ 역순·①② 배지·
  정렬 중 토글 24→23 매핑 검증.
- **ExtDot 이중 점 진범**: scoped()의 "점 유실 시 복원"이 정당한 결과
  ("INS.txt")에 점을 또 붙임. **전수 점검**(같은 경로 공유): 연번/날짜 앞삽입
  동일 증상·치환의 점 제거 무효화 — Case 무영향·Move/ChangeExt 비사용.
  픽스 = **결과 그대로 채택 + 전체 이름 재분해**(NameExt 규약 동일).
  테스트 신설·개정(187 green).
- **동작 6종 확정**: Replace Text·Replace RegEx·Insert Text·Change Case·
  Add Number Sequence·Add Date(en.lang 대소문자 그대로) — 구간 이동/확장자
  변경 카드 제거(코어 op·프리셋 파서 유지, 복원 시 스킵·전량 스킵 = 기본 카드).
- **NxSegmented 화살표**: "→ /← " 라벨 = AA 벡터 화살표(긴 샤프트+큰 머리 —
  둥근 캡 2px)·[화살표+텍스트] 묶음 중앙 정렬. Insert 카드 캡처 검증.

## Tab 내비 + 3단 체크·헤더 전체 토글 (`feat/bulk-tab-tristate`)

> **사용자 요청 3건**: ① 입력 후 Tab = 배치 순서 포커스 이동 ② Apply 헤더에
> 체크박스 — 클릭 = 전체 체크/해제, 일부만 체크 = **부분선택(흐릿한 ✓)** +
> NxCheckBox를 **2단(체크/해제)·3단(체크/부분/해제)** 선택 지정으로 보완
> ③ 체크 열 폭 = 필요한 만큼만.

- **Tab 내비**: 모달 루프 3곳(본 다이얼로그·Save·Edit)에 IsDialogMessageW —
  Tab = 생성(배치) 순서. 컨테이너 = **WS_EX_CONTROLPARENT**(NxGroupCard·
  NxTextBox·NxSpin 래퍼 — 탭 착지 = 내부 EDIT, 래퍼 TABSTOP 제거) ·
  방향키 소비 컨트롤(콤보/세그/메뉴버튼)에 **WM_GETDLGCODE = WANTARROWS**
  (IsDialogMessage가 화살표를 포커스 이동으로 뺏는 것 방지 — 그리드는 기왕).
- **NxCheckBox 2단/3단**(`CheckMode::Two|Three`): 상태 0/1/2(해제/체크/부분),
  클릭 순환 3단 = 0→1→2→0 · 부분 = accent 박스 + **bg·accent 50% 블렌드 ✓**.
  NXCHK_GET/SETCHECK 값 확장(0/1/2). 갤러리 = 3단 예시로 교체.
- **그리드 헤더 체크**(Mark::Check + 헤더): 컬럼 0 헤더에 동일 체크박스 —
  상태 = 전 행 집계(전체/부분/해제, 부분 = 흐릿한 ✓·해제 = 백지+외곽선[밴드
  위 구별]). 클릭 = 전체 체크↔해제(부분이면 전체 체크), `NXGR_ROW_ALL`(-2)
  통지 → 호스트 전 행 재동기(bulkrename excluded 일괄 갱신).
- 체크 열 폭 = 글꼴 정사각+여백(≈29px — 타이틀 제거, 헤더 체크가 대신).
- 실기: 24→행 해제 23(헤더 흐릿한 ✓)→헤더 클릭 24→재클릭 0 · 갤러리 3단
  순환 1→2→0 캡처. 186 green·clippy 0.

## NxGrid 깜빡임 제거 — 더블버퍼 (`fix/grid-flicker`)

> **사용자 QA**: 입력 변경·키보드 방향키 이동 시 그리드 blink.

- 진범 = 매 갱신 전체 무효화(erase=true) + WM_ERASEBKGND 기본 배경 지우기 →
  흰 배경 플래시. 픽스 = **WM_PAINT 더블버퍼**(메모리 DC 전면 도장 후 1회
  BitBlt — 메인 창 규약 동일) + WM_ERASEBKGND=1 + InvalidateRect erase=false
  전면 전환. GdipCtx(AA)는 메모리 DC 위에서 동일 동작.
- 스모크: 선택 프레임·체크·헤더 렌더 동일(fl1~3 캡처)·186 green·clippy 0.

## 하단 행 재편 — Rename NxButton·프리셋 이동·그리드 전고 (`feat/bulk-bottom-bar`)

> **사용자 요청 4건+1**: ① Apply → **커스텀 버튼 "Rename"**(기본 버튼 = Default
> accent) ② 하단 우측 [Cancel][Rename] 순 배치 ③ 프리셋 기능을 구 Apply
> 자리(하단 좌측)로 ④ 프리셋이 빠진 만큼 **변경 목록 그리드 확장** ⑤ 새
> 그리드 **행 높이 = 파일 목록 그리드와 동일**.

- 하단 행 확정 구도: 좌 [… ⌄]+변경 건수 · 중 검증 오류 · 우
  [취소][이름 변경(NxButton Default)] — place_buttons_br 실측 우정렬.
  classic BUTTON 2개 폐기(WM_COMMAND (id,0)→(id,1 NXBTN_CLICK), 활성 =
  EnableWindow→**NXBTN_SETENABLE**(내부 상태 — 그리기 반영)).
- 미리보기 그리드 = 우측 컬럼 **상단 PAD부터 전고**(프리셋 행 제거분 흡수).
- **GridOpts.row_h 신설**(≤0 = 자동): bulkrename `file_row_h` =
  win.rs panel_metrics 동일 산식(**20px @96dpi·DPI 스케일·min 14**) —
  미리보기·프리셋 관리 목록 모두 파일 목록과 행 높이 일치.
- 실기: idle Rename 비활성(회색)→변경 시 파랑 활성 · [취소 x=1189][이름 변경
  x=1253] 우하단 · 메뉴 y=893 하단 · Rename 클릭 = 24건 실변경 후 원복.
  QA 부검: 실패 원인은 회귀가 아니라 **Ctrl+A 주입의 포그라운드 경합** —
  파일 목록은 마우스 Shift+클릭(MK_SHIFT)로 연속 선택 가능(주입 안정 경로).
  186 green·clippy 0.

## 프리셋 팝업 macOS 시안 + NxGrid 행 선택·목록 모드 (`feat/nx-grid` 후반)

> **사용자 요청 4건(시안 3장)**: ① 그리드 선택 = 파일 목록과 동일(Shift 연속·
> Ctrl 비연속·Ctrl+방향키 후 Space 등 기본 동작) ② Save 팝업 = 무캐션 라운드·
> 상단 라벨·**기본 이름 전체 선택**·우하단 [취소][OK] ③ 저장 상태 = […⌄] 옆
> 변경 건수 라벨(메뉴 [프리셋들/─/Save/Edit]) ④ Edit = 무헤더 지브라 목록 +
> 행별 빨간 ⊖ 삭제. **설계 문의 회신**: 삭제 목록은 별도 컨트롤이 아니라
> **기본 NxGrid 옵션 확장**(계약의 "셀 데이터화" 규약 — 스크롤/히트테스트 중복 방지).

- **NxGrid 행 선택**(파일 목록 규약): 클릭 = 단일, Shift+클릭/방향키 = 앵커
  연속, Ctrl+클릭 = 비연속 토글, **Ctrl+방향키 = 포커스만 이동** 후
  Space(Ctrl+Space = 토글), Ctrl+A 전체, Home/End/PgUp/PgDn·스크롤 추종.
  선택 = sel_bg 필·포커스 행 = accent 1px 프레임(WM_SETFOCUS 조건).
  `NXGR_SELCHANGE` 통지 + `selected_rows` 조회. 마크 클릭은 선택 불변.
  WM_GETDLGCODE = WANTARROWS|WANTCHARS.
- **GridOpts 확장**: `no_header`(목록 모드)·`zebra`(bg↔sel_bg 50% 블렌드 —
  빈 슬롯까지 줄무늬)·`outline`·`mark: None|Check|Minus`. `Mark::Minus` =
  행 우측 빨간 ⊖(Style.**danger** 신설 — AA 원판+백색 바), 클릭 =
  NXGR_TOGGLE 통지(삭제 판단 = 호스트). create 인자 check_col:bool → GridOpts.
- **Save 팝업 시안**: WS_POPUP+WS_BORDER + **DWM 라운드 코너**(round_popup —
  DWMWA_WINDOW_CORNER_PREFERENCE, Win10 = 각) · 상단 NxLabel · NxTextBox에
  기본 이름(`bulk.preset.savedSeq`) 프리필 + **EM_SETSEL 전체 선택**(NxTextBox
  위임 확장) · [취소][OK(Default)] 우하단 실측 정렬(place_buttons_br).
- **Edit 팝업 시안**: NxGrid(no_header·zebra·outline·Minus) 1컬럼 목록 —
  ⊖ 클릭 = 목록 즉시 제거(**스테이징**), OK = 파일 삭제 확정·취소 = 폐기.
  구 체크열+[삭제][닫기] 구도 폐기.
- **메뉴 줄 배치**: […⌄]를 우측 컬럼 상단으로 이동 + 변경 건수 라벨을 오른쪽
  동일 행(시안) — 검증 오류 문구는 하단으로.
- 실기 QA(캡처+파일 왕복): 선택 3종(연속 1–4·비연속 +6·↓ 단일 이동+프레임) ·
  Save 기본 이름 저장 → **취소 후 파일 보존 1 → OK 후 0** · 지브라+⊖ 렌더 ·
  […⌄]+40건 라벨 동일 행. 186 green·clippy 0.

## NxGrid 신설 — 오버레이 스크롤바·크래시 이분·프리셋 메뉴 v2 (`feat/nx-grid` 전반)

> **사용자 요청 3건(시안 2장)**: ① rename 미리보기 = Apply/Before/After 그리드
> (리사이즈 가능·수정 행 체크 이미지) — 기본 그리드 + 확장 설계 ② 스크롤바 =
> macOS 오버레이(스크롤 시 얇은 썸·페이드, 썸 드래그 시 일반 바) — **가로도
> 동일** ③ 프리셋 메뉴 = [저장된 프리셋들/─/Save Renaming Sequence…/Edit
> Renaming Sequences…] + Save 이름 입력 팝업.

- **NxGrid(ctl 14호)**: 컬럼 헤더+경계 드래그 리사이즈(±4px·COL_MIN 40) ·
  체크 마크 열(행별 `Option<bool>` — AA 체크·클릭 토글 NXGR_TOGGLE/GETROW) ·
  `set_rows` 전체 교체 API. 미리보기 적용(행별 제외 excluded — 카운트 연동).
- **user32 AV 크래시 이분**(런타임 코드 무력화 반복): set_rows→paint→체크→
  **텍스트 패스** 순 격리 — 진범 = **빈 문자열 셀의 빈 `Vec<u16>`(댕글링
  포인터)를 DrawTextW에 전달**. 픽스 = 빈 셀은 그리지 않는다(모듈 문서 채록).
- **오버레이 스크롤바 세로/가로**: 평소 숨김 → 스크롤 순간 얇은 썸(BAR_THIN 6)
  표시 후 900ms 페이드 → 썸 드래그 = 일반 바 모드(BAR_WIDE 10+트랙 필).
  가로 = 컬럼 합>폭 시(h_off — 헤더·셀·체크·히트 전부 반영)·Shift+휠.
  픽셀 검증: 휠 7px ↔ 드래그 12px(트랙 가장자리 밀착)·업 후 복귀 7px.
- **NxMenuButton 구분선**: 항목 `"-"` = 수평선(pick·hover·키보드 스킵).
- **프리셋 메뉴 v2**: rebuild_preset_menu(프리셋들→─→Save→Edit) ·
  Save = NexaPromptName 모달(금지 문자 정제) · Edit = NexaManagePresets 모달.
  실기: 메뉴 2항목→저장→4항목 재구성→Edit 삭제 왕복.
- QA 방법론 추가: keybd_event Ctrl+A(전체 선택 = GetKeyState 기반이라 실제
  키 주입 필요)·argv[1] 시작 루트로 다수 행 시나리오 구성.
